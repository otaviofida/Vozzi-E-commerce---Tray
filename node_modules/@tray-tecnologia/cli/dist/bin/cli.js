#!/usr/bin/env node
import { g as Y, T as U, G as du, l as Fu, P as mu, h as Pn, c as kn, n as Rn, F as pu } from "../Tray-D1BHK8Cg.js";
import gu from "node:events";
import pt, { execFile as Se } from "node:child_process";
import gt, { extname as $n, resolve as vt, join as In, relative as Cu, sep as Eu, dirname as _u } from "node:path";
import oe from "node:fs";
import y from "node:process";
import { createRequire as wu } from "node:module";
import { fileURLToPath as Ln } from "node:url";
import * as bu from "node:readline";
import { AsyncLocalStorage as Bu, AsyncResource as Ct } from "node:async_hooks";
import Au from "stream";
import { stripVTControlCharacters as yu, promisify as De } from "node:util";
import vu from "tty";
import xu from "node:tty";
import Ou, { EOL as Et } from "node:os";
import { Buffer as Mn } from "node:buffer";
import ot, { constants as Wn, lstat as xt, stat as Su, readdir as Tu, realpath as Pu } from "node:fs/promises";
import { unwatchFile as Ot, watchFile as ku, watch as Ru, stat as $u } from "fs";
import { realpath as $e, stat as _t, lstat as Iu, open as Lu, readdir as Mu } from "fs/promises";
import { EventEmitter as Wu } from "events";
import * as b from "path";
import { Readable as Hu } from "node:stream";
import { type as ju } from "os";
var R = {}, ce = {}, he = {}, St;
function Te() {
  if (St) return he;
  St = 1;
  class e extends Error {
    /**
     * Constructs the CommanderError class
     * @param {number} exitCode suggested exit code which could be used with process.exit
     * @param {string} code an id string representing the error
     * @param {string} message human-readable description of the error
     */
    constructor(o, n, i) {
      super(i), Error.captureStackTrace(this, this.constructor), this.name = this.constructor.name, this.code = n, this.exitCode = o, this.nestedError = void 0;
    }
  }
  class t extends e {
    /**
     * Constructs the InvalidArgumentError class
     * @param {string} [message] explanation of why argument is invalid
     */
    constructor(o) {
      super(1, "commander.invalidArgument", o), Error.captureStackTrace(this, this.constructor), this.name = this.constructor.name;
    }
  }
  return he.CommanderError = e, he.InvalidArgumentError = t, he;
}
var Tt;
function wt() {
  if (Tt) return ce;
  Tt = 1;
  const { InvalidArgumentError: e } = Te();
  class t {
    /**
     * Initialize a new command argument with the given name and description.
     * The default is that the argument is required, and you can explicitly
     * indicate this with <> around the name. Put [] around the name for an optional argument.
     *
     * @param {string} name
     * @param {string} [description]
     */
    constructor(n, i) {
      switch (this.description = i || "", this.variadic = !1, this.parseArg = void 0, this.defaultValue = void 0, this.defaultValueDescription = void 0, this.argChoices = void 0, n[0]) {
        case "<":
          this.required = !0, this._name = n.slice(1, -1);
          break;
        case "[":
          this.required = !1, this._name = n.slice(1, -1);
          break;
        default:
          this.required = !0, this._name = n;
          break;
      }
      this._name.length > 3 && this._name.slice(-3) === "..." && (this.variadic = !0, this._name = this._name.slice(0, -3));
    }
    /**
     * Return argument name.
     *
     * @return {string}
     */
    name() {
      return this._name;
    }
    /**
     * @package
     */
    _concatValue(n, i) {
      return i === this.defaultValue || !Array.isArray(i) ? [n] : i.concat(n);
    }
    /**
     * Set the default value, and optionally supply the description to be displayed in the help.
     *
     * @param {*} value
     * @param {string} [description]
     * @return {Argument}
     */
    default(n, i) {
      return this.defaultValue = n, this.defaultValueDescription = i, this;
    }
    /**
     * Set the custom handler for processing CLI command arguments into argument values.
     *
     * @param {Function} [fn]
     * @return {Argument}
     */
    argParser(n) {
      return this.parseArg = n, this;
    }
    /**
     * Only allow argument value to be one of choices.
     *
     * @param {string[]} values
     * @return {Argument}
     */
    choices(n) {
      return this.argChoices = n.slice(), this.parseArg = (i, r) => {
        if (!this.argChoices.includes(i))
          throw new e(
            `Allowed choices are ${this.argChoices.join(", ")}.`
          );
        return this.variadic ? this._concatValue(i, r) : i;
      }, this;
    }
    /**
     * Make argument required.
     *
     * @returns {Argument}
     */
    argRequired() {
      return this.required = !0, this;
    }
    /**
     * Make argument optional.
     *
     * @returns {Argument}
     */
    argOptional() {
      return this.required = !1, this;
    }
  }
  function u(o) {
    const n = o.name() + (o.variadic === !0 ? "..." : "");
    return o.required ? "<" + n + ">" : "[" + n + "]";
  }
  return ce.Argument = t, ce.humanReadableArgName = u, ce;
}
var fe = {}, de = {}, Pt;
function Hn() {
  if (Pt) return de;
  Pt = 1;
  const { humanReadableArgName: e } = wt();
  class t {
    constructor() {
      this.helpWidth = void 0, this.minWidthToWrap = 40, this.sortSubcommands = !1, this.sortOptions = !1, this.showGlobalOptions = !1;
    }
    /**
     * prepareContext is called by Commander after applying overrides from `Command.configureHelp()`
     * and just before calling `formatHelp()`.
     *
     * Commander just uses the helpWidth and the rest is provided for optional use by more complex subclasses.
     *
     * @param {{ error?: boolean, helpWidth?: number, outputHasColors?: boolean }} contextOptions
     */
    prepareContext(n) {
      this.helpWidth = this.helpWidth ?? n.helpWidth ?? 80;
    }
    /**
     * Get an array of the visible subcommands. Includes a placeholder for the implicit help command, if there is one.
     *
     * @param {Command} cmd
     * @returns {Command[]}
     */
    visibleCommands(n) {
      const i = n.commands.filter((s) => !s._hidden), r = n._getHelpCommand();
      return r && !r._hidden && i.push(r), this.sortSubcommands && i.sort((s, a) => s.name().localeCompare(a.name())), i;
    }
    /**
     * Compare options for sort.
     *
     * @param {Option} a
     * @param {Option} b
     * @returns {number}
     */
    compareOptions(n, i) {
      const r = (s) => s.short ? s.short.replace(/^-/, "") : s.long.replace(/^--/, "");
      return r(n).localeCompare(r(i));
    }
    /**
     * Get an array of the visible options. Includes a placeholder for the implicit help option, if there is one.
     *
     * @param {Command} cmd
     * @returns {Option[]}
     */
    visibleOptions(n) {
      const i = n.options.filter((s) => !s.hidden), r = n._getHelpOption();
      if (r && !r.hidden) {
        const s = r.short && n._findOption(r.short), a = r.long && n._findOption(r.long);
        !s && !a ? i.push(r) : r.long && !a ? i.push(
          n.createOption(r.long, r.description)
        ) : r.short && !s && i.push(
          n.createOption(r.short, r.description)
        );
      }
      return this.sortOptions && i.sort(this.compareOptions), i;
    }
    /**
     * Get an array of the visible global options. (Not including help.)
     *
     * @param {Command} cmd
     * @returns {Option[]}
     */
    visibleGlobalOptions(n) {
      if (!this.showGlobalOptions) return [];
      const i = [];
      for (let r = n.parent; r; r = r.parent) {
        const s = r.options.filter(
          (a) => !a.hidden
        );
        i.push(...s);
      }
      return this.sortOptions && i.sort(this.compareOptions), i;
    }
    /**
     * Get an array of the arguments if any have a description.
     *
     * @param {Command} cmd
     * @returns {Argument[]}
     */
    visibleArguments(n) {
      return n._argsDescription && n.registeredArguments.forEach((i) => {
        i.description = i.description || n._argsDescription[i.name()] || "";
      }), n.registeredArguments.find((i) => i.description) ? n.registeredArguments : [];
    }
    /**
     * Get the command term to show in the list of subcommands.
     *
     * @param {Command} cmd
     * @returns {string}
     */
    subcommandTerm(n) {
      const i = n.registeredArguments.map((r) => e(r)).join(" ");
      return n._name + (n._aliases[0] ? "|" + n._aliases[0] : "") + (n.options.length ? " [options]" : "") + // simplistic check for non-help option
      (i ? " " + i : "");
    }
    /**
     * Get the option term to show in the list of options.
     *
     * @param {Option} option
     * @returns {string}
     */
    optionTerm(n) {
      return n.flags;
    }
    /**
     * Get the argument term to show in the list of arguments.
     *
     * @param {Argument} argument
     * @returns {string}
     */
    argumentTerm(n) {
      return n.name();
    }
    /**
     * Get the longest command term length.
     *
     * @param {Command} cmd
     * @param {Help} helper
     * @returns {number}
     */
    longestSubcommandTermLength(n, i) {
      return i.visibleCommands(n).reduce((r, s) => Math.max(
        r,
        this.displayWidth(
          i.styleSubcommandTerm(i.subcommandTerm(s))
        )
      ), 0);
    }
    /**
     * Get the longest option term length.
     *
     * @param {Command} cmd
     * @param {Help} helper
     * @returns {number}
     */
    longestOptionTermLength(n, i) {
      return i.visibleOptions(n).reduce((r, s) => Math.max(
        r,
        this.displayWidth(i.styleOptionTerm(i.optionTerm(s)))
      ), 0);
    }
    /**
     * Get the longest global option term length.
     *
     * @param {Command} cmd
     * @param {Help} helper
     * @returns {number}
     */
    longestGlobalOptionTermLength(n, i) {
      return i.visibleGlobalOptions(n).reduce((r, s) => Math.max(
        r,
        this.displayWidth(i.styleOptionTerm(i.optionTerm(s)))
      ), 0);
    }
    /**
     * Get the longest argument term length.
     *
     * @param {Command} cmd
     * @param {Help} helper
     * @returns {number}
     */
    longestArgumentTermLength(n, i) {
      return i.visibleArguments(n).reduce((r, s) => Math.max(
        r,
        this.displayWidth(
          i.styleArgumentTerm(i.argumentTerm(s))
        )
      ), 0);
    }
    /**
     * Get the command usage to be displayed at the top of the built-in help.
     *
     * @param {Command} cmd
     * @returns {string}
     */
    commandUsage(n) {
      let i = n._name;
      n._aliases[0] && (i = i + "|" + n._aliases[0]);
      let r = "";
      for (let s = n.parent; s; s = s.parent)
        r = s.name() + " " + r;
      return r + i + " " + n.usage();
    }
    /**
     * Get the description for the command.
     *
     * @param {Command} cmd
     * @returns {string}
     */
    commandDescription(n) {
      return n.description();
    }
    /**
     * Get the subcommand summary to show in the list of subcommands.
     * (Fallback to description for backwards compatibility.)
     *
     * @param {Command} cmd
     * @returns {string}
     */
    subcommandDescription(n) {
      return n.summary() || n.description();
    }
    /**
     * Get the option description to show in the list of options.
     *
     * @param {Option} option
     * @return {string}
     */
    optionDescription(n) {
      const i = [];
      if (n.argChoices && i.push(
        // use stringify to match the display of the default value
        `choices: ${n.argChoices.map((r) => JSON.stringify(r)).join(", ")}`
      ), n.defaultValue !== void 0 && (n.required || n.optional || n.isBoolean() && typeof n.defaultValue == "boolean") && i.push(
        `default: ${n.defaultValueDescription || JSON.stringify(n.defaultValue)}`
      ), n.presetArg !== void 0 && n.optional && i.push(`preset: ${JSON.stringify(n.presetArg)}`), n.envVar !== void 0 && i.push(`env: ${n.envVar}`), i.length > 0) {
        const r = `(${i.join(", ")})`;
        return n.description ? `${n.description} ${r}` : r;
      }
      return n.description;
    }
    /**
     * Get the argument description to show in the list of arguments.
     *
     * @param {Argument} argument
     * @return {string}
     */
    argumentDescription(n) {
      const i = [];
      if (n.argChoices && i.push(
        // use stringify to match the display of the default value
        `choices: ${n.argChoices.map((r) => JSON.stringify(r)).join(", ")}`
      ), n.defaultValue !== void 0 && i.push(
        `default: ${n.defaultValueDescription || JSON.stringify(n.defaultValue)}`
      ), i.length > 0) {
        const r = `(${i.join(", ")})`;
        return n.description ? `${n.description} ${r}` : r;
      }
      return n.description;
    }
    /**
     * Format a list of items, given a heading and an array of formatted items.
     *
     * @param {string} heading
     * @param {string[]} items
     * @param {Help} helper
     * @returns string[]
     */
    formatItemList(n, i, r) {
      return i.length === 0 ? [] : [r.styleTitle(n), ...i, ""];
    }
    /**
     * Group items by their help group heading.
     *
     * @param {Command[] | Option[]} unsortedItems
     * @param {Command[] | Option[]} visibleItems
     * @param {Function} getGroup
     * @returns {Map<string, Command[] | Option[]>}
     */
    groupItems(n, i, r) {
      const s = /* @__PURE__ */ new Map();
      return n.forEach((a) => {
        const l = r(a);
        s.has(l) || s.set(l, []);
      }), i.forEach((a) => {
        const l = r(a);
        s.has(l) || s.set(l, []), s.get(l).push(a);
      }), s;
    }
    /**
     * Generate the built-in help text.
     *
     * @param {Command} cmd
     * @param {Help} helper
     * @returns {string}
     */
    formatHelp(n, i) {
      const r = i.padWidth(n, i), s = i.helpWidth ?? 80;
      function a(C, _) {
        return i.formatItem(C, r, _, i);
      }
      let l = [
        `${i.styleTitle("Usage:")} ${i.styleUsage(i.commandUsage(n))}`,
        ""
      ];
      const c = i.commandDescription(n);
      c.length > 0 && (l = l.concat([
        i.boxWrap(
          i.styleCommandDescription(c),
          s
        ),
        ""
      ]));
      const f = i.visibleArguments(n).map((C) => a(
        i.styleArgumentTerm(i.argumentTerm(C)),
        i.styleArgumentDescription(i.argumentDescription(C))
      ));
      if (l = l.concat(
        this.formatItemList("Arguments:", f, i)
      ), this.groupItems(
        n.options,
        i.visibleOptions(n),
        (C) => C.helpGroupHeading ?? "Options:"
      ).forEach((C, _) => {
        const O = C.map((D) => a(
          i.styleOptionTerm(i.optionTerm(D)),
          i.styleOptionDescription(i.optionDescription(D))
        ));
        l = l.concat(this.formatItemList(_, O, i));
      }), i.showGlobalOptions) {
        const C = i.visibleGlobalOptions(n).map((_) => a(
          i.styleOptionTerm(i.optionTerm(_)),
          i.styleOptionDescription(i.optionDescription(_))
        ));
        l = l.concat(
          this.formatItemList("Global Options:", C, i)
        );
      }
      return this.groupItems(
        n.commands,
        i.visibleCommands(n),
        (C) => C.helpGroup() || "Commands:"
      ).forEach((C, _) => {
        const O = C.map((D) => a(
          i.styleSubcommandTerm(i.subcommandTerm(D)),
          i.styleSubcommandDescription(i.subcommandDescription(D))
        ));
        l = l.concat(this.formatItemList(_, O, i));
      }), l.join(`
`);
    }
    /**
     * Return display width of string, ignoring ANSI escape sequences. Used in padding and wrapping calculations.
     *
     * @param {string} str
     * @returns {number}
     */
    displayWidth(n) {
      return u(n).length;
    }
    /**
     * Style the title for displaying in the help. Called with 'Usage:', 'Options:', etc.
     *
     * @param {string} str
     * @returns {string}
     */
    styleTitle(n) {
      return n;
    }
    styleUsage(n) {
      return n.split(" ").map((i) => i === "[options]" ? this.styleOptionText(i) : i === "[command]" ? this.styleSubcommandText(i) : i[0] === "[" || i[0] === "<" ? this.styleArgumentText(i) : this.styleCommandText(i)).join(" ");
    }
    styleCommandDescription(n) {
      return this.styleDescriptionText(n);
    }
    styleOptionDescription(n) {
      return this.styleDescriptionText(n);
    }
    styleSubcommandDescription(n) {
      return this.styleDescriptionText(n);
    }
    styleArgumentDescription(n) {
      return this.styleDescriptionText(n);
    }
    styleDescriptionText(n) {
      return n;
    }
    styleOptionTerm(n) {
      return this.styleOptionText(n);
    }
    styleSubcommandTerm(n) {
      return n.split(" ").map((i) => i === "[options]" ? this.styleOptionText(i) : i[0] === "[" || i[0] === "<" ? this.styleArgumentText(i) : this.styleSubcommandText(i)).join(" ");
    }
    styleArgumentTerm(n) {
      return this.styleArgumentText(n);
    }
    styleOptionText(n) {
      return n;
    }
    styleArgumentText(n) {
      return n;
    }
    styleSubcommandText(n) {
      return n;
    }
    styleCommandText(n) {
      return n;
    }
    /**
     * Calculate the pad width from the maximum term length.
     *
     * @param {Command} cmd
     * @param {Help} helper
     * @returns {number}
     */
    padWidth(n, i) {
      return Math.max(
        i.longestOptionTermLength(n, i),
        i.longestGlobalOptionTermLength(n, i),
        i.longestSubcommandTermLength(n, i),
        i.longestArgumentTermLength(n, i)
      );
    }
    /**
     * Detect manually wrapped and indented strings by checking for line break followed by whitespace.
     *
     * @param {string} str
     * @returns {boolean}
     */
    preformatted(n) {
      return /\n[^\S\r\n]/.test(n);
    }
    /**
     * Format the "item", which consists of a term and description. Pad the term and wrap the description, indenting the following lines.
     *
     * So "TTT", 5, "DDD DDDD DD DDD" might be formatted for this.helpWidth=17 like so:
     *   TTT  DDD DDDD
     *        DD DDD
     *
     * @param {string} term
     * @param {number} termWidth
     * @param {string} description
     * @param {Help} helper
     * @returns {string}
     */
    formatItem(n, i, r, s) {
      const l = " ".repeat(2);
      if (!r) return l + n;
      const c = n.padEnd(
        i + n.length - s.displayWidth(n)
      ), f = 2, g = (this.helpWidth ?? 80) - i - f - 2;
      let C;
      return g < this.minWidthToWrap || s.preformatted(r) ? C = r : C = s.boxWrap(r, g).replace(
        /\n/g,
        `
` + " ".repeat(i + f)
      ), l + c + " ".repeat(f) + C.replace(/\n/g, `
${l}`);
    }
    /**
     * Wrap a string at whitespace, preserving existing line breaks.
     * Wrapping is skipped if the width is less than `minWidthToWrap`.
     *
     * @param {string} str
     * @param {number} width
     * @returns {string}
     */
    boxWrap(n, i) {
      if (i < this.minWidthToWrap) return n;
      const r = n.split(/\r\n|\n/), s = /[\s]*[^\s]+/g, a = [];
      return r.forEach((l) => {
        const c = l.match(s);
        if (c === null) {
          a.push("");
          return;
        }
        let f = [c.shift()], p = this.displayWidth(f[0]);
        c.forEach((g) => {
          const C = this.displayWidth(g);
          if (p + C <= i) {
            f.push(g), p += C;
            return;
          }
          a.push(f.join(""));
          const _ = g.trimStart();
          f = [_], p = this.displayWidth(_);
        }), a.push(f.join(""));
      }), a.join(`
`);
    }
  }
  function u(o) {
    const n = /\x1b\[\d*(;\d*)*m/g;
    return o.replace(n, "");
  }
  return de.Help = t, de.stripColor = u, de;
}
var Fe = {}, kt;
function jn() {
  if (kt) return Fe;
  kt = 1;
  const { InvalidArgumentError: e } = Te();
  class t {
    /**
     * Initialize a new `Option` with the given `flags` and `description`.
     *
     * @param {string} flags
     * @param {string} [description]
     */
    constructor(r, s) {
      this.flags = r, this.description = s || "", this.required = r.includes("<"), this.optional = r.includes("["), this.variadic = /\w\.\.\.[>\]]$/.test(r), this.mandatory = !1;
      const a = n(r);
      this.short = a.shortFlag, this.long = a.longFlag, this.negate = !1, this.long && (this.negate = this.long.startsWith("--no-")), this.defaultValue = void 0, this.defaultValueDescription = void 0, this.presetArg = void 0, this.envVar = void 0, this.parseArg = void 0, this.hidden = !1, this.argChoices = void 0, this.conflictsWith = [], this.implied = void 0, this.helpGroupHeading = void 0;
    }
    /**
     * Set the default value, and optionally supply the description to be displayed in the help.
     *
     * @param {*} value
     * @param {string} [description]
     * @return {Option}
     */
    default(r, s) {
      return this.defaultValue = r, this.defaultValueDescription = s, this;
    }
    /**
     * Preset to use when option used without option-argument, especially optional but also boolean and negated.
     * The custom processing (parseArg) is called.
     *
     * @example
     * new Option('--color').default('GREYSCALE').preset('RGB');
     * new Option('--donate [amount]').preset('20').argParser(parseFloat);
     *
     * @param {*} arg
     * @return {Option}
     */
    preset(r) {
      return this.presetArg = r, this;
    }
    /**
     * Add option name(s) that conflict with this option.
     * An error will be displayed if conflicting options are found during parsing.
     *
     * @example
     * new Option('--rgb').conflicts('cmyk');
     * new Option('--js').conflicts(['ts', 'jsx']);
     *
     * @param {(string | string[])} names
     * @return {Option}
     */
    conflicts(r) {
      return this.conflictsWith = this.conflictsWith.concat(r), this;
    }
    /**
     * Specify implied option values for when this option is set and the implied options are not.
     *
     * The custom processing (parseArg) is not called on the implied values.
     *
     * @example
     * program
     *   .addOption(new Option('--log', 'write logging information to file'))
     *   .addOption(new Option('--trace', 'log extra details').implies({ log: 'trace.txt' }));
     *
     * @param {object} impliedOptionValues
     * @return {Option}
     */
    implies(r) {
      let s = r;
      return typeof r == "string" && (s = { [r]: !0 }), this.implied = Object.assign(this.implied || {}, s), this;
    }
    /**
     * Set environment variable to check for option value.
     *
     * An environment variable is only used if when processed the current option value is
     * undefined, or the source of the current value is 'default' or 'config' or 'env'.
     *
     * @param {string} name
     * @return {Option}
     */
    env(r) {
      return this.envVar = r, this;
    }
    /**
     * Set the custom handler for processing CLI option arguments into option values.
     *
     * @param {Function} [fn]
     * @return {Option}
     */
    argParser(r) {
      return this.parseArg = r, this;
    }
    /**
     * Whether the option is mandatory and must have a value after parsing.
     *
     * @param {boolean} [mandatory=true]
     * @return {Option}
     */
    makeOptionMandatory(r = !0) {
      return this.mandatory = !!r, this;
    }
    /**
     * Hide option in help.
     *
     * @param {boolean} [hide=true]
     * @return {Option}
     */
    hideHelp(r = !0) {
      return this.hidden = !!r, this;
    }
    /**
     * @package
     */
    _concatValue(r, s) {
      return s === this.defaultValue || !Array.isArray(s) ? [r] : s.concat(r);
    }
    /**
     * Only allow option value to be one of choices.
     *
     * @param {string[]} values
     * @return {Option}
     */
    choices(r) {
      return this.argChoices = r.slice(), this.parseArg = (s, a) => {
        if (!this.argChoices.includes(s))
          throw new e(
            `Allowed choices are ${this.argChoices.join(", ")}.`
          );
        return this.variadic ? this._concatValue(s, a) : s;
      }, this;
    }
    /**
     * Return option name.
     *
     * @return {string}
     */
    name() {
      return this.long ? this.long.replace(/^--/, "") : this.short.replace(/^-/, "");
    }
    /**
     * Return option name, in a camelcase format that can be used
     * as an object attribute key.
     *
     * @return {string}
     */
    attributeName() {
      return this.negate ? o(this.name().replace(/^no-/, "")) : o(this.name());
    }
    /**
     * Set the help group heading.
     *
     * @param {string} heading
     * @return {Option}
     */
    helpGroup(r) {
      return this.helpGroupHeading = r, this;
    }
    /**
     * Check if `arg` matches the short or long flag.
     *
     * @param {string} arg
     * @return {boolean}
     * @package
     */
    is(r) {
      return this.short === r || this.long === r;
    }
    /**
     * Return whether a boolean option.
     *
     * Options are one of boolean, negated, required argument, or optional argument.
     *
     * @return {boolean}
     * @package
     */
    isBoolean() {
      return !this.required && !this.optional && !this.negate;
    }
  }
  class u {
    /**
     * @param {Option[]} options
     */
    constructor(r) {
      this.positiveOptions = /* @__PURE__ */ new Map(), this.negativeOptions = /* @__PURE__ */ new Map(), this.dualOptions = /* @__PURE__ */ new Set(), r.forEach((s) => {
        s.negate ? this.negativeOptions.set(s.attributeName(), s) : this.positiveOptions.set(s.attributeName(), s);
      }), this.negativeOptions.forEach((s, a) => {
        this.positiveOptions.has(a) && this.dualOptions.add(a);
      });
    }
    /**
     * Did the value come from the option, and not from possible matching dual option?
     *
     * @param {*} value
     * @param {Option} option
     * @returns {boolean}
     */
    valueFromOption(r, s) {
      const a = s.attributeName();
      if (!this.dualOptions.has(a)) return !0;
      const l = this.negativeOptions.get(a).presetArg, c = l !== void 0 ? l : !1;
      return s.negate === (c === r);
    }
  }
  function o(i) {
    return i.split("-").reduce((r, s) => r + s[0].toUpperCase() + s.slice(1));
  }
  function n(i) {
    let r, s;
    const a = /^-[^-]$/, l = /^--[^-]/, c = i.split(/[ |,]+/).concat("guard");
    if (a.test(c[0]) && (r = c.shift()), l.test(c[0]) && (s = c.shift()), !r && a.test(c[0]) && (r = c.shift()), !r && l.test(c[0]) && (r = s, s = c.shift()), c[0].startsWith("-")) {
      const f = c[0], p = `option creation failed due to '${f}' in option flags '${i}'`;
      throw /^-[^-][^-]/.test(f) ? new Error(
        `${p}
- a short flag is a single dash and a single character
  - either use a single dash and a single character (for a short flag)
  - or use a double dash for a long option (and can have two, like '--ws, --workspace')`
      ) : a.test(f) ? new Error(`${p}
- too many short flags`) : l.test(f) ? new Error(`${p}
- too many long flags`) : new Error(`${p}
- unrecognised flag format`);
    }
    if (r === void 0 && s === void 0)
      throw new Error(
        `option creation failed due to no flags found in '${i}'.`
      );
    return { shortFlag: r, longFlag: s };
  }
  return Fe.Option = t, Fe.DualOptions = u, Fe;
}
var Ie = {}, Rt;
function Nu() {
  if (Rt) return Ie;
  Rt = 1;
  const e = 3;
  function t(o, n) {
    if (Math.abs(o.length - n.length) > e)
      return Math.max(o.length, n.length);
    const i = [];
    for (let r = 0; r <= o.length; r++)
      i[r] = [r];
    for (let r = 0; r <= n.length; r++)
      i[0][r] = r;
    for (let r = 1; r <= n.length; r++)
      for (let s = 1; s <= o.length; s++) {
        let a = 1;
        o[s - 1] === n[r - 1] ? a = 0 : a = 1, i[s][r] = Math.min(
          i[s - 1][r] + 1,
          // deletion
          i[s][r - 1] + 1,
          // insertion
          i[s - 1][r - 1] + a
          // substitution
        ), s > 1 && r > 1 && o[s - 1] === n[r - 2] && o[s - 2] === n[r - 1] && (i[s][r] = Math.min(i[s][r], i[s - 2][r - 2] + 1));
      }
    return i[o.length][n.length];
  }
  function u(o, n) {
    if (!n || n.length === 0) return "";
    n = Array.from(new Set(n));
    const i = o.startsWith("--");
    i && (o = o.slice(2), n = n.map((l) => l.slice(2)));
    let r = [], s = e;
    const a = 0.4;
    return n.forEach((l) => {
      if (l.length <= 1) return;
      const c = t(o, l), f = Math.max(o.length, l.length);
      (f - c) / f > a && (c < s ? (s = c, r = [l]) : c === s && r.push(l));
    }), r.sort((l, c) => l.localeCompare(c)), i && (r = r.map((l) => `--${l}`)), r.length > 1 ? `
(Did you mean one of ${r.join(", ")}?)` : r.length === 1 ? `
(Did you mean ${r[0]}?)` : "";
  }
  return Ie.suggestSimilar = u, Ie;
}
var $t;
function Uu() {
  if ($t) return fe;
  $t = 1;
  const e = gu.EventEmitter, t = pt, u = gt, o = oe, n = y, { Argument: i, humanReadableArgName: r } = wt(), { CommanderError: s } = Te(), { Help: a, stripColor: l } = Hn(), { Option: c, DualOptions: f } = jn(), { suggestSimilar: p } = Nu();
  class g extends e {
    /**
     * Initialize a new `Command`.
     *
     * @param {string} [name]
     */
    constructor(D) {
      super(), this.commands = [], this.options = [], this.parent = null, this._allowUnknownOption = !1, this._allowExcessArguments = !1, this.registeredArguments = [], this._args = this.registeredArguments, this.args = [], this.rawArgs = [], this.processedArgs = [], this._scriptPath = null, this._name = D || "", this._optionValues = {}, this._optionValueSources = {}, this._storeOptionsAsProperties = !1, this._actionHandler = null, this._executableHandler = !1, this._executableFile = null, this._executableDir = null, this._defaultCommandName = null, this._exitCallback = null, this._aliases = [], this._combineFlagAndOptionalValue = !0, this._description = "", this._summary = "", this._argsDescription = void 0, this._enablePositionalOptions = !1, this._passThroughOptions = !1, this._lifeCycleHooks = {}, this._showHelpAfterError = !1, this._showSuggestionAfterError = !0, this._savedState = null, this._outputConfiguration = {
        writeOut: (h) => n.stdout.write(h),
        writeErr: (h) => n.stderr.write(h),
        outputError: (h, d) => d(h),
        getOutHelpWidth: () => n.stdout.isTTY ? n.stdout.columns : void 0,
        getErrHelpWidth: () => n.stderr.isTTY ? n.stderr.columns : void 0,
        getOutHasColors: () => _() ?? (n.stdout.isTTY && n.stdout.hasColors?.()),
        getErrHasColors: () => _() ?? (n.stderr.isTTY && n.stderr.hasColors?.()),
        stripColor: (h) => l(h)
      }, this._hidden = !1, this._helpOption = void 0, this._addImplicitHelpCommand = void 0, this._helpCommand = void 0, this._helpConfiguration = {}, this._helpGroupHeading = void 0, this._defaultCommandGroup = void 0, this._defaultOptionGroup = void 0;
    }
    /**
     * Copy settings that are useful to have in common across root command and subcommands.
     *
     * (Used internally when adding a command using `.command()` so subcommands inherit parent settings.)
     *
     * @param {Command} sourceCommand
     * @return {Command} `this` command for chaining
     */
    copyInheritedSettings(D) {
      return this._outputConfiguration = D._outputConfiguration, this._helpOption = D._helpOption, this._helpCommand = D._helpCommand, this._helpConfiguration = D._helpConfiguration, this._exitCallback = D._exitCallback, this._storeOptionsAsProperties = D._storeOptionsAsProperties, this._combineFlagAndOptionalValue = D._combineFlagAndOptionalValue, this._allowExcessArguments = D._allowExcessArguments, this._enablePositionalOptions = D._enablePositionalOptions, this._showHelpAfterError = D._showHelpAfterError, this._showSuggestionAfterError = D._showSuggestionAfterError, this;
    }
    /**
     * @returns {Command[]}
     * @private
     */
    _getCommandAndAncestors() {
      const D = [];
      for (let h = this; h; h = h.parent)
        D.push(h);
      return D;
    }
    /**
     * Define a command.
     *
     * There are two styles of command: pay attention to where to put the description.
     *
     * @example
     * // Command implemented using action handler (description is supplied separately to `.command`)
     * program
     *   .command('clone <source> [destination]')
     *   .description('clone a repository into a newly created directory')
     *   .action((source, destination) => {
     *     console.log('clone command called');
     *   });
     *
     * // Command implemented using separate executable file (description is second parameter to `.command`)
     * program
     *   .command('start <service>', 'start named service')
     *   .command('stop [service]', 'stop named service, or all if no name supplied');
     *
     * @param {string} nameAndArgs - command name and arguments, args are `<required>` or `[optional]` and last may also be `variadic...`
     * @param {(object | string)} [actionOptsOrExecDesc] - configuration options (for action), or description (for executable)
     * @param {object} [execOpts] - configuration options (for executable)
     * @return {Command} returns new command for action handler, or `this` for executable command
     */
    command(D, h, d) {
      let F = h, m = d;
      typeof F == "object" && F !== null && (m = F, F = null), m = m || {};
      const [, E, w] = D.match(/([^ ]+) *(.*)/), v = this.createCommand(E);
      return F && (v.description(F), v._executableHandler = !0), m.isDefault && (this._defaultCommandName = v._name), v._hidden = !!(m.noHelp || m.hidden), v._executableFile = m.executableFile || null, w && v.arguments(w), this._registerCommand(v), v.parent = this, v.copyInheritedSettings(this), F ? this : v;
    }
    /**
     * Factory routine to create a new unattached command.
     *
     * See .command() for creating an attached subcommand, which uses this routine to
     * create the command. You can override createCommand to customise subcommands.
     *
     * @param {string} [name]
     * @return {Command} new command
     */
    createCommand(D) {
      return new g(D);
    }
    /**
     * You can customise the help with a subclass of Help by overriding createHelp,
     * or by overriding Help properties using configureHelp().
     *
     * @return {Help}
     */
    createHelp() {
      return Object.assign(new a(), this.configureHelp());
    }
    /**
     * You can customise the help by overriding Help properties using configureHelp(),
     * or with a subclass of Help by overriding createHelp().
     *
     * @param {object} [configuration] - configuration options
     * @return {(Command | object)} `this` command for chaining, or stored configuration
     */
    configureHelp(D) {
      return D === void 0 ? this._helpConfiguration : (this._helpConfiguration = D, this);
    }
    /**
     * The default output goes to stdout and stderr. You can customise this for special
     * applications. You can also customise the display of errors by overriding outputError.
     *
     * The configuration properties are all functions:
     *
     *     // change how output being written, defaults to stdout and stderr
     *     writeOut(str)
     *     writeErr(str)
     *     // change how output being written for errors, defaults to writeErr
     *     outputError(str, write) // used for displaying errors and not used for displaying help
     *     // specify width for wrapping help
     *     getOutHelpWidth()
     *     getErrHelpWidth()
     *     // color support, currently only used with Help
     *     getOutHasColors()
     *     getErrHasColors()
     *     stripColor() // used to remove ANSI escape codes if output does not have colors
     *
     * @param {object} [configuration] - configuration options
     * @return {(Command | object)} `this` command for chaining, or stored configuration
     */
    configureOutput(D) {
      return D === void 0 ? this._outputConfiguration : (this._outputConfiguration = Object.assign(
        {},
        this._outputConfiguration,
        D
      ), this);
    }
    /**
     * Display the help or a custom message after an error occurs.
     *
     * @param {(boolean|string)} [displayHelp]
     * @return {Command} `this` command for chaining
     */
    showHelpAfterError(D = !0) {
      return typeof D != "string" && (D = !!D), this._showHelpAfterError = D, this;
    }
    /**
     * Display suggestion of similar commands for unknown commands, or options for unknown options.
     *
     * @param {boolean} [displaySuggestion]
     * @return {Command} `this` command for chaining
     */
    showSuggestionAfterError(D = !0) {
      return this._showSuggestionAfterError = !!D, this;
    }
    /**
     * Add a prepared subcommand.
     *
     * See .command() for creating an attached subcommand which inherits settings from its parent.
     *
     * @param {Command} cmd - new subcommand
     * @param {object} [opts] - configuration options
     * @return {Command} `this` command for chaining
     */
    addCommand(D, h) {
      if (!D._name)
        throw new Error(`Command passed to .addCommand() must have a name
- specify the name in Command constructor or using .name()`);
      return h = h || {}, h.isDefault && (this._defaultCommandName = D._name), (h.noHelp || h.hidden) && (D._hidden = !0), this._registerCommand(D), D.parent = this, D._checkForBrokenPassThrough(), this;
    }
    /**
     * Factory routine to create a new unattached argument.
     *
     * See .argument() for creating an attached argument, which uses this routine to
     * create the argument. You can override createArgument to return a custom argument.
     *
     * @param {string} name
     * @param {string} [description]
     * @return {Argument} new argument
     */
    createArgument(D, h) {
      return new i(D, h);
    }
    /**
     * Define argument syntax for command.
     *
     * The default is that the argument is required, and you can explicitly
     * indicate this with <> around the name. Put [] around the name for an optional argument.
     *
     * @example
     * program.argument('<input-file>');
     * program.argument('[output-file]');
     *
     * @param {string} name
     * @param {string} [description]
     * @param {(Function|*)} [parseArg] - custom argument processing function or default value
     * @param {*} [defaultValue]
     * @return {Command} `this` command for chaining
     */
    argument(D, h, d, F) {
      const m = this.createArgument(D, h);
      return typeof d == "function" ? m.default(F).argParser(d) : m.default(d), this.addArgument(m), this;
    }
    /**
     * Define argument syntax for command, adding multiple at once (without descriptions).
     *
     * See also .argument().
     *
     * @example
     * program.arguments('<cmd> [env]');
     *
     * @param {string} names
     * @return {Command} `this` command for chaining
     */
    arguments(D) {
      return D.trim().split(/ +/).forEach((h) => {
        this.argument(h);
      }), this;
    }
    /**
     * Define argument syntax for command, adding a prepared argument.
     *
     * @param {Argument} argument
     * @return {Command} `this` command for chaining
     */
    addArgument(D) {
      const h = this.registeredArguments.slice(-1)[0];
      if (h && h.variadic)
        throw new Error(
          `only the last argument can be variadic '${h.name()}'`
        );
      if (D.required && D.defaultValue !== void 0 && D.parseArg === void 0)
        throw new Error(
          `a default value for a required argument is never used: '${D.name()}'`
        );
      return this.registeredArguments.push(D), this;
    }
    /**
     * Customise or override default help command. By default a help command is automatically added if your command has subcommands.
     *
     * @example
     *    program.helpCommand('help [cmd]');
     *    program.helpCommand('help [cmd]', 'show help');
     *    program.helpCommand(false); // suppress default help command
     *    program.helpCommand(true); // add help command even if no subcommands
     *
     * @param {string|boolean} enableOrNameAndArgs - enable with custom name and/or arguments, or boolean to override whether added
     * @param {string} [description] - custom description
     * @return {Command} `this` command for chaining
     */
    helpCommand(D, h) {
      if (typeof D == "boolean")
        return this._addImplicitHelpCommand = D, D && this._defaultCommandGroup && this._initCommandGroup(this._getHelpCommand()), this;
      const d = D ?? "help [command]", [, F, m] = d.match(/([^ ]+) *(.*)/), E = h ?? "display help for command", w = this.createCommand(F);
      return w.helpOption(!1), m && w.arguments(m), E && w.description(E), this._addImplicitHelpCommand = !0, this._helpCommand = w, (D || h) && this._initCommandGroup(w), this;
    }
    /**
     * Add prepared custom help command.
     *
     * @param {(Command|string|boolean)} helpCommand - custom help command, or deprecated enableOrNameAndArgs as for `.helpCommand()`
     * @param {string} [deprecatedDescription] - deprecated custom description used with custom name only
     * @return {Command} `this` command for chaining
     */
    addHelpCommand(D, h) {
      return typeof D != "object" ? (this.helpCommand(D, h), this) : (this._addImplicitHelpCommand = !0, this._helpCommand = D, this._initCommandGroup(D), this);
    }
    /**
     * Lazy create help command.
     *
     * @return {(Command|null)}
     * @package
     */
    _getHelpCommand() {
      return this._addImplicitHelpCommand ?? (this.commands.length && !this._actionHandler && !this._findCommand("help")) ? (this._helpCommand === void 0 && this.helpCommand(void 0, void 0), this._helpCommand) : null;
    }
    /**
     * Add hook for life cycle event.
     *
     * @param {string} event
     * @param {Function} listener
     * @return {Command} `this` command for chaining
     */
    hook(D, h) {
      const d = ["preSubcommand", "preAction", "postAction"];
      if (!d.includes(D))
        throw new Error(`Unexpected value for event passed to hook : '${D}'.
Expecting one of '${d.join("', '")}'`);
      return this._lifeCycleHooks[D] ? this._lifeCycleHooks[D].push(h) : this._lifeCycleHooks[D] = [h], this;
    }
    /**
     * Register callback to use as replacement for calling process.exit.
     *
     * @param {Function} [fn] optional callback which will be passed a CommanderError, defaults to throwing
     * @return {Command} `this` command for chaining
     */
    exitOverride(D) {
      return D ? this._exitCallback = D : this._exitCallback = (h) => {
        if (h.code !== "commander.executeSubCommandAsync")
          throw h;
      }, this;
    }
    /**
     * Call process.exit, and _exitCallback if defined.
     *
     * @param {number} exitCode exit code for using with process.exit
     * @param {string} code an id string representing the error
     * @param {string} message human-readable description of the error
     * @return never
     * @private
     */
    _exit(D, h, d) {
      this._exitCallback && this._exitCallback(new s(D, h, d)), n.exit(D);
    }
    /**
     * Register callback `fn` for the command.
     *
     * @example
     * program
     *   .command('serve')
     *   .description('start service')
     *   .action(function() {
     *      // do work here
     *   });
     *
     * @param {Function} fn
     * @return {Command} `this` command for chaining
     */
    action(D) {
      const h = (d) => {
        const F = this.registeredArguments.length, m = d.slice(0, F);
        return this._storeOptionsAsProperties ? m[F] = this : m[F] = this.opts(), m.push(this), D.apply(this, m);
      };
      return this._actionHandler = h, this;
    }
    /**
     * Factory routine to create a new unattached option.
     *
     * See .option() for creating an attached option, which uses this routine to
     * create the option. You can override createOption to return a custom option.
     *
     * @param {string} flags
     * @param {string} [description]
     * @return {Option} new option
     */
    createOption(D, h) {
      return new c(D, h);
    }
    /**
     * Wrap parseArgs to catch 'commander.invalidArgument'.
     *
     * @param {(Option | Argument)} target
     * @param {string} value
     * @param {*} previous
     * @param {string} invalidArgumentMessage
     * @private
     */
    _callParseArg(D, h, d, F) {
      try {
        return D.parseArg(h, d);
      } catch (m) {
        if (m.code === "commander.invalidArgument") {
          const E = `${F} ${m.message}`;
          this.error(E, { exitCode: m.exitCode, code: m.code });
        }
        throw m;
      }
    }
    /**
     * Check for option flag conflicts.
     * Register option if no conflicts found, or throw on conflict.
     *
     * @param {Option} option
     * @private
     */
    _registerOption(D) {
      const h = D.short && this._findOption(D.short) || D.long && this._findOption(D.long);
      if (h) {
        const d = D.long && this._findOption(D.long) ? D.long : D.short;
        throw new Error(`Cannot add option '${D.flags}'${this._name && ` to command '${this._name}'`} due to conflicting flag '${d}'
-  already used by option '${h.flags}'`);
      }
      this._initOptionGroup(D), this.options.push(D);
    }
    /**
     * Check for command name and alias conflicts with existing commands.
     * Register command if no conflicts found, or throw on conflict.
     *
     * @param {Command} command
     * @private
     */
    _registerCommand(D) {
      const h = (F) => [F.name()].concat(F.aliases()), d = h(D).find(
        (F) => this._findCommand(F)
      );
      if (d) {
        const F = h(this._findCommand(d)).join("|"), m = h(D).join("|");
        throw new Error(
          `cannot add command '${m}' as already have command '${F}'`
        );
      }
      this._initCommandGroup(D), this.commands.push(D);
    }
    /**
     * Add an option.
     *
     * @param {Option} option
     * @return {Command} `this` command for chaining
     */
    addOption(D) {
      this._registerOption(D);
      const h = D.name(), d = D.attributeName();
      if (D.negate) {
        const m = D.long.replace(/^--no-/, "--");
        this._findOption(m) || this.setOptionValueWithSource(
          d,
          D.defaultValue === void 0 ? !0 : D.defaultValue,
          "default"
        );
      } else D.defaultValue !== void 0 && this.setOptionValueWithSource(d, D.defaultValue, "default");
      const F = (m, E, w) => {
        m == null && D.presetArg !== void 0 && (m = D.presetArg);
        const v = this.getOptionValue(d);
        m !== null && D.parseArg ? m = this._callParseArg(D, m, v, E) : m !== null && D.variadic && (m = D._concatValue(m, v)), m == null && (D.negate ? m = !1 : D.isBoolean() || D.optional ? m = !0 : m = ""), this.setOptionValueWithSource(d, m, w);
      };
      return this.on("option:" + h, (m) => {
        const E = `error: option '${D.flags}' argument '${m}' is invalid.`;
        F(m, E, "cli");
      }), D.envVar && this.on("optionEnv:" + h, (m) => {
        const E = `error: option '${D.flags}' value '${m}' from env '${D.envVar}' is invalid.`;
        F(m, E, "env");
      }), this;
    }
    /**
     * Internal implementation shared by .option() and .requiredOption()
     *
     * @return {Command} `this` command for chaining
     * @private
     */
    _optionEx(D, h, d, F, m) {
      if (typeof h == "object" && h instanceof c)
        throw new Error(
          "To add an Option object use addOption() instead of option() or requiredOption()"
        );
      const E = this.createOption(h, d);
      if (E.makeOptionMandatory(!!D.mandatory), typeof F == "function")
        E.default(m).argParser(F);
      else if (F instanceof RegExp) {
        const w = F;
        F = (v, B) => {
          const A = w.exec(v);
          return A ? A[0] : B;
        }, E.default(m).argParser(F);
      } else
        E.default(F);
      return this.addOption(E);
    }
    /**
     * Define option with `flags`, `description`, and optional argument parsing function or `defaultValue` or both.
     *
     * The `flags` string contains the short and/or long flags, separated by comma, a pipe or space. A required
     * option-argument is indicated by `<>` and an optional option-argument by `[]`.
     *
     * See the README for more details, and see also addOption() and requiredOption().
     *
     * @example
     * program
     *     .option('-p, --pepper', 'add pepper')
     *     .option('--pt, --pizza-type <TYPE>', 'type of pizza') // required option-argument
     *     .option('-c, --cheese [CHEESE]', 'add extra cheese', 'mozzarella') // optional option-argument with default
     *     .option('-t, --tip <VALUE>', 'add tip to purchase cost', parseFloat) // custom parse function
     *
     * @param {string} flags
     * @param {string} [description]
     * @param {(Function|*)} [parseArg] - custom option processing function or default value
     * @param {*} [defaultValue]
     * @return {Command} `this` command for chaining
     */
    option(D, h, d, F) {
      return this._optionEx({}, D, h, d, F);
    }
    /**
     * Add a required option which must have a value after parsing. This usually means
     * the option must be specified on the command line. (Otherwise the same as .option().)
     *
     * The `flags` string contains the short and/or long flags, separated by comma, a pipe or space.
     *
     * @param {string} flags
     * @param {string} [description]
     * @param {(Function|*)} [parseArg] - custom option processing function or default value
     * @param {*} [defaultValue]
     * @return {Command} `this` command for chaining
     */
    requiredOption(D, h, d, F) {
      return this._optionEx(
        { mandatory: !0 },
        D,
        h,
        d,
        F
      );
    }
    /**
     * Alter parsing of short flags with optional values.
     *
     * @example
     * // for `.option('-f,--flag [value]'):
     * program.combineFlagAndOptionalValue(true);  // `-f80` is treated like `--flag=80`, this is the default behaviour
     * program.combineFlagAndOptionalValue(false) // `-fb` is treated like `-f -b`
     *
     * @param {boolean} [combine] - if `true` or omitted, an optional value can be specified directly after the flag.
     * @return {Command} `this` command for chaining
     */
    combineFlagAndOptionalValue(D = !0) {
      return this._combineFlagAndOptionalValue = !!D, this;
    }
    /**
     * Allow unknown options on the command line.
     *
     * @param {boolean} [allowUnknown] - if `true` or omitted, no error will be thrown for unknown options.
     * @return {Command} `this` command for chaining
     */
    allowUnknownOption(D = !0) {
      return this._allowUnknownOption = !!D, this;
    }
    /**
     * Allow excess command-arguments on the command line. Pass false to make excess arguments an error.
     *
     * @param {boolean} [allowExcess] - if `true` or omitted, no error will be thrown for excess arguments.
     * @return {Command} `this` command for chaining
     */
    allowExcessArguments(D = !0) {
      return this._allowExcessArguments = !!D, this;
    }
    /**
     * Enable positional options. Positional means global options are specified before subcommands which lets
     * subcommands reuse the same option names, and also enables subcommands to turn on passThroughOptions.
     * The default behaviour is non-positional and global options may appear anywhere on the command line.
     *
     * @param {boolean} [positional]
     * @return {Command} `this` command for chaining
     */
    enablePositionalOptions(D = !0) {
      return this._enablePositionalOptions = !!D, this;
    }
    /**
     * Pass through options that come after command-arguments rather than treat them as command-options,
     * so actual command-options come before command-arguments. Turning this on for a subcommand requires
     * positional options to have been enabled on the program (parent commands).
     * The default behaviour is non-positional and options may appear before or after command-arguments.
     *
     * @param {boolean} [passThrough] for unknown options.
     * @return {Command} `this` command for chaining
     */
    passThroughOptions(D = !0) {
      return this._passThroughOptions = !!D, this._checkForBrokenPassThrough(), this;
    }
    /**
     * @private
     */
    _checkForBrokenPassThrough() {
      if (this.parent && this._passThroughOptions && !this.parent._enablePositionalOptions)
        throw new Error(
          `passThroughOptions cannot be used for '${this._name}' without turning on enablePositionalOptions for parent command(s)`
        );
    }
    /**
     * Whether to store option values as properties on command object,
     * or store separately (specify false). In both cases the option values can be accessed using .opts().
     *
     * @param {boolean} [storeAsProperties=true]
     * @return {Command} `this` command for chaining
     */
    storeOptionsAsProperties(D = !0) {
      if (this.options.length)
        throw new Error("call .storeOptionsAsProperties() before adding options");
      if (Object.keys(this._optionValues).length)
        throw new Error(
          "call .storeOptionsAsProperties() before setting option values"
        );
      return this._storeOptionsAsProperties = !!D, this;
    }
    /**
     * Retrieve option value.
     *
     * @param {string} key
     * @return {object} value
     */
    getOptionValue(D) {
      return this._storeOptionsAsProperties ? this[D] : this._optionValues[D];
    }
    /**
     * Store option value.
     *
     * @param {string} key
     * @param {object} value
     * @return {Command} `this` command for chaining
     */
    setOptionValue(D, h) {
      return this.setOptionValueWithSource(D, h, void 0);
    }
    /**
     * Store option value and where the value came from.
     *
     * @param {string} key
     * @param {object} value
     * @param {string} source - expected values are default/config/env/cli/implied
     * @return {Command} `this` command for chaining
     */
    setOptionValueWithSource(D, h, d) {
      return this._storeOptionsAsProperties ? this[D] = h : this._optionValues[D] = h, this._optionValueSources[D] = d, this;
    }
    /**
     * Get source of option value.
     * Expected values are default | config | env | cli | implied
     *
     * @param {string} key
     * @return {string}
     */
    getOptionValueSource(D) {
      return this._optionValueSources[D];
    }
    /**
     * Get source of option value. See also .optsWithGlobals().
     * Expected values are default | config | env | cli | implied
     *
     * @param {string} key
     * @return {string}
     */
    getOptionValueSourceWithGlobals(D) {
      let h;
      return this._getCommandAndAncestors().forEach((d) => {
        d.getOptionValueSource(D) !== void 0 && (h = d.getOptionValueSource(D));
      }), h;
    }
    /**
     * Get user arguments from implied or explicit arguments.
     * Side-effects: set _scriptPath if args included script. Used for default program name, and subcommand searches.
     *
     * @private
     */
    _prepareUserArgs(D, h) {
      if (D !== void 0 && !Array.isArray(D))
        throw new Error("first parameter to parse must be array or undefined");
      if (h = h || {}, D === void 0 && h.from === void 0) {
        n.versions?.electron && (h.from = "electron");
        const F = n.execArgv ?? [];
        (F.includes("-e") || F.includes("--eval") || F.includes("-p") || F.includes("--print")) && (h.from = "eval");
      }
      D === void 0 && (D = n.argv), this.rawArgs = D.slice();
      let d;
      switch (h.from) {
        case void 0:
        case "node":
          this._scriptPath = D[1], d = D.slice(2);
          break;
        case "electron":
          n.defaultApp ? (this._scriptPath = D[1], d = D.slice(2)) : d = D.slice(1);
          break;
        case "user":
          d = D.slice(0);
          break;
        case "eval":
          d = D.slice(1);
          break;
        default:
          throw new Error(
            `unexpected parse option { from: '${h.from}' }`
          );
      }
      return !this._name && this._scriptPath && this.nameFromFilename(this._scriptPath), this._name = this._name || "program", d;
    }
    /**
     * Parse `argv`, setting options and invoking commands when defined.
     *
     * Use parseAsync instead of parse if any of your action handlers are async.
     *
     * Call with no parameters to parse `process.argv`. Detects Electron and special node options like `node --eval`. Easy mode!
     *
     * Or call with an array of strings to parse, and optionally where the user arguments start by specifying where the arguments are `from`:
     * - `'node'`: default, `argv[0]` is the application and `argv[1]` is the script being run, with user arguments after that
     * - `'electron'`: `argv[0]` is the application and `argv[1]` varies depending on whether the electron application is packaged
     * - `'user'`: just user arguments
     *
     * @example
     * program.parse(); // parse process.argv and auto-detect electron and special node flags
     * program.parse(process.argv); // assume argv[0] is app and argv[1] is script
     * program.parse(my-args, { from: 'user' }); // just user supplied arguments, nothing special about argv[0]
     *
     * @param {string[]} [argv] - optional, defaults to process.argv
     * @param {object} [parseOptions] - optionally specify style of options with from: node/user/electron
     * @param {string} [parseOptions.from] - where the args are from: 'node', 'user', 'electron'
     * @return {Command} `this` command for chaining
     */
    parse(D, h) {
      this._prepareForParse();
      const d = this._prepareUserArgs(D, h);
      return this._parseCommand([], d), this;
    }
    /**
     * Parse `argv`, setting options and invoking commands when defined.
     *
     * Call with no parameters to parse `process.argv`. Detects Electron and special node options like `node --eval`. Easy mode!
     *
     * Or call with an array of strings to parse, and optionally where the user arguments start by specifying where the arguments are `from`:
     * - `'node'`: default, `argv[0]` is the application and `argv[1]` is the script being run, with user arguments after that
     * - `'electron'`: `argv[0]` is the application and `argv[1]` varies depending on whether the electron application is packaged
     * - `'user'`: just user arguments
     *
     * @example
     * await program.parseAsync(); // parse process.argv and auto-detect electron and special node flags
     * await program.parseAsync(process.argv); // assume argv[0] is app and argv[1] is script
     * await program.parseAsync(my-args, { from: 'user' }); // just user supplied arguments, nothing special about argv[0]
     *
     * @param {string[]} [argv]
     * @param {object} [parseOptions]
     * @param {string} parseOptions.from - where the args are from: 'node', 'user', 'electron'
     * @return {Promise}
     */
    async parseAsync(D, h) {
      this._prepareForParse();
      const d = this._prepareUserArgs(D, h);
      return await this._parseCommand([], d), this;
    }
    _prepareForParse() {
      this._savedState === null ? this.saveStateBeforeParse() : this.restoreStateBeforeParse();
    }
    /**
     * Called the first time parse is called to save state and allow a restore before subsequent calls to parse.
     * Not usually called directly, but available for subclasses to save their custom state.
     *
     * This is called in a lazy way. Only commands used in parsing chain will have state saved.
     */
    saveStateBeforeParse() {
      this._savedState = {
        // name is stable if supplied by author, but may be unspecified for root command and deduced during parsing
        _name: this._name,
        // option values before parse have default values (including false for negated options)
        // shallow clones
        _optionValues: { ...this._optionValues },
        _optionValueSources: { ...this._optionValueSources }
      };
    }
    /**
     * Restore state before parse for calls after the first.
     * Not usually called directly, but available for subclasses to save their custom state.
     *
     * This is called in a lazy way. Only commands used in parsing chain will have state restored.
     */
    restoreStateBeforeParse() {
      if (this._storeOptionsAsProperties)
        throw new Error(`Can not call parse again when storeOptionsAsProperties is true.
- either make a new Command for each call to parse, or stop storing options as properties`);
      this._name = this._savedState._name, this._scriptPath = null, this.rawArgs = [], this._optionValues = { ...this._savedState._optionValues }, this._optionValueSources = { ...this._savedState._optionValueSources }, this.args = [], this.processedArgs = [];
    }
    /**
     * Throw if expected executable is missing. Add lots of help for author.
     *
     * @param {string} executableFile
     * @param {string} executableDir
     * @param {string} subcommandName
     */
    _checkForMissingExecutable(D, h, d) {
      if (o.existsSync(D)) return;
      const F = h ? `searched for local subcommand relative to directory '${h}'` : "no directory for search for local subcommand, use .executableDir() to supply a custom directory", m = `'${D}' does not exist
 - if '${d}' is not meant to be an executable command, remove description parameter from '.command()' and use '.description()' instead
 - if the default executable name is not suitable, use the executableFile option to supply a custom name or path
 - ${F}`;
      throw new Error(m);
    }
    /**
     * Execute a sub-command executable.
     *
     * @private
     */
    _executeSubCommand(D, h) {
      h = h.slice();
      let d = !1;
      const F = [".js", ".ts", ".tsx", ".mjs", ".cjs"];
      function m(A, x) {
        const le = u.resolve(A, x);
        if (o.existsSync(le)) return le;
        if (F.includes(u.extname(x))) return;
        const yt = F.find(
          (fu) => o.existsSync(`${le}${fu}`)
        );
        if (yt) return `${le}${yt}`;
      }
      this._checkForMissingMandatoryOptions(), this._checkForConflictingOptions();
      let E = D._executableFile || `${this._name}-${D._name}`, w = this._executableDir || "";
      if (this._scriptPath) {
        let A;
        try {
          A = o.realpathSync(this._scriptPath);
        } catch {
          A = this._scriptPath;
        }
        w = u.resolve(
          u.dirname(A),
          w
        );
      }
      if (w) {
        let A = m(w, E);
        if (!A && !D._executableFile && this._scriptPath) {
          const x = u.basename(
            this._scriptPath,
            u.extname(this._scriptPath)
          );
          x !== this._name && (A = m(
            w,
            `${x}-${D._name}`
          ));
        }
        E = A || E;
      }
      d = F.includes(u.extname(E));
      let v;
      n.platform !== "win32" ? d ? (h.unshift(E), h = C(n.execArgv).concat(h), v = t.spawn(n.argv[0], h, { stdio: "inherit" })) : v = t.spawn(E, h, { stdio: "inherit" }) : (this._checkForMissingExecutable(
        E,
        w,
        D._name
      ), h.unshift(E), h = C(n.execArgv).concat(h), v = t.spawn(n.execPath, h, { stdio: "inherit" })), v.killed || ["SIGUSR1", "SIGUSR2", "SIGTERM", "SIGINT", "SIGHUP"].forEach((x) => {
        n.on(x, () => {
          v.killed === !1 && v.exitCode === null && v.kill(x);
        });
      });
      const B = this._exitCallback;
      v.on("close", (A) => {
        A = A ?? 1, B ? B(
          new s(
            A,
            "commander.executeSubCommandAsync",
            "(close)"
          )
        ) : n.exit(A);
      }), v.on("error", (A) => {
        if (A.code === "ENOENT")
          this._checkForMissingExecutable(
            E,
            w,
            D._name
          );
        else if (A.code === "EACCES")
          throw new Error(`'${E}' not executable`);
        if (!B)
          n.exit(1);
        else {
          const x = new s(
            1,
            "commander.executeSubCommandAsync",
            "(error)"
          );
          x.nestedError = A, B(x);
        }
      }), this.runningCommand = v;
    }
    /**
     * @private
     */
    _dispatchSubcommand(D, h, d) {
      const F = this._findCommand(D);
      F || this.help({ error: !0 }), F._prepareForParse();
      let m;
      return m = this._chainOrCallSubCommandHook(
        m,
        F,
        "preSubcommand"
      ), m = this._chainOrCall(m, () => {
        if (F._executableHandler)
          this._executeSubCommand(F, h.concat(d));
        else
          return F._parseCommand(h, d);
      }), m;
    }
    /**
     * Invoke help directly if possible, or dispatch if necessary.
     * e.g. help foo
     *
     * @private
     */
    _dispatchHelpCommand(D) {
      D || this.help();
      const h = this._findCommand(D);
      return h && !h._executableHandler && h.help(), this._dispatchSubcommand(
        D,
        [],
        [this._getHelpOption()?.long ?? this._getHelpOption()?.short ?? "--help"]
      );
    }
    /**
     * Check this.args against expected this.registeredArguments.
     *
     * @private
     */
    _checkNumberOfArguments() {
      this.registeredArguments.forEach((D, h) => {
        D.required && this.args[h] == null && this.missingArgument(D.name());
      }), !(this.registeredArguments.length > 0 && this.registeredArguments[this.registeredArguments.length - 1].variadic) && this.args.length > this.registeredArguments.length && this._excessArguments(this.args);
    }
    /**
     * Process this.args using this.registeredArguments and save as this.processedArgs!
     *
     * @private
     */
    _processArguments() {
      const D = (d, F, m) => {
        let E = F;
        if (F !== null && d.parseArg) {
          const w = `error: command-argument value '${F}' is invalid for argument '${d.name()}'.`;
          E = this._callParseArg(
            d,
            F,
            m,
            w
          );
        }
        return E;
      };
      this._checkNumberOfArguments();
      const h = [];
      this.registeredArguments.forEach((d, F) => {
        let m = d.defaultValue;
        d.variadic ? F < this.args.length ? (m = this.args.slice(F), d.parseArg && (m = m.reduce((E, w) => D(d, w, E), d.defaultValue))) : m === void 0 && (m = []) : F < this.args.length && (m = this.args[F], d.parseArg && (m = D(d, m, d.defaultValue))), h[F] = m;
      }), this.processedArgs = h;
    }
    /**
     * Once we have a promise we chain, but call synchronously until then.
     *
     * @param {(Promise|undefined)} promise
     * @param {Function} fn
     * @return {(Promise|undefined)}
     * @private
     */
    _chainOrCall(D, h) {
      return D && D.then && typeof D.then == "function" ? D.then(() => h()) : h();
    }
    /**
     *
     * @param {(Promise|undefined)} promise
     * @param {string} event
     * @return {(Promise|undefined)}
     * @private
     */
    _chainOrCallHooks(D, h) {
      let d = D;
      const F = [];
      return this._getCommandAndAncestors().reverse().filter((m) => m._lifeCycleHooks[h] !== void 0).forEach((m) => {
        m._lifeCycleHooks[h].forEach((E) => {
          F.push({ hookedCommand: m, callback: E });
        });
      }), h === "postAction" && F.reverse(), F.forEach((m) => {
        d = this._chainOrCall(d, () => m.callback(m.hookedCommand, this));
      }), d;
    }
    /**
     *
     * @param {(Promise|undefined)} promise
     * @param {Command} subCommand
     * @param {string} event
     * @return {(Promise|undefined)}
     * @private
     */
    _chainOrCallSubCommandHook(D, h, d) {
      let F = D;
      return this._lifeCycleHooks[d] !== void 0 && this._lifeCycleHooks[d].forEach((m) => {
        F = this._chainOrCall(F, () => m(this, h));
      }), F;
    }
    /**
     * Process arguments in context of this command.
     * Returns action result, in case it is a promise.
     *
     * @private
     */
    _parseCommand(D, h) {
      const d = this.parseOptions(h);
      if (this._parseOptionsEnv(), this._parseOptionsImplied(), D = D.concat(d.operands), h = d.unknown, this.args = D.concat(h), D && this._findCommand(D[0]))
        return this._dispatchSubcommand(D[0], D.slice(1), h);
      if (this._getHelpCommand() && D[0] === this._getHelpCommand().name())
        return this._dispatchHelpCommand(D[1]);
      if (this._defaultCommandName)
        return this._outputHelpIfRequested(h), this._dispatchSubcommand(
          this._defaultCommandName,
          D,
          h
        );
      this.commands.length && this.args.length === 0 && !this._actionHandler && !this._defaultCommandName && this.help({ error: !0 }), this._outputHelpIfRequested(d.unknown), this._checkForMissingMandatoryOptions(), this._checkForConflictingOptions();
      const F = () => {
        d.unknown.length > 0 && this.unknownOption(d.unknown[0]);
      }, m = `command:${this.name()}`;
      if (this._actionHandler) {
        F(), this._processArguments();
        let E;
        return E = this._chainOrCallHooks(E, "preAction"), E = this._chainOrCall(
          E,
          () => this._actionHandler(this.processedArgs)
        ), this.parent && (E = this._chainOrCall(E, () => {
          this.parent.emit(m, D, h);
        })), E = this._chainOrCallHooks(E, "postAction"), E;
      }
      if (this.parent && this.parent.listenerCount(m))
        F(), this._processArguments(), this.parent.emit(m, D, h);
      else if (D.length) {
        if (this._findCommand("*"))
          return this._dispatchSubcommand("*", D, h);
        this.listenerCount("command:*") ? this.emit("command:*", D, h) : this.commands.length ? this.unknownCommand() : (F(), this._processArguments());
      } else this.commands.length ? (F(), this.help({ error: !0 })) : (F(), this._processArguments());
    }
    /**
     * Find matching command.
     *
     * @private
     * @return {Command | undefined}
     */
    _findCommand(D) {
      if (D)
        return this.commands.find(
          (h) => h._name === D || h._aliases.includes(D)
        );
    }
    /**
     * Return an option matching `arg` if any.
     *
     * @param {string} arg
     * @return {Option}
     * @package
     */
    _findOption(D) {
      return this.options.find((h) => h.is(D));
    }
    /**
     * Display an error message if a mandatory option does not have a value.
     * Called after checking for help flags in leaf subcommand.
     *
     * @private
     */
    _checkForMissingMandatoryOptions() {
      this._getCommandAndAncestors().forEach((D) => {
        D.options.forEach((h) => {
          h.mandatory && D.getOptionValue(h.attributeName()) === void 0 && D.missingMandatoryOptionValue(h);
        });
      });
    }
    /**
     * Display an error message if conflicting options are used together in this.
     *
     * @private
     */
    _checkForConflictingLocalOptions() {
      const D = this.options.filter((d) => {
        const F = d.attributeName();
        return this.getOptionValue(F) === void 0 ? !1 : this.getOptionValueSource(F) !== "default";
      });
      D.filter(
        (d) => d.conflictsWith.length > 0
      ).forEach((d) => {
        const F = D.find(
          (m) => d.conflictsWith.includes(m.attributeName())
        );
        F && this._conflictingOption(d, F);
      });
    }
    /**
     * Display an error message if conflicting options are used together.
     * Called after checking for help flags in leaf subcommand.
     *
     * @private
     */
    _checkForConflictingOptions() {
      this._getCommandAndAncestors().forEach((D) => {
        D._checkForConflictingLocalOptions();
      });
    }
    /**
     * Parse options from `argv` removing known options,
     * and return argv split into operands and unknown arguments.
     *
     * Side effects: modifies command by storing options. Does not reset state if called again.
     *
     * Examples:
     *
     *     argv => operands, unknown
     *     --known kkk op => [op], []
     *     op --known kkk => [op], []
     *     sub --unknown uuu op => [sub], [--unknown uuu op]
     *     sub -- --unknown uuu op => [sub --unknown uuu op], []
     *
     * @param {string[]} argv
     * @return {{operands: string[], unknown: string[]}}
     */
    parseOptions(D) {
      const h = [], d = [];
      let F = h;
      const m = D.slice();
      function E(B) {
        return B.length > 1 && B[0] === "-";
      }
      const w = (B) => /^-\d*\.?\d+(e[+-]?\d+)?$/.test(B) ? !this._getCommandAndAncestors().some(
        (A) => A.options.map((x) => x.short).some((x) => /^-\d$/.test(x))
      ) : !1;
      let v = null;
      for (; m.length; ) {
        const B = m.shift();
        if (B === "--") {
          F === d && F.push(B), F.push(...m);
          break;
        }
        if (v && (!E(B) || w(B))) {
          this.emit(`option:${v.name()}`, B);
          continue;
        }
        if (v = null, E(B)) {
          const A = this._findOption(B);
          if (A) {
            if (A.required) {
              const x = m.shift();
              x === void 0 && this.optionMissingArgument(A), this.emit(`option:${A.name()}`, x);
            } else if (A.optional) {
              let x = null;
              m.length > 0 && (!E(m[0]) || w(m[0])) && (x = m.shift()), this.emit(`option:${A.name()}`, x);
            } else
              this.emit(`option:${A.name()}`);
            v = A.variadic ? A : null;
            continue;
          }
        }
        if (B.length > 2 && B[0] === "-" && B[1] !== "-") {
          const A = this._findOption(`-${B[1]}`);
          if (A) {
            A.required || A.optional && this._combineFlagAndOptionalValue ? this.emit(`option:${A.name()}`, B.slice(2)) : (this.emit(`option:${A.name()}`), m.unshift(`-${B.slice(2)}`));
            continue;
          }
        }
        if (/^--[^=]+=/.test(B)) {
          const A = B.indexOf("="), x = this._findOption(B.slice(0, A));
          if (x && (x.required || x.optional)) {
            this.emit(`option:${x.name()}`, B.slice(A + 1));
            continue;
          }
        }
        if (F === h && E(B) && !(this.commands.length === 0 && w(B)) && (F = d), (this._enablePositionalOptions || this._passThroughOptions) && h.length === 0 && d.length === 0) {
          if (this._findCommand(B)) {
            h.push(B), m.length > 0 && d.push(...m);
            break;
          } else if (this._getHelpCommand() && B === this._getHelpCommand().name()) {
            h.push(B), m.length > 0 && h.push(...m);
            break;
          } else if (this._defaultCommandName) {
            d.push(B), m.length > 0 && d.push(...m);
            break;
          }
        }
        if (this._passThroughOptions) {
          F.push(B), m.length > 0 && F.push(...m);
          break;
        }
        F.push(B);
      }
      return { operands: h, unknown: d };
    }
    /**
     * Return an object containing local option values as key-value pairs.
     *
     * @return {object}
     */
    opts() {
      if (this._storeOptionsAsProperties) {
        const D = {}, h = this.options.length;
        for (let d = 0; d < h; d++) {
          const F = this.options[d].attributeName();
          D[F] = F === this._versionOptionName ? this._version : this[F];
        }
        return D;
      }
      return this._optionValues;
    }
    /**
     * Return an object containing merged local and global option values as key-value pairs.
     *
     * @return {object}
     */
    optsWithGlobals() {
      return this._getCommandAndAncestors().reduce(
        (D, h) => Object.assign(D, h.opts()),
        {}
      );
    }
    /**
     * Display error message and exit (or call exitOverride).
     *
     * @param {string} message
     * @param {object} [errorOptions]
     * @param {string} [errorOptions.code] - an id string representing the error
     * @param {number} [errorOptions.exitCode] - used with process.exit
     */
    error(D, h) {
      this._outputConfiguration.outputError(
        `${D}
`,
        this._outputConfiguration.writeErr
      ), typeof this._showHelpAfterError == "string" ? this._outputConfiguration.writeErr(`${this._showHelpAfterError}
`) : this._showHelpAfterError && (this._outputConfiguration.writeErr(`
`), this.outputHelp({ error: !0 }));
      const d = h || {}, F = d.exitCode || 1, m = d.code || "commander.error";
      this._exit(F, m, D);
    }
    /**
     * Apply any option related environment variables, if option does
     * not have a value from cli or client code.
     *
     * @private
     */
    _parseOptionsEnv() {
      this.options.forEach((D) => {
        if (D.envVar && D.envVar in n.env) {
          const h = D.attributeName();
          (this.getOptionValue(h) === void 0 || ["default", "config", "env"].includes(
            this.getOptionValueSource(h)
          )) && (D.required || D.optional ? this.emit(`optionEnv:${D.name()}`, n.env[D.envVar]) : this.emit(`optionEnv:${D.name()}`));
        }
      });
    }
    /**
     * Apply any implied option values, if option is undefined or default value.
     *
     * @private
     */
    _parseOptionsImplied() {
      const D = new f(this.options), h = (d) => this.getOptionValue(d) !== void 0 && !["default", "implied"].includes(this.getOptionValueSource(d));
      this.options.filter(
        (d) => d.implied !== void 0 && h(d.attributeName()) && D.valueFromOption(
          this.getOptionValue(d.attributeName()),
          d
        )
      ).forEach((d) => {
        Object.keys(d.implied).filter((F) => !h(F)).forEach((F) => {
          this.setOptionValueWithSource(
            F,
            d.implied[F],
            "implied"
          );
        });
      });
    }
    /**
     * Argument `name` is missing.
     *
     * @param {string} name
     * @private
     */
    missingArgument(D) {
      const h = `error: missing required argument '${D}'`;
      this.error(h, { code: "commander.missingArgument" });
    }
    /**
     * `Option` is missing an argument.
     *
     * @param {Option} option
     * @private
     */
    optionMissingArgument(D) {
      const h = `error: option '${D.flags}' argument missing`;
      this.error(h, { code: "commander.optionMissingArgument" });
    }
    /**
     * `Option` does not have a value, and is a mandatory option.
     *
     * @param {Option} option
     * @private
     */
    missingMandatoryOptionValue(D) {
      const h = `error: required option '${D.flags}' not specified`;
      this.error(h, { code: "commander.missingMandatoryOptionValue" });
    }
    /**
     * `Option` conflicts with another option.
     *
     * @param {Option} option
     * @param {Option} conflictingOption
     * @private
     */
    _conflictingOption(D, h) {
      const d = (E) => {
        const w = E.attributeName(), v = this.getOptionValue(w), B = this.options.find(
          (x) => x.negate && w === x.attributeName()
        ), A = this.options.find(
          (x) => !x.negate && w === x.attributeName()
        );
        return B && (B.presetArg === void 0 && v === !1 || B.presetArg !== void 0 && v === B.presetArg) ? B : A || E;
      }, F = (E) => {
        const w = d(E), v = w.attributeName();
        return this.getOptionValueSource(v) === "env" ? `environment variable '${w.envVar}'` : `option '${w.flags}'`;
      }, m = `error: ${F(D)} cannot be used with ${F(h)}`;
      this.error(m, { code: "commander.conflictingOption" });
    }
    /**
     * Unknown option `flag`.
     *
     * @param {string} flag
     * @private
     */
    unknownOption(D) {
      if (this._allowUnknownOption) return;
      let h = "";
      if (D.startsWith("--") && this._showSuggestionAfterError) {
        let F = [], m = this;
        do {
          const E = m.createHelp().visibleOptions(m).filter((w) => w.long).map((w) => w.long);
          F = F.concat(E), m = m.parent;
        } while (m && !m._enablePositionalOptions);
        h = p(D, F);
      }
      const d = `error: unknown option '${D}'${h}`;
      this.error(d, { code: "commander.unknownOption" });
    }
    /**
     * Excess arguments, more than expected.
     *
     * @param {string[]} receivedArgs
     * @private
     */
    _excessArguments(D) {
      if (this._allowExcessArguments) return;
      const h = this.registeredArguments.length, d = h === 1 ? "" : "s", m = `error: too many arguments${this.parent ? ` for '${this.name()}'` : ""}. Expected ${h} argument${d} but got ${D.length}.`;
      this.error(m, { code: "commander.excessArguments" });
    }
    /**
     * Unknown command.
     *
     * @private
     */
    unknownCommand() {
      const D = this.args[0];
      let h = "";
      if (this._showSuggestionAfterError) {
        const F = [];
        this.createHelp().visibleCommands(this).forEach((m) => {
          F.push(m.name()), m.alias() && F.push(m.alias());
        }), h = p(D, F);
      }
      const d = `error: unknown command '${D}'${h}`;
      this.error(d, { code: "commander.unknownCommand" });
    }
    /**
     * Get or set the program version.
     *
     * This method auto-registers the "-V, --version" option which will print the version number.
     *
     * You can optionally supply the flags and description to override the defaults.
     *
     * @param {string} [str]
     * @param {string} [flags]
     * @param {string} [description]
     * @return {(this | string | undefined)} `this` command for chaining, or version string if no arguments
     */
    version(D, h, d) {
      if (D === void 0) return this._version;
      this._version = D, h = h || "-V, --version", d = d || "output the version number";
      const F = this.createOption(h, d);
      return this._versionOptionName = F.attributeName(), this._registerOption(F), this.on("option:" + F.name(), () => {
        this._outputConfiguration.writeOut(`${D}
`), this._exit(0, "commander.version", D);
      }), this;
    }
    /**
     * Set the description.
     *
     * @param {string} [str]
     * @param {object} [argsDescription]
     * @return {(string|Command)}
     */
    description(D, h) {
      return D === void 0 && h === void 0 ? this._description : (this._description = D, h && (this._argsDescription = h), this);
    }
    /**
     * Set the summary. Used when listed as subcommand of parent.
     *
     * @param {string} [str]
     * @return {(string|Command)}
     */
    summary(D) {
      return D === void 0 ? this._summary : (this._summary = D, this);
    }
    /**
     * Set an alias for the command.
     *
     * You may call more than once to add multiple aliases. Only the first alias is shown in the auto-generated help.
     *
     * @param {string} [alias]
     * @return {(string|Command)}
     */
    alias(D) {
      if (D === void 0) return this._aliases[0];
      let h = this;
      if (this.commands.length !== 0 && this.commands[this.commands.length - 1]._executableHandler && (h = this.commands[this.commands.length - 1]), D === h._name)
        throw new Error("Command alias can't be the same as its name");
      const d = this.parent?._findCommand(D);
      if (d) {
        const F = [d.name()].concat(d.aliases()).join("|");
        throw new Error(
          `cannot add alias '${D}' to command '${this.name()}' as already have command '${F}'`
        );
      }
      return h._aliases.push(D), this;
    }
    /**
     * Set aliases for the command.
     *
     * Only the first alias is shown in the auto-generated help.
     *
     * @param {string[]} [aliases]
     * @return {(string[]|Command)}
     */
    aliases(D) {
      return D === void 0 ? this._aliases : (D.forEach((h) => this.alias(h)), this);
    }
    /**
     * Set / get the command usage `str`.
     *
     * @param {string} [str]
     * @return {(string|Command)}
     */
    usage(D) {
      if (D === void 0) {
        if (this._usage) return this._usage;
        const h = this.registeredArguments.map((d) => r(d));
        return [].concat(
          this.options.length || this._helpOption !== null ? "[options]" : [],
          this.commands.length ? "[command]" : [],
          this.registeredArguments.length ? h : []
        ).join(" ");
      }
      return this._usage = D, this;
    }
    /**
     * Get or set the name of the command.
     *
     * @param {string} [str]
     * @return {(string|Command)}
     */
    name(D) {
      return D === void 0 ? this._name : (this._name = D, this);
    }
    /**
     * Set/get the help group heading for this subcommand in parent command's help.
     *
     * @param {string} [heading]
     * @return {Command | string}
     */
    helpGroup(D) {
      return D === void 0 ? this._helpGroupHeading ?? "" : (this._helpGroupHeading = D, this);
    }
    /**
     * Set/get the default help group heading for subcommands added to this command.
     * (This does not override a group set directly on the subcommand using .helpGroup().)
     *
     * @example
     * program.commandsGroup('Development Commands:);
     * program.command('watch')...
     * program.command('lint')...
     * ...
     *
     * @param {string} [heading]
     * @returns {Command | string}
     */
    commandsGroup(D) {
      return D === void 0 ? this._defaultCommandGroup ?? "" : (this._defaultCommandGroup = D, this);
    }
    /**
     * Set/get the default help group heading for options added to this command.
     * (This does not override a group set directly on the option using .helpGroup().)
     *
     * @example
     * program
     *   .optionsGroup('Development Options:')
     *   .option('-d, --debug', 'output extra debugging')
     *   .option('-p, --profile', 'output profiling information')
     *
     * @param {string} [heading]
     * @returns {Command | string}
     */
    optionsGroup(D) {
      return D === void 0 ? this._defaultOptionGroup ?? "" : (this._defaultOptionGroup = D, this);
    }
    /**
     * @param {Option} option
     * @private
     */
    _initOptionGroup(D) {
      this._defaultOptionGroup && !D.helpGroupHeading && D.helpGroup(this._defaultOptionGroup);
    }
    /**
     * @param {Command} cmd
     * @private
     */
    _initCommandGroup(D) {
      this._defaultCommandGroup && !D.helpGroup() && D.helpGroup(this._defaultCommandGroup);
    }
    /**
     * Set the name of the command from script filename, such as process.argv[1],
     * or require.main.filename, or __filename.
     *
     * (Used internally and public although not documented in README.)
     *
     * @example
     * program.nameFromFilename(require.main.filename);
     *
     * @param {string} filename
     * @return {Command}
     */
    nameFromFilename(D) {
      return this._name = u.basename(D, u.extname(D)), this;
    }
    /**
     * Get or set the directory for searching for executable subcommands of this command.
     *
     * @example
     * program.executableDir(__dirname);
     * // or
     * program.executableDir('subcommands');
     *
     * @param {string} [path]
     * @return {(string|null|Command)}
     */
    executableDir(D) {
      return D === void 0 ? this._executableDir : (this._executableDir = D, this);
    }
    /**
     * Return program help documentation.
     *
     * @param {{ error: boolean }} [contextOptions] - pass {error:true} to wrap for stderr instead of stdout
     * @return {string}
     */
    helpInformation(D) {
      const h = this.createHelp(), d = this._getOutputContext(D);
      h.prepareContext({
        error: d.error,
        helpWidth: d.helpWidth,
        outputHasColors: d.hasColors
      });
      const F = h.formatHelp(this, h);
      return d.hasColors ? F : this._outputConfiguration.stripColor(F);
    }
    /**
     * @typedef HelpContext
     * @type {object}
     * @property {boolean} error
     * @property {number} helpWidth
     * @property {boolean} hasColors
     * @property {function} write - includes stripColor if needed
     *
     * @returns {HelpContext}
     * @private
     */
    _getOutputContext(D) {
      D = D || {};
      const h = !!D.error;
      let d, F, m;
      return h ? (d = (w) => this._outputConfiguration.writeErr(w), F = this._outputConfiguration.getErrHasColors(), m = this._outputConfiguration.getErrHelpWidth()) : (d = (w) => this._outputConfiguration.writeOut(w), F = this._outputConfiguration.getOutHasColors(), m = this._outputConfiguration.getOutHelpWidth()), { error: h, write: (w) => (F || (w = this._outputConfiguration.stripColor(w)), d(w)), hasColors: F, helpWidth: m };
    }
    /**
     * Output help information for this command.
     *
     * Outputs built-in help, and custom text added using `.addHelpText()`.
     *
     * @param {{ error: boolean } | Function} [contextOptions] - pass {error:true} to write to stderr instead of stdout
     */
    outputHelp(D) {
      let h;
      typeof D == "function" && (h = D, D = void 0);
      const d = this._getOutputContext(D), F = {
        error: d.error,
        write: d.write,
        command: this
      };
      this._getCommandAndAncestors().reverse().forEach((E) => E.emit("beforeAllHelp", F)), this.emit("beforeHelp", F);
      let m = this.helpInformation({ error: d.error });
      if (h && (m = h(m), typeof m != "string" && !Buffer.isBuffer(m)))
        throw new Error("outputHelp callback must return a string or a Buffer");
      d.write(m), this._getHelpOption()?.long && this.emit(this._getHelpOption().long), this.emit("afterHelp", F), this._getCommandAndAncestors().forEach(
        (E) => E.emit("afterAllHelp", F)
      );
    }
    /**
     * You can pass in flags and a description to customise the built-in help option.
     * Pass in false to disable the built-in help option.
     *
     * @example
     * program.helpOption('-?, --help' 'show help'); // customise
     * program.helpOption(false); // disable
     *
     * @param {(string | boolean)} flags
     * @param {string} [description]
     * @return {Command} `this` command for chaining
     */
    helpOption(D, h) {
      return typeof D == "boolean" ? (D ? (this._helpOption === null && (this._helpOption = void 0), this._defaultOptionGroup && this._initOptionGroup(this._getHelpOption())) : this._helpOption = null, this) : (this._helpOption = this.createOption(
        D ?? "-h, --help",
        h ?? "display help for command"
      ), (D || h) && this._initOptionGroup(this._helpOption), this);
    }
    /**
     * Lazy create help option.
     * Returns null if has been disabled with .helpOption(false).
     *
     * @returns {(Option | null)} the help option
     * @package
     */
    _getHelpOption() {
      return this._helpOption === void 0 && this.helpOption(void 0, void 0), this._helpOption;
    }
    /**
     * Supply your own option to use for the built-in help option.
     * This is an alternative to using helpOption() to customise the flags and description etc.
     *
     * @param {Option} option
     * @return {Command} `this` command for chaining
     */
    addHelpOption(D) {
      return this._helpOption = D, this._initOptionGroup(D), this;
    }
    /**
     * Output help information and exit.
     *
     * Outputs built-in help, and custom text added using `.addHelpText()`.
     *
     * @param {{ error: boolean }} [contextOptions] - pass {error:true} to write to stderr instead of stdout
     */
    help(D) {
      this.outputHelp(D);
      let h = Number(n.exitCode ?? 0);
      h === 0 && D && typeof D != "function" && D.error && (h = 1), this._exit(h, "commander.help", "(outputHelp)");
    }
    /**
     * // Do a little typing to coordinate emit and listener for the help text events.
     * @typedef HelpTextEventContext
     * @type {object}
     * @property {boolean} error
     * @property {Command} command
     * @property {function} write
     */
    /**
     * Add additional text to be displayed with the built-in help.
     *
     * Position is 'before' or 'after' to affect just this command,
     * and 'beforeAll' or 'afterAll' to affect this command and all its subcommands.
     *
     * @param {string} position - before or after built-in help
     * @param {(string | Function)} text - string to add, or a function returning a string
     * @return {Command} `this` command for chaining
     */
    addHelpText(D, h) {
      const d = ["beforeAll", "before", "after", "afterAll"];
      if (!d.includes(D))
        throw new Error(`Unexpected value for position to addHelpText.
Expecting one of '${d.join("', '")}'`);
      const F = `${D}Help`;
      return this.on(F, (m) => {
        let E;
        typeof h == "function" ? E = h({ error: m.error, command: m.command }) : E = h, E && m.write(`${E}
`);
      }), this;
    }
    /**
     * Output help information if help flags specified
     *
     * @param {Array} args - array of options to search for help flags
     * @private
     */
    _outputHelpIfRequested(D) {
      const h = this._getHelpOption();
      h && D.find((F) => h.is(F)) && (this.outputHelp(), this._exit(0, "commander.helpDisplayed", "(outputHelp)"));
    }
  }
  function C(O) {
    return O.map((D) => {
      if (!D.startsWith("--inspect"))
        return D;
      let h, d = "127.0.0.1", F = "9229", m;
      return (m = D.match(/^(--inspect(-brk)?)$/)) !== null ? h = m[1] : (m = D.match(/^(--inspect(-brk|-port)?)=([^:]+)$/)) !== null ? (h = m[1], /^\d+$/.test(m[3]) ? F = m[3] : d = m[3]) : (m = D.match(/^(--inspect(-brk|-port)?)=([^:]+):(\d+)$/)) !== null && (h = m[1], d = m[3], F = m[4]), h && F !== "0" ? `${h}=${d}:${parseInt(F) + 1}` : D;
    });
  }
  function _() {
    if (n.env.NO_COLOR || n.env.FORCE_COLOR === "0" || n.env.FORCE_COLOR === "false")
      return !1;
    if (n.env.FORCE_COLOR || n.env.CLICOLOR_FORCE !== void 0)
      return !0;
  }
  return fe.Command = g, fe.useColor = _, fe;
}
var It;
function Vu() {
  if (It) return R;
  It = 1;
  const { Argument: e } = wt(), { Command: t } = Uu(), { CommanderError: u, InvalidArgumentError: o } = Te(), { Help: n } = Hn(), { Option: i } = jn();
  return R.program = new t(), R.createCommand = (r) => new t(r), R.createOption = (r, s) => new i(r, s), R.createArgument = (r, s) => new e(r, s), R.Command = t, R.Option = i, R.Argument = e, R.Help = n, R.CommanderError = u, R.InvalidArgumentError = o, R.InvalidOptionArgumentError = o, R;
}
var Gu = Vu();
const qu = /* @__PURE__ */ Y(Gu), {
  program: W,
  createCommand: YD,
  createArgument: KD,
  createOption: JD,
  CommanderError: XD,
  InvalidArgumentError: ZD,
  InvalidOptionArgumentError: QD,
  // deprecated old name
  Command: ea,
  Argument: ta,
  Option: na,
  Help: ua
} = qu, zu = (e) => e.name === "backspace", Nn = (e) => e.name === "tab", Un = (e) => e.name === "enter" || e.name === "return";
class Yu extends Error {
  name = "AbortPromptError";
  message = "Prompt was aborted";
  constructor(t) {
    super(), this.cause = t?.cause;
  }
}
class Ku extends Error {
  name = "CancelPromptError";
  message = "Prompt was canceled";
}
class Lt extends Error {
  name = "ExitPromptError";
}
class Ju extends Error {
  name = "HookError";
}
class Xu extends Error {
  name = "ValidationError";
}
const Vn = new Bu();
function Zu(e) {
  return {
    rl: e,
    hooks: [],
    hooksCleanup: [],
    hooksEffect: [],
    index: 0,
    handleChange() {
    }
  };
}
function Qu(e, t) {
  const u = Zu(e);
  return Vn.run(u, () => {
    function o(n) {
      u.handleChange = () => {
        u.index = 0, n();
      }, u.handleChange();
    }
    return t(o);
  });
}
function G() {
  const e = Vn.getStore();
  if (!e)
    throw new Ju("[Inquirer] Hook functions can only be called from within a prompt");
  return e;
}
function Gn() {
  return G().rl;
}
function qn(e) {
  const t = (...u) => {
    const o = G();
    let n = !1;
    const i = o.handleChange;
    o.handleChange = () => {
      n = !0;
    };
    const r = e(...u);
    return n && i(), o.handleChange = i, r;
  };
  return Ct.bind(t);
}
function zn(e) {
  const t = G(), { index: u } = t, o = {
    get() {
      return t.hooks[u];
    },
    set(i) {
      t.hooks[u] = i;
    },
    initialized: u in t.hooks
  }, n = e(o);
  return t.index++, n;
}
function ei() {
  G().handleChange();
}
const ne = {
  queue(e) {
    const t = G(), { index: u } = t;
    t.hooksEffect.push(() => {
      t.hooksCleanup[u]?.();
      const o = e(Gn());
      if (o != null && typeof o != "function")
        throw new Xu("useEffect return value must be a cleanup function or nothing.");
      t.hooksCleanup[u] = o;
    });
  },
  run() {
    const e = G();
    qn(() => {
      e.hooksEffect.forEach((t) => {
        t();
      }), e.hooksEffect.length = 0;
    })();
  },
  clearAll() {
    const e = G();
    e.hooksCleanup.forEach((t) => {
      t?.();
    }), e.hooksEffect.length = 0, e.hooksCleanup.length = 0;
  }
};
function N(e) {
  return zn((t) => {
    const u = Ct.bind(function(i) {
      t.get() !== i && (t.set(i), ei());
    });
    if (t.initialized)
      return [t.get(), u];
    const o = typeof e == "function" ? e() : e;
    return t.set(o), [o, u];
  });
}
function bt(e, t) {
  zn((u) => {
    const o = u.get();
    (!Array.isArray(o) || t.some((i, r) => !Object.is(i, o[r]))) && ne.queue(e), u.set(t);
  });
}
var Le, Mt;
function ti() {
  if (Mt) return Le;
  Mt = 1;
  const t = xu?.WriteStream?.prototype?.hasColors?.() ?? !1, u = (n, i) => {
    if (!t)
      return (a) => a;
    const r = `\x1B[${n}m`, s = `\x1B[${i}m`;
    return (a) => {
      const l = a + "";
      let c = l.indexOf(s);
      if (c === -1)
        return r + l + s;
      let f = r, p = 0;
      const C = (i === 22 ? s : "") + r;
      for (; c !== -1; )
        f += l.slice(p, c) + C, p = c + s.length, c = l.indexOf(s, p);
      return f += l.slice(p) + s, f;
    };
  }, o = {};
  return o.reset = u(0, 0), o.bold = u(1, 22), o.dim = u(2, 22), o.italic = u(3, 23), o.underline = u(4, 24), o.overline = u(53, 55), o.inverse = u(7, 27), o.hidden = u(8, 28), o.strikethrough = u(9, 29), o.black = u(30, 39), o.red = u(31, 39), o.green = u(32, 39), o.yellow = u(33, 39), o.blue = u(34, 39), o.magenta = u(35, 39), o.cyan = u(36, 39), o.white = u(37, 39), o.gray = u(90, 39), o.bgBlack = u(40, 49), o.bgRed = u(41, 49), o.bgGreen = u(42, 49), o.bgYellow = u(43, 49), o.bgBlue = u(44, 49), o.bgMagenta = u(45, 49), o.bgCyan = u(46, 49), o.bgWhite = u(47, 49), o.bgGray = u(100, 49), o.redBright = u(91, 39), o.greenBright = u(92, 39), o.yellowBright = u(93, 39), o.blueBright = u(94, 39), o.magentaBright = u(95, 39), o.cyanBright = u(96, 39), o.whiteBright = u(97, 39), o.bgRedBright = u(101, 49), o.bgGreenBright = u(102, 49), o.bgYellowBright = u(103, 49), o.bgBlueBright = u(104, 49), o.bgMagentaBright = u(105, 49), o.bgCyanBright = u(106, 49), o.bgWhiteBright = u(107, 49), Le = o, Le;
}
var ni = /* @__PURE__ */ ti();
const L = /* @__PURE__ */ Y(ni);
function ui() {
  return y.platform !== "win32" ? y.env.TERM !== "linux" : !!y.env.WT_SESSION || // Windows Terminal
  !!y.env.TERMINUS_SUBLIME || // Terminus (<0.2.27)
  y.env.ConEmuTask === "{cmd::Cmder}" || // ConEmu and cmder
  y.env.TERM_PROGRAM === "Terminus-Sublime" || y.env.TERM_PROGRAM === "vscode" || y.env.TERM === "xterm-256color" || y.env.TERM === "alacritty" || y.env.TERMINAL_EMULATOR === "JetBrains-JediTerm";
}
const Yn = {
  circleQuestionMark: "(?)",
  questionMarkPrefix: "(?)",
  square: "█",
  squareDarkShade: "▓",
  squareMediumShade: "▒",
  squareLightShade: "░",
  squareTop: "▀",
  squareBottom: "▄",
  squareLeft: "▌",
  squareRight: "▐",
  squareCenter: "■",
  bullet: "●",
  dot: "․",
  ellipsis: "…",
  pointerSmall: "›",
  triangleUp: "▲",
  triangleUpSmall: "▴",
  triangleDown: "▼",
  triangleDownSmall: "▾",
  triangleLeftSmall: "◂",
  triangleRightSmall: "▸",
  home: "⌂",
  heart: "♥",
  musicNote: "♪",
  musicNoteBeamed: "♫",
  arrowUp: "↑",
  arrowDown: "↓",
  arrowLeft: "←",
  arrowRight: "→",
  arrowLeftRight: "↔",
  arrowUpDown: "↕",
  almostEqual: "≈",
  notEqual: "≠",
  lessOrEqual: "≤",
  greaterOrEqual: "≥",
  identical: "≡",
  infinity: "∞",
  subscriptZero: "₀",
  subscriptOne: "₁",
  subscriptTwo: "₂",
  subscriptThree: "₃",
  subscriptFour: "₄",
  subscriptFive: "₅",
  subscriptSix: "₆",
  subscriptSeven: "₇",
  subscriptEight: "₈",
  subscriptNine: "₉",
  oneHalf: "½",
  oneThird: "⅓",
  oneQuarter: "¼",
  oneFifth: "⅕",
  oneSixth: "⅙",
  oneEighth: "⅛",
  twoThirds: "⅔",
  twoFifths: "⅖",
  threeQuarters: "¾",
  threeFifths: "⅗",
  threeEighths: "⅜",
  fourFifths: "⅘",
  fiveSixths: "⅚",
  fiveEighths: "⅝",
  sevenEighths: "⅞",
  line: "─",
  lineBold: "━",
  lineDouble: "═",
  lineDashed0: "┄",
  lineDashed1: "┅",
  lineDashed2: "┈",
  lineDashed3: "┉",
  lineDashed4: "╌",
  lineDashed5: "╍",
  lineDashed6: "╴",
  lineDashed7: "╶",
  lineDashed8: "╸",
  lineDashed9: "╺",
  lineDashed10: "╼",
  lineDashed11: "╾",
  lineDashed12: "−",
  lineDashed13: "–",
  lineDashed14: "‐",
  lineDashed15: "⁃",
  lineVertical: "│",
  lineVerticalBold: "┃",
  lineVerticalDouble: "║",
  lineVerticalDashed0: "┆",
  lineVerticalDashed1: "┇",
  lineVerticalDashed2: "┊",
  lineVerticalDashed3: "┋",
  lineVerticalDashed4: "╎",
  lineVerticalDashed5: "╏",
  lineVerticalDashed6: "╵",
  lineVerticalDashed7: "╷",
  lineVerticalDashed8: "╹",
  lineVerticalDashed9: "╻",
  lineVerticalDashed10: "╽",
  lineVerticalDashed11: "╿",
  lineDownLeft: "┐",
  lineDownLeftArc: "╮",
  lineDownBoldLeftBold: "┓",
  lineDownBoldLeft: "┒",
  lineDownLeftBold: "┑",
  lineDownDoubleLeftDouble: "╗",
  lineDownDoubleLeft: "╖",
  lineDownLeftDouble: "╕",
  lineDownRight: "┌",
  lineDownRightArc: "╭",
  lineDownBoldRightBold: "┏",
  lineDownBoldRight: "┎",
  lineDownRightBold: "┍",
  lineDownDoubleRightDouble: "╔",
  lineDownDoubleRight: "╓",
  lineDownRightDouble: "╒",
  lineUpLeft: "┘",
  lineUpLeftArc: "╯",
  lineUpBoldLeftBold: "┛",
  lineUpBoldLeft: "┚",
  lineUpLeftBold: "┙",
  lineUpDoubleLeftDouble: "╝",
  lineUpDoubleLeft: "╜",
  lineUpLeftDouble: "╛",
  lineUpRight: "└",
  lineUpRightArc: "╰",
  lineUpBoldRightBold: "┗",
  lineUpBoldRight: "┖",
  lineUpRightBold: "┕",
  lineUpDoubleRightDouble: "╚",
  lineUpDoubleRight: "╙",
  lineUpRightDouble: "╘",
  lineUpDownLeft: "┤",
  lineUpBoldDownBoldLeftBold: "┫",
  lineUpBoldDownBoldLeft: "┨",
  lineUpDownLeftBold: "┥",
  lineUpBoldDownLeftBold: "┩",
  lineUpDownBoldLeftBold: "┪",
  lineUpDownBoldLeft: "┧",
  lineUpBoldDownLeft: "┦",
  lineUpDoubleDownDoubleLeftDouble: "╣",
  lineUpDoubleDownDoubleLeft: "╢",
  lineUpDownLeftDouble: "╡",
  lineUpDownRight: "├",
  lineUpBoldDownBoldRightBold: "┣",
  lineUpBoldDownBoldRight: "┠",
  lineUpDownRightBold: "┝",
  lineUpBoldDownRightBold: "┡",
  lineUpDownBoldRightBold: "┢",
  lineUpDownBoldRight: "┟",
  lineUpBoldDownRight: "┞",
  lineUpDoubleDownDoubleRightDouble: "╠",
  lineUpDoubleDownDoubleRight: "╟",
  lineUpDownRightDouble: "╞",
  lineDownLeftRight: "┬",
  lineDownBoldLeftBoldRightBold: "┳",
  lineDownLeftBoldRightBold: "┯",
  lineDownBoldLeftRight: "┰",
  lineDownBoldLeftBoldRight: "┱",
  lineDownBoldLeftRightBold: "┲",
  lineDownLeftRightBold: "┮",
  lineDownLeftBoldRight: "┭",
  lineDownDoubleLeftDoubleRightDouble: "╦",
  lineDownDoubleLeftRight: "╥",
  lineDownLeftDoubleRightDouble: "╤",
  lineUpLeftRight: "┴",
  lineUpBoldLeftBoldRightBold: "┻",
  lineUpLeftBoldRightBold: "┷",
  lineUpBoldLeftRight: "┸",
  lineUpBoldLeftBoldRight: "┹",
  lineUpBoldLeftRightBold: "┺",
  lineUpLeftRightBold: "┶",
  lineUpLeftBoldRight: "┵",
  lineUpDoubleLeftDoubleRightDouble: "╩",
  lineUpDoubleLeftRight: "╨",
  lineUpLeftDoubleRightDouble: "╧",
  lineUpDownLeftRight: "┼",
  lineUpBoldDownBoldLeftBoldRightBold: "╋",
  lineUpDownBoldLeftBoldRightBold: "╈",
  lineUpBoldDownLeftBoldRightBold: "╇",
  lineUpBoldDownBoldLeftRightBold: "╊",
  lineUpBoldDownBoldLeftBoldRight: "╉",
  lineUpBoldDownLeftRight: "╀",
  lineUpDownBoldLeftRight: "╁",
  lineUpDownLeftBoldRight: "┽",
  lineUpDownLeftRightBold: "┾",
  lineUpBoldDownBoldLeftRight: "╂",
  lineUpDownLeftBoldRightBold: "┿",
  lineUpBoldDownLeftBoldRight: "╃",
  lineUpBoldDownLeftRightBold: "╄",
  lineUpDownBoldLeftBoldRight: "╅",
  lineUpDownBoldLeftRightBold: "╆",
  lineUpDoubleDownDoubleLeftDoubleRightDouble: "╬",
  lineUpDoubleDownDoubleLeftRight: "╫",
  lineUpDownLeftDoubleRightDouble: "╪",
  lineCross: "╳",
  lineBackslash: "╲",
  lineSlash: "╱"
}, ii = {
  tick: "✔",
  info: "ℹ",
  warning: "⚠",
  cross: "✘",
  squareSmall: "◻",
  squareSmallFilled: "◼",
  circle: "◯",
  circleFilled: "◉",
  circleDotted: "◌",
  circleDouble: "◎",
  circleCircle: "ⓞ",
  circleCross: "ⓧ",
  circlePipe: "Ⓘ",
  radioOn: "◉",
  radioOff: "◯",
  checkboxOn: "☒",
  checkboxOff: "☐",
  checkboxCircleOn: "ⓧ",
  checkboxCircleOff: "Ⓘ",
  pointer: "❯",
  triangleUpOutline: "△",
  triangleLeft: "◀",
  triangleRight: "▶",
  lozenge: "◆",
  lozengeOutline: "◇",
  hamburger: "☰",
  smiley: "㋡",
  mustache: "෴",
  star: "★",
  play: "▶",
  nodejs: "⬢",
  oneSeventh: "⅐",
  oneNinth: "⅑",
  oneTenth: "⅒"
}, ri = {
  tick: "√",
  info: "i",
  warning: "‼",
  cross: "×",
  squareSmall: "□",
  squareSmallFilled: "■",
  circle: "( )",
  circleFilled: "(*)",
  circleDotted: "( )",
  circleDouble: "( )",
  circleCircle: "(○)",
  circleCross: "(×)",
  circlePipe: "(│)",
  radioOn: "(*)",
  radioOff: "( )",
  checkboxOn: "[×]",
  checkboxOff: "[ ]",
  checkboxCircleOn: "(×)",
  checkboxCircleOff: "( )",
  pointer: ">",
  triangleUpOutline: "∆",
  triangleLeft: "◄",
  triangleRight: "►",
  lozenge: "♦",
  lozengeOutline: "◊",
  hamburger: "≡",
  smiley: "☺",
  mustache: "┌─┐",
  star: "✶",
  play: "►",
  nodejs: "♦",
  oneSeventh: "1/7",
  oneNinth: "1/9",
  oneTenth: "1/10"
}, si = { ...Yn, ...ii }, oi = {
  ...Yn,
  ...ri
}, Di = ui(), ai = Di ? si : oi, li = {
  prefix: {
    idle: L.blue("?"),
    // TODO: use figure
    done: L.green(ai.tick)
  },
  spinner: {
    interval: 80,
    frames: ["⠋", "⠙", "⠹", "⠸", "⠼", "⠴", "⠦", "⠧", "⠇", "⠏"].map((e) => L.yellow(e))
  },
  style: {
    answer: L.cyan,
    message: L.bold,
    error: (e) => L.red(`> ${e}`),
    defaultAnswer: (e) => L.dim(`(${e})`),
    help: L.dim,
    highlight: L.cyan,
    key: (e) => L.cyan(L.bold(`<${e}>`))
  }
};
function Wt(e) {
  if (typeof e != "object" || e === null)
    return !1;
  let t = e;
  for (; Object.getPrototypeOf(t) !== null; )
    t = Object.getPrototypeOf(t);
  return Object.getPrototypeOf(e) === t;
}
function Kn(...e) {
  const t = {};
  for (const u of e)
    for (const [o, n] of Object.entries(u)) {
      const i = t[o];
      t[o] = Wt(i) && Wt(n) ? Kn(i, n) : n;
    }
  return t;
}
function Bt(...e) {
  const t = [
    li,
    ...e.filter((u) => u != null)
  ];
  return Kn(...t);
}
function Jn({ status: e = "idle", theme: t }) {
  const [u, o] = N(!1), [n, i] = N(0), { prefix: r, spinner: s } = Bt(t);
  return bt(() => {
    if (e === "loading") {
      let l, c = -1;
      const f = setTimeout(() => {
        o(!0), l = setInterval(() => {
          c = c + 1, i(c % s.frames.length);
        }, s.interval);
      }, 300);
      return () => {
        clearTimeout(f), clearInterval(l);
      };
    } else
      o(!1);
  }, [e]), u ? s.frames[n] : typeof r == "string" ? r : r[e === "loading" ? "idle" : e] ?? r.idle;
}
function ci(e) {
  return N({ current: e })[0];
}
function Xn(e) {
  const t = ci(e);
  t.current = e, bt((u) => {
    let o = !1;
    const n = qn((i, r) => {
      o || t.current(r, u);
    });
    return u.input.on("keypress", n), () => {
      o = !0, u.input.removeListener("keypress", n);
    };
  }, []);
}
var Me, Ht;
function hi() {
  if (Ht) return Me;
  Ht = 1, Me = t;
  function e(u) {
    const o = {
      defaultWidth: 0,
      output: process.stdout,
      tty: vu
    };
    return u ? (Object.keys(o).forEach(function(n) {
      u[n] || (u[n] = o[n]);
    }), u) : o;
  }
  function t(u) {
    const o = e(u);
    if (o.output.getWindowSize)
      return o.output.getWindowSize()[0] || o.defaultWidth;
    if (o.tty.getWindowSize)
      return o.tty.getWindowSize()[1] || o.defaultWidth;
    if (o.output.columns)
      return o.output.columns;
    if (process.env.CLI_WIDTH) {
      const n = parseInt(process.env.CLI_WIDTH, 10);
      if (!isNaN(n) && n !== 0)
        return n;
    }
    return o.defaultWidth;
  }
  return Me;
}
var fi = hi();
const di = /* @__PURE__ */ Y(fi);
var me = { exports: {} }, We, jt;
function Fi() {
  return jt || (jt = 1, We = ({ onlyFirst: e = !1 } = {}) => {
    const t = [
      "[\\u001B\\u009B][[\\]()#;?]*(?:(?:(?:(?:;[-a-zA-Z\\d\\/#&.:=?%@~_]+)*|[a-zA-Z\\d]+(?:;[-a-zA-Z\\d\\/#&.:=?%@~_]*)*)?\\u0007)",
      "(?:(?:\\d{1,4}(?:;\\d{0,4})*)?[\\dA-PR-TZcf-ntqry=><~]))"
    ].join("|");
    return new RegExp(t, e ? void 0 : "g");
  }), We;
}
var He, Nt;
function Zn() {
  if (Nt) return He;
  Nt = 1;
  const e = Fi();
  return He = (t) => typeof t == "string" ? t.replace(e(), "") : t, He;
}
var pe = { exports: {} }, Ut;
function mi() {
  if (Ut) return pe.exports;
  Ut = 1;
  const e = (t) => Number.isNaN(t) ? !1 : t >= 4352 && (t <= 4447 || // Hangul Jamo
  t === 9001 || // LEFT-POINTING ANGLE BRACKET
  t === 9002 || // RIGHT-POINTING ANGLE BRACKET
  // CJK Radicals Supplement .. Enclosed CJK Letters and Months
  11904 <= t && t <= 12871 && t !== 12351 || // Enclosed CJK Letters and Months .. CJK Unified Ideographs Extension A
  12880 <= t && t <= 19903 || // CJK Unified Ideographs .. Yi Radicals
  19968 <= t && t <= 42182 || // Hangul Jamo Extended-A
  43360 <= t && t <= 43388 || // Hangul Syllables
  44032 <= t && t <= 55203 || // CJK Compatibility Ideographs
  63744 <= t && t <= 64255 || // Vertical Forms
  65040 <= t && t <= 65049 || // CJK Compatibility Forms .. Small Form Variants
  65072 <= t && t <= 65131 || // Halfwidth and Fullwidth Forms
  65281 <= t && t <= 65376 || 65504 <= t && t <= 65510 || // Kana Supplement
  110592 <= t && t <= 110593 || // Enclosed Ideographic Supplement
  127488 <= t && t <= 127569 || // CJK Unified Ideographs Extension B .. Tertiary Ideographic Plane
  131072 <= t && t <= 262141);
  return pe.exports = e, pe.exports.default = e, pe.exports;
}
var je, Vt;
function pi() {
  return Vt || (Vt = 1, je = function() {
    return /\uD83C\uDFF4\uDB40\uDC67\uDB40\uDC62(?:\uDB40\uDC65\uDB40\uDC6E\uDB40\uDC67|\uDB40\uDC73\uDB40\uDC63\uDB40\uDC74|\uDB40\uDC77\uDB40\uDC6C\uDB40\uDC73)\uDB40\uDC7F|\uD83D\uDC68(?:\uD83C\uDFFC\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68\uD83C\uDFFB|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFF\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB-\uDFFE])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFE\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB-\uDFFD])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFD\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB\uDFFC])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\u200D(?:\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D)?\uD83D\uDC68|(?:\uD83D[\uDC68\uDC69])\u200D(?:\uD83D\uDC66\u200D\uD83D\uDC66|\uD83D\uDC67\u200D(?:\uD83D[\uDC66\uDC67]))|\uD83D\uDC66\u200D\uD83D\uDC66|\uD83D\uDC67\u200D(?:\uD83D[\uDC66\uDC67])|(?:\uD83D[\uDC68\uDC69])\u200D(?:\uD83D[\uDC66\uDC67])|[\u2695\u2696\u2708]\uFE0F|\uD83D[\uDC66\uDC67]|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|(?:\uD83C\uDFFB\u200D[\u2695\u2696\u2708]|\uD83C\uDFFF\u200D[\u2695\u2696\u2708]|\uD83C\uDFFE\u200D[\u2695\u2696\u2708]|\uD83C\uDFFD\u200D[\u2695\u2696\u2708]|\uD83C\uDFFC\u200D[\u2695\u2696\u2708])\uFE0F|\uD83C\uDFFB\u200D(?:\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C[\uDFFB-\uDFFF])|(?:\uD83E\uDDD1\uD83C\uDFFB\u200D\uD83E\uDD1D\u200D\uD83E\uDDD1|\uD83D\uDC69\uD83C\uDFFC\u200D\uD83E\uDD1D\u200D\uD83D\uDC69)\uD83C\uDFFB|\uD83E\uDDD1(?:\uD83C\uDFFF\u200D\uD83E\uDD1D\u200D\uD83E\uDDD1(?:\uD83C[\uDFFB-\uDFFF])|\u200D\uD83E\uDD1D\u200D\uD83E\uDDD1)|(?:\uD83E\uDDD1\uD83C\uDFFE\u200D\uD83E\uDD1D\u200D\uD83E\uDDD1|\uD83D\uDC69\uD83C\uDFFF\u200D\uD83E\uDD1D\u200D(?:\uD83D[\uDC68\uDC69]))(?:\uD83C[\uDFFB-\uDFFE])|(?:\uD83E\uDDD1\uD83C\uDFFC\u200D\uD83E\uDD1D\u200D\uD83E\uDDD1|\uD83D\uDC69\uD83C\uDFFD\u200D\uD83E\uDD1D\u200D\uD83D\uDC69)(?:\uD83C[\uDFFB\uDFFC])|\uD83D\uDC69(?:\uD83C\uDFFE\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFC\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFB\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFC-\uDFFF])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFD\u200D(?:\uD83E\uDD1D\u200D\uD83D\uDC68(?:\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\u200D(?:\u2764\uFE0F\u200D(?:\uD83D\uDC8B\u200D(?:\uD83D[\uDC68\uDC69])|\uD83D[\uDC68\uDC69])|\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD])|\uD83C\uDFFF\u200D(?:\uD83C[\uDF3E\uDF73\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E[\uDDAF-\uDDB3\uDDBC\uDDBD]))|\uD83D\uDC69\u200D\uD83D\uDC69\u200D(?:\uD83D\uDC66\u200D\uD83D\uDC66|\uD83D\uDC67\u200D(?:\uD83D[\uDC66\uDC67]))|(?:\uD83E\uDDD1\uD83C\uDFFD\u200D\uD83E\uDD1D\u200D\uD83E\uDDD1|\uD83D\uDC69\uD83C\uDFFE\u200D\uD83E\uDD1D\u200D\uD83D\uDC69)(?:\uD83C[\uDFFB-\uDFFD])|\uD83D\uDC69\u200D\uD83D\uDC66\u200D\uD83D\uDC66|\uD83D\uDC69\u200D\uD83D\uDC69\u200D(?:\uD83D[\uDC66\uDC67])|(?:\uD83D\uDC41\uFE0F\u200D\uD83D\uDDE8|\uD83D\uDC69(?:\uD83C\uDFFF\u200D[\u2695\u2696\u2708]|\uD83C\uDFFE\u200D[\u2695\u2696\u2708]|\uD83C\uDFFC\u200D[\u2695\u2696\u2708]|\uD83C\uDFFB\u200D[\u2695\u2696\u2708]|\uD83C\uDFFD\u200D[\u2695\u2696\u2708]|\u200D[\u2695\u2696\u2708])|(?:(?:\u26F9|\uD83C[\uDFCB\uDFCC]|\uD83D\uDD75)\uFE0F|\uD83D\uDC6F|\uD83E[\uDD3C\uDDDE\uDDDF])\u200D[\u2640\u2642]|(?:\u26F9|\uD83C[\uDFCB\uDFCC]|\uD83D\uDD75)(?:\uD83C[\uDFFB-\uDFFF])\u200D[\u2640\u2642]|(?:\uD83C[\uDFC3\uDFC4\uDFCA]|\uD83D[\uDC6E\uDC71\uDC73\uDC77\uDC81\uDC82\uDC86\uDC87\uDE45-\uDE47\uDE4B\uDE4D\uDE4E\uDEA3\uDEB4-\uDEB6]|\uD83E[\uDD26\uDD37-\uDD39\uDD3D\uDD3E\uDDB8\uDDB9\uDDCD-\uDDCF\uDDD6-\uDDDD])(?:(?:\uD83C[\uDFFB-\uDFFF])\u200D[\u2640\u2642]|\u200D[\u2640\u2642])|\uD83C\uDFF4\u200D\u2620)\uFE0F|\uD83D\uDC69\u200D\uD83D\uDC67\u200D(?:\uD83D[\uDC66\uDC67])|\uD83C\uDFF3\uFE0F\u200D\uD83C\uDF08|\uD83D\uDC15\u200D\uD83E\uDDBA|\uD83D\uDC69\u200D\uD83D\uDC66|\uD83D\uDC69\u200D\uD83D\uDC67|\uD83C\uDDFD\uD83C\uDDF0|\uD83C\uDDF4\uD83C\uDDF2|\uD83C\uDDF6\uD83C\uDDE6|[#\*0-9]\uFE0F\u20E3|\uD83C\uDDE7(?:\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEF\uDDF1-\uDDF4\uDDF6-\uDDF9\uDDFB\uDDFC\uDDFE\uDDFF])|\uD83C\uDDF9(?:\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDED\uDDEF-\uDDF4\uDDF7\uDDF9\uDDFB\uDDFC\uDDFF])|\uD83C\uDDEA(?:\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDED\uDDF7-\uDDFA])|\uD83E\uDDD1(?:\uD83C[\uDFFB-\uDFFF])|\uD83C\uDDF7(?:\uD83C[\uDDEA\uDDF4\uDDF8\uDDFA\uDDFC])|\uD83D\uDC69(?:\uD83C[\uDFFB-\uDFFF])|\uD83C\uDDF2(?:\uD83C[\uDDE6\uDDE8-\uDDED\uDDF0-\uDDFF])|\uD83C\uDDE6(?:\uD83C[\uDDE8-\uDDEC\uDDEE\uDDF1\uDDF2\uDDF4\uDDF6-\uDDFA\uDDFC\uDDFD\uDDFF])|\uD83C\uDDF0(?:\uD83C[\uDDEA\uDDEC-\uDDEE\uDDF2\uDDF3\uDDF5\uDDF7\uDDFC\uDDFE\uDDFF])|\uD83C\uDDED(?:\uD83C[\uDDF0\uDDF2\uDDF3\uDDF7\uDDF9\uDDFA])|\uD83C\uDDE9(?:\uD83C[\uDDEA\uDDEC\uDDEF\uDDF0\uDDF2\uDDF4\uDDFF])|\uD83C\uDDFE(?:\uD83C[\uDDEA\uDDF9])|\uD83C\uDDEC(?:\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEE\uDDF1-\uDDF3\uDDF5-\uDDFA\uDDFC\uDDFE])|\uD83C\uDDF8(?:\uD83C[\uDDE6-\uDDEA\uDDEC-\uDDF4\uDDF7-\uDDF9\uDDFB\uDDFD-\uDDFF])|\uD83C\uDDEB(?:\uD83C[\uDDEE-\uDDF0\uDDF2\uDDF4\uDDF7])|\uD83C\uDDF5(?:\uD83C[\uDDE6\uDDEA-\uDDED\uDDF0-\uDDF3\uDDF7-\uDDF9\uDDFC\uDDFE])|\uD83C\uDDFB(?:\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDEE\uDDF3\uDDFA])|\uD83C\uDDF3(?:\uD83C[\uDDE6\uDDE8\uDDEA-\uDDEC\uDDEE\uDDF1\uDDF4\uDDF5\uDDF7\uDDFA\uDDFF])|\uD83C\uDDE8(?:\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDEE\uDDF0-\uDDF5\uDDF7\uDDFA-\uDDFF])|\uD83C\uDDF1(?:\uD83C[\uDDE6-\uDDE8\uDDEE\uDDF0\uDDF7-\uDDFB\uDDFE])|\uD83C\uDDFF(?:\uD83C[\uDDE6\uDDF2\uDDFC])|\uD83C\uDDFC(?:\uD83C[\uDDEB\uDDF8])|\uD83C\uDDFA(?:\uD83C[\uDDE6\uDDEC\uDDF2\uDDF3\uDDF8\uDDFE\uDDFF])|\uD83C\uDDEE(?:\uD83C[\uDDE8-\uDDEA\uDDF1-\uDDF4\uDDF6-\uDDF9])|\uD83C\uDDEF(?:\uD83C[\uDDEA\uDDF2\uDDF4\uDDF5])|(?:\uD83C[\uDFC3\uDFC4\uDFCA]|\uD83D[\uDC6E\uDC71\uDC73\uDC77\uDC81\uDC82\uDC86\uDC87\uDE45-\uDE47\uDE4B\uDE4D\uDE4E\uDEA3\uDEB4-\uDEB6]|\uD83E[\uDD26\uDD37-\uDD39\uDD3D\uDD3E\uDDB8\uDDB9\uDDCD-\uDDCF\uDDD6-\uDDDD])(?:\uD83C[\uDFFB-\uDFFF])|(?:\u26F9|\uD83C[\uDFCB\uDFCC]|\uD83D\uDD75)(?:\uD83C[\uDFFB-\uDFFF])|(?:[\u261D\u270A-\u270D]|\uD83C[\uDF85\uDFC2\uDFC7]|\uD83D[\uDC42\uDC43\uDC46-\uDC50\uDC66\uDC67\uDC6B-\uDC6D\uDC70\uDC72\uDC74-\uDC76\uDC78\uDC7C\uDC83\uDC85\uDCAA\uDD74\uDD7A\uDD90\uDD95\uDD96\uDE4C\uDE4F\uDEC0\uDECC]|\uD83E[\uDD0F\uDD18-\uDD1C\uDD1E\uDD1F\uDD30-\uDD36\uDDB5\uDDB6\uDDBB\uDDD2-\uDDD5])(?:\uD83C[\uDFFB-\uDFFF])|(?:[\u231A\u231B\u23E9-\u23EC\u23F0\u23F3\u25FD\u25FE\u2614\u2615\u2648-\u2653\u267F\u2693\u26A1\u26AA\u26AB\u26BD\u26BE\u26C4\u26C5\u26CE\u26D4\u26EA\u26F2\u26F3\u26F5\u26FA\u26FD\u2705\u270A\u270B\u2728\u274C\u274E\u2753-\u2755\u2757\u2795-\u2797\u27B0\u27BF\u2B1B\u2B1C\u2B50\u2B55]|\uD83C[\uDC04\uDCCF\uDD8E\uDD91-\uDD9A\uDDE6-\uDDFF\uDE01\uDE1A\uDE2F\uDE32-\uDE36\uDE38-\uDE3A\uDE50\uDE51\uDF00-\uDF20\uDF2D-\uDF35\uDF37-\uDF7C\uDF7E-\uDF93\uDFA0-\uDFCA\uDFCF-\uDFD3\uDFE0-\uDFF0\uDFF4\uDFF8-\uDFFF]|\uD83D[\uDC00-\uDC3E\uDC40\uDC42-\uDCFC\uDCFF-\uDD3D\uDD4B-\uDD4E\uDD50-\uDD67\uDD7A\uDD95\uDD96\uDDA4\uDDFB-\uDE4F\uDE80-\uDEC5\uDECC\uDED0-\uDED2\uDED5\uDEEB\uDEEC\uDEF4-\uDEFA\uDFE0-\uDFEB]|\uD83E[\uDD0D-\uDD3A\uDD3C-\uDD45\uDD47-\uDD71\uDD73-\uDD76\uDD7A-\uDDA2\uDDA5-\uDDAA\uDDAE-\uDDCA\uDDCD-\uDDFF\uDE70-\uDE73\uDE78-\uDE7A\uDE80-\uDE82\uDE90-\uDE95])|(?:[#\*0-9\xA9\xAE\u203C\u2049\u2122\u2139\u2194-\u2199\u21A9\u21AA\u231A\u231B\u2328\u23CF\u23E9-\u23F3\u23F8-\u23FA\u24C2\u25AA\u25AB\u25B6\u25C0\u25FB-\u25FE\u2600-\u2604\u260E\u2611\u2614\u2615\u2618\u261D\u2620\u2622\u2623\u2626\u262A\u262E\u262F\u2638-\u263A\u2640\u2642\u2648-\u2653\u265F\u2660\u2663\u2665\u2666\u2668\u267B\u267E\u267F\u2692-\u2697\u2699\u269B\u269C\u26A0\u26A1\u26AA\u26AB\u26B0\u26B1\u26BD\u26BE\u26C4\u26C5\u26C8\u26CE\u26CF\u26D1\u26D3\u26D4\u26E9\u26EA\u26F0-\u26F5\u26F7-\u26FA\u26FD\u2702\u2705\u2708-\u270D\u270F\u2712\u2714\u2716\u271D\u2721\u2728\u2733\u2734\u2744\u2747\u274C\u274E\u2753-\u2755\u2757\u2763\u2764\u2795-\u2797\u27A1\u27B0\u27BF\u2934\u2935\u2B05-\u2B07\u2B1B\u2B1C\u2B50\u2B55\u3030\u303D\u3297\u3299]|\uD83C[\uDC04\uDCCF\uDD70\uDD71\uDD7E\uDD7F\uDD8E\uDD91-\uDD9A\uDDE6-\uDDFF\uDE01\uDE02\uDE1A\uDE2F\uDE32-\uDE3A\uDE50\uDE51\uDF00-\uDF21\uDF24-\uDF93\uDF96\uDF97\uDF99-\uDF9B\uDF9E-\uDFF0\uDFF3-\uDFF5\uDFF7-\uDFFF]|\uD83D[\uDC00-\uDCFD\uDCFF-\uDD3D\uDD49-\uDD4E\uDD50-\uDD67\uDD6F\uDD70\uDD73-\uDD7A\uDD87\uDD8A-\uDD8D\uDD90\uDD95\uDD96\uDDA4\uDDA5\uDDA8\uDDB1\uDDB2\uDDBC\uDDC2-\uDDC4\uDDD1-\uDDD3\uDDDC-\uDDDE\uDDE1\uDDE3\uDDE8\uDDEF\uDDF3\uDDFA-\uDE4F\uDE80-\uDEC5\uDECB-\uDED2\uDED5\uDEE0-\uDEE5\uDEE9\uDEEB\uDEEC\uDEF0\uDEF3-\uDEFA\uDFE0-\uDFEB]|\uD83E[\uDD0D-\uDD3A\uDD3C-\uDD45\uDD47-\uDD71\uDD73-\uDD76\uDD7A-\uDDA2\uDDA5-\uDDAA\uDDAE-\uDDCA\uDDCD-\uDDFF\uDE70-\uDE73\uDE78-\uDE7A\uDE80-\uDE82\uDE90-\uDE95])\uFE0F|(?:[\u261D\u26F9\u270A-\u270D]|\uD83C[\uDF85\uDFC2-\uDFC4\uDFC7\uDFCA-\uDFCC]|\uD83D[\uDC42\uDC43\uDC46-\uDC50\uDC66-\uDC78\uDC7C\uDC81-\uDC83\uDC85-\uDC87\uDC8F\uDC91\uDCAA\uDD74\uDD75\uDD7A\uDD90\uDD95\uDD96\uDE45-\uDE47\uDE4B-\uDE4F\uDEA3\uDEB4-\uDEB6\uDEC0\uDECC]|\uD83E[\uDD0F\uDD18-\uDD1F\uDD26\uDD30-\uDD39\uDD3C-\uDD3E\uDDB5\uDDB6\uDDB8\uDDB9\uDDBB\uDDCD-\uDDCF\uDDD1-\uDDDD])/g;
  }), je;
}
var Gt;
function gi() {
  if (Gt) return me.exports;
  Gt = 1;
  const e = Zn(), t = mi(), u = pi(), o = (n) => {
    if (typeof n != "string" || n.length === 0 || (n = e(n), n.length === 0))
      return 0;
    n = n.replace(u(), "  ");
    let i = 0;
    for (let r = 0; r < n.length; r++) {
      const s = n.codePointAt(r);
      s <= 31 || s >= 127 && s <= 159 || s >= 768 && s <= 879 || (s > 65535 && r++, i += t(s) ? 2 : 1);
    }
    return i;
  };
  return me.exports = o, me.exports.default = o, me.exports;
}
var Ee = { exports: {} }, Ne, qt;
function Ci() {
  return qt || (qt = 1, Ne = {
    aliceblue: [240, 248, 255],
    antiquewhite: [250, 235, 215],
    aqua: [0, 255, 255],
    aquamarine: [127, 255, 212],
    azure: [240, 255, 255],
    beige: [245, 245, 220],
    bisque: [255, 228, 196],
    black: [0, 0, 0],
    blanchedalmond: [255, 235, 205],
    blue: [0, 0, 255],
    blueviolet: [138, 43, 226],
    brown: [165, 42, 42],
    burlywood: [222, 184, 135],
    cadetblue: [95, 158, 160],
    chartreuse: [127, 255, 0],
    chocolate: [210, 105, 30],
    coral: [255, 127, 80],
    cornflowerblue: [100, 149, 237],
    cornsilk: [255, 248, 220],
    crimson: [220, 20, 60],
    cyan: [0, 255, 255],
    darkblue: [0, 0, 139],
    darkcyan: [0, 139, 139],
    darkgoldenrod: [184, 134, 11],
    darkgray: [169, 169, 169],
    darkgreen: [0, 100, 0],
    darkgrey: [169, 169, 169],
    darkkhaki: [189, 183, 107],
    darkmagenta: [139, 0, 139],
    darkolivegreen: [85, 107, 47],
    darkorange: [255, 140, 0],
    darkorchid: [153, 50, 204],
    darkred: [139, 0, 0],
    darksalmon: [233, 150, 122],
    darkseagreen: [143, 188, 143],
    darkslateblue: [72, 61, 139],
    darkslategray: [47, 79, 79],
    darkslategrey: [47, 79, 79],
    darkturquoise: [0, 206, 209],
    darkviolet: [148, 0, 211],
    deeppink: [255, 20, 147],
    deepskyblue: [0, 191, 255],
    dimgray: [105, 105, 105],
    dimgrey: [105, 105, 105],
    dodgerblue: [30, 144, 255],
    firebrick: [178, 34, 34],
    floralwhite: [255, 250, 240],
    forestgreen: [34, 139, 34],
    fuchsia: [255, 0, 255],
    gainsboro: [220, 220, 220],
    ghostwhite: [248, 248, 255],
    gold: [255, 215, 0],
    goldenrod: [218, 165, 32],
    gray: [128, 128, 128],
    green: [0, 128, 0],
    greenyellow: [173, 255, 47],
    grey: [128, 128, 128],
    honeydew: [240, 255, 240],
    hotpink: [255, 105, 180],
    indianred: [205, 92, 92],
    indigo: [75, 0, 130],
    ivory: [255, 255, 240],
    khaki: [240, 230, 140],
    lavender: [230, 230, 250],
    lavenderblush: [255, 240, 245],
    lawngreen: [124, 252, 0],
    lemonchiffon: [255, 250, 205],
    lightblue: [173, 216, 230],
    lightcoral: [240, 128, 128],
    lightcyan: [224, 255, 255],
    lightgoldenrodyellow: [250, 250, 210],
    lightgray: [211, 211, 211],
    lightgreen: [144, 238, 144],
    lightgrey: [211, 211, 211],
    lightpink: [255, 182, 193],
    lightsalmon: [255, 160, 122],
    lightseagreen: [32, 178, 170],
    lightskyblue: [135, 206, 250],
    lightslategray: [119, 136, 153],
    lightslategrey: [119, 136, 153],
    lightsteelblue: [176, 196, 222],
    lightyellow: [255, 255, 224],
    lime: [0, 255, 0],
    limegreen: [50, 205, 50],
    linen: [250, 240, 230],
    magenta: [255, 0, 255],
    maroon: [128, 0, 0],
    mediumaquamarine: [102, 205, 170],
    mediumblue: [0, 0, 205],
    mediumorchid: [186, 85, 211],
    mediumpurple: [147, 112, 219],
    mediumseagreen: [60, 179, 113],
    mediumslateblue: [123, 104, 238],
    mediumspringgreen: [0, 250, 154],
    mediumturquoise: [72, 209, 204],
    mediumvioletred: [199, 21, 133],
    midnightblue: [25, 25, 112],
    mintcream: [245, 255, 250],
    mistyrose: [255, 228, 225],
    moccasin: [255, 228, 181],
    navajowhite: [255, 222, 173],
    navy: [0, 0, 128],
    oldlace: [253, 245, 230],
    olive: [128, 128, 0],
    olivedrab: [107, 142, 35],
    orange: [255, 165, 0],
    orangered: [255, 69, 0],
    orchid: [218, 112, 214],
    palegoldenrod: [238, 232, 170],
    palegreen: [152, 251, 152],
    paleturquoise: [175, 238, 238],
    palevioletred: [219, 112, 147],
    papayawhip: [255, 239, 213],
    peachpuff: [255, 218, 185],
    peru: [205, 133, 63],
    pink: [255, 192, 203],
    plum: [221, 160, 221],
    powderblue: [176, 224, 230],
    purple: [128, 0, 128],
    rebeccapurple: [102, 51, 153],
    red: [255, 0, 0],
    rosybrown: [188, 143, 143],
    royalblue: [65, 105, 225],
    saddlebrown: [139, 69, 19],
    salmon: [250, 128, 114],
    sandybrown: [244, 164, 96],
    seagreen: [46, 139, 87],
    seashell: [255, 245, 238],
    sienna: [160, 82, 45],
    silver: [192, 192, 192],
    skyblue: [135, 206, 235],
    slateblue: [106, 90, 205],
    slategray: [112, 128, 144],
    slategrey: [112, 128, 144],
    snow: [255, 250, 250],
    springgreen: [0, 255, 127],
    steelblue: [70, 130, 180],
    tan: [210, 180, 140],
    teal: [0, 128, 128],
    thistle: [216, 191, 216],
    tomato: [255, 99, 71],
    turquoise: [64, 224, 208],
    violet: [238, 130, 238],
    wheat: [245, 222, 179],
    white: [255, 255, 255],
    whitesmoke: [245, 245, 245],
    yellow: [255, 255, 0],
    yellowgreen: [154, 205, 50]
  }), Ne;
}
var Ue, zt;
function Qn() {
  if (zt) return Ue;
  zt = 1;
  const e = Ci(), t = {};
  for (const n of Object.keys(e))
    t[e[n]] = n;
  const u = {
    rgb: { channels: 3, labels: "rgb" },
    hsl: { channels: 3, labels: "hsl" },
    hsv: { channels: 3, labels: "hsv" },
    hwb: { channels: 3, labels: "hwb" },
    cmyk: { channels: 4, labels: "cmyk" },
    xyz: { channels: 3, labels: "xyz" },
    lab: { channels: 3, labels: "lab" },
    lch: { channels: 3, labels: "lch" },
    hex: { channels: 1, labels: ["hex"] },
    keyword: { channels: 1, labels: ["keyword"] },
    ansi16: { channels: 1, labels: ["ansi16"] },
    ansi256: { channels: 1, labels: ["ansi256"] },
    hcg: { channels: 3, labels: ["h", "c", "g"] },
    apple: { channels: 3, labels: ["r16", "g16", "b16"] },
    gray: { channels: 1, labels: ["gray"] }
  };
  Ue = u;
  for (const n of Object.keys(u)) {
    if (!("channels" in u[n]))
      throw new Error("missing channels property: " + n);
    if (!("labels" in u[n]))
      throw new Error("missing channel labels property: " + n);
    if (u[n].labels.length !== u[n].channels)
      throw new Error("channel and label counts mismatch: " + n);
    const { channels: i, labels: r } = u[n];
    delete u[n].channels, delete u[n].labels, Object.defineProperty(u[n], "channels", { value: i }), Object.defineProperty(u[n], "labels", { value: r });
  }
  u.rgb.hsl = function(n) {
    const i = n[0] / 255, r = n[1] / 255, s = n[2] / 255, a = Math.min(i, r, s), l = Math.max(i, r, s), c = l - a;
    let f, p;
    l === a ? f = 0 : i === l ? f = (r - s) / c : r === l ? f = 2 + (s - i) / c : s === l && (f = 4 + (i - r) / c), f = Math.min(f * 60, 360), f < 0 && (f += 360);
    const g = (a + l) / 2;
    return l === a ? p = 0 : g <= 0.5 ? p = c / (l + a) : p = c / (2 - l - a), [f, p * 100, g * 100];
  }, u.rgb.hsv = function(n) {
    let i, r, s, a, l;
    const c = n[0] / 255, f = n[1] / 255, p = n[2] / 255, g = Math.max(c, f, p), C = g - Math.min(c, f, p), _ = function(O) {
      return (g - O) / 6 / C + 1 / 2;
    };
    return C === 0 ? (a = 0, l = 0) : (l = C / g, i = _(c), r = _(f), s = _(p), c === g ? a = s - r : f === g ? a = 1 / 3 + i - s : p === g && (a = 2 / 3 + r - i), a < 0 ? a += 1 : a > 1 && (a -= 1)), [
      a * 360,
      l * 100,
      g * 100
    ];
  }, u.rgb.hwb = function(n) {
    const i = n[0], r = n[1];
    let s = n[2];
    const a = u.rgb.hsl(n)[0], l = 1 / 255 * Math.min(i, Math.min(r, s));
    return s = 1 - 1 / 255 * Math.max(i, Math.max(r, s)), [a, l * 100, s * 100];
  }, u.rgb.cmyk = function(n) {
    const i = n[0] / 255, r = n[1] / 255, s = n[2] / 255, a = Math.min(1 - i, 1 - r, 1 - s), l = (1 - i - a) / (1 - a) || 0, c = (1 - r - a) / (1 - a) || 0, f = (1 - s - a) / (1 - a) || 0;
    return [l * 100, c * 100, f * 100, a * 100];
  };
  function o(n, i) {
    return (n[0] - i[0]) ** 2 + (n[1] - i[1]) ** 2 + (n[2] - i[2]) ** 2;
  }
  return u.rgb.keyword = function(n) {
    const i = t[n];
    if (i)
      return i;
    let r = 1 / 0, s;
    for (const a of Object.keys(e)) {
      const l = e[a], c = o(n, l);
      c < r && (r = c, s = a);
    }
    return s;
  }, u.keyword.rgb = function(n) {
    return e[n];
  }, u.rgb.xyz = function(n) {
    let i = n[0] / 255, r = n[1] / 255, s = n[2] / 255;
    i = i > 0.04045 ? ((i + 0.055) / 1.055) ** 2.4 : i / 12.92, r = r > 0.04045 ? ((r + 0.055) / 1.055) ** 2.4 : r / 12.92, s = s > 0.04045 ? ((s + 0.055) / 1.055) ** 2.4 : s / 12.92;
    const a = i * 0.4124 + r * 0.3576 + s * 0.1805, l = i * 0.2126 + r * 0.7152 + s * 0.0722, c = i * 0.0193 + r * 0.1192 + s * 0.9505;
    return [a * 100, l * 100, c * 100];
  }, u.rgb.lab = function(n) {
    const i = u.rgb.xyz(n);
    let r = i[0], s = i[1], a = i[2];
    r /= 95.047, s /= 100, a /= 108.883, r = r > 8856e-6 ? r ** (1 / 3) : 7.787 * r + 16 / 116, s = s > 8856e-6 ? s ** (1 / 3) : 7.787 * s + 16 / 116, a = a > 8856e-6 ? a ** (1 / 3) : 7.787 * a + 16 / 116;
    const l = 116 * s - 16, c = 500 * (r - s), f = 200 * (s - a);
    return [l, c, f];
  }, u.hsl.rgb = function(n) {
    const i = n[0] / 360, r = n[1] / 100, s = n[2] / 100;
    let a, l, c;
    if (r === 0)
      return c = s * 255, [c, c, c];
    s < 0.5 ? a = s * (1 + r) : a = s + r - s * r;
    const f = 2 * s - a, p = [0, 0, 0];
    for (let g = 0; g < 3; g++)
      l = i + 1 / 3 * -(g - 1), l < 0 && l++, l > 1 && l--, 6 * l < 1 ? c = f + (a - f) * 6 * l : 2 * l < 1 ? c = a : 3 * l < 2 ? c = f + (a - f) * (2 / 3 - l) * 6 : c = f, p[g] = c * 255;
    return p;
  }, u.hsl.hsv = function(n) {
    const i = n[0];
    let r = n[1] / 100, s = n[2] / 100, a = r;
    const l = Math.max(s, 0.01);
    s *= 2, r *= s <= 1 ? s : 2 - s, a *= l <= 1 ? l : 2 - l;
    const c = (s + r) / 2, f = s === 0 ? 2 * a / (l + a) : 2 * r / (s + r);
    return [i, f * 100, c * 100];
  }, u.hsv.rgb = function(n) {
    const i = n[0] / 60, r = n[1] / 100;
    let s = n[2] / 100;
    const a = Math.floor(i) % 6, l = i - Math.floor(i), c = 255 * s * (1 - r), f = 255 * s * (1 - r * l), p = 255 * s * (1 - r * (1 - l));
    switch (s *= 255, a) {
      case 0:
        return [s, p, c];
      case 1:
        return [f, s, c];
      case 2:
        return [c, s, p];
      case 3:
        return [c, f, s];
      case 4:
        return [p, c, s];
      case 5:
        return [s, c, f];
    }
  }, u.hsv.hsl = function(n) {
    const i = n[0], r = n[1] / 100, s = n[2] / 100, a = Math.max(s, 0.01);
    let l, c;
    c = (2 - r) * s;
    const f = (2 - r) * a;
    return l = r * a, l /= f <= 1 ? f : 2 - f, l = l || 0, c /= 2, [i, l * 100, c * 100];
  }, u.hwb.rgb = function(n) {
    const i = n[0] / 360;
    let r = n[1] / 100, s = n[2] / 100;
    const a = r + s;
    let l;
    a > 1 && (r /= a, s /= a);
    const c = Math.floor(6 * i), f = 1 - s;
    l = 6 * i - c, (c & 1) !== 0 && (l = 1 - l);
    const p = r + l * (f - r);
    let g, C, _;
    switch (c) {
      default:
      case 6:
      case 0:
        g = f, C = p, _ = r;
        break;
      case 1:
        g = p, C = f, _ = r;
        break;
      case 2:
        g = r, C = f, _ = p;
        break;
      case 3:
        g = r, C = p, _ = f;
        break;
      case 4:
        g = p, C = r, _ = f;
        break;
      case 5:
        g = f, C = r, _ = p;
        break;
    }
    return [g * 255, C * 255, _ * 255];
  }, u.cmyk.rgb = function(n) {
    const i = n[0] / 100, r = n[1] / 100, s = n[2] / 100, a = n[3] / 100, l = 1 - Math.min(1, i * (1 - a) + a), c = 1 - Math.min(1, r * (1 - a) + a), f = 1 - Math.min(1, s * (1 - a) + a);
    return [l * 255, c * 255, f * 255];
  }, u.xyz.rgb = function(n) {
    const i = n[0] / 100, r = n[1] / 100, s = n[2] / 100;
    let a, l, c;
    return a = i * 3.2406 + r * -1.5372 + s * -0.4986, l = i * -0.9689 + r * 1.8758 + s * 0.0415, c = i * 0.0557 + r * -0.204 + s * 1.057, a = a > 31308e-7 ? 1.055 * a ** (1 / 2.4) - 0.055 : a * 12.92, l = l > 31308e-7 ? 1.055 * l ** (1 / 2.4) - 0.055 : l * 12.92, c = c > 31308e-7 ? 1.055 * c ** (1 / 2.4) - 0.055 : c * 12.92, a = Math.min(Math.max(0, a), 1), l = Math.min(Math.max(0, l), 1), c = Math.min(Math.max(0, c), 1), [a * 255, l * 255, c * 255];
  }, u.xyz.lab = function(n) {
    let i = n[0], r = n[1], s = n[2];
    i /= 95.047, r /= 100, s /= 108.883, i = i > 8856e-6 ? i ** (1 / 3) : 7.787 * i + 16 / 116, r = r > 8856e-6 ? r ** (1 / 3) : 7.787 * r + 16 / 116, s = s > 8856e-6 ? s ** (1 / 3) : 7.787 * s + 16 / 116;
    const a = 116 * r - 16, l = 500 * (i - r), c = 200 * (r - s);
    return [a, l, c];
  }, u.lab.xyz = function(n) {
    const i = n[0], r = n[1], s = n[2];
    let a, l, c;
    l = (i + 16) / 116, a = r / 500 + l, c = l - s / 200;
    const f = l ** 3, p = a ** 3, g = c ** 3;
    return l = f > 8856e-6 ? f : (l - 16 / 116) / 7.787, a = p > 8856e-6 ? p : (a - 16 / 116) / 7.787, c = g > 8856e-6 ? g : (c - 16 / 116) / 7.787, a *= 95.047, l *= 100, c *= 108.883, [a, l, c];
  }, u.lab.lch = function(n) {
    const i = n[0], r = n[1], s = n[2];
    let a;
    a = Math.atan2(s, r) * 360 / 2 / Math.PI, a < 0 && (a += 360);
    const c = Math.sqrt(r * r + s * s);
    return [i, c, a];
  }, u.lch.lab = function(n) {
    const i = n[0], r = n[1], a = n[2] / 360 * 2 * Math.PI, l = r * Math.cos(a), c = r * Math.sin(a);
    return [i, l, c];
  }, u.rgb.ansi16 = function(n, i = null) {
    const [r, s, a] = n;
    let l = i === null ? u.rgb.hsv(n)[2] : i;
    if (l = Math.round(l / 50), l === 0)
      return 30;
    let c = 30 + (Math.round(a / 255) << 2 | Math.round(s / 255) << 1 | Math.round(r / 255));
    return l === 2 && (c += 60), c;
  }, u.hsv.ansi16 = function(n) {
    return u.rgb.ansi16(u.hsv.rgb(n), n[2]);
  }, u.rgb.ansi256 = function(n) {
    const i = n[0], r = n[1], s = n[2];
    return i === r && r === s ? i < 8 ? 16 : i > 248 ? 231 : Math.round((i - 8) / 247 * 24) + 232 : 16 + 36 * Math.round(i / 255 * 5) + 6 * Math.round(r / 255 * 5) + Math.round(s / 255 * 5);
  }, u.ansi16.rgb = function(n) {
    let i = n % 10;
    if (i === 0 || i === 7)
      return n > 50 && (i += 3.5), i = i / 10.5 * 255, [i, i, i];
    const r = (~~(n > 50) + 1) * 0.5, s = (i & 1) * r * 255, a = (i >> 1 & 1) * r * 255, l = (i >> 2 & 1) * r * 255;
    return [s, a, l];
  }, u.ansi256.rgb = function(n) {
    if (n >= 232) {
      const l = (n - 232) * 10 + 8;
      return [l, l, l];
    }
    n -= 16;
    let i;
    const r = Math.floor(n / 36) / 5 * 255, s = Math.floor((i = n % 36) / 6) / 5 * 255, a = i % 6 / 5 * 255;
    return [r, s, a];
  }, u.rgb.hex = function(n) {
    const r = (((Math.round(n[0]) & 255) << 16) + ((Math.round(n[1]) & 255) << 8) + (Math.round(n[2]) & 255)).toString(16).toUpperCase();
    return "000000".substring(r.length) + r;
  }, u.hex.rgb = function(n) {
    const i = n.toString(16).match(/[a-f0-9]{6}|[a-f0-9]{3}/i);
    if (!i)
      return [0, 0, 0];
    let r = i[0];
    i[0].length === 3 && (r = r.split("").map((f) => f + f).join(""));
    const s = parseInt(r, 16), a = s >> 16 & 255, l = s >> 8 & 255, c = s & 255;
    return [a, l, c];
  }, u.rgb.hcg = function(n) {
    const i = n[0] / 255, r = n[1] / 255, s = n[2] / 255, a = Math.max(Math.max(i, r), s), l = Math.min(Math.min(i, r), s), c = a - l;
    let f, p;
    return c < 1 ? f = l / (1 - c) : f = 0, c <= 0 ? p = 0 : a === i ? p = (r - s) / c % 6 : a === r ? p = 2 + (s - i) / c : p = 4 + (i - r) / c, p /= 6, p %= 1, [p * 360, c * 100, f * 100];
  }, u.hsl.hcg = function(n) {
    const i = n[1] / 100, r = n[2] / 100, s = r < 0.5 ? 2 * i * r : 2 * i * (1 - r);
    let a = 0;
    return s < 1 && (a = (r - 0.5 * s) / (1 - s)), [n[0], s * 100, a * 100];
  }, u.hsv.hcg = function(n) {
    const i = n[1] / 100, r = n[2] / 100, s = i * r;
    let a = 0;
    return s < 1 && (a = (r - s) / (1 - s)), [n[0], s * 100, a * 100];
  }, u.hcg.rgb = function(n) {
    const i = n[0] / 360, r = n[1] / 100, s = n[2] / 100;
    if (r === 0)
      return [s * 255, s * 255, s * 255];
    const a = [0, 0, 0], l = i % 1 * 6, c = l % 1, f = 1 - c;
    let p = 0;
    switch (Math.floor(l)) {
      case 0:
        a[0] = 1, a[1] = c, a[2] = 0;
        break;
      case 1:
        a[0] = f, a[1] = 1, a[2] = 0;
        break;
      case 2:
        a[0] = 0, a[1] = 1, a[2] = c;
        break;
      case 3:
        a[0] = 0, a[1] = f, a[2] = 1;
        break;
      case 4:
        a[0] = c, a[1] = 0, a[2] = 1;
        break;
      default:
        a[0] = 1, a[1] = 0, a[2] = f;
    }
    return p = (1 - r) * s, [
      (r * a[0] + p) * 255,
      (r * a[1] + p) * 255,
      (r * a[2] + p) * 255
    ];
  }, u.hcg.hsv = function(n) {
    const i = n[1] / 100, r = n[2] / 100, s = i + r * (1 - i);
    let a = 0;
    return s > 0 && (a = i / s), [n[0], a * 100, s * 100];
  }, u.hcg.hsl = function(n) {
    const i = n[1] / 100, s = n[2] / 100 * (1 - i) + 0.5 * i;
    let a = 0;
    return s > 0 && s < 0.5 ? a = i / (2 * s) : s >= 0.5 && s < 1 && (a = i / (2 * (1 - s))), [n[0], a * 100, s * 100];
  }, u.hcg.hwb = function(n) {
    const i = n[1] / 100, r = n[2] / 100, s = i + r * (1 - i);
    return [n[0], (s - i) * 100, (1 - s) * 100];
  }, u.hwb.hcg = function(n) {
    const i = n[1] / 100, s = 1 - n[2] / 100, a = s - i;
    let l = 0;
    return a < 1 && (l = (s - a) / (1 - a)), [n[0], a * 100, l * 100];
  }, u.apple.rgb = function(n) {
    return [n[0] / 65535 * 255, n[1] / 65535 * 255, n[2] / 65535 * 255];
  }, u.rgb.apple = function(n) {
    return [n[0] / 255 * 65535, n[1] / 255 * 65535, n[2] / 255 * 65535];
  }, u.gray.rgb = function(n) {
    return [n[0] / 100 * 255, n[0] / 100 * 255, n[0] / 100 * 255];
  }, u.gray.hsl = function(n) {
    return [0, 0, n[0]];
  }, u.gray.hsv = u.gray.hsl, u.gray.hwb = function(n) {
    return [0, 100, n[0]];
  }, u.gray.cmyk = function(n) {
    return [0, 0, 0, n[0]];
  }, u.gray.lab = function(n) {
    return [n[0], 0, 0];
  }, u.gray.hex = function(n) {
    const i = Math.round(n[0] / 100 * 255) & 255, s = ((i << 16) + (i << 8) + i).toString(16).toUpperCase();
    return "000000".substring(s.length) + s;
  }, u.rgb.gray = function(n) {
    return [(n[0] + n[1] + n[2]) / 3 / 255 * 100];
  }, Ue;
}
var Ve, Yt;
function Ei() {
  if (Yt) return Ve;
  Yt = 1;
  const e = Qn();
  function t() {
    const i = {}, r = Object.keys(e);
    for (let s = r.length, a = 0; a < s; a++)
      i[r[a]] = {
        // http://jsperf.com/1-vs-infinity
        // micro-opt, but this is simple.
        distance: -1,
        parent: null
      };
    return i;
  }
  function u(i) {
    const r = t(), s = [i];
    for (r[i].distance = 0; s.length; ) {
      const a = s.pop(), l = Object.keys(e[a]);
      for (let c = l.length, f = 0; f < c; f++) {
        const p = l[f], g = r[p];
        g.distance === -1 && (g.distance = r[a].distance + 1, g.parent = a, s.unshift(p));
      }
    }
    return r;
  }
  function o(i, r) {
    return function(s) {
      return r(i(s));
    };
  }
  function n(i, r) {
    const s = [r[i].parent, i];
    let a = e[r[i].parent][i], l = r[i].parent;
    for (; r[l].parent; )
      s.unshift(r[l].parent), a = o(e[r[l].parent][l], a), l = r[l].parent;
    return a.conversion = s, a;
  }
  return Ve = function(i) {
    const r = u(i), s = {}, a = Object.keys(r);
    for (let l = a.length, c = 0; c < l; c++) {
      const f = a[c];
      r[f].parent !== null && (s[f] = n(f, r));
    }
    return s;
  }, Ve;
}
var Ge, Kt;
function _i() {
  if (Kt) return Ge;
  Kt = 1;
  const e = Qn(), t = Ei(), u = {}, o = Object.keys(e);
  function n(r) {
    const s = function(...a) {
      const l = a[0];
      return l == null ? l : (l.length > 1 && (a = l), r(a));
    };
    return "conversion" in r && (s.conversion = r.conversion), s;
  }
  function i(r) {
    const s = function(...a) {
      const l = a[0];
      if (l == null)
        return l;
      l.length > 1 && (a = l);
      const c = r(a);
      if (typeof c == "object")
        for (let f = c.length, p = 0; p < f; p++)
          c[p] = Math.round(c[p]);
      return c;
    };
    return "conversion" in r && (s.conversion = r.conversion), s;
  }
  return o.forEach((r) => {
    u[r] = {}, Object.defineProperty(u[r], "channels", { value: e[r].channels }), Object.defineProperty(u[r], "labels", { value: e[r].labels });
    const s = t(r);
    Object.keys(s).forEach((l) => {
      const c = s[l];
      u[r][l] = i(c), u[r][l].raw = n(c);
    });
  }), Ge = u, Ge;
}
Ee.exports;
var Jt;
function wi() {
  return Jt || (Jt = 1, (function(e) {
    const t = (c, f) => (...p) => `\x1B[${c(...p) + f}m`, u = (c, f) => (...p) => {
      const g = c(...p);
      return `\x1B[${38 + f};5;${g}m`;
    }, o = (c, f) => (...p) => {
      const g = c(...p);
      return `\x1B[${38 + f};2;${g[0]};${g[1]};${g[2]}m`;
    }, n = (c) => c, i = (c, f, p) => [c, f, p], r = (c, f, p) => {
      Object.defineProperty(c, f, {
        get: () => {
          const g = p();
          return Object.defineProperty(c, f, {
            value: g,
            enumerable: !0,
            configurable: !0
          }), g;
        },
        enumerable: !0,
        configurable: !0
      });
    };
    let s;
    const a = (c, f, p, g) => {
      s === void 0 && (s = _i());
      const C = g ? 10 : 0, _ = {};
      for (const [O, D] of Object.entries(s)) {
        const h = O === "ansi16" ? "ansi" : O;
        O === f ? _[h] = c(p, C) : typeof D == "object" && (_[h] = c(D[f], C));
      }
      return _;
    };
    function l() {
      const c = /* @__PURE__ */ new Map(), f = {
        modifier: {
          reset: [0, 0],
          // 21 isn't widely supported and 22 does the same thing
          bold: [1, 22],
          dim: [2, 22],
          italic: [3, 23],
          underline: [4, 24],
          inverse: [7, 27],
          hidden: [8, 28],
          strikethrough: [9, 29]
        },
        color: {
          black: [30, 39],
          red: [31, 39],
          green: [32, 39],
          yellow: [33, 39],
          blue: [34, 39],
          magenta: [35, 39],
          cyan: [36, 39],
          white: [37, 39],
          // Bright color
          blackBright: [90, 39],
          redBright: [91, 39],
          greenBright: [92, 39],
          yellowBright: [93, 39],
          blueBright: [94, 39],
          magentaBright: [95, 39],
          cyanBright: [96, 39],
          whiteBright: [97, 39]
        },
        bgColor: {
          bgBlack: [40, 49],
          bgRed: [41, 49],
          bgGreen: [42, 49],
          bgYellow: [43, 49],
          bgBlue: [44, 49],
          bgMagenta: [45, 49],
          bgCyan: [46, 49],
          bgWhite: [47, 49],
          // Bright color
          bgBlackBright: [100, 49],
          bgRedBright: [101, 49],
          bgGreenBright: [102, 49],
          bgYellowBright: [103, 49],
          bgBlueBright: [104, 49],
          bgMagentaBright: [105, 49],
          bgCyanBright: [106, 49],
          bgWhiteBright: [107, 49]
        }
      };
      f.color.gray = f.color.blackBright, f.bgColor.bgGray = f.bgColor.bgBlackBright, f.color.grey = f.color.blackBright, f.bgColor.bgGrey = f.bgColor.bgBlackBright;
      for (const [p, g] of Object.entries(f)) {
        for (const [C, _] of Object.entries(g))
          f[C] = {
            open: `\x1B[${_[0]}m`,
            close: `\x1B[${_[1]}m`
          }, g[C] = f[C], c.set(_[0], _[1]);
        Object.defineProperty(f, p, {
          value: g,
          enumerable: !1
        });
      }
      return Object.defineProperty(f, "codes", {
        value: c,
        enumerable: !1
      }), f.color.close = "\x1B[39m", f.bgColor.close = "\x1B[49m", r(f.color, "ansi", () => a(t, "ansi16", n, !1)), r(f.color, "ansi256", () => a(u, "ansi256", n, !1)), r(f.color, "ansi16m", () => a(o, "rgb", i, !1)), r(f.bgColor, "ansi", () => a(t, "ansi16", n, !0)), r(f.bgColor, "ansi256", () => a(u, "ansi256", n, !0)), r(f.bgColor, "ansi16m", () => a(o, "rgb", i, !0)), f;
    }
    Object.defineProperty(e, "exports", {
      enumerable: !0,
      get: l
    });
  })(Ee)), Ee.exports;
}
var qe, Xt;
function bi() {
  if (Xt) return qe;
  Xt = 1;
  const e = gi(), t = Zn(), u = wi(), o = /* @__PURE__ */ new Set([
    "\x1B",
    ""
  ]), n = 39, i = (c) => `${o.values().next().value}[${c}m`, r = (c) => c.split(" ").map((f) => e(f)), s = (c, f, p) => {
    const g = [...f];
    let C = !1, _ = e(t(c[c.length - 1]));
    for (const [O, D] of g.entries()) {
      const h = e(D);
      if (_ + h <= p ? c[c.length - 1] += D : (c.push(D), _ = 0), o.has(D))
        C = !0;
      else if (C && D === "m") {
        C = !1;
        continue;
      }
      C || (_ += h, _ === p && O < g.length - 1 && (c.push(""), _ = 0));
    }
    !_ && c[c.length - 1].length > 0 && c.length > 1 && (c[c.length - 2] += c.pop());
  }, a = (c) => {
    const f = c.split(" ");
    let p = f.length;
    for (; p > 0 && !(e(f[p - 1]) > 0); )
      p--;
    return p === f.length ? c : f.slice(0, p).join(" ") + f.slice(p).join("");
  }, l = (c, f, p = {}) => {
    if (p.trim !== !1 && c.trim() === "")
      return "";
    let g = "", C = "", _;
    const O = r(c);
    let D = [""];
    for (const [h, d] of c.split(" ").entries()) {
      p.trim !== !1 && (D[D.length - 1] = D[D.length - 1].trimLeft());
      let F = e(D[D.length - 1]);
      if (h !== 0 && (F >= f && (p.wordWrap === !1 || p.trim === !1) && (D.push(""), F = 0), (F > 0 || p.trim === !1) && (D[D.length - 1] += " ", F++)), p.hard && O[h] > f) {
        const m = f - F, E = 1 + Math.floor((O[h] - m - 1) / f);
        Math.floor((O[h] - 1) / f) < E && D.push(""), s(D, d, f);
        continue;
      }
      if (F + O[h] > f && F > 0 && O[h] > 0) {
        if (p.wordWrap === !1 && F < f) {
          s(D, d, f);
          continue;
        }
        D.push("");
      }
      if (F + O[h] > f && p.wordWrap === !1) {
        s(D, d, f);
        continue;
      }
      D[D.length - 1] += d;
    }
    p.trim !== !1 && (D = D.map(a)), g = D.join(`
`);
    for (const [h, d] of [...g].entries()) {
      if (C += d, o.has(d)) {
        const m = parseFloat(/\d[^m]*/.exec(g.slice(h, h + 4)));
        _ = m === n ? null : m;
      }
      const F = u.codes.get(Number(_));
      _ && F && (g[h + 1] === `
` ? C += i(F) : d === `
` && (C += i(_)));
    }
    return C;
  };
  return qe = (c, f, p) => String(c).normalize().replace(/\r\n/g, `
`).split(`
`).map((g) => l(g, f, p)).join(`
`), qe;
}
var Bi = bi();
const Ai = /* @__PURE__ */ Y(Bi);
function Zt(e, t) {
  return e.split(`
`).flatMap((u) => Ai(u, t, { trim: !1, hard: !0 }).split(`
`).map((o) => o.trimEnd())).join(`
`);
}
function yi() {
  return di({ defaultWidth: 80, output: Gn().output });
}
var ze, Qt;
function vi() {
  if (Qt) return ze;
  Qt = 1;
  const e = Au;
  class t extends e {
    #u = null;
    constructor(o = {}) {
      super(o), this.writable = this.readable = !0, this.muted = !1, this.on("pipe", this._onpipe), this.replace = o.replace, this._prompt = o.prompt || null, this._hadControl = !1;
    }
    #n(o, n) {
      return this._dest ? this._dest[o] : this._src ? this._src[o] : n;
    }
    #e(o, ...n) {
      typeof this._dest?.[o] == "function" && this._dest[o](...n), typeof this._src?.[o] == "function" && this._src[o](...n);
    }
    get isTTY() {
      return this.#u !== null ? this.#u : this.#n("isTTY", !1);
    }
    // basically just get replace the getter/setter with a regular value
    set isTTY(o) {
      this.#u = o;
    }
    get rows() {
      return this.#n("rows");
    }
    get columns() {
      return this.#n("columns");
    }
    mute() {
      this.muted = !0;
    }
    unmute() {
      this.muted = !1;
    }
    _onpipe(o) {
      this._src = o;
    }
    pipe(o, n) {
      return this._dest = o, super.pipe(o, n);
    }
    pause() {
      if (this._src)
        return this._src.pause();
    }
    resume() {
      if (this._src)
        return this._src.resume();
    }
    write(o) {
      if (this.muted) {
        if (!this.replace)
          return !0;
        if (o.match(/^\u001b/))
          return o.indexOf(this._prompt) === 0 && (o = o.slice(this._prompt.length), o = o.replace(/./g, this.replace), o = this._prompt + o), this._hadControl = !0, this.emit("data", o);
        this._prompt && this._hadControl && o.indexOf(this._prompt) === 0 && (this._hadControl = !1, this.emit("data", this._prompt), o = o.slice(this._prompt.length)), o = o.toString().replace(/./g, this.replace);
      }
      this.emit("data", o);
    }
    end(o) {
      this.muted && (o && this.replace ? o = o.toString().replace(/./g, this.replace) : o = null), o && this.emit("data", o), this.emit("end");
    }
    destroy(...o) {
      return this.#e("destroy", ...o);
    }
    destroySoon(...o) {
      return this.#e("destroySoon", ...o);
    }
    close(...o) {
      return this.#e("close", ...o);
    }
  }
  return ze = t, ze;
}
var xi = vi();
const Oi = /* @__PURE__ */ Y(xi), X = [];
X.push("SIGHUP", "SIGINT", "SIGTERM");
process.platform !== "win32" && X.push(
  "SIGALRM",
  "SIGABRT",
  "SIGVTALRM",
  "SIGXCPU",
  "SIGXFSZ",
  "SIGUSR2",
  "SIGTRAP",
  "SIGSYS",
  "SIGQUIT",
  "SIGIOT"
  // should detect profiler and enable/disable accordingly.
  // see #21
  // 'SIGPROF'
);
process.platform === "linux" && X.push("SIGIO", "SIGPOLL", "SIGPWR", "SIGSTKFLT");
const _e = (e) => !!e && typeof e == "object" && typeof e.removeListener == "function" && typeof e.emit == "function" && typeof e.reallyExit == "function" && typeof e.listeners == "function" && typeof e.kill == "function" && typeof e.pid == "number" && typeof e.on == "function", Ye = Symbol.for("signal-exit emitter"), Ke = globalThis, Si = Object.defineProperty.bind(Object);
class Ti {
  emitted = {
    afterExit: !1,
    exit: !1
  };
  listeners = {
    afterExit: [],
    exit: []
  };
  count = 0;
  id = Math.random();
  constructor() {
    if (Ke[Ye])
      return Ke[Ye];
    Si(Ke, Ye, {
      value: this,
      writable: !1,
      enumerable: !1,
      configurable: !1
    });
  }
  on(t, u) {
    this.listeners[t].push(u);
  }
  removeListener(t, u) {
    const o = this.listeners[t], n = o.indexOf(u);
    n !== -1 && (n === 0 && o.length === 1 ? o.length = 0 : o.splice(n, 1));
  }
  emit(t, u, o) {
    if (this.emitted[t])
      return !1;
    this.emitted[t] = !0;
    let n = !1;
    for (const i of this.listeners[t])
      n = i(u, o) === !0 || n;
    return t === "exit" && (n = this.emit("afterExit", u, o) || n), n;
  }
}
class eu {
}
const Pi = (e) => ({
  onExit(t, u) {
    return e.onExit(t, u);
  },
  load() {
    return e.load();
  },
  unload() {
    return e.unload();
  }
});
class ki extends eu {
  onExit() {
    return () => {
    };
  }
  load() {
  }
  unload() {
  }
}
class Ri extends eu {
  // "SIGHUP" throws an `ENOSYS` error on Windows,
  // so use a supported signal instead
  /* c8 ignore start */
  #u = Dt.platform === "win32" ? "SIGINT" : "SIGHUP";
  /* c8 ignore stop */
  #n = new Ti();
  #e;
  #r;
  #h;
  #t = {};
  #s = !1;
  constructor(t) {
    super(), this.#e = t, this.#t = {};
    for (const u of X)
      this.#t[u] = () => {
        const o = this.#e.listeners(u);
        let { count: n } = this.#n;
        const i = t;
        if (typeof i.__signal_exit_emitter__ == "object" && typeof i.__signal_exit_emitter__.count == "number" && (n += i.__signal_exit_emitter__.count), o.length === n) {
          this.unload();
          const r = this.#n.emit("exit", null, u), s = u === "SIGHUP" ? this.#u : u;
          r || t.kill(t.pid, s);
        }
      };
    this.#h = t.reallyExit, this.#r = t.emit;
  }
  onExit(t, u) {
    if (!_e(this.#e))
      return () => {
      };
    this.#s === !1 && this.load();
    const o = u?.alwaysLast ? "afterExit" : "exit";
    return this.#n.on(o, t), () => {
      this.#n.removeListener(o, t), this.#n.listeners.exit.length === 0 && this.#n.listeners.afterExit.length === 0 && this.unload();
    };
  }
  load() {
    if (!this.#s) {
      this.#s = !0, this.#n.count += 1;
      for (const t of X)
        try {
          const u = this.#t[t];
          u && this.#e.on(t, u);
        } catch {
        }
      this.#e.emit = (t, ...u) => this.#f(t, ...u), this.#e.reallyExit = (t) => this.#i(t);
    }
  }
  unload() {
    this.#s && (this.#s = !1, X.forEach((t) => {
      const u = this.#t[t];
      if (!u)
        throw new Error("Listener not defined for signal: " + t);
      try {
        this.#e.removeListener(t, u);
      } catch {
      }
    }), this.#e.emit = this.#r, this.#e.reallyExit = this.#h, this.#n.count -= 1);
  }
  #i(t) {
    return _e(this.#e) ? (this.#e.exitCode = t || 0, this.#n.emit("exit", this.#e.exitCode, null), this.#h.call(this.#e, this.#e.exitCode)) : 0;
  }
  #f(t, ...u) {
    const o = this.#r;
    if (t === "exit" && _e(this.#e)) {
      typeof u[0] == "number" && (this.#e.exitCode = u[0]);
      const n = o.call(this.#e, t, ...u);
      return this.#n.emit("exit", this.#e.exitCode, null), n;
    } else
      return o.call(this.#e, t, ...u);
  }
}
const Dt = globalThis.process, {
  /**
   * Called when the process is exiting, whether via signal, explicit
   * exit, or running out of stuff to do.
   *
   * If the global process object is not suitable for instrumentation,
   * then this will be a no-op.
   *
   * Returns a function that may be used to unload signal-exit.
   */
  onExit: tu
} = Pi(_e(Dt) ? new Ri(Dt) : new ki());
var Je = { exports: {} }, en;
function $i() {
  return en || (en = 1, (function(e) {
    const t = e.exports;
    e.exports.default = t;
    const u = "\x1B[", o = "\x1B]", n = "\x07", i = ";", r = process.env.TERM_PROGRAM === "Apple_Terminal";
    t.cursorTo = (s, a) => {
      if (typeof s != "number")
        throw new TypeError("The `x` argument is required");
      return typeof a != "number" ? u + (s + 1) + "G" : u + (a + 1) + ";" + (s + 1) + "H";
    }, t.cursorMove = (s, a) => {
      if (typeof s != "number")
        throw new TypeError("The `x` argument is required");
      let l = "";
      return s < 0 ? l += u + -s + "D" : s > 0 && (l += u + s + "C"), a < 0 ? l += u + -a + "A" : a > 0 && (l += u + a + "B"), l;
    }, t.cursorUp = (s = 1) => u + s + "A", t.cursorDown = (s = 1) => u + s + "B", t.cursorForward = (s = 1) => u + s + "C", t.cursorBackward = (s = 1) => u + s + "D", t.cursorLeft = u + "G", t.cursorSavePosition = r ? "\x1B7" : u + "s", t.cursorRestorePosition = r ? "\x1B8" : u + "u", t.cursorGetPosition = u + "6n", t.cursorNextLine = u + "E", t.cursorPrevLine = u + "F", t.cursorHide = u + "?25l", t.cursorShow = u + "?25h", t.eraseLines = (s) => {
      let a = "";
      for (let l = 0; l < s; l++)
        a += t.eraseLine + (l < s - 1 ? t.cursorUp() : "");
      return s && (a += t.cursorLeft), a;
    }, t.eraseEndLine = u + "K", t.eraseStartLine = u + "1K", t.eraseLine = u + "2K", t.eraseDown = u + "J", t.eraseUp = u + "1J", t.eraseScreen = u + "2J", t.scrollUp = u + "S", t.scrollDown = u + "T", t.clearScreen = "\x1Bc", t.clearTerminal = process.platform === "win32" ? `${t.eraseScreen}${u}0f` : (
      // 1. Erases the screen (Only done in case `2` is not supported)
      // 2. Erases the whole screen including scrollback buffer
      // 3. Moves cursor to the top-left position
      // More info: https://www.real-world-systems.com/docs/ANSIcode.html
      `${t.eraseScreen}${u}3J${u}H`
    ), t.beep = n, t.link = (s, a) => [
      o,
      "8",
      i,
      i,
      a,
      n,
      s,
      o,
      "8",
      i,
      i,
      n
    ].join(""), t.image = (s, a = {}) => {
      let l = `${o}1337;File=inline=1`;
      return a.width && (l += `;width=${a.width}`), a.height && (l += `;height=${a.height}`), a.preserveAspectRatio === !1 && (l += ";preserveAspectRatio=0"), l + ":" + s.toString("base64") + n;
    }, t.iTerm = {
      setCwd: (s = process.cwd()) => `${o}50;CurrentDir=${s}${n}`,
      annotation: (s, a = {}) => {
        let l = `${o}1337;`;
        const c = typeof a.x < "u", f = typeof a.y < "u";
        if ((c || f) && !(c && f && typeof a.length < "u"))
          throw new Error("`x`, `y` and `length` must be defined when `x` or `y` is defined");
        return s = s.replace(/\|/g, ""), l += a.isHidden ? "AddHiddenAnnotation=" : "AddAnnotation=", a.length > 0 ? l += (c ? [s, a.length, a.x, a.y] : [a.length, s]).join("|") : l += s, l + n;
      }
    };
  })(Je)), Je.exports;
}
var Ii = $i();
const V = /* @__PURE__ */ Y(Ii), tn = (e) => e.split(`
`).length, Li = (e) => e.split(`
`).pop() ?? "";
function nn(e) {
  return e > 0 ? V.cursorDown(e) : "";
}
class Mi {
  // These variables are keeping information to allow correct prompt re-rendering
  height = 0;
  extraLinesUnderPrompt = 0;
  cursorPos;
  rl;
  constructor(t) {
    this.rl = t, this.cursorPos = t.getCursorPos();
  }
  write(t) {
    this.rl.output.unmute(), this.rl.output.write(t), this.rl.output.mute();
  }
  render(t, u = "") {
    const o = Li(t), n = yu(o);
    let i = n;
    this.rl.line.length > 0 && (i = i.slice(0, -this.rl.line.length)), this.rl.setPrompt(i), this.cursorPos = this.rl.getCursorPos();
    const r = yi();
    t = Zt(t, r), u = Zt(u, r), n.length % r === 0 && (t += `
`);
    let s = t + (u ? `
` + u : "");
    const l = Math.floor(n.length / r) - this.cursorPos.rows + (u ? tn(u) : 0);
    l > 0 && (s += V.cursorUp(l)), s += V.cursorTo(this.cursorPos.cols), this.write(nn(this.extraLinesUnderPrompt) + V.eraseLines(this.height) + s), this.extraLinesUnderPrompt = l, this.height = tn(s);
  }
  checkCursorPos() {
    const t = this.rl.getCursorPos();
    t.cols !== this.cursorPos.cols && (this.write(V.cursorTo(t.cols)), this.cursorPos = t);
  }
  done({ clearContent: t }) {
    this.rl.setPrompt("");
    let u = nn(this.extraLinesUnderPrompt);
    u += t ? V.eraseLines(this.height) : `
`, u += V.cursorShow, this.write(u), this.rl.close();
  }
}
class Wi extends Promise {
  // Available starting from Node 22
  // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Promise/withResolvers
  static withResolver() {
    let t, u;
    return { promise: new Promise((n, i) => {
      t = n, u = i;
    }), resolve: t, reject: u };
  }
}
function Hi() {
  const e = Error.prepareStackTrace;
  let t = [];
  try {
    Error.prepareStackTrace = (u, o) => {
      const n = o.slice(1);
      return t = n, n;
    }, new Error().stack;
  } catch {
    return t;
  }
  return Error.prepareStackTrace = e, t;
}
function nu(e) {
  const t = Hi();
  return (o, n = {}) => {
    const { input: i = process.stdin, signal: r } = n, s = /* @__PURE__ */ new Set(), a = new Oi();
    a.pipe(n.output ?? process.stdout);
    const l = bu.createInterface({
      terminal: !0,
      input: i,
      output: a
    }), c = new Mi(l), { promise: f, resolve: p, reject: g } = Wi.withResolver(), C = () => g(new Ku());
    if (r) {
      const D = () => g(new Yu({ cause: r.reason }));
      if (r.aborted)
        return D(), Object.assign(f, { cancel: C });
      r.addEventListener("abort", D), s.add(() => r.removeEventListener("abort", D));
    }
    s.add(tu((D, h) => {
      g(new Lt(`User force closed the prompt with ${D} ${h}`));
    }));
    const _ = () => g(new Lt("User force closed the prompt with SIGINT"));
    l.on("SIGINT", _), s.add(() => l.removeListener("SIGINT", _));
    const O = () => c.checkCursorPos();
    return l.input.on("keypress", O), s.add(() => l.input.removeListener("keypress", O)), Qu(l, (D) => {
      const h = Ct.bind(() => ne.clearAll());
      return l.on("close", h), s.add(() => l.removeListener("close", h)), D(() => {
        try {
          const d = e(o, (E) => {
            setImmediate(() => p(E));
          });
          if (d === void 0) {
            const E = t[1]?.getFileName();
            throw new Error(`Prompt functions must return a string.
    at ${E}`);
          }
          const [F, m] = typeof d == "string" ? [d] : d;
          c.render(F, m), ne.run();
        } catch (d) {
          g(d);
        }
      }), Object.assign(f.then((d) => (ne.clearAll(), d), (d) => {
        throw ne.clearAll(), d;
      }).finally(() => {
        s.forEach((d) => d()), c.done({ clearContent: !!n.clearPromptOnDone }), a.end();
      }).then(() => f), { cancel: C });
    });
  };
}
function un(e, t) {
  let u = t !== !1;
  return /^(y|yes)/i.test(e) ? u = !0 : /^(n|no)/i.test(e) && (u = !1), u;
}
function rn(e) {
  return e ? "Yes" : "No";
}
const Pe = nu((e, t) => {
  const { transformer: u = rn } = e, [o, n] = N("idle"), [i, r] = N(""), s = Bt(e.theme), a = Jn({ status: o, theme: s });
  Xn((p, g) => {
    if (o === "idle")
      if (Un(p)) {
        const C = un(i, e.default);
        r(u(C)), n("done"), t(C);
      } else if (Nn(p)) {
        const C = rn(!un(i, e.default));
        g.clearLine(0), g.write(C), r(C);
      } else
        r(g.line);
  });
  let l = i, c = "";
  o === "done" ? l = s.style.answer(i) : c = ` ${s.style.defaultAnswer(e.default === !1 ? "y/N" : "Y/n")}`;
  const f = s.style.message(e.message, o);
  return `${a} ${f}${c} ${l}`;
}), ji = {
  validationFailureMode: "keep"
}, at = nu((e, t) => {
  const { required: u, validate: o = () => !0, prefill: n = "tab" } = e, i = Bt(ji, e.theme), [r, s] = N("idle"), [a = "", l] = N(e.default), [c, f] = N(), [p, g] = N(""), C = Jn({ status: r, theme: i });
  Xn(async (d, F) => {
    if (r === "idle")
      if (Un(d)) {
        const m = p || a;
        s("loading");
        const E = u && !m ? "You must provide a value" : await o(m);
        E === !0 ? (g(m), s("done"), t(m)) : (i.validationFailureMode === "clear" ? g("") : F.write(p), f(E || "You must provide a valid value"), s("idle"));
      } else zu(d) && !p ? l(void 0) : Nn(d) && !p ? (l(void 0), F.clearLine(0), F.write(a), g(a)) : (g(F.line), f(void 0));
  }), bt((d) => {
    n === "editable" && a && (d.write(a), g(a));
  }, []);
  const _ = i.style.message(e.message, r);
  let O = p;
  typeof e.transformer == "function" ? O = e.transformer(p, { isFinal: r === "done" }) : r === "done" && (O = i.style.answer(p));
  let D;
  a && r !== "done" && !p && (D = i.style.defaultAnswer(a));
  let h = "";
  return c && (h = i.style.error(c)), [
    [C, _, D, O].filter((d) => d !== void 0).join(" "),
    h
  ];
}), Xe = 10, sn = (e = 0) => (t) => `\x1B[${t + e}m`, on = (e = 0) => (t) => `\x1B[${38 + e};5;${t}m`, Dn = (e = 0) => (t, u, o) => `\x1B[${38 + e};2;${t};${u};${o}m`, T = {
  modifier: {
    reset: [0, 0],
    // 21 isn't widely supported and 22 does the same thing
    bold: [1, 22],
    dim: [2, 22],
    italic: [3, 23],
    underline: [4, 24],
    overline: [53, 55],
    inverse: [7, 27],
    hidden: [8, 28],
    strikethrough: [9, 29]
  },
  color: {
    black: [30, 39],
    red: [31, 39],
    green: [32, 39],
    yellow: [33, 39],
    blue: [34, 39],
    magenta: [35, 39],
    cyan: [36, 39],
    white: [37, 39],
    // Bright color
    blackBright: [90, 39],
    gray: [90, 39],
    // Alias of `blackBright`
    grey: [90, 39],
    // Alias of `blackBright`
    redBright: [91, 39],
    greenBright: [92, 39],
    yellowBright: [93, 39],
    blueBright: [94, 39],
    magentaBright: [95, 39],
    cyanBright: [96, 39],
    whiteBright: [97, 39]
  },
  bgColor: {
    bgBlack: [40, 49],
    bgRed: [41, 49],
    bgGreen: [42, 49],
    bgYellow: [43, 49],
    bgBlue: [44, 49],
    bgMagenta: [45, 49],
    bgCyan: [46, 49],
    bgWhite: [47, 49],
    // Bright color
    bgBlackBright: [100, 49],
    bgGray: [100, 49],
    // Alias of `bgBlackBright`
    bgGrey: [100, 49],
    // Alias of `bgBlackBright`
    bgRedBright: [101, 49],
    bgGreenBright: [102, 49],
    bgYellowBright: [103, 49],
    bgBlueBright: [104, 49],
    bgMagentaBright: [105, 49],
    bgCyanBright: [106, 49],
    bgWhiteBright: [107, 49]
  }
};
Object.keys(T.modifier);
const Ni = Object.keys(T.color), Ui = Object.keys(T.bgColor);
[...Ni, ...Ui];
function Vi() {
  const e = /* @__PURE__ */ new Map();
  for (const [t, u] of Object.entries(T)) {
    for (const [o, n] of Object.entries(u))
      T[o] = {
        open: `\x1B[${n[0]}m`,
        close: `\x1B[${n[1]}m`
      }, u[o] = T[o], e.set(n[0], n[1]);
    Object.defineProperty(T, t, {
      value: u,
      enumerable: !1
    });
  }
  return Object.defineProperty(T, "codes", {
    value: e,
    enumerable: !1
  }), T.color.close = "\x1B[39m", T.bgColor.close = "\x1B[49m", T.color.ansi = sn(), T.color.ansi256 = on(), T.color.ansi16m = Dn(), T.bgColor.ansi = sn(Xe), T.bgColor.ansi256 = on(Xe), T.bgColor.ansi16m = Dn(Xe), Object.defineProperties(T, {
    rgbToAnsi256: {
      value(t, u, o) {
        return t === u && u === o ? t < 8 ? 16 : t > 248 ? 231 : Math.round((t - 8) / 247 * 24) + 232 : 16 + 36 * Math.round(t / 255 * 5) + 6 * Math.round(u / 255 * 5) + Math.round(o / 255 * 5);
      },
      enumerable: !1
    },
    hexToRgb: {
      value(t) {
        const u = /[a-f\d]{6}|[a-f\d]{3}/i.exec(t.toString(16));
        if (!u)
          return [0, 0, 0];
        let [o] = u;
        o.length === 3 && (o = [...o].map((i) => i + i).join(""));
        const n = Number.parseInt(o, 16);
        return [
          /* eslint-disable no-bitwise */
          n >> 16 & 255,
          n >> 8 & 255,
          n & 255
          /* eslint-enable no-bitwise */
        ];
      },
      enumerable: !1
    },
    hexToAnsi256: {
      value: (t) => T.rgbToAnsi256(...T.hexToRgb(t)),
      enumerable: !1
    },
    ansi256ToAnsi: {
      value(t) {
        if (t < 8)
          return 30 + t;
        if (t < 16)
          return 90 + (t - 8);
        let u, o, n;
        if (t >= 232)
          u = ((t - 232) * 10 + 8) / 255, o = u, n = u;
        else {
          t -= 16;
          const s = t % 36;
          u = Math.floor(t / 36) / 5, o = Math.floor(s / 6) / 5, n = s % 6 / 5;
        }
        const i = Math.max(u, o, n) * 2;
        if (i === 0)
          return 30;
        let r = 30 + (Math.round(n) << 2 | Math.round(o) << 1 | Math.round(u));
        return i === 2 && (r += 60), r;
      },
      enumerable: !1
    },
    rgbToAnsi: {
      value: (t, u, o) => T.ansi256ToAnsi(T.rgbToAnsi256(t, u, o)),
      enumerable: !1
    },
    hexToAnsi: {
      value: (t) => T.ansi256ToAnsi(T.hexToAnsi256(t)),
      enumerable: !1
    }
  }), T;
}
const H = Vi(), an = (() => {
  if (!("navigator" in globalThis))
    return 0;
  if (globalThis.navigator.userAgentData) {
    const e = navigator.userAgentData.brands.find(({ brand: t }) => t === "Chromium");
    if (e && e.version > 93)
      return 3;
  }
  return /\b(Chrome|Chromium)\//.test(globalThis.navigator.userAgent) ? 1 : 0;
})(), ln = an !== 0 && {
  level: an
}, Gi = {
  stdout: ln,
  stderr: ln
};
function qi(e, t, u) {
  let o = e.indexOf(t);
  if (o === -1)
    return e;
  const n = t.length;
  let i = 0, r = "";
  do
    r += e.slice(i, o) + t + u, i = o + n, o = e.indexOf(t, i);
  while (o !== -1);
  return r += e.slice(i), r;
}
function zi(e, t, u, o) {
  let n = 0, i = "";
  do {
    const r = e[o - 1] === "\r";
    i += e.slice(n, r ? o - 1 : o) + t + (r ? `\r
` : `
`) + u, n = o + 1, o = e.indexOf(`
`, n);
  } while (o !== -1);
  return i += e.slice(n), i;
}
const { stdout: cn, stderr: hn } = Gi, lt = Symbol("GENERATOR"), Q = Symbol("STYLER"), se = Symbol("IS_EMPTY"), fn = [
  "ansi",
  "ansi",
  "ansi256",
  "ansi16m"
], ee = /* @__PURE__ */ Object.create(null), Yi = (e, t = {}) => {
  if (t.level && !(Number.isInteger(t.level) && t.level >= 0 && t.level <= 3))
    throw new Error("The `level` option should be an integer from 0 to 3");
  const u = cn ? cn.level : 0;
  e.level = t.level === void 0 ? u : t.level;
}, Ki = (e) => {
  const t = (...u) => u.join(" ");
  return Yi(t, e), Object.setPrototypeOf(t, ae.prototype), t;
};
function ae(e) {
  return Ki(e);
}
Object.setPrototypeOf(ae.prototype, Function.prototype);
for (const [e, t] of Object.entries(H))
  ee[e] = {
    get() {
      const u = ye(this, ht(t.open, t.close, this[Q]), this[se]);
      return Object.defineProperty(this, e, { value: u }), u;
    }
  };
ee.visible = {
  get() {
    const e = ye(this, this[Q], !0);
    return Object.defineProperty(this, "visible", { value: e }), e;
  }
};
const ct = (e, t, u, ...o) => e === "rgb" ? t === "ansi16m" ? H[u].ansi16m(...o) : t === "ansi256" ? H[u].ansi256(H.rgbToAnsi256(...o)) : H[u].ansi(H.rgbToAnsi(...o)) : e === "hex" ? ct("rgb", t, u, ...H.hexToRgb(...o)) : H[u][e](...o), Ji = ["rgb", "hex", "ansi256"];
for (const e of Ji) {
  ee[e] = {
    get() {
      const { level: u } = this;
      return function(...o) {
        const n = ht(ct(e, fn[u], "color", ...o), H.color.close, this[Q]);
        return ye(this, n, this[se]);
      };
    }
  };
  const t = "bg" + e[0].toUpperCase() + e.slice(1);
  ee[t] = {
    get() {
      const { level: u } = this;
      return function(...o) {
        const n = ht(ct(e, fn[u], "bgColor", ...o), H.bgColor.close, this[Q]);
        return ye(this, n, this[se]);
      };
    }
  };
}
const Xi = Object.defineProperties(() => {
}, {
  ...ee,
  level: {
    enumerable: !0,
    get() {
      return this[lt].level;
    },
    set(e) {
      this[lt].level = e;
    }
  }
}), ht = (e, t, u) => {
  let o, n;
  return u === void 0 ? (o = e, n = t) : (o = u.openAll + e, n = t + u.closeAll), {
    open: e,
    close: t,
    openAll: o,
    closeAll: n,
    parent: u
  };
}, ye = (e, t, u) => {
  const o = (...n) => Zi(o, n.length === 1 ? "" + n[0] : n.join(" "));
  return Object.setPrototypeOf(o, Xi), o[lt] = e, o[Q] = t, o[se] = u, o;
}, Zi = (e, t) => {
  if (e.level <= 0 || !t)
    return e[se] ? "" : t;
  let u = e[Q];
  if (u === void 0)
    return t;
  const { openAll: o, closeAll: n } = u;
  if (t.includes("\x1B"))
    for (; u !== void 0; )
      t = qi(t, u.close, u.open), u = u.parent;
  const i = t.indexOf(`
`);
  return i !== -1 && (t = zi(t, n, o, i)), o + t + n;
};
Object.defineProperties(ae.prototype, ee);
const k = ae();
ae({ level: hn ? hn.level : 0 });
const Qi = (e, t, u, o) => {
  if (u === "length" || u === "prototype" || u === "arguments" || u === "caller")
    return;
  const n = Object.getOwnPropertyDescriptor(e, u), i = Object.getOwnPropertyDescriptor(t, u);
  !er(n, i) && o || Object.defineProperty(e, u, i);
}, er = function(e, t) {
  return e === void 0 || e.configurable || e.writable === t.writable && e.enumerable === t.enumerable && e.configurable === t.configurable && (e.writable || e.value === t.value);
}, tr = (e, t) => {
  const u = Object.getPrototypeOf(t);
  u !== Object.getPrototypeOf(e) && Object.setPrototypeOf(e, u);
}, nr = (e, t) => `/* Wrapped ${e}*/
${t}`, ur = Object.getOwnPropertyDescriptor(Function.prototype, "toString"), ir = Object.getOwnPropertyDescriptor(Function.prototype.toString, "name"), rr = (e, t, u) => {
  const o = u === "" ? "" : `with ${u.trim()}() `, n = nr.bind(null, o, t.toString());
  Object.defineProperty(n, "name", ir);
  const { writable: i, enumerable: r, configurable: s } = ur;
  Object.defineProperty(e, "toString", { value: n, writable: i, enumerable: r, configurable: s });
};
function sr(e, t, { ignoreNonConfigurable: u = !1 } = {}) {
  const { name: o } = e;
  for (const n of Reflect.ownKeys(t))
    Qi(e, t, n, u);
  return tr(e, t), rr(e, t, o), e;
}
const ve = /* @__PURE__ */ new WeakMap(), uu = (e, t = {}) => {
  if (typeof e != "function")
    throw new TypeError("Expected a function");
  let u, o = 0;
  const n = e.displayName || e.name || "<anonymous>", i = function(...r) {
    if (ve.set(i, ++o), o === 1)
      u = e.apply(this, r), e = void 0;
    else if (t.throw === !0)
      throw new Error(`Function \`${n}\` can only be called once`);
    return u;
  };
  return sr(i, e), ve.set(i, o), i;
};
uu.callCount = (e) => {
  if (!ve.has(e))
    throw new Error(`The given function \`${e.name}\` is not wrapped by the \`onetime\` package`);
  return ve.get(e);
};
const dn = y.stderr.isTTY ? y.stderr : y.stdout.isTTY ? y.stdout : void 0, or = dn ? uu(() => {
  tu(() => {
    dn.write("\x1B[?25h");
  }, { alwaysLast: !0 });
}) : () => {
};
let xe = !1;
const q = {};
q.show = (e = y.stderr) => {
  e.isTTY && (xe = !1, e.write("\x1B[?25h"));
};
q.hide = (e = y.stderr) => {
  e.isTTY && (or(), xe = !0, e.write("\x1B[?25l"));
};
q.toggle = (e, t) => {
  e !== void 0 && (xe = e), xe ? q.show(t) : q.hide(t);
};
const Dr = { interval: 80, frames: ["⠋", "⠙", "⠹", "⠸", "⠼", "⠴", "⠦", "⠧", "⠇", "⠏"] }, ar = { interval: 80, frames: ["⣾", "⣽", "⣻", "⢿", "⡿", "⣟", "⣯", "⣷"] }, lr = { interval: 80, frames: ["⠋", "⠙", "⠚", "⠞", "⠖", "⠦", "⠴", "⠲", "⠳", "⠓"] }, cr = { interval: 80, frames: ["⠄", "⠆", "⠇", "⠋", "⠙", "⠸", "⠰", "⠠", "⠰", "⠸", "⠙", "⠋", "⠇", "⠆"] }, hr = { interval: 80, frames: ["⠋", "⠙", "⠚", "⠒", "⠂", "⠂", "⠒", "⠲", "⠴", "⠦", "⠖", "⠒", "⠐", "⠐", "⠒", "⠓", "⠋"] }, fr = { interval: 80, frames: ["⠁", "⠉", "⠙", "⠚", "⠒", "⠂", "⠂", "⠒", "⠲", "⠴", "⠤", "⠄", "⠄", "⠤", "⠴", "⠲", "⠒", "⠂", "⠂", "⠒", "⠚", "⠙", "⠉", "⠁"] }, dr = { interval: 80, frames: ["⠈", "⠉", "⠋", "⠓", "⠒", "⠐", "⠐", "⠒", "⠖", "⠦", "⠤", "⠠", "⠠", "⠤", "⠦", "⠖", "⠒", "⠐", "⠐", "⠒", "⠓", "⠋", "⠉", "⠈"] }, Fr = { interval: 80, frames: ["⠁", "⠁", "⠉", "⠙", "⠚", "⠒", "⠂", "⠂", "⠒", "⠲", "⠴", "⠤", "⠄", "⠄", "⠤", "⠠", "⠠", "⠤", "⠦", "⠖", "⠒", "⠐", "⠐", "⠒", "⠓", "⠋", "⠉", "⠈", "⠈"] }, mr = { interval: 80, frames: ["⢹", "⢺", "⢼", "⣸", "⣇", "⡧", "⡗", "⡏"] }, pr = { interval: 80, frames: ["⢄", "⢂", "⢁", "⡁", "⡈", "⡐", "⡠"] }, gr = { interval: 100, frames: ["⠁", "⠂", "⠄", "⡀", "⢀", "⠠", "⠐", "⠈"] }, Cr = { interval: 80, frames: ["⢀⠀", "⡀⠀", "⠄⠀", "⢂⠀", "⡂⠀", "⠅⠀", "⢃⠀", "⡃⠀", "⠍⠀", "⢋⠀", "⡋⠀", "⠍⠁", "⢋⠁", "⡋⠁", "⠍⠉", "⠋⠉", "⠋⠉", "⠉⠙", "⠉⠙", "⠉⠩", "⠈⢙", "⠈⡙", "⢈⠩", "⡀⢙", "⠄⡙", "⢂⠩", "⡂⢘", "⠅⡘", "⢃⠨", "⡃⢐", "⠍⡐", "⢋⠠", "⡋⢀", "⠍⡁", "⢋⠁", "⡋⠁", "⠍⠉", "⠋⠉", "⠋⠉", "⠉⠙", "⠉⠙", "⠉⠩", "⠈⢙", "⠈⡙", "⠈⠩", "⠀⢙", "⠀⡙", "⠀⠩", "⠀⢘", "⠀⡘", "⠀⠨", "⠀⢐", "⠀⡐", "⠀⠠", "⠀⢀", "⠀⡀"] }, Er = { interval: 80, frames: ["⣼", "⣹", "⢻", "⠿", "⡟", "⣏", "⣧", "⣶"] }, _r = { interval: 80, frames: ["⠀", "⠁", "⠂", "⠃", "⠄", "⠅", "⠆", "⠇", "⡀", "⡁", "⡂", "⡃", "⡄", "⡅", "⡆", "⡇", "⠈", "⠉", "⠊", "⠋", "⠌", "⠍", "⠎", "⠏", "⡈", "⡉", "⡊", "⡋", "⡌", "⡍", "⡎", "⡏", "⠐", "⠑", "⠒", "⠓", "⠔", "⠕", "⠖", "⠗", "⡐", "⡑", "⡒", "⡓", "⡔", "⡕", "⡖", "⡗", "⠘", "⠙", "⠚", "⠛", "⠜", "⠝", "⠞", "⠟", "⡘", "⡙", "⡚", "⡛", "⡜", "⡝", "⡞", "⡟", "⠠", "⠡", "⠢", "⠣", "⠤", "⠥", "⠦", "⠧", "⡠", "⡡", "⡢", "⡣", "⡤", "⡥", "⡦", "⡧", "⠨", "⠩", "⠪", "⠫", "⠬", "⠭", "⠮", "⠯", "⡨", "⡩", "⡪", "⡫", "⡬", "⡭", "⡮", "⡯", "⠰", "⠱", "⠲", "⠳", "⠴", "⠵", "⠶", "⠷", "⡰", "⡱", "⡲", "⡳", "⡴", "⡵", "⡶", "⡷", "⠸", "⠹", "⠺", "⠻", "⠼", "⠽", "⠾", "⠿", "⡸", "⡹", "⡺", "⡻", "⡼", "⡽", "⡾", "⡿", "⢀", "⢁", "⢂", "⢃", "⢄", "⢅", "⢆", "⢇", "⣀", "⣁", "⣂", "⣃", "⣄", "⣅", "⣆", "⣇", "⢈", "⢉", "⢊", "⢋", "⢌", "⢍", "⢎", "⢏", "⣈", "⣉", "⣊", "⣋", "⣌", "⣍", "⣎", "⣏", "⢐", "⢑", "⢒", "⢓", "⢔", "⢕", "⢖", "⢗", "⣐", "⣑", "⣒", "⣓", "⣔", "⣕", "⣖", "⣗", "⢘", "⢙", "⢚", "⢛", "⢜", "⢝", "⢞", "⢟", "⣘", "⣙", "⣚", "⣛", "⣜", "⣝", "⣞", "⣟", "⢠", "⢡", "⢢", "⢣", "⢤", "⢥", "⢦", "⢧", "⣠", "⣡", "⣢", "⣣", "⣤", "⣥", "⣦", "⣧", "⢨", "⢩", "⢪", "⢫", "⢬", "⢭", "⢮", "⢯", "⣨", "⣩", "⣪", "⣫", "⣬", "⣭", "⣮", "⣯", "⢰", "⢱", "⢲", "⢳", "⢴", "⢵", "⢶", "⢷", "⣰", "⣱", "⣲", "⣳", "⣴", "⣵", "⣶", "⣷", "⢸", "⢹", "⢺", "⢻", "⢼", "⢽", "⢾", "⢿", "⣸", "⣹", "⣺", "⣻", "⣼", "⣽", "⣾", "⣿"] }, wr = { interval: 80, frames: ["⠁", "⠂", "⠄", "⡀", "⡈", "⡐", "⡠", "⣀", "⣁", "⣂", "⣄", "⣌", "⣔", "⣤", "⣥", "⣦", "⣮", "⣶", "⣷", "⣿", "⡿", "⠿", "⢟", "⠟", "⡛", "⠛", "⠫", "⢋", "⠋", "⠍", "⡉", "⠉", "⠑", "⠡", "⢁"] }, br = { interval: 130, frames: ["-", "\\", "|", "/"] }, Br = { interval: 100, frames: ["⠂", "-", "–", "—", "–", "-"] }, Ar = { interval: 100, frames: ["┤", "┘", "┴", "└", "├", "┌", "┬", "┐"] }, yr = { interval: 400, frames: [".  ", ".. ", "...", "   "] }, vr = { interval: 200, frames: [".  ", ".. ", "...", " ..", "  .", "   "] }, xr = { interval: 70, frames: ["✶", "✸", "✹", "✺", "✹", "✷"] }, Or = { interval: 80, frames: ["+", "x", "*"] }, Sr = { interval: 70, frames: ["_", "_", "_", "-", "`", "`", "'", "´", "-", "_", "_", "_"] }, Tr = { interval: 100, frames: ["☱", "☲", "☴"] }, Pr = { interval: 120, frames: ["▁", "▃", "▄", "▅", "▆", "▇", "▆", "▅", "▄", "▃"] }, kr = { interval: 120, frames: ["▏", "▎", "▍", "▌", "▋", "▊", "▉", "▊", "▋", "▌", "▍", "▎"] }, Rr = { interval: 140, frames: [" ", ".", "o", "O", "@", "*", " "] }, $r = { interval: 120, frames: [".", "o", "O", "°", "O", "o", "."] }, Ir = { interval: 100, frames: ["▓", "▒", "░"] }, Lr = { interval: 120, frames: ["⠁", "⠂", "⠄", "⠂"] }, Mr = { interval: 120, frames: ["▖", "▘", "▝", "▗"] }, Wr = { interval: 100, frames: ["▌", "▀", "▐", "▄"] }, Hr = { interval: 50, frames: ["◢", "◣", "◤", "◥"] }, jr = { interval: 80, frames: ["010010", "001100", "100101", "111010", "111101", "010111", "101011", "111000", "110011", "110101"] }, Nr = { interval: 100, frames: ["◜", "◠", "◝", "◞", "◡", "◟"] }, Ur = { interval: 120, frames: ["◡", "⊙", "◠"] }, Vr = { interval: 180, frames: ["◰", "◳", "◲", "◱"] }, Gr = { interval: 120, frames: ["◴", "◷", "◶", "◵"] }, qr = { interval: 50, frames: ["◐", "◓", "◑", "◒"] }, zr = { interval: 100, frames: ["╫", "╪"] }, Yr = { interval: 250, frames: ["⊶", "⊷"] }, Kr = { interval: 80, frames: ["▫", "▪"] }, Jr = { interval: 120, frames: ["□", "■"] }, Xr = { interval: 100, frames: ["■", "□", "▪", "▫"] }, Zr = { interval: 100, frames: ["▮", "▯"] }, Qr = { interval: 300, frames: ["ဝ", "၀"] }, es = { interval: 80, frames: ["⦾", "⦿"] }, ts = { interval: 100, frames: ["◍", "◌"] }, ns = { interval: 100, frames: ["◉", "◎"] }, us = { interval: 100, frames: ["㊂", "㊀", "㊁"] }, is = { interval: 50, frames: ["⧇", "⧆"] }, rs = { interval: 120, frames: ["☗", "☖"] }, ss = { interval: 80, frames: ["=", "*", "-"] }, os = { interval: 100, frames: ["←", "↖", "↑", "↗", "→", "↘", "↓", "↙"] }, Ds = { interval: 80, frames: ["⬆️ ", "↗️ ", "➡️ ", "↘️ ", "⬇️ ", "↙️ ", "⬅️ ", "↖️ "] }, as = { interval: 120, frames: ["▹▹▹▹▹", "▸▹▹▹▹", "▹▸▹▹▹", "▹▹▸▹▹", "▹▹▹▸▹", "▹▹▹▹▸"] }, ls = { interval: 80, frames: ["[    ]", "[=   ]", "[==  ]", "[=== ]", "[====]", "[ ===]", "[  ==]", "[   =]", "[    ]", "[   =]", "[  ==]", "[ ===]", "[====]", "[=== ]", "[==  ]", "[=   ]"] }, cs = { interval: 80, frames: ["( ●    )", "(  ●   )", "(   ●  )", "(    ● )", "(     ●)", "(    ● )", "(   ●  )", "(  ●   )", "( ●    )", "(●     )"] }, hs = { interval: 200, frames: ["😄 ", "😝 "] }, fs = { interval: 300, frames: ["🙈 ", "🙈 ", "🙉 ", "🙊 "] }, ds = { interval: 100, frames: ["💛 ", "💙 ", "💜 ", "💚 ", "❤️ "] }, Fs = { interval: 100, frames: ["🕛 ", "🕐 ", "🕑 ", "🕒 ", "🕓 ", "🕔 ", "🕕 ", "🕖 ", "🕗 ", "🕘 ", "🕙 ", "🕚 "] }, ms = { interval: 180, frames: ["🌍 ", "🌎 ", "🌏 "] }, ps = { interval: 17, frames: ["█▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁", "██▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁", "███▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁", "████▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁", "██████▁▁▁▁▁▁▁▁▁▁▁▁▁▁", "██████▁▁▁▁▁▁▁▁▁▁▁▁▁▁", "███████▁▁▁▁▁▁▁▁▁▁▁▁▁", "████████▁▁▁▁▁▁▁▁▁▁▁▁", "█████████▁▁▁▁▁▁▁▁▁▁▁", "█████████▁▁▁▁▁▁▁▁▁▁▁", "██████████▁▁▁▁▁▁▁▁▁▁", "███████████▁▁▁▁▁▁▁▁▁", "█████████████▁▁▁▁▁▁▁", "██████████████▁▁▁▁▁▁", "██████████████▁▁▁▁▁▁", "▁██████████████▁▁▁▁▁", "▁██████████████▁▁▁▁▁", "▁██████████████▁▁▁▁▁", "▁▁██████████████▁▁▁▁", "▁▁▁██████████████▁▁▁", "▁▁▁▁█████████████▁▁▁", "▁▁▁▁██████████████▁▁", "▁▁▁▁██████████████▁▁", "▁▁▁▁▁██████████████▁", "▁▁▁▁▁██████████████▁", "▁▁▁▁▁██████████████▁", "▁▁▁▁▁▁██████████████", "▁▁▁▁▁▁██████████████", "▁▁▁▁▁▁▁█████████████", "▁▁▁▁▁▁▁█████████████", "▁▁▁▁▁▁▁▁████████████", "▁▁▁▁▁▁▁▁████████████", "▁▁▁▁▁▁▁▁▁███████████", "▁▁▁▁▁▁▁▁▁███████████", "▁▁▁▁▁▁▁▁▁▁██████████", "▁▁▁▁▁▁▁▁▁▁██████████", "▁▁▁▁▁▁▁▁▁▁▁▁████████", "▁▁▁▁▁▁▁▁▁▁▁▁▁███████", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁██████", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁█████", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁█████", "█▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁████", "██▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁███", "██▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁███", "███▁▁▁▁▁▁▁▁▁▁▁▁▁▁███", "████▁▁▁▁▁▁▁▁▁▁▁▁▁▁██", "█████▁▁▁▁▁▁▁▁▁▁▁▁▁▁█", "█████▁▁▁▁▁▁▁▁▁▁▁▁▁▁█", "██████▁▁▁▁▁▁▁▁▁▁▁▁▁█", "████████▁▁▁▁▁▁▁▁▁▁▁▁", "█████████▁▁▁▁▁▁▁▁▁▁▁", "█████████▁▁▁▁▁▁▁▁▁▁▁", "█████████▁▁▁▁▁▁▁▁▁▁▁", "█████████▁▁▁▁▁▁▁▁▁▁▁", "███████████▁▁▁▁▁▁▁▁▁", "████████████▁▁▁▁▁▁▁▁", "████████████▁▁▁▁▁▁▁▁", "██████████████▁▁▁▁▁▁", "██████████████▁▁▁▁▁▁", "▁██████████████▁▁▁▁▁", "▁██████████████▁▁▁▁▁", "▁▁▁█████████████▁▁▁▁", "▁▁▁▁▁████████████▁▁▁", "▁▁▁▁▁████████████▁▁▁", "▁▁▁▁▁▁███████████▁▁▁", "▁▁▁▁▁▁▁▁█████████▁▁▁", "▁▁▁▁▁▁▁▁█████████▁▁▁", "▁▁▁▁▁▁▁▁▁█████████▁▁", "▁▁▁▁▁▁▁▁▁█████████▁▁", "▁▁▁▁▁▁▁▁▁▁█████████▁", "▁▁▁▁▁▁▁▁▁▁▁████████▁", "▁▁▁▁▁▁▁▁▁▁▁████████▁", "▁▁▁▁▁▁▁▁▁▁▁▁███████▁", "▁▁▁▁▁▁▁▁▁▁▁▁███████▁", "▁▁▁▁▁▁▁▁▁▁▁▁▁███████", "▁▁▁▁▁▁▁▁▁▁▁▁▁███████", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁█████", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁████", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁████", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁████", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁███", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁███", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁██", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁██", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁██", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁█", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁█", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁█", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁", "▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁▁"] }, gs = { interval: 80, frames: ["🌑 ", "🌒 ", "🌓 ", "🌔 ", "🌕 ", "🌖 ", "🌗 ", "🌘 "] }, Cs = { interval: 140, frames: ["🚶 ", "🏃 "] }, Es = { interval: 80, frames: ["▐⠂       ▌", "▐⠈       ▌", "▐ ⠂      ▌", "▐ ⠠      ▌", "▐  ⡀     ▌", "▐  ⠠     ▌", "▐   ⠂    ▌", "▐   ⠈    ▌", "▐    ⠂   ▌", "▐    ⠠   ▌", "▐     ⡀  ▌", "▐     ⠠  ▌", "▐      ⠂ ▌", "▐      ⠈ ▌", "▐       ⠂▌", "▐       ⠠▌", "▐       ⡀▌", "▐      ⠠ ▌", "▐      ⠂ ▌", "▐     ⠈  ▌", "▐     ⠂  ▌", "▐    ⠠   ▌", "▐    ⡀   ▌", "▐   ⠠    ▌", "▐   ⠂    ▌", "▐  ⠈     ▌", "▐  ⠂     ▌", "▐ ⠠      ▌", "▐ ⡀      ▌", "▐⠠       ▌"] }, _s = { interval: 120, frames: ["▐|\\____________▌", "▐_|\\___________▌", "▐__|\\__________▌", "▐___|\\_________▌", "▐____|\\________▌", "▐_____|\\_______▌", "▐______|\\______▌", "▐_______|\\_____▌", "▐________|\\____▌", "▐_________|\\___▌", "▐__________|\\__▌", "▐___________|\\_▌", "▐____________|\\▌", "▐____________/|▌", "▐___________/|_▌", "▐__________/|__▌", "▐_________/|___▌", "▐________/|____▌", "▐_______/|_____▌", "▐______/|______▌", "▐_____/|_______▌", "▐____/|________▌", "▐___/|_________▌", "▐__/|__________▌", "▐_/|___________▌", "▐/|____________▌"] }, ws = { interval: 100, frames: ["d", "q", "p", "b"] }, bs = { interval: 100, frames: ["☀️ ", "☀️ ", "☀️ ", "🌤 ", "⛅️ ", "🌥 ", "☁️ ", "🌧 ", "🌨 ", "🌧 ", "🌨 ", "🌧 ", "🌨 ", "⛈ ", "🌨 ", "🌧 ", "🌨 ", "☁️ ", "🌥 ", "⛅️ ", "🌤 ", "☀️ ", "☀️ "] }, Bs = { interval: 400, frames: ["🌲", "🎄"] }, As = { interval: 80, frames: ["،  ", "′  ", " ´ ", " ‾ ", "  ⸌", "  ⸊", "  |", "  ⁎", "  ⁕", " ෴ ", "  ⁓", "   ", "   ", "   "] }, ys = { interval: 125, frames: ["∙∙∙", "●∙∙", "∙●∙", "∙∙●", "∙∙∙"] }, vs = { interval: 150, frames: ["-", "=", "≡"] }, xs = { interval: 80, frames: ["ρββββββ", "βρβββββ", "ββρββββ", "βββρβββ", "ββββρββ", "βββββρβ", "ββββββρ"] }, Os = { interval: 160, frames: ["🤘 ", "🤟 ", "🖖 ", "✋ ", "🤚 ", "👆 "] }, Ss = { interval: 80, frames: ["🤜　　　　🤛 ", "🤜　　　　🤛 ", "🤜　　　　🤛 ", "　🤜　　🤛　 ", "　　🤜🤛　　 ", "　🤜✨🤛　　 ", "🤜　✨　🤛　 "] }, Ts = { interval: 80, frames: [" 🧑⚽️       🧑 ", "🧑  ⚽️      🧑 ", "🧑   ⚽️     🧑 ", "🧑    ⚽️    🧑 ", "🧑     ⚽️   🧑 ", "🧑      ⚽️  🧑 ", "🧑       ⚽️🧑  ", "🧑      ⚽️  🧑 ", "🧑     ⚽️   🧑 ", "🧑    ⚽️    🧑 ", "🧑   ⚽️     🧑 ", "🧑  ⚽️      🧑 "] }, Ps = { interval: 160, frames: ["😐 ", "😐 ", "😮 ", "😮 ", "😦 ", "😦 ", "😧 ", "😧 ", "🤯 ", "💥 ", "✨ ", "　 ", "　 ", "　 "] }, ks = { interval: 160, frames: ["🔈 ", "🔉 ", "🔊 ", "🔉 "] }, Rs = { interval: 100, frames: ["🔸 ", "🔶 ", "🟠 ", "🟠 ", "🔶 "] }, $s = { interval: 100, frames: ["🔹 ", "🔷 ", "🔵 ", "🔵 ", "🔷 "] }, Is = { interval: 100, frames: ["🔸 ", "🔶 ", "🟠 ", "🟠 ", "🔶 ", "🔹 ", "🔷 ", "🔵 ", "🔵 ", "🔷 "] }, Ls = { interval: 100, frames: ["🕛 ", "🕚 ", "🕙 ", "🕘 ", "🕗 ", "🕖 ", "🕕 ", "🕔 ", "🕓 ", "🕒 ", "🕑 ", "🕐 "] }, Ms = { interval: 80, frames: ["▰▱▱▱▱▱▱", "▰▰▱▱▱▱▱", "▰▰▰▱▱▱▱", "▰▰▰▰▱▱▱", "▰▰▰▰▰▱▱", "▰▰▰▰▰▰▱", "▰▰▰▰▰▰▰", "▰▱▱▱▱▱▱"] }, Ws = { interval: 80, frames: [" ██████£££  ", "☺██████£££  ", "☺██████£££  ", "☺▓█████£££  ", "☺▓█████£££  ", "☺▒█████£££  ", "☺▒█████£££  ", "☺░█████£££  ", "☺░█████£££  ", "☺ █████£££  ", " ☺█████£££  ", " ☺█████£££  ", " ☺▓████£££  ", " ☺▓████£££  ", " ☺▒████£££  ", " ☺▒████£££  ", " ☺░████£££  ", " ☺░████£££  ", " ☺ ████£££  ", "  ☺████£££  ", "  ☺████£££  ", "  ☺▓███£££  ", "  ☺▓███£££  ", "  ☺▒███£££  ", "  ☺▒███£££  ", "  ☺░███£££  ", "  ☺░███£££  ", "  ☺ ███£££  ", "   ☺███£££  ", "   ☺███£££  ", "   ☺▓██£££  ", "   ☺▓██£££  ", "   ☺▒██£££  ", "   ☺▒██£££  ", "   ☺░██£££  ", "   ☺░██£££  ", "   ☺ ██£££  ", "    ☺██£££  ", "    ☺██£££  ", "    ☺▓█£££  ", "    ☺▓█£££  ", "    ☺▒█£££  ", "    ☺▒█£££  ", "    ☺░█£££  ", "    ☺░█£££  ", "    ☺ █£££  ", "     ☺█£££  ", "     ☺█£££  ", "     ☺▓£££  ", "     ☺▓£££  ", "     ☺▒£££  ", "     ☺▒£££  ", "     ☺░£££  ", "     ☺░£££  ", "     ☺ £££  ", "      ☺£££  ", "      ☺£££  ", "      ☺▓££  ", "      ☺▓££  ", "      ☺▒££  ", "      ☺▒££  ", "      ☺░££  ", "      ☺░££  ", "      ☺ ££  ", "       ☺££  ", "       ☺££  ", "       ☺▓£  ", "       ☺▓£  ", "       ☺▒£  ", "       ☺▒£  ", "       ☺░£  ", "       ☺░£  ", "       ☺ £  ", "        ☺£  ", "        ☺£  ", "        ☺▓  ", "        ☺▓  ", "        ☺▒  ", "        ☺▒  ", "        ☺░  ", "        ☺░  ", "        ☺   ", "        ☺  &", "        ☺ ☼&", "       ☺ ☼ &", "       ☺☼  &", "      ☺☼  & ", "      ‼   & ", "     ☺   &  ", "    ‼    &  ", "   ☺    &   ", "  ‼     &   ", " ☺     &    ", "‼      &    ", "      &     ", "      &     ", "     &   ░  ", "     &   ▒  ", "    &    ▓  ", "    &    £  ", "   &    ░£  ", "   &    ▒£  ", "  &     ▓£  ", "  &     ££  ", " &     ░££  ", " &     ▒££  ", "&      ▓££  ", "&      £££  ", "      ░£££  ", "      ▒£££  ", "      ▓£££  ", "      █£££  ", "     ░█£££  ", "     ▒█£££  ", "     ▓█£££  ", "     ██£££  ", "    ░██£££  ", "    ▒██£££  ", "    ▓██£££  ", "    ███£££  ", "   ░███£££  ", "   ▒███£££  ", "   ▓███£££  ", "   ████£££  ", "  ░████£££  ", "  ▒████£££  ", "  ▓████£££  ", "  █████£££  ", " ░█████£££  ", " ▒█████£££  ", " ▓█████£££  ", " ██████£££  ", " ██████£££  "] }, Hs = {
  dots: Dr,
  dots2: ar,
  dots3: lr,
  dots4: cr,
  dots5: hr,
  dots6: fr,
  dots7: dr,
  dots8: Fr,
  dots9: mr,
  dots10: pr,
  dots11: gr,
  dots12: Cr,
  dots13: Er,
  dots8Bit: _r,
  sand: wr,
  line: br,
  line2: Br,
  pipe: Ar,
  simpleDots: yr,
  simpleDotsScrolling: vr,
  star: xr,
  star2: Or,
  flip: Sr,
  hamburger: Tr,
  growVertical: Pr,
  growHorizontal: kr,
  balloon: Rr,
  balloon2: $r,
  noise: Ir,
  bounce: Lr,
  boxBounce: Mr,
  boxBounce2: Wr,
  triangle: Hr,
  binary: jr,
  arc: Nr,
  circle: Ur,
  squareCorners: Vr,
  circleQuarters: Gr,
  circleHalves: qr,
  squish: zr,
  toggle: Yr,
  toggle2: Kr,
  toggle3: Jr,
  toggle4: Xr,
  toggle5: Zr,
  toggle6: Qr,
  toggle7: es,
  toggle8: ts,
  toggle9: ns,
  toggle10: us,
  toggle11: is,
  toggle12: rs,
  toggle13: ss,
  arrow: os,
  arrow2: Ds,
  arrow3: as,
  bouncingBar: ls,
  bouncingBall: cs,
  smiley: hs,
  monkey: fs,
  hearts: ds,
  clock: Fs,
  earth: ms,
  material: ps,
  moon: gs,
  runner: Cs,
  pong: Es,
  shark: _s,
  dqpb: ws,
  weather: bs,
  christmas: Bs,
  grenade: As,
  point: ys,
  layer: vs,
  betaWave: xs,
  fingerDance: Os,
  fistBump: Ss,
  soccerHeader: Ts,
  mindblown: Ps,
  speaker: ks,
  orangePulse: Rs,
  bluePulse: $s,
  orangeBluePulse: Is,
  timeTravel: Ls,
  aesthetic: Ms,
  dwarfFortress: Ws
};
var Ze, Fn;
function js() {
  if (Fn) return Ze;
  Fn = 1;
  const e = Object.assign({}, Hs), t = Object.keys(e);
  return Object.defineProperty(e, "random", {
    get() {
      const u = Math.floor(Math.random() * t.length), o = t[u];
      return e[o];
    }
  }), Ze = e, Ze;
}
var Ns = js();
const ge = /* @__PURE__ */ Y(Ns), Ce = {
  info: "ℹ️",
  success: "✅",
  warning: "⚠️",
  error: "❌️"
};
function Us({ onlyFirst: e = !1 } = {}) {
  const n = "(?:\\u001B\\][\\s\\S]*?(?:\\u0007|\\u001B\\u005C|\\u009C))|[\\u001B\\u009B][[\\]()#;?]*(?:\\d{1,4}(?:[;:]\\d{0,4})*)?[\\dA-PR-TZcf-nq-uy=><~]";
  return new RegExp(n, e ? void 0 : "g");
}
const Vs = Us();
function iu(e) {
  if (typeof e != "string")
    throw new TypeError(`Expected a \`string\`, got \`${typeof e}\``);
  return e.replace(Vs, "");
}
function Gs(e) {
  return e === 161 || e === 164 || e === 167 || e === 168 || e === 170 || e === 173 || e === 174 || e >= 176 && e <= 180 || e >= 182 && e <= 186 || e >= 188 && e <= 191 || e === 198 || e === 208 || e === 215 || e === 216 || e >= 222 && e <= 225 || e === 230 || e >= 232 && e <= 234 || e === 236 || e === 237 || e === 240 || e === 242 || e === 243 || e >= 247 && e <= 250 || e === 252 || e === 254 || e === 257 || e === 273 || e === 275 || e === 283 || e === 294 || e === 295 || e === 299 || e >= 305 && e <= 307 || e === 312 || e >= 319 && e <= 322 || e === 324 || e >= 328 && e <= 331 || e === 333 || e === 338 || e === 339 || e === 358 || e === 359 || e === 363 || e === 462 || e === 464 || e === 466 || e === 468 || e === 470 || e === 472 || e === 474 || e === 476 || e === 593 || e === 609 || e === 708 || e === 711 || e >= 713 && e <= 715 || e === 717 || e === 720 || e >= 728 && e <= 731 || e === 733 || e === 735 || e >= 768 && e <= 879 || e >= 913 && e <= 929 || e >= 931 && e <= 937 || e >= 945 && e <= 961 || e >= 963 && e <= 969 || e === 1025 || e >= 1040 && e <= 1103 || e === 1105 || e === 8208 || e >= 8211 && e <= 8214 || e === 8216 || e === 8217 || e === 8220 || e === 8221 || e >= 8224 && e <= 8226 || e >= 8228 && e <= 8231 || e === 8240 || e === 8242 || e === 8243 || e === 8245 || e === 8251 || e === 8254 || e === 8308 || e === 8319 || e >= 8321 && e <= 8324 || e === 8364 || e === 8451 || e === 8453 || e === 8457 || e === 8467 || e === 8470 || e === 8481 || e === 8482 || e === 8486 || e === 8491 || e === 8531 || e === 8532 || e >= 8539 && e <= 8542 || e >= 8544 && e <= 8555 || e >= 8560 && e <= 8569 || e === 8585 || e >= 8592 && e <= 8601 || e === 8632 || e === 8633 || e === 8658 || e === 8660 || e === 8679 || e === 8704 || e === 8706 || e === 8707 || e === 8711 || e === 8712 || e === 8715 || e === 8719 || e === 8721 || e === 8725 || e === 8730 || e >= 8733 && e <= 8736 || e === 8739 || e === 8741 || e >= 8743 && e <= 8748 || e === 8750 || e >= 8756 && e <= 8759 || e === 8764 || e === 8765 || e === 8776 || e === 8780 || e === 8786 || e === 8800 || e === 8801 || e >= 8804 && e <= 8807 || e === 8810 || e === 8811 || e === 8814 || e === 8815 || e === 8834 || e === 8835 || e === 8838 || e === 8839 || e === 8853 || e === 8857 || e === 8869 || e === 8895 || e === 8978 || e >= 9312 && e <= 9449 || e >= 9451 && e <= 9547 || e >= 9552 && e <= 9587 || e >= 9600 && e <= 9615 || e >= 9618 && e <= 9621 || e === 9632 || e === 9633 || e >= 9635 && e <= 9641 || e === 9650 || e === 9651 || e === 9654 || e === 9655 || e === 9660 || e === 9661 || e === 9664 || e === 9665 || e >= 9670 && e <= 9672 || e === 9675 || e >= 9678 && e <= 9681 || e >= 9698 && e <= 9701 || e === 9711 || e === 9733 || e === 9734 || e === 9737 || e === 9742 || e === 9743 || e === 9756 || e === 9758 || e === 9792 || e === 9794 || e === 9824 || e === 9825 || e >= 9827 && e <= 9829 || e >= 9831 && e <= 9834 || e === 9836 || e === 9837 || e === 9839 || e === 9886 || e === 9887 || e === 9919 || e >= 9926 && e <= 9933 || e >= 9935 && e <= 9939 || e >= 9941 && e <= 9953 || e === 9955 || e === 9960 || e === 9961 || e >= 9963 && e <= 9969 || e === 9972 || e >= 9974 && e <= 9977 || e === 9979 || e === 9980 || e === 9982 || e === 9983 || e === 10045 || e >= 10102 && e <= 10111 || e >= 11094 && e <= 11097 || e >= 12872 && e <= 12879 || e >= 57344 && e <= 63743 || e >= 65024 && e <= 65039 || e === 65533 || e >= 127232 && e <= 127242 || e >= 127248 && e <= 127277 || e >= 127280 && e <= 127337 || e >= 127344 && e <= 127373 || e === 127375 || e === 127376 || e >= 127387 && e <= 127404 || e >= 917760 && e <= 917999 || e >= 983040 && e <= 1048573 || e >= 1048576 && e <= 1114109;
}
function qs(e) {
  return e === 12288 || e >= 65281 && e <= 65376 || e >= 65504 && e <= 65510;
}
function zs(e) {
  return e >= 4352 && e <= 4447 || e === 8986 || e === 8987 || e === 9001 || e === 9002 || e >= 9193 && e <= 9196 || e === 9200 || e === 9203 || e === 9725 || e === 9726 || e === 9748 || e === 9749 || e >= 9776 && e <= 9783 || e >= 9800 && e <= 9811 || e === 9855 || e >= 9866 && e <= 9871 || e === 9875 || e === 9889 || e === 9898 || e === 9899 || e === 9917 || e === 9918 || e === 9924 || e === 9925 || e === 9934 || e === 9940 || e === 9962 || e === 9970 || e === 9971 || e === 9973 || e === 9978 || e === 9981 || e === 9989 || e === 9994 || e === 9995 || e === 10024 || e === 10060 || e === 10062 || e >= 10067 && e <= 10069 || e === 10071 || e >= 10133 && e <= 10135 || e === 10160 || e === 10175 || e === 11035 || e === 11036 || e === 11088 || e === 11093 || e >= 11904 && e <= 11929 || e >= 11931 && e <= 12019 || e >= 12032 && e <= 12245 || e >= 12272 && e <= 12287 || e >= 12289 && e <= 12350 || e >= 12353 && e <= 12438 || e >= 12441 && e <= 12543 || e >= 12549 && e <= 12591 || e >= 12593 && e <= 12686 || e >= 12688 && e <= 12773 || e >= 12783 && e <= 12830 || e >= 12832 && e <= 12871 || e >= 12880 && e <= 42124 || e >= 42128 && e <= 42182 || e >= 43360 && e <= 43388 || e >= 44032 && e <= 55203 || e >= 63744 && e <= 64255 || e >= 65040 && e <= 65049 || e >= 65072 && e <= 65106 || e >= 65108 && e <= 65126 || e >= 65128 && e <= 65131 || e >= 94176 && e <= 94180 || e === 94192 || e === 94193 || e >= 94208 && e <= 100343 || e >= 100352 && e <= 101589 || e >= 101631 && e <= 101640 || e >= 110576 && e <= 110579 || e >= 110581 && e <= 110587 || e === 110589 || e === 110590 || e >= 110592 && e <= 110882 || e === 110898 || e >= 110928 && e <= 110930 || e === 110933 || e >= 110948 && e <= 110951 || e >= 110960 && e <= 111355 || e >= 119552 && e <= 119638 || e >= 119648 && e <= 119670 || e === 126980 || e === 127183 || e === 127374 || e >= 127377 && e <= 127386 || e >= 127488 && e <= 127490 || e >= 127504 && e <= 127547 || e >= 127552 && e <= 127560 || e === 127568 || e === 127569 || e >= 127584 && e <= 127589 || e >= 127744 && e <= 127776 || e >= 127789 && e <= 127797 || e >= 127799 && e <= 127868 || e >= 127870 && e <= 127891 || e >= 127904 && e <= 127946 || e >= 127951 && e <= 127955 || e >= 127968 && e <= 127984 || e === 127988 || e >= 127992 && e <= 128062 || e === 128064 || e >= 128066 && e <= 128252 || e >= 128255 && e <= 128317 || e >= 128331 && e <= 128334 || e >= 128336 && e <= 128359 || e === 128378 || e === 128405 || e === 128406 || e === 128420 || e >= 128507 && e <= 128591 || e >= 128640 && e <= 128709 || e === 128716 || e >= 128720 && e <= 128722 || e >= 128725 && e <= 128727 || e >= 128732 && e <= 128735 || e === 128747 || e === 128748 || e >= 128756 && e <= 128764 || e >= 128992 && e <= 129003 || e === 129008 || e >= 129292 && e <= 129338 || e >= 129340 && e <= 129349 || e >= 129351 && e <= 129535 || e >= 129648 && e <= 129660 || e >= 129664 && e <= 129673 || e >= 129679 && e <= 129734 || e >= 129742 && e <= 129756 || e >= 129759 && e <= 129769 || e >= 129776 && e <= 129784 || e >= 131072 && e <= 196605 || e >= 196608 && e <= 262141;
}
function Ys(e) {
  if (!Number.isSafeInteger(e))
    throw new TypeError(`Expected a code point, got \`${typeof e}\`.`);
}
function Ks(e, { ambiguousAsWide: t = !1 } = {}) {
  return Ys(e), qs(e) || zs(e) || t && Gs(e) ? 2 : 1;
}
const Js = () => /[#*0-9]\uFE0F?\u20E3|[\xA9\xAE\u203C\u2049\u2122\u2139\u2194-\u2199\u21A9\u21AA\u231A\u231B\u2328\u23CF\u23ED-\u23EF\u23F1\u23F2\u23F8-\u23FA\u24C2\u25AA\u25AB\u25B6\u25C0\u25FB\u25FC\u25FE\u2600-\u2604\u260E\u2611\u2614\u2615\u2618\u2620\u2622\u2623\u2626\u262A\u262E\u262F\u2638-\u263A\u2640\u2642\u2648-\u2653\u265F\u2660\u2663\u2665\u2666\u2668\u267B\u267E\u267F\u2692\u2694-\u2697\u2699\u269B\u269C\u26A0\u26A7\u26AA\u26B0\u26B1\u26BD\u26BE\u26C4\u26C8\u26CF\u26D1\u26E9\u26F0-\u26F5\u26F7\u26F8\u26FA\u2702\u2708\u2709\u270F\u2712\u2714\u2716\u271D\u2721\u2733\u2734\u2744\u2747\u2757\u2763\u27A1\u2934\u2935\u2B05-\u2B07\u2B1B\u2B1C\u2B55\u3030\u303D\u3297\u3299]\uFE0F?|[\u261D\u270C\u270D](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?|[\u270A\u270B](?:\uD83C[\uDFFB-\uDFFF])?|[\u23E9-\u23EC\u23F0\u23F3\u25FD\u2693\u26A1\u26AB\u26C5\u26CE\u26D4\u26EA\u26FD\u2705\u2728\u274C\u274E\u2753-\u2755\u2795-\u2797\u27B0\u27BF\u2B50]|\u26D3\uFE0F?(?:\u200D\uD83D\uDCA5)?|\u26F9(?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|\u2764\uFE0F?(?:\u200D(?:\uD83D\uDD25|\uD83E\uDE79))?|\uD83C(?:[\uDC04\uDD70\uDD71\uDD7E\uDD7F\uDE02\uDE37\uDF21\uDF24-\uDF2C\uDF36\uDF7D\uDF96\uDF97\uDF99-\uDF9B\uDF9E\uDF9F\uDFCD\uDFCE\uDFD4-\uDFDF\uDFF5\uDFF7]\uFE0F?|[\uDF85\uDFC2\uDFC7](?:\uD83C[\uDFFB-\uDFFF])?|[\uDFC4\uDFCA](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDFCB\uDFCC](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDCCF\uDD8E\uDD91-\uDD9A\uDE01\uDE1A\uDE2F\uDE32-\uDE36\uDE38-\uDE3A\uDE50\uDE51\uDF00-\uDF20\uDF2D-\uDF35\uDF37-\uDF43\uDF45-\uDF4A\uDF4C-\uDF7C\uDF7E-\uDF84\uDF86-\uDF93\uDFA0-\uDFC1\uDFC5\uDFC6\uDFC8\uDFC9\uDFCF-\uDFD3\uDFE0-\uDFF0\uDFF8-\uDFFF]|\uDDE6\uD83C[\uDDE8-\uDDEC\uDDEE\uDDF1\uDDF2\uDDF4\uDDF6-\uDDFA\uDDFC\uDDFD\uDDFF]|\uDDE7\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEF\uDDF1-\uDDF4\uDDF6-\uDDF9\uDDFB\uDDFC\uDDFE\uDDFF]|\uDDE8\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDEE\uDDF0-\uDDF7\uDDFA-\uDDFF]|\uDDE9\uD83C[\uDDEA\uDDEC\uDDEF\uDDF0\uDDF2\uDDF4\uDDFF]|\uDDEA\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDED\uDDF7-\uDDFA]|\uDDEB\uD83C[\uDDEE-\uDDF0\uDDF2\uDDF4\uDDF7]|\uDDEC\uD83C[\uDDE6\uDDE7\uDDE9-\uDDEE\uDDF1-\uDDF3\uDDF5-\uDDFA\uDDFC\uDDFE]|\uDDED\uD83C[\uDDF0\uDDF2\uDDF3\uDDF7\uDDF9\uDDFA]|\uDDEE\uD83C[\uDDE8-\uDDEA\uDDF1-\uDDF4\uDDF6-\uDDF9]|\uDDEF\uD83C[\uDDEA\uDDF2\uDDF4\uDDF5]|\uDDF0\uD83C[\uDDEA\uDDEC-\uDDEE\uDDF2\uDDF3\uDDF5\uDDF7\uDDFC\uDDFE\uDDFF]|\uDDF1\uD83C[\uDDE6-\uDDE8\uDDEE\uDDF0\uDDF7-\uDDFB\uDDFE]|\uDDF2\uD83C[\uDDE6\uDDE8-\uDDED\uDDF0-\uDDFF]|\uDDF3\uD83C[\uDDE6\uDDE8\uDDEA-\uDDEC\uDDEE\uDDF1\uDDF4\uDDF5\uDDF7\uDDFA\uDDFF]|\uDDF4\uD83C\uDDF2|\uDDF5\uD83C[\uDDE6\uDDEA-\uDDED\uDDF0-\uDDF3\uDDF7-\uDDF9\uDDFC\uDDFE]|\uDDF6\uD83C\uDDE6|\uDDF7\uD83C[\uDDEA\uDDF4\uDDF8\uDDFA\uDDFC]|\uDDF8\uD83C[\uDDE6-\uDDEA\uDDEC-\uDDF4\uDDF7-\uDDF9\uDDFB\uDDFD-\uDDFF]|\uDDF9\uD83C[\uDDE6\uDDE8\uDDE9\uDDEB-\uDDED\uDDEF-\uDDF4\uDDF7\uDDF9\uDDFB\uDDFC\uDDFF]|\uDDFA\uD83C[\uDDE6\uDDEC\uDDF2\uDDF3\uDDF8\uDDFE\uDDFF]|\uDDFB\uD83C[\uDDE6\uDDE8\uDDEA\uDDEC\uDDEE\uDDF3\uDDFA]|\uDDFC\uD83C[\uDDEB\uDDF8]|\uDDFD\uD83C\uDDF0|\uDDFE\uD83C[\uDDEA\uDDF9]|\uDDFF\uD83C[\uDDE6\uDDF2\uDDFC]|\uDF44(?:\u200D\uD83D\uDFEB)?|\uDF4B(?:\u200D\uD83D\uDFE9)?|\uDFC3(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?|\uDFF3\uFE0F?(?:\u200D(?:\u26A7\uFE0F?|\uD83C\uDF08))?|\uDFF4(?:\u200D\u2620\uFE0F?|\uDB40\uDC67\uDB40\uDC62\uDB40(?:\uDC65\uDB40\uDC6E\uDB40\uDC67|\uDC73\uDB40\uDC63\uDB40\uDC74|\uDC77\uDB40\uDC6C\uDB40\uDC73)\uDB40\uDC7F)?)|\uD83D(?:[\uDC3F\uDCFD\uDD49\uDD4A\uDD6F\uDD70\uDD73\uDD76-\uDD79\uDD87\uDD8A-\uDD8D\uDDA5\uDDA8\uDDB1\uDDB2\uDDBC\uDDC2-\uDDC4\uDDD1-\uDDD3\uDDDC-\uDDDE\uDDE1\uDDE3\uDDE8\uDDEF\uDDF3\uDDFA\uDECB\uDECD-\uDECF\uDEE0-\uDEE5\uDEE9\uDEF0\uDEF3]\uFE0F?|[\uDC42\uDC43\uDC46-\uDC50\uDC66\uDC67\uDC6B-\uDC6D\uDC72\uDC74-\uDC76\uDC78\uDC7C\uDC83\uDC85\uDC8F\uDC91\uDCAA\uDD7A\uDD95\uDD96\uDE4C\uDE4F\uDEC0\uDECC](?:\uD83C[\uDFFB-\uDFFF])?|[\uDC6E-\uDC71\uDC73\uDC77\uDC81\uDC82\uDC86\uDC87\uDE45-\uDE47\uDE4B\uDE4D\uDE4E\uDEA3\uDEB4\uDEB5](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDD74\uDD90](?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?|[\uDC00-\uDC07\uDC09-\uDC14\uDC16-\uDC25\uDC27-\uDC3A\uDC3C-\uDC3E\uDC40\uDC44\uDC45\uDC51-\uDC65\uDC6A\uDC79-\uDC7B\uDC7D-\uDC80\uDC84\uDC88-\uDC8E\uDC90\uDC92-\uDCA9\uDCAB-\uDCFC\uDCFF-\uDD3D\uDD4B-\uDD4E\uDD50-\uDD67\uDDA4\uDDFB-\uDE2D\uDE2F-\uDE34\uDE37-\uDE41\uDE43\uDE44\uDE48-\uDE4A\uDE80-\uDEA2\uDEA4-\uDEB3\uDEB7-\uDEBF\uDEC1-\uDEC5\uDED0-\uDED2\uDED5-\uDED8\uDEDC-\uDEDF\uDEEB\uDEEC\uDEF4-\uDEFC\uDFE0-\uDFEB\uDFF0]|\uDC08(?:\u200D\u2B1B)?|\uDC15(?:\u200D\uD83E\uDDBA)?|\uDC26(?:\u200D(?:\u2B1B|\uD83D\uDD25))?|\uDC3B(?:\u200D\u2744\uFE0F?)?|\uDC41\uFE0F?(?:\u200D\uD83D\uDDE8\uFE0F?)?|\uDC68(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDC68\uDC69]\u200D\uD83D(?:\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?)|[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?)|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFC-\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFC-\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFD-\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFD\uDFFF]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?\uDC68\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFE])|\uD83E(?:[\uDD1D\uDEEF]\u200D\uD83D\uDC68\uD83C[\uDFFB-\uDFFE]|[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3])))?))?|\uDC69(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:\uDC8B\u200D\uD83D)?[\uDC68\uDC69]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?|\uDC69\u200D\uD83D(?:\uDC66(?:\u200D\uD83D\uDC66)?|\uDC67(?:\u200D\uD83D[\uDC66\uDC67])?))|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFC-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFC-\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFC-\uDFFF])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB\uDFFD-\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFD-\uDFFF])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB-\uDFFD\uDFFF]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFD\uDFFF])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D\uD83D(?:[\uDC68\uDC69]|\uDC8B\u200D\uD83D[\uDC68\uDC69])\uD83C[\uDFFB-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFE])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3]|\uDD1D\u200D\uD83D[\uDC68\uDC69]\uD83C[\uDFFB-\uDFFE]|\uDEEF\u200D\uD83D\uDC69\uD83C[\uDFFB-\uDFFE])))?))?|\uDD75(?:\uD83C[\uDFFB-\uDFFF]|\uFE0F)?(?:\u200D[\u2640\u2642]\uFE0F?)?|\uDE2E(?:\u200D\uD83D\uDCA8)?|\uDE35(?:\u200D\uD83D\uDCAB)?|\uDE36(?:\u200D\uD83C\uDF2B\uFE0F?)?|\uDE42(?:\u200D[\u2194\u2195]\uFE0F?)?|\uDEB6(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?)|\uD83E(?:[\uDD0C\uDD0F\uDD18-\uDD1F\uDD30-\uDD34\uDD36\uDD77\uDDB5\uDDB6\uDDBB\uDDD2\uDDD3\uDDD5\uDEC3-\uDEC5\uDEF0\uDEF2-\uDEF8](?:\uD83C[\uDFFB-\uDFFF])?|[\uDD26\uDD35\uDD37-\uDD39\uDD3C-\uDD3E\uDDB8\uDDB9\uDDCD\uDDCF\uDDD4\uDDD6-\uDDDD](?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDDDE\uDDDF](?:\u200D[\u2640\u2642]\uFE0F?)?|[\uDD0D\uDD0E\uDD10-\uDD17\uDD20-\uDD25\uDD27-\uDD2F\uDD3A\uDD3F-\uDD45\uDD47-\uDD76\uDD78-\uDDB4\uDDB7\uDDBA\uDDBC-\uDDCC\uDDD0\uDDE0-\uDDFF\uDE70-\uDE7C\uDE80-\uDE8A\uDE8E-\uDEC2\uDEC6\uDEC8\uDECD-\uDEDC\uDEDF-\uDEEA\uDEEF]|\uDDCE(?:\uD83C[\uDFFB-\uDFFF])?(?:\u200D(?:[\u2640\u2642]\uFE0F?(?:\u200D\u27A1\uFE0F?)?|\u27A1\uFE0F?))?|\uDDD1(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1|\uDDD1\u200D\uD83E\uDDD2(?:\u200D\uD83E\uDDD2)?|\uDDD2(?:\u200D\uD83E\uDDD2)?))|\uD83C(?:\uDFFB(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFC-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFC-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFC-\uDFFF])))?|\uDFFC(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB\uDFFD-\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFD-\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFD-\uDFFF])))?|\uDFFD(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])))?|\uDFFE(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB-\uDFFD\uDFFF]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFD\uDFFF])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFD\uDFFF])))?|\uDFFF(?:\u200D(?:[\u2695\u2696\u2708]\uFE0F?|\u2764\uFE0F?\u200D(?:\uD83D\uDC8B\u200D)?\uD83E\uDDD1\uD83C[\uDFFB-\uDFFE]|\uD83C[\uDF3E\uDF73\uDF7C\uDF84\uDF93\uDFA4\uDFA8\uDFEB\uDFED]|\uD83D(?:[\uDCBB\uDCBC\uDD27\uDD2C\uDE80\uDE92]|\uDC30\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFE])|\uD83E(?:[\uDDAF\uDDBC\uDDBD](?:\u200D\u27A1\uFE0F?)?|[\uDDB0-\uDDB3\uDE70]|\uDD1D\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFF]|\uDEEF\u200D\uD83E\uDDD1\uD83C[\uDFFB-\uDFFE])))?))?|\uDEF1(?:\uD83C(?:\uDFFB(?:\u200D\uD83E\uDEF2\uD83C[\uDFFC-\uDFFF])?|\uDFFC(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB\uDFFD-\uDFFF])?|\uDFFD(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB\uDFFC\uDFFE\uDFFF])?|\uDFFE(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB-\uDFFD\uDFFF])?|\uDFFF(?:\u200D\uD83E\uDEF2\uD83C[\uDFFB-\uDFFE])?))?)/g, Xs = new Intl.Segmenter(), Zs = new RegExp("^\\p{Default_Ignorable_Code_Point}$", "u");
function Qs(e, t = {}) {
  if (typeof e != "string" || e.length === 0)
    return 0;
  const {
    ambiguousIsNarrow: u = !0,
    countAnsiEscapeCodes: o = !1
  } = t;
  if (o || (e = iu(e)), e.length === 0)
    return 0;
  let n = 0;
  const i = { ambiguousAsWide: !u };
  for (const { segment: r } of Xs.segment(e)) {
    const s = r.codePointAt(0);
    if (!(s <= 31 || s >= 127 && s <= 159) && !(s >= 8203 && s <= 8207 || s === 65279) && !(s >= 768 && s <= 879 || s >= 6832 && s <= 6911 || s >= 7616 && s <= 7679 || s >= 8400 && s <= 8447 || s >= 65056 && s <= 65071) && !(s >= 55296 && s <= 57343) && !(s >= 65024 && s <= 65039) && !Zs.test(r)) {
      if (Js().test(r)) {
        n += 2;
        continue;
      }
      n += Ks(s, i);
    }
  }
  return n;
}
function eo({ stream: e = process.stdout } = {}) {
  return !!(e && e.isTTY && process.env.TERM !== "dumb" && !("CI" in process.env));
}
function to() {
  const { env: e } = y, { TERM: t, TERM_PROGRAM: u } = e;
  return y.platform !== "win32" ? t !== "linux" : !!e.WT_SESSION || !!e.TERMINUS_SUBLIME || e.ConEmuTask === "{cmd::Cmder}" || u === "Terminus-Sublime" || u === "vscode" || t === "xterm-256color" || t === "alacritty" || t === "rxvt-unicode" || t === "rxvt-unicode-256color" || e.TERMINAL_EMULATOR === "JetBrains-JediTerm";
}
const no = 3;
class uo {
  #u = 0;
  start() {
    this.#u++, this.#u === 1 && this.#n();
  }
  stop() {
    if (this.#u <= 0)
      throw new Error("`stop` called more times than `start`");
    this.#u--, this.#u === 0 && this.#e();
  }
  #n() {
    y.platform === "win32" || !y.stdin.isTTY || (y.stdin.setRawMode(!0), y.stdin.on("data", this.#r), y.stdin.resume());
  }
  #e() {
    y.stdin.isTTY && (y.stdin.off("data", this.#r), y.stdin.pause(), y.stdin.setRawMode(!1));
  }
  #r(t) {
    t[0] === no && y.emit("SIGINT");
  }
}
const mn = new uo();
class io {
  #u = 0;
  #n = !1;
  #e = 0;
  #r = -1;
  #h = 0;
  #t;
  #s;
  #i;
  #f;
  #F;
  #a;
  #l;
  #c;
  #m;
  #o;
  #D;
  color;
  constructor(t) {
    typeof t == "string" && (t = {
      text: t
    }), this.#t = {
      color: "cyan",
      stream: y.stderr,
      discardStdin: !0,
      hideCursor: !0,
      ...t
    }, this.color = this.#t.color, this.spinner = this.#t.spinner, this.#F = this.#t.interval, this.#i = this.#t.stream, this.#a = typeof this.#t.isEnabled == "boolean" ? this.#t.isEnabled : eo({ stream: this.#i }), this.#l = typeof this.#t.isSilent == "boolean" ? this.#t.isSilent : !1, this.text = this.#t.text, this.prefixText = this.#t.prefixText, this.suffixText = this.#t.suffixText, this.indent = this.#t.indent, y.env.NODE_ENV === "test" && (this._stream = this.#i, this._isEnabled = this.#a, Object.defineProperty(this, "_linesToClear", {
      get() {
        return this.#u;
      },
      set(u) {
        this.#u = u;
      }
    }), Object.defineProperty(this, "_frameIndex", {
      get() {
        return this.#r;
      }
    }), Object.defineProperty(this, "_lineCount", {
      get() {
        return this.#e;
      }
    }));
  }
  get indent() {
    return this.#c;
  }
  set indent(t = 0) {
    if (!(t >= 0 && Number.isInteger(t)))
      throw new Error("The `indent` option must be an integer from 0 and up");
    this.#c = t, this.#d();
  }
  get interval() {
    return this.#F ?? this.#s.interval ?? 100;
  }
  get spinner() {
    return this.#s;
  }
  set spinner(t) {
    if (this.#r = -1, this.#F = void 0, typeof t == "object") {
      if (t.frames === void 0)
        throw new Error("The given spinner must have a `frames` property");
      this.#s = t;
    } else if (!to())
      this.#s = ge.line;
    else if (t === void 0)
      this.#s = ge.dots;
    else if (t !== "default" && ge[t])
      this.#s = ge[t];
    else
      throw new Error(`There is no built-in spinner named '${t}'. See https://github.com/sindresorhus/cli-spinners/blob/main/spinners.json for a full list.`);
  }
  get text() {
    return this.#m;
  }
  set text(t = "") {
    this.#m = t, this.#d();
  }
  get prefixText() {
    return this.#o;
  }
  set prefixText(t = "") {
    this.#o = t, this.#d();
  }
  get suffixText() {
    return this.#D;
  }
  set suffixText(t = "") {
    this.#D = t, this.#d();
  }
  get isSpinning() {
    return this.#f !== void 0;
  }
  #p(t = this.#o, u = " ") {
    return typeof t == "string" && t !== "" ? t + u : typeof t == "function" ? t() + u : "";
  }
  #g(t = this.#D, u = " ") {
    return typeof t == "string" && t !== "" ? u + t : typeof t == "function" ? u + t() : "";
  }
  #d() {
    const t = this.#i.columns ?? 80, u = this.#p(this.#o, "-"), o = this.#g(this.#D, "-"), n = " ".repeat(this.#c) + u + "--" + this.#m + "--" + o;
    this.#e = 0;
    for (const i of iu(n).split(`
`))
      this.#e += Math.max(1, Math.ceil(Qs(i, { countAnsiEscapeCodes: !0 }) / t));
  }
  get isEnabled() {
    return this.#a && !this.#l;
  }
  set isEnabled(t) {
    if (typeof t != "boolean")
      throw new TypeError("The `isEnabled` option must be a boolean");
    this.#a = t;
  }
  get isSilent() {
    return this.#l;
  }
  set isSilent(t) {
    if (typeof t != "boolean")
      throw new TypeError("The `isSilent` option must be a boolean");
    this.#l = t;
  }
  frame() {
    const t = Date.now();
    (this.#r === -1 || t - this.#h >= this.interval) && (this.#r = ++this.#r % this.#s.frames.length, this.#h = t);
    const { frames: u } = this.#s;
    let o = u[this.#r];
    this.color && (o = k[this.color](o));
    const n = typeof this.#o == "string" && this.#o !== "" ? this.#o + " " : "", i = typeof this.text == "string" ? " " + this.text : "", r = typeof this.#D == "string" && this.#D !== "" ? " " + this.#D : "";
    return n + o + i + r;
  }
  clear() {
    if (!this.#a || !this.#i.isTTY)
      return this;
    this.#i.cursorTo(0);
    for (let t = 0; t < this.#u; t++)
      t > 0 && this.#i.moveCursor(0, -1), this.#i.clearLine(1);
    return (this.#c || this.lastIndent !== this.#c) && this.#i.cursorTo(this.#c), this.lastIndent = this.#c, this.#u = 0, this;
  }
  render() {
    return this.#l ? this : (this.clear(), this.#i.write(this.frame()), this.#u = this.#e, this);
  }
  start(t) {
    return t && (this.text = t), this.#l ? this : this.#a ? this.isSpinning ? this : (this.#t.hideCursor && q.hide(this.#i), this.#t.discardStdin && y.stdin.isTTY && (this.#n = !0, mn.start()), this.render(), this.#f = setInterval(this.render.bind(this), this.interval), this) : (this.text && this.#i.write(`- ${this.text}
`), this);
  }
  stop() {
    return this.#a ? (clearInterval(this.#f), this.#f = void 0, this.#r = 0, this.clear(), this.#t.hideCursor && q.show(this.#i), this.#t.discardStdin && y.stdin.isTTY && this.#n && (mn.stop(), this.#n = !1), this) : this;
  }
  succeed(t) {
    return this.stopAndPersist({ symbol: Ce.success, text: t });
  }
  fail(t) {
    return this.stopAndPersist({ symbol: Ce.error, text: t });
  }
  warn(t) {
    return this.stopAndPersist({ symbol: Ce.warning, text: t });
  }
  info(t) {
    return this.stopAndPersist({ symbol: Ce.info, text: t });
  }
  stopAndPersist(t = {}) {
    if (this.#l)
      return this;
    const u = t.prefixText ?? this.#o, o = this.#p(u, " "), n = t.symbol ?? " ", i = t.text ?? this.text, s = typeof i == "string" ? (n ? " " : "") + i : "", a = t.suffixText ?? this.#D, l = this.#g(a, " "), c = o + n + s + l + `
`;
    return this.stop(), this.#i.write(c), this;
  }
}
function S(e) {
  return new io(e);
}
function ro() {
  W.command("configure").argument("[token]", "Api token").argument("[theme-id]", "Theme id").option("--debug", "Enable debug mode").description("Create config.json file").action(async (e, t, u) => {
    let o = {
      token: e,
      themeId: t,
      debug: u.debug ?? !1
    };
    o.token || (o.token = await at({
      message: "Enter api token"
    })), o.themeId || (o.themeId = await at({
      message: "Enter theme id"
    })), (!o.token || !o.themeId) && (o.debug = await Pe({
      message: "Enabled debug mode?",
      default: !1
    }));
    const n = new U({
      token: o.token,
      themeId: o.themeId,
      debug: o.debug
    }), i = S("Setting up CLI...").start();
    n.configure().then((r) => {
      i.succeed(r);
    }).catch((r) => {
      i.fail(r.toString());
    }), i.succeed("CLI configured successfully");
  });
}
function so() {
  W.command("create").argument("[token]", "Api token").option("--debug", "Enable debug mode").description("Create a new theme in store").action(async (e, t) => {
    let u = {
      token: e,
      debug: t.debug ?? !1
    };
    u.token || (u.token = await at({
      message: "Enter api token"
    }), u.debug = await Pe({
      message: "Enabled debug mode?",
      default: !1
    }));
    const o = new U({
      token: u.token,
      debug: u.debug
    }), n = S("Creating clean theme...").start();
    o.createCleanTheme().then((i) => {
      n.succeed(`Theme created under id ${i?.id}.`);
    }).catch((i) => {
      n.fail(i.toString());
    });
  });
}
function oo() {
  W.command("delete").argument("[theme-id]", "Theme id to be deleted. (Default: current theme)").description("Delete theme from store").action(async (e) => {
    if (await Pe({
      message: "Do you really want to delete this theme? This action cannot be undone.",
      default: !1
    }))
      try {
        const u = await U.initiateFromConfigFile(), o = e ?? u.themeId, n = S(`Deleting theme ${o}...`).start();
        await u.delete(o), n.succeed("Theme deleted.");
      } catch (u) {
        S().start().fail(u.toString());
      }
    else
      S().fail("Operation aborted by user");
  });
}
function Do() {
  W.command("download").argument("[files...]", "Files to download. (Default: all files)").description("Download theme files from store").action((e) => {
    U.initiateFromConfigFile().then((t) => {
      const u = e && e.length ? "Files" : "Theme";
      S().start().info("If you have a lot of files, this operation may take a while due to the API rate limit.");
      const o = S(`Downloading ${u.toLowerCase()}...`).start();
      t.download(e).then((n) => {
        if (n.fails.length) {
          const i = n.fails.length, r = n.fails.map((s) => `${k.magenta(s.file)} -> ${s.error.message}`).join(Et);
          i === n.total ? o.fail(
            `Unable to download ${u.toLowerCase()} correctly due to errors. Files affected listed bellow:`
          ) : o.warn(
            `${u} download with ${i} errors. Files affected listed bellow:`
          ), console.log(r);
        } else
          o.succeed(`${u} downloaded.`);
      }).catch((n) => {
        o.fail(n.toString());
      });
    }).catch((t) => {
      S().start().fail(t.toString());
    });
  });
}
function ao() {
  W.command("list").description("List all themes available on store").action(() => {
    U.initiateFromConfigFile().then((e) => {
      const t = S("Getting all available themes").start();
      e.list().then((u) => {
        if (t.succeed("Themes retrieved. Showing available:"), !u) throw new du("No themes found");
        const o = u.map((n) => {
          const { id: i, name: r, created_at: s, updated_at: a, theme_id: l } = n;
          return {
            id: i,
            theme_id: l,
            name: r,
            created_at: s,
            updated_at: a
          };
        });
        console.table(o);
      }).catch((u) => {
        t.fail(u.toString());
      });
    }).catch((e) => {
      S().start().fail(e.toString());
    });
  });
}
let Qe;
function lo() {
  try {
    return oe.statSync("/.dockerenv"), !0;
  } catch {
    return !1;
  }
}
function co() {
  try {
    return oe.readFileSync("/proc/self/cgroup", "utf8").includes("docker");
  } catch {
    return !1;
  }
}
function ho() {
  return Qe === void 0 && (Qe = lo() || co()), Qe;
}
let et;
const fo = () => {
  try {
    return oe.statSync("/run/.containerenv"), !0;
  } catch {
    return !1;
  }
};
function ft() {
  return et === void 0 && (et = fo() || ho()), et;
}
const pn = () => {
  if (y.platform !== "linux")
    return !1;
  if (Ou.release().toLowerCase().includes("microsoft"))
    return !ft();
  try {
    return oe.readFileSync("/proc/version", "utf8").toLowerCase().includes("microsoft") ? !ft() : !1;
  } catch {
    return !1;
  }
}, ue = y.env.__IS_WSL_TEST__ ? pn : pn(), Fo = /* @__PURE__ */ (() => {
  const e = "/mnt/";
  let t;
  return async function() {
    if (t)
      return t;
    const u = "/etc/wsl.conf";
    let o = !1;
    try {
      await ot.access(u, Wn.F_OK), o = !0;
    } catch {
    }
    if (!o)
      return e;
    const n = await ot.readFile(u, { encoding: "utf8" }), i = /(?<!#.*)root\s*=\s*(?<mountPoint>.*)/g.exec(n);
    return i ? (t = i.groups.mountPoint.trim(), t = t.endsWith("/") ? t : `${t}/`, t) : e;
  };
})(), mo = async () => `${await Fo()}c/Windows/System32/WindowsPowerShell/v1.0/powershell.exe`, ru = async () => ue ? mo() : `${y.env.SYSTEMROOT || y.env.windir || String.raw`C:\Windows`}\\System32\\WindowsPowerShell\\v1.0\\powershell.exe`;
function te(e, t, u) {
  const o = (n) => Object.defineProperty(e, t, { value: n, enumerable: !0, writable: !0 });
  return Object.defineProperty(e, t, {
    configurable: !0,
    enumerable: !0,
    get() {
      const n = u();
      return o(n), n;
    },
    set(n) {
      o(n);
    }
  }), e;
}
const po = De(Se);
async function go() {
  if (y.platform !== "darwin")
    throw new Error("macOS only");
  const { stdout: e } = await po("defaults", ["read", "com.apple.LaunchServices/com.apple.launchservices.secure", "LSHandlers"]);
  return /LSHandlerRoleAll = "(?!-)(?<id>[^"]+?)";\s+?LSHandlerURLScheme = (?:http|https);/.exec(e)?.groups.id ?? "com.apple.Safari";
}
const Co = De(Se);
async function Eo(e, { humanReadableOutput: t = !0 } = {}) {
  if (y.platform !== "darwin")
    throw new Error("macOS only");
  const u = t ? [] : ["-ss"], { stdout: o } = await Co("osascript", ["-e", e, u]);
  return o.trim();
}
async function _o(e) {
  return Eo(`tell application "Finder" to set app_path to application file id "${e}" as string
tell application "System Events" to get value of property list item "CFBundleName" of property list file (app_path & ":Contents:Info.plist")`);
}
const wo = De(Se), bo = {
  AppXq0fevzme2pys62n3e0fbqa7peapykr8v: { name: "Edge", id: "com.microsoft.edge.old" },
  MSEdgeDHTML: { name: "Edge", id: "com.microsoft.edge" },
  // On macOS, it's "com.microsoft.edgemac"
  MSEdgeHTM: { name: "Edge", id: "com.microsoft.edge" },
  // Newer Edge/Win10 releases
  "IE.HTTP": { name: "Internet Explorer", id: "com.microsoft.ie" },
  FirefoxURL: { name: "Firefox", id: "org.mozilla.firefox" },
  ChromeHTML: { name: "Chrome", id: "com.google.chrome" },
  BraveHTML: { name: "Brave", id: "com.brave.Browser" },
  BraveBHTML: { name: "Brave Beta", id: "com.brave.Browser.beta" },
  BraveSSHTM: { name: "Brave Nightly", id: "com.brave.Browser.nightly" }
};
class gn extends Error {
}
async function Bo(e = wo) {
  const { stdout: t } = await e("reg", [
    "QUERY",
    " HKEY_CURRENT_USER\\Software\\Microsoft\\Windows\\Shell\\Associations\\UrlAssociations\\http\\UserChoice",
    "/v",
    "ProgId"
  ]), u = /ProgId\s*REG_SZ\s*(?<id>\S+)/.exec(t);
  if (!u)
    throw new gn(`Cannot find Windows browser in stdout: ${JSON.stringify(t)}`);
  const { id: o } = u.groups, n = bo[o];
  if (!n)
    throw new gn(`Unknown browser ID: ${o}`);
  return n;
}
const Ao = De(Se), yo = (e) => e.toLowerCase().replaceAll(/(?:^|\s|-)\S/g, (t) => t.toUpperCase());
async function vo() {
  if (y.platform === "darwin") {
    const e = await go();
    return { name: await _o(e), id: e };
  }
  if (y.platform === "linux") {
    const { stdout: e } = await Ao("xdg-mime", ["query", "default", "x-scheme-handler/http"]), t = e.trim();
    return { name: yo(t.replace(/.desktop$/, "").replace("-", " ")), id: t };
  }
  if (y.platform === "win32")
    return Bo();
  throw new Error("Only macOS, Linux, and Windows are supported");
}
const xo = De(pt.execFile), dt = gt.dirname(Ln(import.meta.url)), Cn = gt.join(dt, "xdg-open"), { platform: J, arch: En } = y;
async function Oo() {
  const e = await ru(), t = String.raw`(Get-ItemProperty -Path "HKCU:\Software\Microsoft\Windows\Shell\Associations\UrlAssociations\http\UserChoice").ProgId`, u = Mn.from(t, "utf16le").toString("base64"), { stdout: o } = await xo(
    e,
    [
      "-NoProfile",
      "-NonInteractive",
      "-ExecutionPolicy",
      "Bypass",
      "-EncodedCommand",
      u
    ],
    { encoding: "utf8" }
  ), n = o.trim(), i = {
    ChromeHTML: "com.google.chrome",
    BraveHTML: "com.brave.Browser",
    MSEdgeHTM: "com.microsoft.edge",
    FirefoxURL: "org.mozilla.firefox"
  };
  return i[n] ? { id: i[n] } : {};
}
const _n = async (e, t) => {
  let u;
  for (const o of e)
    try {
      return await t(o);
    } catch (n) {
      u = n;
    }
  throw u;
}, we = async (e) => {
  if (e = {
    wait: !1,
    background: !1,
    newInstance: !1,
    allowNonzeroExitCode: !1,
    ...e
  }, Array.isArray(e.app))
    return _n(e.app, (s) => we({
      ...e,
      app: s
    }));
  let { name: t, arguments: u = [] } = e.app ?? {};
  if (u = [...u], Array.isArray(t))
    return _n(t, (s) => we({
      ...e,
      app: {
        name: s,
        arguments: u
      }
    }));
  if (t === "browser" || t === "browserPrivate") {
    const s = {
      "com.google.chrome": "chrome",
      "google-chrome.desktop": "chrome",
      "com.brave.Browser": "brave",
      "org.mozilla.firefox": "firefox",
      "firefox.desktop": "firefox",
      "com.microsoft.msedge": "edge",
      "com.microsoft.edge": "edge",
      "com.microsoft.edgemac": "edge",
      "microsoft-edge.desktop": "edge"
    }, a = {
      chrome: "--incognito",
      brave: "--incognito",
      firefox: "--private-window",
      edge: "--inPrivate"
    }, l = ue ? await Oo() : await vo();
    if (l.id in s) {
      const c = s[l.id];
      return t === "browserPrivate" && u.push(a[c]), we({
        ...e,
        app: {
          name: K[c],
          arguments: u
        }
      });
    }
    throw new Error(`${l.name} is not supported as a default browser`);
  }
  let o;
  const n = [], i = {};
  if (J === "darwin")
    o = "open", e.wait && n.push("--wait-apps"), e.background && n.push("--background"), e.newInstance && n.push("--new"), t && n.push("-a", t);
  else if (J === "win32" || ue && !ft() && !t) {
    o = await ru(), n.push(
      "-NoProfile",
      "-NonInteractive",
      "-ExecutionPolicy",
      "Bypass",
      "-EncodedCommand"
    ), ue || (i.windowsVerbatimArguments = !0);
    const s = ["Start"];
    e.wait && s.push("-Wait"), t ? (s.push(`"\`"${t}\`""`), e.target && u.push(e.target)) : e.target && s.push(`"${e.target}"`), u.length > 0 && (u = u.map((a) => `"\`"${a}\`""`), s.push("-ArgumentList", u.join(","))), e.target = Mn.from(s.join(" "), "utf16le").toString("base64");
  } else {
    if (t)
      o = t;
    else {
      const s = !dt || dt === "/";
      let a = !1;
      try {
        await ot.access(Cn, Wn.X_OK), a = !0;
      } catch {
      }
      o = y.versions.electron ?? (J === "android" || s || !a) ? "xdg-open" : Cn;
    }
    u.length > 0 && n.push(...u), e.wait || (i.stdio = "ignore", i.detached = !0);
  }
  J === "darwin" && u.length > 0 && n.push("--args", ...u), e.target && n.push(e.target);
  const r = pt.spawn(o, n, i);
  return e.wait ? new Promise((s, a) => {
    r.once("error", a), r.once("close", (l) => {
      if (!e.allowNonzeroExitCode && l > 0) {
        a(new Error(`Exited with code ${l}`));
        return;
      }
      s(r);
    });
  }) : (r.unref(), r);
}, So = (e, t) => {
  if (typeof e != "string")
    throw new TypeError("Expected a `target`");
  return we({
    ...t,
    target: e
  });
};
function wn(e) {
  if (typeof e == "string" || Array.isArray(e))
    return e;
  const { [En]: t } = e;
  if (!t)
    throw new Error(`${En} is not supported`);
  return t;
}
function ke({ [J]: e }, { wsl: t }) {
  if (t && ue)
    return wn(t);
  if (!e)
    throw new Error(`${J} is not supported`);
  return wn(e);
}
const K = {};
te(K, "chrome", () => ke({
  darwin: "google chrome",
  win32: "chrome",
  linux: ["google-chrome", "google-chrome-stable", "chromium"]
}, {
  wsl: {
    ia32: "/mnt/c/Program Files (x86)/Google/Chrome/Application/chrome.exe",
    x64: ["/mnt/c/Program Files/Google/Chrome/Application/chrome.exe", "/mnt/c/Program Files (x86)/Google/Chrome/Application/chrome.exe"]
  }
}));
te(K, "brave", () => ke({
  darwin: "brave browser",
  win32: "brave",
  linux: ["brave-browser", "brave"]
}, {
  wsl: {
    ia32: "/mnt/c/Program Files (x86)/BraveSoftware/Brave-Browser/Application/brave.exe",
    x64: ["/mnt/c/Program Files/BraveSoftware/Brave-Browser/Application/brave.exe", "/mnt/c/Program Files (x86)/BraveSoftware/Brave-Browser/Application/brave.exe"]
  }
}));
te(K, "firefox", () => ke({
  darwin: "firefox",
  win32: String.raw`C:\Program Files\Mozilla Firefox\firefox.exe`,
  linux: "firefox"
}, {
  wsl: "/mnt/c/Program Files/Mozilla Firefox/firefox.exe"
}));
te(K, "edge", () => ke({
  darwin: "microsoft edge",
  win32: "msedge",
  linux: ["microsoft-edge", "microsoft-edge-dev"]
}, {
  wsl: "/mnt/c/Program Files (x86)/Microsoft/Edge/Application/msedge.exe"
}));
te(K, "browser", () => "browser");
te(K, "browserPrivate", () => "browserPrivate");
function To() {
  W.command("open").description("Open theme preview url in default browser").action(() => {
    const e = S("Opening theme preview page...").start();
    Fu().then((t) => t.previewUrl ? So(t.previewUrl).then(() => e.succeed("Theme preview page opened in default browser.")).catch((u) => Promise.reject(u)) : Promise.reject(new mu("Preview url"))).catch((t) => {
      e.fail(t.toString());
    });
  });
}
function Po() {
  W.command("remove").argument("<files...>", "Files to remove").description("Removes files from theme").action(async (e) => {
    if (await Pe({
      message: "Do you really want to delete this files? This action cannot be undone.",
      default: !1
    }))
      try {
        const u = await U.initiateFromConfigFile();
        let o = [];
        e.forEach((r) => {
          (Pn(r) || $n(r)) && o.push(...kn(Rn(r), { nodir: !0 }));
        }), o = o.filter((r) => r !== "config.json"), S().start().warn("Folder paths are not supported and will be ignored.");
        const n = S("Deleting files...").start(), i = await u.remove(o);
        if (i.fails.length) {
          const r = i.fails.length, s = i.fails.map((a) => `${k.magenta(a.file)} -> ${a.error.message}`).join(Et);
          r === i.total ? n.fail(
            "Unable to delete files correctly due to errors. Files affected listed bellow:"
          ) : n.warn(`Files deleted with ${r} errors. Files affected listed bellow:`), console.log(s);
        } else
          n.succeed("Files deleted.");
      } catch (u) {
        S().start().fail(u.toString());
      }
    else
      S().fail("Operation aborted by user");
  });
}
function ko() {
  W.command("upload").argument("[files...]", "Files to upload. (Default: all files)").option("--core", "Removes settings.json and images folder from upload").description("Upload files to theme").action((e, t) => {
    U.initiateFromConfigFile().then((u) => {
      let o = "upload", n = [];
      if (e.length) {
        if (t.core && S().start().warn(
          "Core option has no affect when used together with files parameter. Option ignored."
        ), e.forEach((r) => {
          (Pn(r) || $n(r)) && n.push(...kn(Rn(r), { nodir: !0 }));
        }), n = n.filter((r) => r !== "config.json"), n.length === 0)
          return Promise.reject(
            new pu({
              details: "Files passed through command was not found in specified path."
            })
          );
        n.length < e.length && S().start().warn("Some request files could not be found. Verify files after operation.");
      } else t.core && (o = "uploadCore");
      S().start().warn("Folder paths are not supported and will be ignored."), S().start().info("If you have a lot of files, this operation may take a while due to the API rate limit.");
      const i = S("Uploading files...").start();
      u[o](n).then((r) => {
        if (r.fails.length) {
          const s = r.fails.length, a = r.fails.map((l) => `${k.magenta(l.file)} -> ${l.error.message}`).join(Et);
          s === r.total ? i.fail(
            "Unable to upload files correctly due to errors. Files affected listed bellow:"
          ) : i.warn(
            `Files upload with ${s} errors. Files affected listed bellow:`
          ), console.log(a);
        } else
          i.succeed("Files uploaded.");
      }).catch((r) => {
        i.fail(r.toString());
      });
    }).catch((u) => {
      S().start().fail(u.toString());
    });
  });
}
const $ = {
  FILE_TYPE: "files",
  DIR_TYPE: "directories",
  FILE_DIR_TYPE: "files_directories",
  EVERYTHING_TYPE: "all"
}, Ft = {
  root: ".",
  fileFilter: (e) => !0,
  directoryFilter: (e) => !0,
  type: $.FILE_TYPE,
  lstat: !1,
  depth: 2147483648,
  alwaysStat: !1,
  highWaterMark: 4096
};
Object.freeze(Ft);
const su = "READDIRP_RECURSIVE_ERROR", Ro = /* @__PURE__ */ new Set(["ENOENT", "EPERM", "EACCES", "ELOOP", su]), bn = [
  $.DIR_TYPE,
  $.EVERYTHING_TYPE,
  $.FILE_DIR_TYPE,
  $.FILE_TYPE
], $o = /* @__PURE__ */ new Set([
  $.DIR_TYPE,
  $.EVERYTHING_TYPE,
  $.FILE_DIR_TYPE
]), Io = /* @__PURE__ */ new Set([
  $.EVERYTHING_TYPE,
  $.FILE_DIR_TYPE,
  $.FILE_TYPE
]), Lo = (e) => Ro.has(e.code), Mo = process.platform === "win32", Bn = (e) => !0, An = (e) => {
  if (e === void 0)
    return Bn;
  if (typeof e == "function")
    return e;
  if (typeof e == "string") {
    const t = e.trim();
    return (u) => u.basename === t;
  }
  if (Array.isArray(e)) {
    const t = e.map((u) => u.trim());
    return (u) => t.some((o) => u.basename === o);
  }
  return Bn;
};
class Wo extends Hu {
  constructor(t = {}) {
    super({
      objectMode: !0,
      autoDestroy: !0,
      highWaterMark: t.highWaterMark
    });
    const u = { ...Ft, ...t }, { root: o, type: n } = u;
    this._fileFilter = An(u.fileFilter), this._directoryFilter = An(u.directoryFilter);
    const i = u.lstat ? xt : Su;
    Mo ? this._stat = (r) => i(r, { bigint: !0 }) : this._stat = i, this._maxDepth = u.depth ?? Ft.depth, this._wantsDir = n ? $o.has(n) : !1, this._wantsFile = n ? Io.has(n) : !1, this._wantsEverything = n === $.EVERYTHING_TYPE, this._root = vt(o), this._isDirent = !u.alwaysStat, this._statsProp = this._isDirent ? "dirent" : "stats", this._rdOptions = { encoding: "utf8", withFileTypes: this._isDirent }, this.parents = [this._exploreDir(o, 1)], this.reading = !1, this.parent = void 0;
  }
  async _read(t) {
    if (!this.reading) {
      this.reading = !0;
      try {
        for (; !this.destroyed && t > 0; ) {
          const u = this.parent, o = u && u.files;
          if (o && o.length > 0) {
            const { path: n, depth: i } = u, r = o.splice(0, t).map((a) => this._formatEntry(a, n)), s = await Promise.all(r);
            for (const a of s) {
              if (!a)
                continue;
              if (this.destroyed)
                return;
              const l = await this._getEntryType(a);
              l === "directory" && this._directoryFilter(a) ? (i <= this._maxDepth && this.parents.push(this._exploreDir(a.fullPath, i + 1)), this._wantsDir && (this.push(a), t--)) : (l === "file" || this._includeAsFile(a)) && this._fileFilter(a) && this._wantsFile && (this.push(a), t--);
            }
          } else {
            const n = this.parents.pop();
            if (!n) {
              this.push(null);
              break;
            }
            if (this.parent = await n, this.destroyed)
              return;
          }
        }
      } catch (u) {
        this.destroy(u);
      } finally {
        this.reading = !1;
      }
    }
  }
  async _exploreDir(t, u) {
    let o;
    try {
      o = await Tu(t, this._rdOptions);
    } catch (n) {
      this._onError(n);
    }
    return { files: o, depth: u, path: t };
  }
  async _formatEntry(t, u) {
    let o;
    const n = this._isDirent ? t.name : t;
    try {
      const i = vt(In(u, n));
      o = { path: Cu(this._root, i), fullPath: i, basename: n }, o[this._statsProp] = this._isDirent ? t : await this._stat(i);
    } catch (i) {
      this._onError(i);
      return;
    }
    return o;
  }
  _onError(t) {
    Lo(t) && !this.destroyed ? this.emit("warn", t) : this.destroy(t);
  }
  async _getEntryType(t) {
    if (!t && this._statsProp in t)
      return "";
    const u = t[this._statsProp];
    if (u.isFile())
      return "file";
    if (u.isDirectory())
      return "directory";
    if (u && u.isSymbolicLink()) {
      const o = t.fullPath;
      try {
        const n = await Pu(o), i = await xt(n);
        if (i.isFile())
          return "file";
        if (i.isDirectory()) {
          const r = n.length;
          if (o.startsWith(n) && o.substr(r, 1) === Eu) {
            const s = new Error(`Circular symlink detected: "${o}" points to "${n}"`);
            return s.code = su, this._onError(s);
          }
          return "directory";
        }
      } catch (n) {
        return this._onError(n), "";
      }
    }
  }
  _includeAsFile(t) {
    const u = t && t[this._statsProp];
    return u && this._wantsEverything && !u.isDirectory();
  }
}
function Ho(e, t = {}) {
  let u = t.entryType || t.type;
  if (u === "both" && (u = $.FILE_DIR_TYPE), u && (t.type = u), e) {
    if (typeof e != "string")
      throw new TypeError("readdirp: root argument must be a string. Usage: readdirp(root, options)");
    if (u && !bn.includes(u))
      throw new Error(`readdirp: Invalid type passed. Use one of ${bn.join(", ")}`);
  } else throw new Error("readdirp: root argument is required. Usage: readdirp(root, options)");
  return t.root = e, new Wo(t);
}
const jo = "data", ou = "end", No = "close", At = () => {
}, Re = process.platform, Du = Re === "win32", Uo = Re === "darwin", Vo = Re === "linux", Go = Re === "freebsd", qo = ju() === "OS400", P = {
  ALL: "all",
  READY: "ready",
  ADD: "add",
  CHANGE: "change",
  ADD_DIR: "addDir",
  UNLINK: "unlink",
  UNLINK_DIR: "unlinkDir",
  RAW: "raw",
  ERROR: "error"
}, M = P, zo = "watch", Yo = { lstat: Iu, stat: _t }, z = "listeners", be = "errHandlers", Z = "rawEmitters", Ko = [z, be, Z], Jo = /* @__PURE__ */ new Set([
  "3dm",
  "3ds",
  "3g2",
  "3gp",
  "7z",
  "a",
  "aac",
  "adp",
  "afdesign",
  "afphoto",
  "afpub",
  "ai",
  "aif",
  "aiff",
  "alz",
  "ape",
  "apk",
  "appimage",
  "ar",
  "arj",
  "asf",
  "au",
  "avi",
  "bak",
  "baml",
  "bh",
  "bin",
  "bk",
  "bmp",
  "btif",
  "bz2",
  "bzip2",
  "cab",
  "caf",
  "cgm",
  "class",
  "cmx",
  "cpio",
  "cr2",
  "cur",
  "dat",
  "dcm",
  "deb",
  "dex",
  "djvu",
  "dll",
  "dmg",
  "dng",
  "doc",
  "docm",
  "docx",
  "dot",
  "dotm",
  "dra",
  "DS_Store",
  "dsk",
  "dts",
  "dtshd",
  "dvb",
  "dwg",
  "dxf",
  "ecelp4800",
  "ecelp7470",
  "ecelp9600",
  "egg",
  "eol",
  "eot",
  "epub",
  "exe",
  "f4v",
  "fbs",
  "fh",
  "fla",
  "flac",
  "flatpak",
  "fli",
  "flv",
  "fpx",
  "fst",
  "fvt",
  "g3",
  "gh",
  "gif",
  "graffle",
  "gz",
  "gzip",
  "h261",
  "h263",
  "h264",
  "icns",
  "ico",
  "ief",
  "img",
  "ipa",
  "iso",
  "jar",
  "jpeg",
  "jpg",
  "jpgv",
  "jpm",
  "jxr",
  "key",
  "ktx",
  "lha",
  "lib",
  "lvp",
  "lz",
  "lzh",
  "lzma",
  "lzo",
  "m3u",
  "m4a",
  "m4v",
  "mar",
  "mdi",
  "mht",
  "mid",
  "midi",
  "mj2",
  "mka",
  "mkv",
  "mmr",
  "mng",
  "mobi",
  "mov",
  "movie",
  "mp3",
  "mp4",
  "mp4a",
  "mpeg",
  "mpg",
  "mpga",
  "mxu",
  "nef",
  "npx",
  "numbers",
  "nupkg",
  "o",
  "odp",
  "ods",
  "odt",
  "oga",
  "ogg",
  "ogv",
  "otf",
  "ott",
  "pages",
  "pbm",
  "pcx",
  "pdb",
  "pdf",
  "pea",
  "pgm",
  "pic",
  "png",
  "pnm",
  "pot",
  "potm",
  "potx",
  "ppa",
  "ppam",
  "ppm",
  "pps",
  "ppsm",
  "ppsx",
  "ppt",
  "pptm",
  "pptx",
  "psd",
  "pya",
  "pyc",
  "pyo",
  "pyv",
  "qt",
  "rar",
  "ras",
  "raw",
  "resources",
  "rgb",
  "rip",
  "rlc",
  "rmf",
  "rmvb",
  "rpm",
  "rtf",
  "rz",
  "s3m",
  "s7z",
  "scpt",
  "sgi",
  "shar",
  "snap",
  "sil",
  "sketch",
  "slk",
  "smv",
  "snk",
  "so",
  "stl",
  "suo",
  "sub",
  "swf",
  "tar",
  "tbz",
  "tbz2",
  "tga",
  "tgz",
  "thmx",
  "tif",
  "tiff",
  "tlz",
  "ttc",
  "ttf",
  "txz",
  "udf",
  "uvh",
  "uvi",
  "uvm",
  "uvp",
  "uvs",
  "uvu",
  "viv",
  "vob",
  "war",
  "wav",
  "wax",
  "wbmp",
  "wdp",
  "weba",
  "webm",
  "webp",
  "whl",
  "wim",
  "wm",
  "wma",
  "wmv",
  "wmx",
  "woff",
  "woff2",
  "wrm",
  "wvx",
  "xbm",
  "xif",
  "xla",
  "xlam",
  "xls",
  "xlsb",
  "xlsm",
  "xlsx",
  "xlt",
  "xltm",
  "xltx",
  "xm",
  "xmind",
  "xpi",
  "xpm",
  "xwd",
  "xz",
  "z",
  "zip",
  "zipx"
]), Xo = (e) => Jo.has(b.extname(e).slice(1).toLowerCase()), mt = (e, t) => {
  e instanceof Set ? e.forEach(t) : t(e);
}, ie = (e, t, u) => {
  let o = e[t];
  o instanceof Set || (e[t] = o = /* @__PURE__ */ new Set([o])), o.add(u);
}, Zo = (e) => (t) => {
  const u = e[t];
  u instanceof Set ? u.clear() : delete e[t];
}, re = (e, t, u) => {
  const o = e[t];
  o instanceof Set ? o.delete(u) : o === u && delete e[t];
}, au = (e) => e instanceof Set ? e.size === 0 : !e, Be = /* @__PURE__ */ new Map();
function yn(e, t, u, o, n) {
  const i = (r, s) => {
    u(e), n(r, s, { watchedPath: e }), s && e !== s && Ae(b.resolve(e, s), z, b.join(e, s));
  };
  try {
    return Ru(e, {
      persistent: t.persistent
    }, i);
  } catch (r) {
    o(r);
    return;
  }
}
const Ae = (e, t, u, o, n) => {
  const i = Be.get(e);
  i && mt(i[t], (r) => {
    r(u, o, n);
  });
}, Qo = (e, t, u, o) => {
  const { listener: n, errHandler: i, rawEmitter: r } = o;
  let s = Be.get(t), a;
  if (!u.persistent)
    return a = yn(e, u, n, i, r), a ? a.close.bind(a) : void 0;
  if (s)
    ie(s, z, n), ie(s, be, i), ie(s, Z, r);
  else {
    if (a = yn(
      e,
      u,
      Ae.bind(null, t, z),
      i,
      // no need to use broadcast here
      Ae.bind(null, t, Z)
    ), !a)
      return;
    a.on(M.ERROR, async (l) => {
      const c = Ae.bind(null, t, be);
      if (s && (s.watcherUnusable = !0), Du && l.code === "EPERM")
        try {
          await (await Lu(e, "r")).close(), c(l);
        } catch {
        }
      else
        c(l);
    }), s = {
      listeners: n,
      errHandlers: i,
      rawEmitters: r,
      watcher: a
    }, Be.set(t, s);
  }
  return () => {
    re(s, z, n), re(s, be, i), re(s, Z, r), au(s.listeners) && (s.watcher.close(), Be.delete(t), Ko.forEach(Zo(s)), s.watcher = void 0, Object.freeze(s));
  };
}, tt = /* @__PURE__ */ new Map(), eD = (e, t, u, o) => {
  const { listener: n, rawEmitter: i } = o;
  let r = tt.get(t);
  const s = r && r.options;
  return s && (s.persistent < u.persistent || s.interval > u.interval) && (Ot(t), r = void 0), r ? (ie(r, z, n), ie(r, Z, i)) : (r = {
    listeners: n,
    rawEmitters: i,
    options: u,
    watcher: ku(t, u, (a, l) => {
      mt(r.rawEmitters, (f) => {
        f(M.CHANGE, t, { curr: a, prev: l });
      });
      const c = a.mtimeMs;
      (a.size !== l.size || c > l.mtimeMs || c === 0) && mt(r.listeners, (f) => f(e, a));
    })
  }, tt.set(t, r)), () => {
    re(r, z, n), re(r, Z, i), au(r.listeners) && (tt.delete(t), Ot(t), r.options = r.watcher = void 0, Object.freeze(r));
  };
};
class tD {
  constructor(t) {
    this.fsw = t, this._boundHandleError = (u) => t._handleError(u);
  }
  /**
   * Watch file for changes with fs_watchFile or fs_watch.
   * @param path to file or dir
   * @param listener on fs change
   * @returns closer for the watcher instance
   */
  _watchWithNodeFs(t, u) {
    const o = this.fsw.options, n = b.dirname(t), i = b.basename(t);
    this.fsw._getWatchedDir(n).add(i);
    const s = b.resolve(t), a = {
      persistent: o.persistent
    };
    u || (u = At);
    let l;
    if (o.usePolling) {
      const c = o.interval !== o.binaryInterval;
      a.interval = c && Xo(i) ? o.binaryInterval : o.interval, l = eD(t, s, a, {
        listener: u,
        rawEmitter: this.fsw._emitRaw
      });
    } else
      l = Qo(t, s, a, {
        listener: u,
        errHandler: this._boundHandleError,
        rawEmitter: this.fsw._emitRaw
      });
    return l;
  }
  /**
   * Watch a file and emit add event if warranted.
   * @returns closer for the watcher instance
   */
  _handleFile(t, u, o) {
    if (this.fsw.closed)
      return;
    const n = b.dirname(t), i = b.basename(t), r = this.fsw._getWatchedDir(n);
    let s = u;
    if (r.has(i))
      return;
    const a = async (c, f) => {
      if (this.fsw._throttle(zo, t, 5)) {
        if (!f || f.mtimeMs === 0)
          try {
            const p = await _t(t);
            if (this.fsw.closed)
              return;
            const g = p.atimeMs, C = p.mtimeMs;
            if ((!g || g <= C || C !== s.mtimeMs) && this.fsw._emit(M.CHANGE, t, p), (Uo || Vo || Go) && s.ino !== p.ino) {
              this.fsw._closeFile(c), s = p;
              const _ = this._watchWithNodeFs(t, a);
              _ && this.fsw._addPathCloser(c, _);
            } else
              s = p;
          } catch {
            this.fsw._remove(n, i);
          }
        else if (r.has(i)) {
          const p = f.atimeMs, g = f.mtimeMs;
          (!p || p <= g || g !== s.mtimeMs) && this.fsw._emit(M.CHANGE, t, f), s = f;
        }
      }
    }, l = this._watchWithNodeFs(t, a);
    if (!(o && this.fsw.options.ignoreInitial) && this.fsw._isntIgnored(t)) {
      if (!this.fsw._throttle(M.ADD, t, 0))
        return;
      this.fsw._emit(M.ADD, t, u);
    }
    return l;
  }
  /**
   * Handle symlinks encountered while reading a dir.
   * @param entry returned by readdirp
   * @param directory path of dir being read
   * @param path of this item
   * @param item basename of this item
   * @returns true if no more processing is needed for this entry.
   */
  async _handleSymlink(t, u, o, n) {
    if (this.fsw.closed)
      return;
    const i = t.fullPath, r = this.fsw._getWatchedDir(u);
    if (!this.fsw.options.followSymlinks) {
      this.fsw._incrReadyCount();
      let s;
      try {
        s = await $e(o);
      } catch {
        return this.fsw._emitReady(), !0;
      }
      return this.fsw.closed ? void 0 : (r.has(n) ? this.fsw._symlinkPaths.get(i) !== s && (this.fsw._symlinkPaths.set(i, s), this.fsw._emit(M.CHANGE, o, t.stats)) : (r.add(n), this.fsw._symlinkPaths.set(i, s), this.fsw._emit(M.ADD, o, t.stats)), this.fsw._emitReady(), !0);
    }
    if (this.fsw._symlinkPaths.has(i))
      return !0;
    this.fsw._symlinkPaths.set(i, !0);
  }
  _handleRead(t, u, o, n, i, r, s) {
    if (t = b.join(t, ""), s = this.fsw._throttle("readdir", t, 1e3), !s)
      return;
    const a = this.fsw._getWatchedDir(o.path), l = /* @__PURE__ */ new Set();
    let c = this.fsw._readdirp(t, {
      fileFilter: (f) => o.filterPath(f),
      directoryFilter: (f) => o.filterDir(f)
    });
    if (c)
      return c.on(jo, async (f) => {
        if (this.fsw.closed) {
          c = void 0;
          return;
        }
        const p = f.path;
        let g = b.join(t, p);
        if (l.add(p), !(f.stats.isSymbolicLink() && await this._handleSymlink(f, t, g, p))) {
          if (this.fsw.closed) {
            c = void 0;
            return;
          }
          (p === n || !n && !a.has(p)) && (this.fsw._incrReadyCount(), g = b.join(i, b.relative(i, g)), this._addToNodeFs(g, u, o, r + 1));
        }
      }).on(M.ERROR, this._boundHandleError), new Promise((f, p) => {
        if (!c)
          return p();
        c.once(ou, () => {
          if (this.fsw.closed) {
            c = void 0;
            return;
          }
          const g = s ? s.clear() : !1;
          f(void 0), a.getChildren().filter((C) => C !== t && !l.has(C)).forEach((C) => {
            this.fsw._remove(t, C);
          }), c = void 0, g && this._handleRead(t, !1, o, n, i, r, s);
        });
      });
  }
  /**
   * Read directory to add / remove files from `@watched` list and re-read it on change.
   * @param dir fs path
   * @param stats
   * @param initialAdd
   * @param depth relative to user-supplied path
   * @param target child path targeted for watch
   * @param wh Common watch helpers for this path
   * @param realpath
   * @returns closer for the watcher instance.
   */
  async _handleDir(t, u, o, n, i, r, s) {
    const a = this.fsw._getWatchedDir(b.dirname(t)), l = a.has(b.basename(t));
    !(o && this.fsw.options.ignoreInitial) && !i && !l && this.fsw._emit(M.ADD_DIR, t, u), a.add(b.basename(t)), this.fsw._getWatchedDir(t);
    let c, f;
    const p = this.fsw.options.depth;
    if ((p == null || n <= p) && !this.fsw._symlinkPaths.has(s)) {
      if (!i && (await this._handleRead(t, o, r, i, t, n, c), this.fsw.closed))
        return;
      f = this._watchWithNodeFs(t, (g, C) => {
        C && C.mtimeMs === 0 || this._handleRead(g, !1, r, i, t, n, c);
      });
    }
    return f;
  }
  /**
   * Handle added file, directory, or glob pattern.
   * Delegates call to _handleFile / _handleDir after checks.
   * @param path to file or ir
   * @param initialAdd was the file added at watch instantiation?
   * @param priorWh depth relative to user-supplied path
   * @param depth Child path actually targeted for watch
   * @param target Child path actually targeted for watch
   */
  async _addToNodeFs(t, u, o, n, i) {
    const r = this.fsw._emitReady;
    if (this.fsw._isIgnored(t) || this.fsw.closed)
      return r(), !1;
    const s = this.fsw._getWatchHelpers(t);
    o && (s.filterPath = (a) => o.filterPath(a), s.filterDir = (a) => o.filterDir(a));
    try {
      const a = await Yo[s.statMethod](s.watchPath);
      if (this.fsw.closed)
        return;
      if (this.fsw._isIgnored(s.watchPath, a))
        return r(), !1;
      const l = this.fsw.options.followSymlinks;
      let c;
      if (a.isDirectory()) {
        const f = b.resolve(t), p = l ? await $e(t) : t;
        if (this.fsw.closed || (c = await this._handleDir(s.watchPath, a, u, n, i, s, p), this.fsw.closed))
          return;
        f !== p && p !== void 0 && this.fsw._symlinkPaths.set(f, p);
      } else if (a.isSymbolicLink()) {
        const f = l ? await $e(t) : t;
        if (this.fsw.closed)
          return;
        const p = b.dirname(s.watchPath);
        if (this.fsw._getWatchedDir(p).add(s.watchPath), this.fsw._emit(M.ADD, s.watchPath, a), c = await this._handleDir(p, a, u, n, t, s, f), this.fsw.closed)
          return;
        f !== void 0 && this.fsw._symlinkPaths.set(b.resolve(t), f);
      } else
        c = this._handleFile(s.watchPath, a, u);
      return r(), c && this.fsw._addPathCloser(t, c), !1;
    } catch (a) {
      if (this.fsw._handleError(a))
        return r(), t;
    }
  }
}
/*! chokidar - MIT License (c) 2012 Paul Miller (paulmillr.com) */
const nt = "/", nD = "//", lu = ".", uD = "..", iD = "string", rD = /\\/g, vn = /\/\//, sD = /\..*\.(sw[px])$|~$|\.subl.*\.tmp/, oD = /^\.[/\\]/;
function Oe(e) {
  return Array.isArray(e) ? e : [e];
}
const ut = (e) => typeof e == "object" && e !== null && !(e instanceof RegExp);
function DD(e) {
  return typeof e == "function" ? e : typeof e == "string" ? (t) => e === t : e instanceof RegExp ? (t) => e.test(t) : typeof e == "object" && e !== null ? (t) => {
    if (e.path === t)
      return !0;
    if (e.recursive) {
      const u = b.relative(e.path, t);
      return u ? !u.startsWith("..") && !b.isAbsolute(u) : !1;
    }
    return !1;
  } : () => !1;
}
function aD(e) {
  if (typeof e != "string")
    throw new Error("string expected");
  e = b.normalize(e), e = e.replace(/\\/g, "/");
  let t = !1;
  e.startsWith("//") && (t = !0);
  const u = /\/\//;
  for (; e.match(u); )
    e = e.replace(u, "/");
  return t && (e = "/" + e), e;
}
function lD(e, t, u) {
  const o = aD(t);
  for (let n = 0; n < e.length; n++) {
    const i = e[n];
    if (i(o, u))
      return !0;
  }
  return !1;
}
function cD(e, t) {
  if (e == null)
    throw new TypeError("anymatch: specify first argument");
  const o = Oe(e).map((n) => DD(n));
  return (n, i) => lD(o, n, i);
}
const xn = (e) => {
  const t = Oe(e).flat();
  if (!t.every((u) => typeof u === iD))
    throw new TypeError(`Non-string provided as watch path: ${t}`);
  return t.map(cu);
}, On = (e) => {
  let t = e.replace(rD, nt), u = !1;
  for (t.startsWith(nD) && (u = !0); t.match(vn); )
    t = t.replace(vn, nt);
  return u && (t = nt + t), t;
}, cu = (e) => On(b.normalize(On(e))), Sn = (e = "") => (t) => typeof t == "string" ? cu(b.isAbsolute(t) ? t : b.join(e, t)) : t, hD = (e, t) => b.isAbsolute(e) ? e : b.join(t, e), fD = Object.freeze(/* @__PURE__ */ new Set());
class dD {
  constructor(t, u) {
    this.path = t, this._removeWatcher = u, this.items = /* @__PURE__ */ new Set();
  }
  add(t) {
    const { items: u } = this;
    u && t !== lu && t !== uD && u.add(t);
  }
  async remove(t) {
    const { items: u } = this;
    if (!u || (u.delete(t), u.size > 0))
      return;
    const o = this.path;
    try {
      await Mu(o);
    } catch {
      this._removeWatcher && this._removeWatcher(b.dirname(o), b.basename(o));
    }
  }
  has(t) {
    const { items: u } = this;
    if (u)
      return u.has(t);
  }
  getChildren() {
    const { items: t } = this;
    return t ? [...t.values()] : [];
  }
  dispose() {
    this.items.clear(), this.path = "", this._removeWatcher = At, this.items = fD, Object.freeze(this);
  }
}
const FD = "stat", mD = "lstat";
class pD {
  constructor(t, u, o) {
    this.fsw = o;
    const n = t;
    this.path = t = t.replace(oD, ""), this.watchPath = n, this.fullWatchPath = b.resolve(n), this.dirParts = [], this.dirParts.forEach((i) => {
      i.length > 1 && i.pop();
    }), this.followSymlinks = u, this.statMethod = u ? FD : mD;
  }
  entryPath(t) {
    return b.join(this.watchPath, b.relative(this.watchPath, t.fullPath));
  }
  filterPath(t) {
    const { stats: u } = t;
    if (u && u.isSymbolicLink())
      return this.filterDir(t);
    const o = this.entryPath(t);
    return this.fsw._isntIgnored(o, u) && this.fsw._hasReadPermissions(u);
  }
  filterDir(t) {
    return this.fsw._isntIgnored(this.entryPath(t), t.stats);
  }
}
class hu extends Wu {
  // Not indenting methods for history sake; for now.
  constructor(t = {}) {
    super(), this.closed = !1, this._closers = /* @__PURE__ */ new Map(), this._ignoredPaths = /* @__PURE__ */ new Set(), this._throttled = /* @__PURE__ */ new Map(), this._streams = /* @__PURE__ */ new Set(), this._symlinkPaths = /* @__PURE__ */ new Map(), this._watched = /* @__PURE__ */ new Map(), this._pendingWrites = /* @__PURE__ */ new Map(), this._pendingUnlinks = /* @__PURE__ */ new Map(), this._readyCount = 0, this._readyEmitted = !1;
    const u = t.awaitWriteFinish, o = { stabilityThreshold: 2e3, pollInterval: 100 }, n = {
      // Defaults
      persistent: !0,
      ignoreInitial: !1,
      ignorePermissionErrors: !1,
      interval: 100,
      binaryInterval: 300,
      followSymlinks: !0,
      usePolling: !1,
      // useAsync: false,
      atomic: !0,
      // NOTE: overwritten later (depends on usePolling)
      ...t,
      // Change format
      ignored: t.ignored ? Oe(t.ignored) : Oe([]),
      awaitWriteFinish: u === !0 ? o : typeof u == "object" ? { ...o, ...u } : !1
    };
    qo && (n.usePolling = !0), n.atomic === void 0 && (n.atomic = !n.usePolling);
    const i = process.env.CHOKIDAR_USEPOLLING;
    if (i !== void 0) {
      const a = i.toLowerCase();
      a === "false" || a === "0" ? n.usePolling = !1 : a === "true" || a === "1" ? n.usePolling = !0 : n.usePolling = !!a;
    }
    const r = process.env.CHOKIDAR_INTERVAL;
    r && (n.interval = Number.parseInt(r, 10));
    let s = 0;
    this._emitReady = () => {
      s++, s >= this._readyCount && (this._emitReady = At, this._readyEmitted = !0, process.nextTick(() => this.emit(P.READY)));
    }, this._emitRaw = (...a) => this.emit(P.RAW, ...a), this._boundRemove = this._remove.bind(this), this.options = n, this._nodeFsHandler = new tD(this), Object.freeze(n);
  }
  _addIgnoredPath(t) {
    if (ut(t)) {
      for (const u of this._ignoredPaths)
        if (ut(u) && u.path === t.path && u.recursive === t.recursive)
          return;
    }
    this._ignoredPaths.add(t);
  }
  _removeIgnoredPath(t) {
    if (this._ignoredPaths.delete(t), typeof t == "string")
      for (const u of this._ignoredPaths)
        ut(u) && u.path === t && this._ignoredPaths.delete(u);
  }
  // Public methods
  /**
   * Adds paths to be watched on an existing FSWatcher instance.
   * @param paths_ file or file list. Other arguments are unused
   */
  add(t, u, o) {
    const { cwd: n } = this.options;
    this.closed = !1, this._closePromise = void 0;
    let i = xn(t);
    return n && (i = i.map((r) => hD(r, n))), i.forEach((r) => {
      this._removeIgnoredPath(r);
    }), this._userIgnored = void 0, this._readyCount || (this._readyCount = 0), this._readyCount += i.length, Promise.all(i.map(async (r) => {
      const s = await this._nodeFsHandler._addToNodeFs(r, !o, void 0, 0, u);
      return s && this._emitReady(), s;
    })).then((r) => {
      this.closed || r.forEach((s) => {
        s && this.add(b.dirname(s), b.basename(u || s));
      });
    }), this;
  }
  /**
   * Close watchers or start ignoring events from specified paths.
   */
  unwatch(t) {
    if (this.closed)
      return this;
    const u = xn(t), { cwd: o } = this.options;
    return u.forEach((n) => {
      !b.isAbsolute(n) && !this._closers.has(n) && (o && (n = b.join(o, n)), n = b.resolve(n)), this._closePath(n), this._addIgnoredPath(n), this._watched.has(n) && this._addIgnoredPath({
        path: n,
        recursive: !0
      }), this._userIgnored = void 0;
    }), this;
  }
  /**
   * Close watchers and remove all listeners from watched paths.
   */
  close() {
    if (this._closePromise)
      return this._closePromise;
    this.closed = !0, this.removeAllListeners();
    const t = [];
    return this._closers.forEach((u) => u.forEach((o) => {
      const n = o();
      n instanceof Promise && t.push(n);
    })), this._streams.forEach((u) => u.destroy()), this._userIgnored = void 0, this._readyCount = 0, this._readyEmitted = !1, this._watched.forEach((u) => u.dispose()), this._closers.clear(), this._watched.clear(), this._streams.clear(), this._symlinkPaths.clear(), this._throttled.clear(), this._closePromise = t.length ? Promise.all(t).then(() => {
    }) : Promise.resolve(), this._closePromise;
  }
  /**
   * Expose list of watched paths
   * @returns for chaining
   */
  getWatched() {
    const t = {};
    return this._watched.forEach((u, o) => {
      const i = (this.options.cwd ? b.relative(this.options.cwd, o) : o) || lu;
      t[i] = u.getChildren().sort();
    }), t;
  }
  emitWithAll(t, u) {
    this.emit(t, ...u), t !== P.ERROR && this.emit(P.ALL, t, ...u);
  }
  // Common helpers
  // --------------
  /**
   * Normalize and emit events.
   * Calling _emit DOES NOT MEAN emit() would be called!
   * @param event Type of event
   * @param path File or directory path
   * @param stats arguments to be passed with event
   * @returns the error if defined, otherwise the value of the FSWatcher instance's `closed` flag
   */
  async _emit(t, u, o) {
    if (this.closed)
      return;
    const n = this.options;
    Du && (u = b.normalize(u)), n.cwd && (u = b.relative(n.cwd, u));
    const i = [u];
    o != null && i.push(o);
    const r = n.awaitWriteFinish;
    let s;
    if (r && (s = this._pendingWrites.get(u)))
      return s.lastChange = /* @__PURE__ */ new Date(), this;
    if (n.atomic) {
      if (t === P.UNLINK)
        return this._pendingUnlinks.set(u, [t, ...i]), setTimeout(() => {
          this._pendingUnlinks.forEach((a, l) => {
            this.emit(...a), this.emit(P.ALL, ...a), this._pendingUnlinks.delete(l);
          });
        }, typeof n.atomic == "number" ? n.atomic : 100), this;
      t === P.ADD && this._pendingUnlinks.has(u) && (t = P.CHANGE, this._pendingUnlinks.delete(u));
    }
    if (r && (t === P.ADD || t === P.CHANGE) && this._readyEmitted) {
      const a = (l, c) => {
        l ? (t = P.ERROR, i[0] = l, this.emitWithAll(t, i)) : c && (i.length > 1 ? i[1] = c : i.push(c), this.emitWithAll(t, i));
      };
      return this._awaitWriteFinish(u, r.stabilityThreshold, t, a), this;
    }
    if (t === P.CHANGE && !this._throttle(P.CHANGE, u, 50))
      return this;
    if (n.alwaysStat && o === void 0 && (t === P.ADD || t === P.ADD_DIR || t === P.CHANGE)) {
      const a = n.cwd ? b.join(n.cwd, u) : u;
      let l;
      try {
        l = await _t(a);
      } catch {
      }
      if (!l || this.closed)
        return;
      i.push(l);
    }
    return this.emitWithAll(t, i), this;
  }
  /**
   * Common handler for errors
   * @returns The error if defined, otherwise the value of the FSWatcher instance's `closed` flag
   */
  _handleError(t) {
    const u = t && t.code;
    return t && u !== "ENOENT" && u !== "ENOTDIR" && (!this.options.ignorePermissionErrors || u !== "EPERM" && u !== "EACCES") && this.emit(P.ERROR, t), t || this.closed;
  }
  /**
   * Helper utility for throttling
   * @param actionType type being throttled
   * @param path being acted upon
   * @param timeout duration of time to suppress duplicate actions
   * @returns tracking object or false if action should be suppressed
   */
  _throttle(t, u, o) {
    this._throttled.has(t) || this._throttled.set(t, /* @__PURE__ */ new Map());
    const n = this._throttled.get(t);
    if (!n)
      throw new Error("invalid throttle");
    const i = n.get(u);
    if (i)
      return i.count++, !1;
    let r;
    const s = () => {
      const l = n.get(u), c = l ? l.count : 0;
      return n.delete(u), clearTimeout(r), l && clearTimeout(l.timeoutObject), c;
    };
    r = setTimeout(s, o);
    const a = { timeoutObject: r, clear: s, count: 0 };
    return n.set(u, a), a;
  }
  _incrReadyCount() {
    return this._readyCount++;
  }
  /**
   * Awaits write operation to finish.
   * Polls a newly created file for size variations. When files size does not change for 'threshold' milliseconds calls callback.
   * @param path being acted upon
   * @param threshold Time in milliseconds a file size must be fixed before acknowledging write OP is finished
   * @param event
   * @param awfEmit Callback to be called when ready for event to be emitted.
   */
  _awaitWriteFinish(t, u, o, n) {
    const i = this.options.awaitWriteFinish;
    if (typeof i != "object")
      return;
    const r = i.pollInterval;
    let s, a = t;
    this.options.cwd && !b.isAbsolute(t) && (a = b.join(this.options.cwd, t));
    const l = /* @__PURE__ */ new Date(), c = this._pendingWrites;
    function f(p) {
      $u(a, (g, C) => {
        if (g || !c.has(t)) {
          g && g.code !== "ENOENT" && n(g);
          return;
        }
        const _ = Number(/* @__PURE__ */ new Date());
        p && C.size !== p.size && (c.get(t).lastChange = _);
        const O = c.get(t);
        _ - O.lastChange >= u ? (c.delete(t), n(void 0, C)) : s = setTimeout(f, r, C);
      });
    }
    c.has(t) || (c.set(t, {
      lastChange: l,
      cancelWait: () => (c.delete(t), clearTimeout(s), o)
    }), s = setTimeout(f, r));
  }
  /**
   * Determines whether user has asked to ignore this path.
   */
  _isIgnored(t, u) {
    if (this.options.atomic && sD.test(t))
      return !0;
    if (!this._userIgnored) {
      const { cwd: o } = this.options, i = (this.options.ignored || []).map(Sn(o)), s = [...[...this._ignoredPaths].map(Sn(o)), ...i];
      this._userIgnored = cD(s);
    }
    return this._userIgnored(t, u);
  }
  _isntIgnored(t, u) {
    return !this._isIgnored(t, u);
  }
  /**
   * Provides a set of common helpers and properties relating to symlink handling.
   * @param path file or directory pattern being watched
   */
  _getWatchHelpers(t) {
    return new pD(t, this.options.followSymlinks, this);
  }
  // Directory helpers
  // -----------------
  /**
   * Provides directory tracking objects
   * @param directory path of the directory
   */
  _getWatchedDir(t) {
    const u = b.resolve(t);
    return this._watched.has(u) || this._watched.set(u, new dD(u, this._boundRemove)), this._watched.get(u);
  }
  // File helpers
  // ------------
  /**
   * Check for read permissions: https://stackoverflow.com/a/11781404/1358405
   */
  _hasReadPermissions(t) {
    return this.options.ignorePermissionErrors ? !0 : !!(Number(t.mode) & 256);
  }
  /**
   * Handles emitting unlink events for
   * files and directories, and via recursion, for
   * files and directories within directories that are unlinked
   * @param directory within which the following item is located
   * @param item      base path of item/directory
   */
  _remove(t, u, o) {
    const n = b.join(t, u), i = b.resolve(n);
    if (o = o ?? (this._watched.has(n) || this._watched.has(i)), !this._throttle("remove", n, 100))
      return;
    !o && this._watched.size === 1 && this.add(t, u, !0), this._getWatchedDir(n).getChildren().forEach((p) => this._remove(n, p));
    const a = this._getWatchedDir(t), l = a.has(u);
    a.remove(u), this._symlinkPaths.has(i) && this._symlinkPaths.delete(i);
    let c = n;
    if (this.options.cwd && (c = b.relative(this.options.cwd, n)), this.options.awaitWriteFinish && this._pendingWrites.has(c) && this._pendingWrites.get(c).cancelWait() === P.ADD)
      return;
    this._watched.delete(n), this._watched.delete(i);
    const f = o ? P.UNLINK_DIR : P.UNLINK;
    l && !this._isIgnored(n) && this._emit(f, n), this._closePath(n);
  }
  /**
   * Closes all watchers for a path
   */
  _closePath(t) {
    this._closeFile(t);
    const u = b.dirname(t);
    this._getWatchedDir(u).remove(b.basename(t));
  }
  /**
   * Closes only file-specific watchers
   */
  _closeFile(t) {
    const u = this._closers.get(t);
    u && (u.forEach((o) => o()), this._closers.delete(t));
  }
  _addPathCloser(t, u) {
    if (!u)
      return;
    let o = this._closers.get(t);
    o || (o = [], this._closers.set(t, o)), o.push(u);
  }
  _readdirp(t, u) {
    if (this.closed)
      return;
    const o = { type: P.ALL, alwaysStat: !0, lstat: !0, ...u, depth: 0 };
    let n = Ho(t, o);
    return this._streams.add(n), n.once(No, () => {
      n = void 0;
    }), n.once(ou, () => {
      n && (this._streams.delete(n), n = void 0);
    }), n;
  }
}
function gD(e, t = {}) {
  const u = new hu(t);
  return u.add(e), u;
}
const CD = { watch: gD, FSWatcher: hu };
function it(e) {
  return e.startsWith("\\\\?\\") ? e : e.replace(/\\/g, "/");
}
const rt = 100, j = {
  add: [],
  change: [],
  remove: []
}, I = {
  add: void 0,
  change: void 0,
  remove: void 0
}, st = {
  add: {
    initial: "Adding",
    success: "added",
    error: "add"
  },
  change: {
    initial: "Updating",
    success: "updated",
    error: "update"
  }
};
async function Tn(e, t) {
  const u = t === "add" ? j.add : j.change;
  for (let o = 0; o < u.length; o++) {
    const n = u[o], i = S(
      `${st[t].initial} ${k.magenta(n)} file...`
    ).start(), r = await e.upload([n]).catch((s) => {
      i.fail(s.toString());
    });
    r && (r.fails.length ? i.fail(
      `Could not ${st[t].error} file ${k.magenta(n)} due to error - ${r.fails[0].error.message}`
    ) : i.succeed(`File ${k.magenta(n)} ${st[t].success}`));
  }
  t === "add" ? (j.add = [], I.add = void 0) : (j.change = [], I.change = void 0);
}
async function ED(e) {
  for (let t = 0; t < j.remove.length; t++) {
    const u = j.remove[t], o = S(`Removing ${k.magenta(u)} file...`).start(), n = await e.remove([u]).catch((i) => {
      o.fail(i.toString());
    });
    n && (n.fails.length ? o.fail(
      `Could not remove file ${k.magenta(u)} due to error - ${n.fails[0].error.message}`
    ) : o.succeed(`File ${k.magenta(u)} removed`));
  }
  j.remove = [], I.remove = void 0;
}
function _D() {
  W.command("watch").description("Watch theme files for changes and reflect into store").action(() => {
    U.initiateFromConfigFile().then((e) => {
      CD.watch(".", {
        ignored: /(^|[/\\])\../,
        // ignore dotfiles
        persistent: !0,
        ignoreInitial: !0,
        awaitWriteFinish: {
          stabilityThreshold: 1e3,
          pollInterval: 100
        }
      }).on("ready", () => {
        S().info("Watching files...");
      }).on("add", async (u) => {
        j.add.push(it(u)), I.add && clearTimeout(I.add), I.add = setTimeout(() => {
          Tn(e, "add");
        }, rt);
      }).on("change", async (u) => {
        j.change.push(it(u)), I.change && clearTimeout(I.change), I.change = setTimeout(() => {
          Tn(e, "change");
        }, rt);
      }).on("unlink", async (u) => {
        j.remove.push(it(u)), I.remove && clearTimeout(I.remove), I.remove = setTimeout(() => {
          ED(e);
        }, rt);
      }).on("addDir", (u) => {
        S().warn(
          `Creating folder ${k.magenta(u)} is not supported by Tray API... ${k.yellow(
            "Ignored"
          )}`
        );
      }).on("unlinkDir", (u) => {
        S().warn(
          `Deleting folder ${k.magenta(u)} is not supported by Tray API... ${k.yellow(
            "Ignored"
          )}`
        );
      }).on("error", (u) => {
        S().fail(`Watcher error: ${u}`);
      });
    }).catch((e) => {
      S().start().fail(e.toString()), process.exit(1);
    });
  });
}
const wD = Ln(import.meta.url), bD = _u(wD), BD = wu(import.meta.url), AD = BD(In(bD, "../../package.json"));
function yD() {
  ro(), ao(), so(), oo(), Do(), ko(), Po(), _D(), To(), W.version(AD.version, "--version", "Display CLI version").helpOption("--help", "Display CLI help").addHelpCommand("help [command]", "Display help per command").name("tray"), W.parse(process.argv);
}
yD();
