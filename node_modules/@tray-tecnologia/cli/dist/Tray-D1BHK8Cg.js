import { EOL as ge } from "node:os";
import { parse as Go, win32 as Ps, posix as Jo, dirname as Ko } from "node:path";
import { appendFile as Vo, realpath as Xo, readlink as Zo, readdir as Yo, lstat as Qo } from "node:fs/promises";
import { fileURLToPath as Pn } from "node:url";
import { realpathSync as ea, readlinkSync as ta, readdirSync as sa, readdir as ia, lstatSync as na } from "fs";
import * as ra from "node:fs";
import { promises as Me } from "node:fs";
import { EventEmitter as Xs } from "node:events";
import Fn from "node:stream";
import { StringDecoder as oa } from "node:string_decoder";
function Dn(s, e) {
  return function() {
    return s.apply(e, arguments);
  };
}
const { toString: aa } = Object.prototype, { getPrototypeOf: Zs } = Object, { iterator: Kt, toStringTag: kn } = Symbol, Vt = /* @__PURE__ */ ((s) => (e) => {
  const t = aa.call(e);
  return s[t] || (s[t] = t.slice(8, -1).toLowerCase());
})(/* @__PURE__ */ Object.create(null)), re = (s) => (s = s.toLowerCase(), (e) => Vt(e) === s), Xt = (s) => (e) => typeof e === s, { isArray: We } = Array, at = Xt("undefined");
function ht(s) {
  return s !== null && !at(s) && s.constructor !== null && !at(s.constructor) && V(s.constructor.isBuffer) && s.constructor.isBuffer(s);
}
const Ln = re("ArrayBuffer");
function la(s) {
  let e;
  return typeof ArrayBuffer < "u" && ArrayBuffer.isView ? e = ArrayBuffer.isView(s) : e = s && s.buffer && Ln(s.buffer), e;
}
const ca = Xt("string"), V = Xt("function"), jn = Xt("number"), ut = (s) => s !== null && typeof s == "object", ha = (s) => s === !0 || s === !1, _t = (s) => {
  if (Vt(s) !== "object")
    return !1;
  const e = Zs(s);
  return (e === null || e === Object.prototype || Object.getPrototypeOf(e) === null) && !(kn in s) && !(Kt in s);
}, ua = (s) => {
  if (!ut(s) || ht(s))
    return !1;
  try {
    return Object.keys(s).length === 0 && Object.getPrototypeOf(s) === Object.prototype;
  } catch {
    return !1;
  }
}, fa = re("Date"), da = re("File"), pa = re("Blob"), ma = re("FileList"), ga = (s) => ut(s) && V(s.pipe), wa = (s) => {
  let e;
  return s && (typeof FormData == "function" && s instanceof FormData || V(s.append) && ((e = Vt(s)) === "formdata" || // detect form-data instance
  e === "object" && V(s.toString) && s.toString() === "[object FormData]"));
}, ba = re("URLSearchParams"), [ya, Ea, Sa, Ra] = ["ReadableStream", "Request", "Response", "Headers"].map(re), Ta = (s) => s.trim ? s.trim() : s.replace(/^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, "");
function ft(s, e, { allOwnKeys: t = !1 } = {}) {
  if (s === null || typeof s > "u")
    return;
  let i, n;
  if (typeof s != "object" && (s = [s]), We(s))
    for (i = 0, n = s.length; i < n; i++)
      e.call(null, s[i], i, s);
  else {
    if (ht(s))
      return;
    const r = t ? Object.getOwnPropertyNames(s) : Object.keys(s), o = r.length;
    let a;
    for (i = 0; i < o; i++)
      a = r[i], e.call(null, s[a], a, s);
  }
}
function Un(s, e) {
  if (ht(s))
    return null;
  e = e.toLowerCase();
  const t = Object.keys(s);
  let i = t.length, n;
  for (; i-- > 0; )
    if (n = t[i], e === n.toLowerCase())
      return n;
  return null;
}
const Ae = typeof globalThis < "u" ? globalThis : typeof self < "u" ? self : typeof window < "u" ? window : global, Bn = (s) => !at(s) && s !== Ae;
function Fs() {
  const { caseless: s } = Bn(this) && this || {}, e = {}, t = (i, n) => {
    const r = s && Un(e, n) || n;
    _t(e[r]) && _t(i) ? e[r] = Fs(e[r], i) : _t(i) ? e[r] = Fs({}, i) : We(i) ? e[r] = i.slice() : e[r] = i;
  };
  for (let i = 0, n = arguments.length; i < n; i++)
    arguments[i] && ft(arguments[i], t);
  return e;
}
const Oa = (s, e, t, { allOwnKeys: i } = {}) => (ft(e, (n, r) => {
  t && V(n) ? s[r] = Dn(n, t) : s[r] = n;
}, { allOwnKeys: i }), s), xa = (s) => (s.charCodeAt(0) === 65279 && (s = s.slice(1)), s), Aa = (s, e, t, i) => {
  s.prototype = Object.create(e.prototype, i), s.prototype.constructor = s, Object.defineProperty(s, "super", {
    value: e.prototype
  }), t && Object.assign(s.prototype, t);
}, Ca = (s, e, t, i) => {
  let n, r, o;
  const a = {};
  if (e = e || {}, s == null) return e;
  do {
    for (n = Object.getOwnPropertyNames(s), r = n.length; r-- > 0; )
      o = n[r], (!i || i(o, s, e)) && !a[o] && (e[o] = s[o], a[o] = !0);
    s = t !== !1 && Zs(s);
  } while (s && (!t || t(s, e)) && s !== Object.prototype);
  return e;
}, va = (s, e, t) => {
  s = String(s), (t === void 0 || t > s.length) && (t = s.length), t -= e.length;
  const i = s.indexOf(e, t);
  return i !== -1 && i === t;
}, Na = (s) => {
  if (!s) return null;
  if (We(s)) return s;
  let e = s.length;
  if (!jn(e)) return null;
  const t = new Array(e);
  for (; e-- > 0; )
    t[e] = s[e];
  return t;
}, _a = /* @__PURE__ */ ((s) => (e) => s && e instanceof s)(typeof Uint8Array < "u" && Zs(Uint8Array)), Pa = (s, e) => {
  const t = (s && s[Kt]).call(s);
  let i;
  for (; (i = t.next()) && !i.done; ) {
    const n = i.value;
    e.call(s, n[0], n[1]);
  }
}, Fa = (s, e) => {
  let t;
  const i = [];
  for (; (t = s.exec(e)) !== null; )
    i.push(t);
  return i;
}, Da = re("HTMLFormElement"), ka = (s) => s.toLowerCase().replace(
  /[-_\s]([a-z\d])(\w*)/g,
  function(e, t, i) {
    return t.toUpperCase() + i;
  }
), Ti = (({ hasOwnProperty: s }) => (e, t) => s.call(e, t))(Object.prototype), La = re("RegExp"), In = (s, e) => {
  const t = Object.getOwnPropertyDescriptors(s), i = {};
  ft(t, (n, r) => {
    let o;
    (o = e(n, r, s)) !== !1 && (i[r] = o || n);
  }), Object.defineProperties(s, i);
}, ja = (s) => {
  In(s, (e, t) => {
    if (V(s) && ["arguments", "caller", "callee"].indexOf(t) !== -1)
      return !1;
    const i = s[t];
    if (V(i)) {
      if (e.enumerable = !1, "writable" in e) {
        e.writable = !1;
        return;
      }
      e.set || (e.set = () => {
        throw Error("Can not rewrite read-only method '" + t + "'");
      });
    }
  });
}, Ua = (s, e) => {
  const t = {}, i = (n) => {
    n.forEach((r) => {
      t[r] = !0;
    });
  };
  return We(s) ? i(s) : i(String(s).split(e)), t;
}, Ba = () => {
}, Ia = (s, e) => s != null && Number.isFinite(s = +s) ? s : e;
function Ma(s) {
  return !!(s && V(s.append) && s[kn] === "FormData" && s[Kt]);
}
const $a = (s) => {
  const e = new Array(10), t = (i, n) => {
    if (ut(i)) {
      if (e.indexOf(i) >= 0)
        return;
      if (ht(i))
        return i;
      if (!("toJSON" in i)) {
        e[n] = i;
        const r = We(i) ? [] : {};
        return ft(i, (o, a) => {
          const l = t(o, n + 1);
          !at(l) && (r[a] = l);
        }), e[n] = void 0, r;
      }
    }
    return i;
  };
  return t(s, 0);
}, za = re("AsyncFunction"), Wa = (s) => s && (ut(s) || V(s)) && V(s.then) && V(s.catch), Mn = ((s, e) => s ? setImmediate : e ? ((t, i) => (Ae.addEventListener("message", ({ source: n, data: r }) => {
  n === Ae && r === t && i.length && i.shift()();
}, !1), (n) => {
  i.push(n), Ae.postMessage(t, "*");
}))(`axios@${Math.random()}`, []) : (t) => setTimeout(t))(
  typeof setImmediate == "function",
  V(Ae.postMessage)
), qa = typeof queueMicrotask < "u" ? queueMicrotask.bind(Ae) : typeof process < "u" && process.nextTick || Mn, Ha = (s) => s != null && V(s[Kt]), g = {
  isArray: We,
  isArrayBuffer: Ln,
  isBuffer: ht,
  isFormData: wa,
  isArrayBufferView: la,
  isString: ca,
  isNumber: jn,
  isBoolean: ha,
  isObject: ut,
  isPlainObject: _t,
  isEmptyObject: ua,
  isReadableStream: ya,
  isRequest: Ea,
  isResponse: Sa,
  isHeaders: Ra,
  isUndefined: at,
  isDate: fa,
  isFile: da,
  isBlob: pa,
  isRegExp: La,
  isFunction: V,
  isStream: ga,
  isURLSearchParams: ba,
  isTypedArray: _a,
  isFileList: ma,
  forEach: ft,
  merge: Fs,
  extend: Oa,
  trim: Ta,
  stripBOM: xa,
  inherits: Aa,
  toFlatObject: Ca,
  kindOf: Vt,
  kindOfTest: re,
  endsWith: va,
  toArray: Na,
  forEachEntry: Pa,
  matchAll: Fa,
  isHTMLForm: Da,
  hasOwnProperty: Ti,
  hasOwnProp: Ti,
  // an alias to avoid ESLint no-prototype-builtins detection
  reduceDescriptors: In,
  freezeMethods: ja,
  toObjectSet: Ua,
  toCamelCase: ka,
  noop: Ba,
  toFiniteNumber: Ia,
  findKey: Un,
  global: Ae,
  isContextDefined: Bn,
  isSpecCompliantForm: Ma,
  toJSONObject: $a,
  isAsyncFn: za,
  isThenable: Wa,
  setImmediate: Mn,
  asap: qa,
  isIterable: Ha
};
function x(s, e, t, i, n) {
  Error.call(this), Error.captureStackTrace ? Error.captureStackTrace(this, this.constructor) : this.stack = new Error().stack, this.message = s, this.name = "AxiosError", e && (this.code = e), t && (this.config = t), i && (this.request = i), n && (this.response = n, this.status = n.status ? n.status : null);
}
g.inherits(x, Error, {
  toJSON: function() {
    return {
      // Standard
      message: this.message,
      name: this.name,
      // Microsoft
      description: this.description,
      number: this.number,
      // Mozilla
      fileName: this.fileName,
      lineNumber: this.lineNumber,
      columnNumber: this.columnNumber,
      stack: this.stack,
      // Axios
      config: g.toJSONObject(this.config),
      code: this.code,
      status: this.status
    };
  }
});
const $n = x.prototype, zn = {};
[
  "ERR_BAD_OPTION_VALUE",
  "ERR_BAD_OPTION",
  "ECONNABORTED",
  "ETIMEDOUT",
  "ERR_NETWORK",
  "ERR_FR_TOO_MANY_REDIRECTS",
  "ERR_DEPRECATED",
  "ERR_BAD_RESPONSE",
  "ERR_BAD_REQUEST",
  "ERR_CANCELED",
  "ERR_NOT_SUPPORT",
  "ERR_INVALID_URL"
  // eslint-disable-next-line func-names
].forEach((s) => {
  zn[s] = { value: s };
});
Object.defineProperties(x, zn);
Object.defineProperty($n, "isAxiosError", { value: !0 });
x.from = (s, e, t, i, n, r) => {
  const o = Object.create($n);
  return g.toFlatObject(s, o, function(a) {
    return a !== Error.prototype;
  }, (a) => a !== "isAxiosError"), x.call(o, s.message, e, t, i, n), o.cause = s, o.name = s.name, r && Object.assign(o, r), o;
};
const Ga = null;
function Ds(s) {
  return g.isPlainObject(s) || g.isArray(s);
}
function Wn(s) {
  return g.endsWith(s, "[]") ? s.slice(0, -2) : s;
}
function Oi(s, e, t) {
  return s ? s.concat(e).map(function(i, n) {
    return i = Wn(i), !t && n ? "[" + i + "]" : i;
  }).join(t ? "." : "") : e;
}
function Ja(s) {
  return g.isArray(s) && !s.some(Ds);
}
const Ka = g.toFlatObject(g, {}, null, function(s) {
  return /^is[A-Z]/.test(s);
});
function Zt(s, e, t) {
  if (!g.isObject(s))
    throw new TypeError("target must be an object");
  e = e || new FormData(), t = g.toFlatObject(t, {
    metaTokens: !0,
    dots: !1,
    indexes: !1
  }, !1, function(d, m) {
    return !g.isUndefined(m[d]);
  });
  const i = t.metaTokens, n = t.visitor || c, r = t.dots, o = t.indexes, a = (t.Blob || typeof Blob < "u" && Blob) && g.isSpecCompliantForm(e);
  if (!g.isFunction(n))
    throw new TypeError("visitor must be a function");
  function l(d) {
    if (d === null) return "";
    if (g.isDate(d))
      return d.toISOString();
    if (g.isBoolean(d))
      return d.toString();
    if (!a && g.isBlob(d))
      throw new x("Blob is not supported. Use a Buffer instead.");
    return g.isArrayBuffer(d) || g.isTypedArray(d) ? a && typeof Blob == "function" ? new Blob([d]) : Buffer.from(d) : d;
  }
  function c(d, m, f) {
    let b = d;
    if (d && !f && typeof d == "object") {
      if (g.endsWith(m, "{}"))
        m = i ? m : m.slice(0, -2), d = JSON.stringify(d);
      else if (g.isArray(d) && Ja(d) || (g.isFileList(d) || g.endsWith(m, "[]")) && (b = g.toArray(d)))
        return m = Wn(m), b.forEach(function(y, E) {
          !(g.isUndefined(y) || y === null) && e.append(
            // eslint-disable-next-line no-nested-ternary
            o === !0 ? Oi([m], E, r) : o === null ? m : m + "[]",
            l(y)
          );
        }), !1;
    }
    return Ds(d) ? !0 : (e.append(Oi(f, m, r), l(d)), !1);
  }
  const h = [], u = Object.assign(Ka, {
    defaultVisitor: c,
    convertValue: l,
    isVisitable: Ds
  });
  function p(d, m) {
    if (!g.isUndefined(d)) {
      if (h.indexOf(d) !== -1)
        throw Error("Circular reference detected in " + m.join("."));
      h.push(d), g.forEach(d, function(f, b) {
        (!(g.isUndefined(f) || f === null) && n.call(
          e,
          f,
          g.isString(b) ? b.trim() : b,
          m,
          u
        )) === !0 && p(f, m ? m.concat(b) : [b]);
      }), h.pop();
    }
  }
  if (!g.isObject(s))
    throw new TypeError("data must be an object");
  return p(s), e;
}
function xi(s) {
  const e = {
    "!": "%21",
    "'": "%27",
    "(": "%28",
    ")": "%29",
    "~": "%7E",
    "%20": "+",
    "%00": "\0"
  };
  return encodeURIComponent(s).replace(/[!'()~]|%20|%00/g, function(t) {
    return e[t];
  });
}
function Ys(s, e) {
  this._pairs = [], s && Zt(s, this, e);
}
const qn = Ys.prototype;
qn.append = function(s, e) {
  this._pairs.push([s, e]);
};
qn.toString = function(s) {
  const e = s ? function(t) {
    return s.call(this, t, xi);
  } : xi;
  return this._pairs.map(function(t) {
    return e(t[0]) + "=" + e(t[1]);
  }, "").join("&");
};
function Va(s) {
  return encodeURIComponent(s).replace(/%3A/gi, ":").replace(/%24/g, "$").replace(/%2C/gi, ",").replace(/%20/g, "+").replace(/%5B/gi, "[").replace(/%5D/gi, "]");
}
function Hn(s, e, t) {
  if (!e)
    return s;
  const i = t && t.encode || Va;
  g.isFunction(t) && (t = {
    serialize: t
  });
  const n = t && t.serialize;
  let r;
  if (n ? r = n(e, t) : r = g.isURLSearchParams(e) ? e.toString() : new Ys(e, t).toString(i), r) {
    const o = s.indexOf("#");
    o !== -1 && (s = s.slice(0, o)), s += (s.indexOf("?") === -1 ? "?" : "&") + r;
  }
  return s;
}
class Ai {
  constructor() {
    this.handlers = [];
  }
  /**
   * Add a new interceptor to the stack
   *
   * @param {Function} fulfilled The function to handle `then` for a `Promise`
   * @param {Function} rejected The function to handle `reject` for a `Promise`
   *
   * @return {Number} An ID used to remove interceptor later
   */
  use(e, t, i) {
    return this.handlers.push({
      fulfilled: e,
      rejected: t,
      synchronous: i ? i.synchronous : !1,
      runWhen: i ? i.runWhen : null
    }), this.handlers.length - 1;
  }
  /**
   * Remove an interceptor from the stack
   *
   * @param {Number} id The ID that was returned by `use`
   *
   * @returns {Boolean} `true` if the interceptor was removed, `false` otherwise
   */
  eject(e) {
    this.handlers[e] && (this.handlers[e] = null);
  }
  /**
   * Clear all interceptors from the stack
   *
   * @returns {void}
   */
  clear() {
    this.handlers && (this.handlers = []);
  }
  /**
   * Iterate over all the registered interceptors
   *
   * This method is particularly useful for skipping over any
   * interceptors that may have become `null` calling `eject`.
   *
   * @param {Function} fn The function to call for each interceptor
   *
   * @returns {void}
   */
  forEach(e) {
    g.forEach(this.handlers, function(t) {
      t !== null && e(t);
    });
  }
}
const Gn = {
  silentJSONParsing: !0,
  forcedJSONParsing: !0,
  clarifyTimeoutError: !1
}, Xa = typeof URLSearchParams < "u" ? URLSearchParams : Ys, Za = typeof FormData < "u" ? FormData : null, Ya = typeof Blob < "u" ? Blob : null, Qa = {
  isBrowser: !0,
  classes: {
    URLSearchParams: Xa,
    FormData: Za,
    Blob: Ya
  },
  protocols: ["http", "https", "file", "blob", "url", "data"]
}, Qs = typeof window < "u" && typeof document < "u", ks = typeof navigator == "object" && navigator || void 0, el = Qs && (!ks || ["ReactNative", "NativeScript", "NS"].indexOf(ks.product) < 0), tl = typeof WorkerGlobalScope < "u" && // eslint-disable-next-line no-undef
self instanceof WorkerGlobalScope && typeof self.importScripts == "function", sl = Qs && window.location.href || "http://localhost", il = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  hasBrowserEnv: Qs,
  hasStandardBrowserEnv: el,
  hasStandardBrowserWebWorkerEnv: tl,
  navigator: ks,
  origin: sl
}, Symbol.toStringTag, { value: "Module" })), M = {
  ...il,
  ...Qa
};
function nl(s, e) {
  return Zt(s, new M.classes.URLSearchParams(), {
    visitor: function(t, i, n, r) {
      return M.isNode && g.isBuffer(t) ? (this.append(i, t.toString("base64")), !1) : r.defaultVisitor.apply(this, arguments);
    },
    ...e
  });
}
function rl(s) {
  return g.matchAll(/\w+|\[(\w*)]/g, s).map((e) => e[0] === "[]" ? "" : e[1] || e[0]);
}
function ol(s) {
  const e = {}, t = Object.keys(s);
  let i;
  const n = t.length;
  let r;
  for (i = 0; i < n; i++)
    r = t[i], e[r] = s[r];
  return e;
}
function Jn(s) {
  function e(t, i, n, r) {
    let o = t[r++];
    if (o === "__proto__") return !0;
    const a = Number.isFinite(+o), l = r >= t.length;
    return o = !o && g.isArray(n) ? n.length : o, l ? (g.hasOwnProp(n, o) ? n[o] = [n[o], i] : n[o] = i, !a) : ((!n[o] || !g.isObject(n[o])) && (n[o] = []), e(t, i, n[o], r) && g.isArray(n[o]) && (n[o] = ol(n[o])), !a);
  }
  if (g.isFormData(s) && g.isFunction(s.entries)) {
    const t = {};
    return g.forEachEntry(s, (i, n) => {
      e(rl(i), n, t, 0);
    }), t;
  }
  return null;
}
function al(s, e, t) {
  if (g.isString(s))
    try {
      return (e || JSON.parse)(s), g.trim(s);
    } catch (i) {
      if (i.name !== "SyntaxError")
        throw i;
    }
  return (t || JSON.stringify)(s);
}
const dt = {
  transitional: Gn,
  adapter: ["xhr", "http", "fetch"],
  transformRequest: [function(s, e) {
    const t = e.getContentType() || "", i = t.indexOf("application/json") > -1, n = g.isObject(s);
    if (n && g.isHTMLForm(s) && (s = new FormData(s)), g.isFormData(s))
      return i ? JSON.stringify(Jn(s)) : s;
    if (g.isArrayBuffer(s) || g.isBuffer(s) || g.isStream(s) || g.isFile(s) || g.isBlob(s) || g.isReadableStream(s))
      return s;
    if (g.isArrayBufferView(s))
      return s.buffer;
    if (g.isURLSearchParams(s))
      return e.setContentType("application/x-www-form-urlencoded;charset=utf-8", !1), s.toString();
    let r;
    if (n) {
      if (t.indexOf("application/x-www-form-urlencoded") > -1)
        return nl(s, this.formSerializer).toString();
      if ((r = g.isFileList(s)) || t.indexOf("multipart/form-data") > -1) {
        const o = this.env && this.env.FormData;
        return Zt(
          r ? { "files[]": s } : s,
          o && new o(),
          this.formSerializer
        );
      }
    }
    return n || i ? (e.setContentType("application/json", !1), al(s)) : s;
  }],
  transformResponse: [function(s) {
    const e = this.transitional || dt.transitional, t = e && e.forcedJSONParsing, i = this.responseType === "json";
    if (g.isResponse(s) || g.isReadableStream(s))
      return s;
    if (s && g.isString(s) && (t && !this.responseType || i)) {
      const n = !(e && e.silentJSONParsing) && i;
      try {
        return JSON.parse(s);
      } catch (r) {
        if (n)
          throw r.name === "SyntaxError" ? x.from(r, x.ERR_BAD_RESPONSE, this, null, this.response) : r;
      }
    }
    return s;
  }],
  /**
   * A timeout in milliseconds to abort a request. If set to 0 (default) a
   * timeout is not created.
   */
  timeout: 0,
  xsrfCookieName: "XSRF-TOKEN",
  xsrfHeaderName: "X-XSRF-TOKEN",
  maxContentLength: -1,
  maxBodyLength: -1,
  env: {
    FormData: M.classes.FormData,
    Blob: M.classes.Blob
  },
  validateStatus: function(s) {
    return s >= 200 && s < 300;
  },
  headers: {
    common: {
      Accept: "application/json, text/plain, */*",
      "Content-Type": void 0
    }
  }
};
g.forEach(["delete", "get", "head", "post", "put", "patch"], (s) => {
  dt.headers[s] = {};
});
const ll = g.toObjectSet([
  "age",
  "authorization",
  "content-length",
  "content-type",
  "etag",
  "expires",
  "from",
  "host",
  "if-modified-since",
  "if-unmodified-since",
  "last-modified",
  "location",
  "max-forwards",
  "proxy-authorization",
  "referer",
  "retry-after",
  "user-agent"
]), cl = (s) => {
  const e = {};
  let t, i, n;
  return s && s.split(`
`).forEach(function(r) {
    n = r.indexOf(":"), t = r.substring(0, n).trim().toLowerCase(), i = r.substring(n + 1).trim(), !(!t || e[t] && ll[t]) && (t === "set-cookie" ? e[t] ? e[t].push(i) : e[t] = [i] : e[t] = e[t] ? e[t] + ", " + i : i);
  }), e;
}, Ci = Symbol("internals");
function Ke(s) {
  return s && String(s).trim().toLowerCase();
}
function Pt(s) {
  return s === !1 || s == null ? s : g.isArray(s) ? s.map(Pt) : String(s);
}
function hl(s) {
  const e = /* @__PURE__ */ Object.create(null), t = /([^\s,;=]+)\s*(?:=\s*([^,;]+))?/g;
  let i;
  for (; i = t.exec(s); )
    e[i[1]] = i[2];
  return e;
}
const ul = (s) => /^[-_a-zA-Z0-9^`|~,!#$%&'*+.]+$/.test(s.trim());
function ps(s, e, t, i, n) {
  if (g.isFunction(i))
    return i.call(this, e, t);
  if (n && (e = t), !!g.isString(e)) {
    if (g.isString(i))
      return e.indexOf(i) !== -1;
    if (g.isRegExp(i))
      return i.test(e);
  }
}
function fl(s) {
  return s.trim().toLowerCase().replace(/([a-z\d])(\w*)/g, (e, t, i) => t.toUpperCase() + i);
}
function dl(s, e) {
  const t = g.toCamelCase(" " + e);
  ["get", "set", "has"].forEach((i) => {
    Object.defineProperty(s, i + t, {
      value: function(n, r, o) {
        return this[i].call(this, e, n, r, o);
      },
      configurable: !0
    });
  });
}
let X = class {
  constructor(s) {
    s && this.set(s);
  }
  set(s, e, t) {
    const i = this;
    function n(o, a, l) {
      const c = Ke(a);
      if (!c)
        throw new Error("header name must be a non-empty string");
      const h = g.findKey(i, c);
      (!h || i[h] === void 0 || l === !0 || l === void 0 && i[h] !== !1) && (i[h || a] = Pt(o));
    }
    const r = (o, a) => g.forEach(o, (l, c) => n(l, c, a));
    if (g.isPlainObject(s) || s instanceof this.constructor)
      r(s, e);
    else if (g.isString(s) && (s = s.trim()) && !ul(s))
      r(cl(s), e);
    else if (g.isObject(s) && g.isIterable(s)) {
      let o = {}, a, l;
      for (const c of s) {
        if (!g.isArray(c))
          throw TypeError("Object iterator must return a key-value pair");
        o[l = c[0]] = (a = o[l]) ? g.isArray(a) ? [...a, c[1]] : [a, c[1]] : c[1];
      }
      r(o, e);
    } else
      s != null && n(e, s, t);
    return this;
  }
  get(s, e) {
    if (s = Ke(s), s) {
      const t = g.findKey(this, s);
      if (t) {
        const i = this[t];
        if (!e)
          return i;
        if (e === !0)
          return hl(i);
        if (g.isFunction(e))
          return e.call(this, i, t);
        if (g.isRegExp(e))
          return e.exec(i);
        throw new TypeError("parser must be boolean|regexp|function");
      }
    }
  }
  has(s, e) {
    if (s = Ke(s), s) {
      const t = g.findKey(this, s);
      return !!(t && this[t] !== void 0 && (!e || ps(this, this[t], t, e)));
    }
    return !1;
  }
  delete(s, e) {
    const t = this;
    let i = !1;
    function n(r) {
      if (r = Ke(r), r) {
        const o = g.findKey(t, r);
        o && (!e || ps(t, t[o], o, e)) && (delete t[o], i = !0);
      }
    }
    return g.isArray(s) ? s.forEach(n) : n(s), i;
  }
  clear(s) {
    const e = Object.keys(this);
    let t = e.length, i = !1;
    for (; t--; ) {
      const n = e[t];
      (!s || ps(this, this[n], n, s, !0)) && (delete this[n], i = !0);
    }
    return i;
  }
  normalize(s) {
    const e = this, t = {};
    return g.forEach(this, (i, n) => {
      const r = g.findKey(t, n);
      if (r) {
        e[r] = Pt(i), delete e[n];
        return;
      }
      const o = s ? fl(n) : String(n).trim();
      o !== n && delete e[n], e[o] = Pt(i), t[o] = !0;
    }), this;
  }
  concat(...s) {
    return this.constructor.concat(this, ...s);
  }
  toJSON(s) {
    const e = /* @__PURE__ */ Object.create(null);
    return g.forEach(this, (t, i) => {
      t != null && t !== !1 && (e[i] = s && g.isArray(t) ? t.join(", ") : t);
    }), e;
  }
  [Symbol.iterator]() {
    return Object.entries(this.toJSON())[Symbol.iterator]();
  }
  toString() {
    return Object.entries(this.toJSON()).map(([s, e]) => s + ": " + e).join(`
`);
  }
  getSetCookie() {
    return this.get("set-cookie") || [];
  }
  get [Symbol.toStringTag]() {
    return "AxiosHeaders";
  }
  static from(s) {
    return s instanceof this ? s : new this(s);
  }
  static concat(s, ...e) {
    const t = new this(s);
    return e.forEach((i) => t.set(i)), t;
  }
  static accessor(s) {
    const e = (this[Ci] = this[Ci] = {
      accessors: {}
    }).accessors, t = this.prototype;
    function i(n) {
      const r = Ke(n);
      e[r] || (dl(t, n), e[r] = !0);
    }
    return g.isArray(s) ? s.forEach(i) : i(s), this;
  }
};
X.accessor(["Content-Type", "Content-Length", "Accept", "Accept-Encoding", "User-Agent", "Authorization"]);
g.reduceDescriptors(X.prototype, ({ value: s }, e) => {
  let t = e[0].toUpperCase() + e.slice(1);
  return {
    get: () => s,
    set(i) {
      this[t] = i;
    }
  };
});
g.freezeMethods(X);
function ms(s, e) {
  const t = this || dt, i = e || t, n = X.from(i.headers);
  let r = i.data;
  return g.forEach(s, function(o) {
    r = o.call(t, r, n.normalize(), e ? e.status : void 0);
  }), n.normalize(), r;
}
function Kn(s) {
  return !!(s && s.__CANCEL__);
}
function qe(s, e, t) {
  x.call(this, s ?? "canceled", x.ERR_CANCELED, e, t), this.name = "CanceledError";
}
g.inherits(qe, x, {
  __CANCEL__: !0
});
function Vn(s, e, t) {
  const i = t.config.validateStatus;
  !t.status || !i || i(t.status) ? s(t) : e(new x(
    "Request failed with status code " + t.status,
    [x.ERR_BAD_REQUEST, x.ERR_BAD_RESPONSE][Math.floor(t.status / 100) - 4],
    t.config,
    t.request,
    t
  ));
}
function pl(s) {
  const e = /^([-+\w]{1,25})(:?\/\/|:)/.exec(s);
  return e && e[1] || "";
}
function ml(s, e) {
  s = s || 10;
  const t = new Array(s), i = new Array(s);
  let n = 0, r = 0, o;
  return e = e !== void 0 ? e : 1e3, function(a) {
    const l = Date.now(), c = i[r];
    o || (o = l), t[n] = a, i[n] = l;
    let h = r, u = 0;
    for (; h !== n; )
      u += t[h++], h = h % s;
    if (n = (n + 1) % s, n === r && (r = (r + 1) % s), l - o < e)
      return;
    const p = c && l - c;
    return p ? Math.round(u * 1e3 / p) : void 0;
  };
}
function gl(s, e) {
  let t = 0, i = 1e3 / e, n, r;
  const o = (a, l = Date.now()) => {
    t = l, n = null, r && (clearTimeout(r), r = null), s(...a);
  };
  return [(...a) => {
    const l = Date.now(), c = l - t;
    c >= i ? o(a, l) : (n = a, r || (r = setTimeout(() => {
      r = null, o(n);
    }, i - c)));
  }, () => n && o(n)];
}
const Bt = (s, e, t = 3) => {
  let i = 0;
  const n = ml(50, 250);
  return gl((r) => {
    const o = r.loaded, a = r.lengthComputable ? r.total : void 0, l = o - i, c = n(l), h = o <= a;
    i = o;
    const u = {
      loaded: o,
      total: a,
      progress: a ? o / a : void 0,
      bytes: l,
      rate: c || void 0,
      estimated: c && a && h ? (a - o) / c : void 0,
      event: r,
      lengthComputable: a != null,
      [e ? "download" : "upload"]: !0
    };
    s(u);
  }, t);
}, vi = (s, e) => {
  const t = s != null;
  return [(i) => e[0]({
    lengthComputable: t,
    total: s,
    loaded: i
  }), e[1]];
}, Ni = (s) => (...e) => g.asap(() => s(...e)), wl = M.hasStandardBrowserEnv ? /* @__PURE__ */ ((s, e) => (t) => (t = new URL(t, M.origin), s.protocol === t.protocol && s.host === t.host && (e || s.port === t.port)))(
  new URL(M.origin),
  M.navigator && /(msie|trident)/i.test(M.navigator.userAgent)
) : () => !0, bl = M.hasStandardBrowserEnv ? (
  // Standard browser envs support document.cookie
  {
    write(s, e, t, i, n, r) {
      const o = [s + "=" + encodeURIComponent(e)];
      g.isNumber(t) && o.push("expires=" + new Date(t).toGMTString()), g.isString(i) && o.push("path=" + i), g.isString(n) && o.push("domain=" + n), r === !0 && o.push("secure"), document.cookie = o.join("; ");
    },
    read(s) {
      const e = document.cookie.match(new RegExp("(^|;\\s*)(" + s + ")=([^;]*)"));
      return e ? decodeURIComponent(e[3]) : null;
    },
    remove(s) {
      this.write(s, "", Date.now() - 864e5);
    }
  }
) : (
  // Non-standard browser env (web workers, react-native) lack needed support.
  {
    write() {
    },
    read() {
      return null;
    },
    remove() {
    }
  }
);
function yl(s) {
  return /^([a-z][a-z\d+\-.]*:)?\/\//i.test(s);
}
function El(s, e) {
  return e ? s.replace(/\/?\/$/, "") + "/" + e.replace(/^\/+/, "") : s;
}
function Xn(s, e, t) {
  let i = !yl(e);
  return s && (i || t == !1) ? El(s, e) : e;
}
const _i = (s) => s instanceof X ? { ...s } : s;
function _e(s, e) {
  e = e || {};
  const t = {};
  function i(c, h, u, p) {
    return g.isPlainObject(c) && g.isPlainObject(h) ? g.merge.call({ caseless: p }, c, h) : g.isPlainObject(h) ? g.merge({}, h) : g.isArray(h) ? h.slice() : h;
  }
  function n(c, h, u, p) {
    if (g.isUndefined(h)) {
      if (!g.isUndefined(c))
        return i(void 0, c, u, p);
    } else return i(c, h, u, p);
  }
  function r(c, h) {
    if (!g.isUndefined(h))
      return i(void 0, h);
  }
  function o(c, h) {
    if (g.isUndefined(h)) {
      if (!g.isUndefined(c))
        return i(void 0, c);
    } else return i(void 0, h);
  }
  function a(c, h, u) {
    if (u in e)
      return i(c, h);
    if (u in s)
      return i(void 0, c);
  }
  const l = {
    url: r,
    method: r,
    data: r,
    baseURL: o,
    transformRequest: o,
    transformResponse: o,
    paramsSerializer: o,
    timeout: o,
    timeoutMessage: o,
    withCredentials: o,
    withXSRFToken: o,
    adapter: o,
    responseType: o,
    xsrfCookieName: o,
    xsrfHeaderName: o,
    onUploadProgress: o,
    onDownloadProgress: o,
    decompress: o,
    maxContentLength: o,
    maxBodyLength: o,
    beforeRedirect: o,
    transport: o,
    httpAgent: o,
    httpsAgent: o,
    cancelToken: o,
    socketPath: o,
    responseEncoding: o,
    validateStatus: a,
    headers: (c, h, u) => n(_i(c), _i(h), u, !0)
  };
  return g.forEach(Object.keys({ ...s, ...e }), function(c) {
    const h = l[c] || n, u = h(s[c], e[c], c);
    g.isUndefined(u) && h !== a || (t[c] = u);
  }), t;
}
const Zn = (s) => {
  const e = _e({}, s);
  let { data: t, withXSRFToken: i, xsrfHeaderName: n, xsrfCookieName: r, headers: o, auth: a } = e;
  e.headers = o = X.from(o), e.url = Hn(Xn(e.baseURL, e.url, e.allowAbsoluteUrls), s.params, s.paramsSerializer), a && o.set(
    "Authorization",
    "Basic " + btoa((a.username || "") + ":" + (a.password ? unescape(encodeURIComponent(a.password)) : ""))
  );
  let l;
  if (g.isFormData(t)) {
    if (M.hasStandardBrowserEnv || M.hasStandardBrowserWebWorkerEnv)
      o.setContentType(void 0);
    else if ((l = o.getContentType()) !== !1) {
      const [c, ...h] = l ? l.split(";").map((u) => u.trim()).filter(Boolean) : [];
      o.setContentType([c || "multipart/form-data", ...h].join("; "));
    }
  }
  if (M.hasStandardBrowserEnv && (i && g.isFunction(i) && (i = i(e)), i || i !== !1 && wl(e.url))) {
    const c = n && r && bl.read(r);
    c && o.set(n, c);
  }
  return e;
}, Sl = typeof XMLHttpRequest < "u", Rl = Sl && function(s) {
  return new Promise(function(e, t) {
    const i = Zn(s);
    let n = i.data;
    const r = X.from(i.headers).normalize();
    let { responseType: o, onUploadProgress: a, onDownloadProgress: l } = i, c, h, u, p, d;
    function m() {
      p && p(), d && d(), i.cancelToken && i.cancelToken.unsubscribe(c), i.signal && i.signal.removeEventListener("abort", c);
    }
    let f = new XMLHttpRequest();
    f.open(i.method.toUpperCase(), i.url, !0), f.timeout = i.timeout;
    function b() {
      if (!f)
        return;
      const E = X.from(
        "getAllResponseHeaders" in f && f.getAllResponseHeaders()
      ), S = {
        data: !o || o === "text" || o === "json" ? f.responseText : f.response,
        status: f.status,
        statusText: f.statusText,
        headers: E,
        config: s,
        request: f
      };
      Vn(function(T) {
        e(T), m();
      }, function(T) {
        t(T), m();
      }, S), f = null;
    }
    "onloadend" in f ? f.onloadend = b : f.onreadystatechange = function() {
      !f || f.readyState !== 4 || f.status === 0 && !(f.responseURL && f.responseURL.indexOf("file:") === 0) || setTimeout(b);
    }, f.onabort = function() {
      f && (t(new x("Request aborted", x.ECONNABORTED, s, f)), f = null);
    }, f.onerror = function() {
      t(new x("Network Error", x.ERR_NETWORK, s, f)), f = null;
    }, f.ontimeout = function() {
      let E = i.timeout ? "timeout of " + i.timeout + "ms exceeded" : "timeout exceeded";
      const S = i.transitional || Gn;
      i.timeoutErrorMessage && (E = i.timeoutErrorMessage), t(new x(
        E,
        S.clarifyTimeoutError ? x.ETIMEDOUT : x.ECONNABORTED,
        s,
        f
      )), f = null;
    }, n === void 0 && r.setContentType(null), "setRequestHeader" in f && g.forEach(r.toJSON(), function(E, S) {
      f.setRequestHeader(S, E);
    }), g.isUndefined(i.withCredentials) || (f.withCredentials = !!i.withCredentials), o && o !== "json" && (f.responseType = i.responseType), l && ([u, d] = Bt(l, !0), f.addEventListener("progress", u)), a && f.upload && ([h, p] = Bt(a), f.upload.addEventListener("progress", h), f.upload.addEventListener("loadend", p)), (i.cancelToken || i.signal) && (c = (E) => {
      f && (t(!E || E.type ? new qe(null, s, f) : E), f.abort(), f = null);
    }, i.cancelToken && i.cancelToken.subscribe(c), i.signal && (i.signal.aborted ? c() : i.signal.addEventListener("abort", c)));
    const y = pl(i.url);
    if (y && M.protocols.indexOf(y) === -1) {
      t(new x("Unsupported protocol " + y + ":", x.ERR_BAD_REQUEST, s));
      return;
    }
    f.send(n || null);
  });
}, Tl = (s, e) => {
  const { length: t } = s = s ? s.filter(Boolean) : [];
  if (e || t) {
    let i = new AbortController(), n;
    const r = function(c) {
      if (!n) {
        n = !0, a();
        const h = c instanceof Error ? c : this.reason;
        i.abort(h instanceof x ? h : new qe(h instanceof Error ? h.message : h));
      }
    };
    let o = e && setTimeout(() => {
      o = null, r(new x(`timeout ${e} of ms exceeded`, x.ETIMEDOUT));
    }, e);
    const a = () => {
      s && (o && clearTimeout(o), o = null, s.forEach((c) => {
        c.unsubscribe ? c.unsubscribe(r) : c.removeEventListener("abort", r);
      }), s = null);
    };
    s.forEach((c) => c.addEventListener("abort", r));
    const { signal: l } = i;
    return l.unsubscribe = () => g.asap(a), l;
  }
}, Ol = function* (s, e) {
  let t = s.byteLength;
  if (t < e) {
    yield s;
    return;
  }
  let i = 0, n;
  for (; i < t; )
    n = i + e, yield s.slice(i, n), i = n;
}, xl = async function* (s, e) {
  for await (const t of Al(s))
    yield* Ol(t, e);
}, Al = async function* (s) {
  if (s[Symbol.asyncIterator]) {
    yield* s;
    return;
  }
  const e = s.getReader();
  try {
    for (; ; ) {
      const { done: t, value: i } = await e.read();
      if (t)
        break;
      yield i;
    }
  } finally {
    await e.cancel();
  }
}, Pi = (s, e, t, i) => {
  const n = xl(s, e);
  let r = 0, o, a = (l) => {
    o || (o = !0, i && i(l));
  };
  return new ReadableStream({
    async pull(l) {
      try {
        const { done: c, value: h } = await n.next();
        if (c) {
          a(), l.close();
          return;
        }
        let u = h.byteLength;
        if (t) {
          let p = r += u;
          t(p);
        }
        l.enqueue(new Uint8Array(h));
      } catch (c) {
        throw a(c), c;
      }
    },
    cancel(l) {
      return a(l), n.return();
    }
  }, {
    highWaterMark: 2
  });
}, Yt = typeof fetch == "function" && typeof Request == "function" && typeof Response == "function", Yn = Yt && typeof ReadableStream == "function", Cl = Yt && (typeof TextEncoder == "function" ? /* @__PURE__ */ ((s) => (e) => s.encode(e))(new TextEncoder()) : async (s) => new Uint8Array(await new Response(s).arrayBuffer())), Qn = (s, ...e) => {
  try {
    return !!s(...e);
  } catch {
    return !1;
  }
}, vl = Yn && Qn(() => {
  let s = !1;
  const e = new Request(M.origin, {
    body: new ReadableStream(),
    method: "POST",
    get duplex() {
      return s = !0, "half";
    }
  }).headers.has("Content-Type");
  return s && !e;
}), Fi = 64 * 1024, Ls = Yn && Qn(() => g.isReadableStream(new Response("").body)), It = {
  stream: Ls && ((s) => s.body)
};
Yt && ((s) => {
  ["text", "arrayBuffer", "blob", "formData", "stream"].forEach((e) => {
    !It[e] && (It[e] = g.isFunction(s[e]) ? (t) => t[e]() : (t, i) => {
      throw new x(`Response type '${e}' is not supported`, x.ERR_NOT_SUPPORT, i);
    });
  });
})(new Response());
const Nl = async (s) => {
  if (s == null)
    return 0;
  if (g.isBlob(s))
    return s.size;
  if (g.isSpecCompliantForm(s))
    return (await new Request(M.origin, {
      method: "POST",
      body: s
    }).arrayBuffer()).byteLength;
  if (g.isArrayBufferView(s) || g.isArrayBuffer(s))
    return s.byteLength;
  if (g.isURLSearchParams(s) && (s = s + ""), g.isString(s))
    return (await Cl(s)).byteLength;
}, _l = async (s, e) => g.toFiniteNumber(s.getContentLength()) ?? Nl(e), Pl = Yt && (async (s) => {
  let {
    url: e,
    method: t,
    data: i,
    signal: n,
    cancelToken: r,
    timeout: o,
    onDownloadProgress: a,
    onUploadProgress: l,
    responseType: c,
    headers: h,
    withCredentials: u = "same-origin",
    fetchOptions: p
  } = Zn(s);
  c = c ? (c + "").toLowerCase() : "text";
  let d = Tl([n, r && r.toAbortSignal()], o), m;
  const f = d && d.unsubscribe && (() => {
    d.unsubscribe();
  });
  let b;
  try {
    if (l && vl && t !== "get" && t !== "head" && (b = await _l(h, i)) !== 0) {
      let O = new Request(e, {
        method: "POST",
        body: i,
        duplex: "half"
      }), v;
      if (g.isFormData(i) && (v = O.headers.get("content-type")) && h.setContentType(v), O.body) {
        const [P, B] = vi(
          b,
          Bt(Ni(l))
        );
        i = Pi(O.body, Fi, P, B);
      }
    }
    g.isString(u) || (u = u ? "include" : "omit");
    const y = "credentials" in Request.prototype;
    m = new Request(e, {
      ...p,
      signal: d,
      method: t.toUpperCase(),
      headers: h.normalize().toJSON(),
      body: i,
      duplex: "half",
      credentials: y ? u : void 0
    });
    let E = await fetch(m, p);
    const S = Ls && (c === "stream" || c === "response");
    if (Ls && (a || S && f)) {
      const O = {};
      ["status", "statusText", "headers"].forEach((z) => {
        O[z] = E[z];
      });
      const v = g.toFiniteNumber(E.headers.get("content-length")), [P, B] = a && vi(
        v,
        Bt(Ni(a), !0)
      ) || [];
      E = new Response(
        Pi(E.body, Fi, P, () => {
          B && B(), f && f();
        }),
        O
      );
    }
    c = c || "text";
    let T = await It[g.findKey(It, c) || "text"](E, s);
    return !S && f && f(), await new Promise((O, v) => {
      Vn(O, v, {
        data: T,
        headers: X.from(E.headers),
        status: E.status,
        statusText: E.statusText,
        config: s,
        request: m
      });
    });
  } catch (y) {
    throw f && f(), y && y.name === "TypeError" && /Load failed|fetch/i.test(y.message) ? Object.assign(
      new x("Network Error", x.ERR_NETWORK, s, m),
      {
        cause: y.cause || y
      }
    ) : x.from(y, y && y.code, s, m);
  }
}), js = {
  http: Ga,
  xhr: Rl,
  fetch: Pl
};
g.forEach(js, (s, e) => {
  if (s) {
    try {
      Object.defineProperty(s, "name", { value: e });
    } catch {
    }
    Object.defineProperty(s, "adapterName", { value: e });
  }
});
const Di = (s) => `- ${s}`, Fl = (s) => g.isFunction(s) || s === null || s === !1, er = {
  getAdapter: (s) => {
    s = g.isArray(s) ? s : [s];
    const { length: e } = s;
    let t, i;
    const n = {};
    for (let r = 0; r < e; r++) {
      t = s[r];
      let o;
      if (i = t, !Fl(t) && (i = js[(o = String(t)).toLowerCase()], i === void 0))
        throw new x(`Unknown adapter '${o}'`);
      if (i)
        break;
      n[o || "#" + r] = i;
    }
    if (!i) {
      const r = Object.entries(n).map(
        ([a, l]) => `adapter ${a} ` + (l === !1 ? "is not supported by the environment" : "is not available in the build")
      );
      let o = e ? r.length > 1 ? `since :
` + r.map(Di).join(`
`) : " " + Di(r[0]) : "as no adapter specified";
      throw new x(
        "There is no suitable adapter to dispatch the request " + o,
        "ERR_NOT_SUPPORT"
      );
    }
    return i;
  },
  adapters: js
};
function gs(s) {
  if (s.cancelToken && s.cancelToken.throwIfRequested(), s.signal && s.signal.aborted)
    throw new qe(null, s);
}
function ki(s) {
  return gs(s), s.headers = X.from(s.headers), s.data = ms.call(
    s,
    s.transformRequest
  ), ["post", "put", "patch"].indexOf(s.method) !== -1 && s.headers.setContentType("application/x-www-form-urlencoded", !1), er.getAdapter(s.adapter || dt.adapter)(s).then(function(e) {
    return gs(s), e.data = ms.call(
      s,
      s.transformResponse,
      e
    ), e.headers = X.from(e.headers), e;
  }, function(e) {
    return Kn(e) || (gs(s), e && e.response && (e.response.data = ms.call(
      s,
      s.transformResponse,
      e.response
    ), e.response.headers = X.from(e.response.headers))), Promise.reject(e);
  });
}
const tr = "1.11.0", Qt = {};
["object", "boolean", "number", "function", "string", "symbol"].forEach((s, e) => {
  Qt[s] = function(t) {
    return typeof t === s || "a" + (e < 1 ? "n " : " ") + s;
  };
});
const Li = {};
Qt.transitional = function(s, e, t) {
  function i(n, r) {
    return "[Axios v" + tr + "] Transitional option '" + n + "'" + r + (t ? ". " + t : "");
  }
  return (n, r, o) => {
    if (s === !1)
      throw new x(
        i(r, " has been removed" + (e ? " in " + e : "")),
        x.ERR_DEPRECATED
      );
    return e && !Li[r] && (Li[r] = !0, console.warn(
      i(
        r,
        " has been deprecated since v" + e + " and will be removed in the near future"
      )
    )), s ? s(n, r, o) : !0;
  };
};
Qt.spelling = function(s) {
  return (e, t) => (console.warn(`${t} is likely a misspelling of ${s}`), !0);
};
function Dl(s, e, t) {
  if (typeof s != "object")
    throw new x("options must be an object", x.ERR_BAD_OPTION_VALUE);
  const i = Object.keys(s);
  let n = i.length;
  for (; n-- > 0; ) {
    const r = i[n], o = e[r];
    if (o) {
      const a = s[r], l = a === void 0 || o(a, r, s);
      if (l !== !0)
        throw new x("option " + r + " must be " + l, x.ERR_BAD_OPTION_VALUE);
      continue;
    }
    if (t !== !0)
      throw new x("Unknown option " + r, x.ERR_BAD_OPTION);
  }
}
const Ft = {
  assertOptions: Dl,
  validators: Qt
}, ae = Ft.validators;
let ve = class {
  constructor(s) {
    this.defaults = s || {}, this.interceptors = {
      request: new Ai(),
      response: new Ai()
    };
  }
  /**
   * Dispatch a request
   *
   * @param {String|Object} configOrUrl The config specific for this request (merged with this.defaults)
   * @param {?Object} config
   *
   * @returns {Promise} The Promise to be fulfilled
   */
  async request(s, e) {
    try {
      return await this._request(s, e);
    } catch (t) {
      if (t instanceof Error) {
        let i = {};
        Error.captureStackTrace ? Error.captureStackTrace(i) : i = new Error();
        const n = i.stack ? i.stack.replace(/^.+\n/, "") : "";
        try {
          t.stack ? n && !String(t.stack).endsWith(n.replace(/^.+\n.+\n/, "")) && (t.stack += `
` + n) : t.stack = n;
        } catch {
        }
      }
      throw t;
    }
  }
  _request(s, e) {
    typeof s == "string" ? (e = e || {}, e.url = s) : e = s || {}, e = _e(this.defaults, e);
    const { transitional: t, paramsSerializer: i, headers: n } = e;
    t !== void 0 && Ft.assertOptions(t, {
      silentJSONParsing: ae.transitional(ae.boolean),
      forcedJSONParsing: ae.transitional(ae.boolean),
      clarifyTimeoutError: ae.transitional(ae.boolean)
    }, !1), i != null && (g.isFunction(i) ? e.paramsSerializer = {
      serialize: i
    } : Ft.assertOptions(i, {
      encode: ae.function,
      serialize: ae.function
    }, !0)), e.allowAbsoluteUrls !== void 0 || (this.defaults.allowAbsoluteUrls !== void 0 ? e.allowAbsoluteUrls = this.defaults.allowAbsoluteUrls : e.allowAbsoluteUrls = !0), Ft.assertOptions(e, {
      baseUrl: ae.spelling("baseURL"),
      withXsrfToken: ae.spelling("withXSRFToken")
    }, !0), e.method = (e.method || this.defaults.method || "get").toLowerCase();
    let r = n && g.merge(
      n.common,
      n[e.method]
    );
    n && g.forEach(
      ["delete", "get", "head", "post", "put", "patch", "common"],
      (d) => {
        delete n[d];
      }
    ), e.headers = X.concat(r, n);
    const o = [];
    let a = !0;
    this.interceptors.request.forEach(function(d) {
      typeof d.runWhen == "function" && d.runWhen(e) === !1 || (a = a && d.synchronous, o.unshift(d.fulfilled, d.rejected));
    });
    const l = [];
    this.interceptors.response.forEach(function(d) {
      l.push(d.fulfilled, d.rejected);
    });
    let c, h = 0, u;
    if (!a) {
      const d = [ki.bind(this), void 0];
      for (d.unshift(...o), d.push(...l), u = d.length, c = Promise.resolve(e); h < u; )
        c = c.then(d[h++], d[h++]);
      return c;
    }
    u = o.length;
    let p = e;
    for (h = 0; h < u; ) {
      const d = o[h++], m = o[h++];
      try {
        p = d(p);
      } catch (f) {
        m.call(this, f);
        break;
      }
    }
    try {
      c = ki.call(this, p);
    } catch (d) {
      return Promise.reject(d);
    }
    for (h = 0, u = l.length; h < u; )
      c = c.then(l[h++], l[h++]);
    return c;
  }
  getUri(s) {
    s = _e(this.defaults, s);
    const e = Xn(s.baseURL, s.url, s.allowAbsoluteUrls);
    return Hn(e, s.params, s.paramsSerializer);
  }
};
g.forEach(["delete", "get", "head", "options"], function(s) {
  ve.prototype[s] = function(e, t) {
    return this.request(_e(t || {}, {
      method: s,
      url: e,
      data: (t || {}).data
    }));
  };
});
g.forEach(["post", "put", "patch"], function(s) {
  function e(t) {
    return function(i, n, r) {
      return this.request(_e(r || {}, {
        method: s,
        headers: t ? {
          "Content-Type": "multipart/form-data"
        } : {},
        url: i,
        data: n
      }));
    };
  }
  ve.prototype[s] = e(), ve.prototype[s + "Form"] = e(!0);
});
let kl = class sr {
  constructor(e) {
    if (typeof e != "function")
      throw new TypeError("executor must be a function.");
    let t;
    this.promise = new Promise(function(n) {
      t = n;
    });
    const i = this;
    this.promise.then((n) => {
      if (!i._listeners) return;
      let r = i._listeners.length;
      for (; r-- > 0; )
        i._listeners[r](n);
      i._listeners = null;
    }), this.promise.then = (n) => {
      let r;
      const o = new Promise((a) => {
        i.subscribe(a), r = a;
      }).then(n);
      return o.cancel = function() {
        i.unsubscribe(r);
      }, o;
    }, e(function(n, r, o) {
      i.reason || (i.reason = new qe(n, r, o), t(i.reason));
    });
  }
  /**
   * Throws a `CanceledError` if cancellation has been requested.
   */
  throwIfRequested() {
    if (this.reason)
      throw this.reason;
  }
  /**
   * Subscribe to the cancel signal
   */
  subscribe(e) {
    if (this.reason) {
      e(this.reason);
      return;
    }
    this._listeners ? this._listeners.push(e) : this._listeners = [e];
  }
  /**
   * Unsubscribe from the cancel signal
   */
  unsubscribe(e) {
    if (!this._listeners)
      return;
    const t = this._listeners.indexOf(e);
    t !== -1 && this._listeners.splice(t, 1);
  }
  toAbortSignal() {
    const e = new AbortController(), t = (i) => {
      e.abort(i);
    };
    return this.subscribe(t), e.signal.unsubscribe = () => this.unsubscribe(t), e.signal;
  }
  /**
   * Returns an object that contains a new `CancelToken` and a function that, when called,
   * cancels the `CancelToken`.
   */
  static source() {
    let e;
    return {
      token: new sr(function(t) {
        e = t;
      }),
      cancel: e
    };
  }
};
function Ll(s) {
  return function(e) {
    return s.apply(null, e);
  };
}
function jl(s) {
  return g.isObject(s) && s.isAxiosError === !0;
}
const Us = {
  Continue: 100,
  SwitchingProtocols: 101,
  Processing: 102,
  EarlyHints: 103,
  Ok: 200,
  Created: 201,
  Accepted: 202,
  NonAuthoritativeInformation: 203,
  NoContent: 204,
  ResetContent: 205,
  PartialContent: 206,
  MultiStatus: 207,
  AlreadyReported: 208,
  ImUsed: 226,
  MultipleChoices: 300,
  MovedPermanently: 301,
  Found: 302,
  SeeOther: 303,
  NotModified: 304,
  UseProxy: 305,
  Unused: 306,
  TemporaryRedirect: 307,
  PermanentRedirect: 308,
  BadRequest: 400,
  Unauthorized: 401,
  PaymentRequired: 402,
  Forbidden: 403,
  NotFound: 404,
  MethodNotAllowed: 405,
  NotAcceptable: 406,
  ProxyAuthenticationRequired: 407,
  RequestTimeout: 408,
  Conflict: 409,
  Gone: 410,
  LengthRequired: 411,
  PreconditionFailed: 412,
  PayloadTooLarge: 413,
  UriTooLong: 414,
  UnsupportedMediaType: 415,
  RangeNotSatisfiable: 416,
  ExpectationFailed: 417,
  ImATeapot: 418,
  MisdirectedRequest: 421,
  UnprocessableEntity: 422,
  Locked: 423,
  FailedDependency: 424,
  TooEarly: 425,
  UpgradeRequired: 426,
  PreconditionRequired: 428,
  TooManyRequests: 429,
  RequestHeaderFieldsTooLarge: 431,
  UnavailableForLegalReasons: 451,
  InternalServerError: 500,
  NotImplemented: 501,
  BadGateway: 502,
  ServiceUnavailable: 503,
  GatewayTimeout: 504,
  HttpVersionNotSupported: 505,
  VariantAlsoNegotiates: 506,
  InsufficientStorage: 507,
  LoopDetected: 508,
  NotExtended: 510,
  NetworkAuthenticationRequired: 511
};
Object.entries(Us).forEach(([s, e]) => {
  Us[e] = s;
});
function ir(s) {
  const e = new ve(s), t = Dn(ve.prototype.request, e);
  return g.extend(t, ve.prototype, e, { allOwnKeys: !0 }), g.extend(t, e, null, { allOwnKeys: !0 }), t.create = function(i) {
    return ir(_e(s, i));
  }, t;
}
const D = ir(dt);
D.Axios = ve;
D.CanceledError = qe;
D.CancelToken = kl;
D.isCancel = Kn;
D.VERSION = tr;
D.toFormData = Zt;
D.AxiosError = x;
D.Cancel = D.CanceledError;
D.all = function(s) {
  return Promise.all(s);
};
D.spread = Ll;
D.isAxiosError = jl;
D.mergeConfig = _e;
D.AxiosHeaders = X;
D.formToJSON = (s) => Jn(g.isHTMLForm(s) ? new FormData(s) : s);
D.getAdapter = er.getAdapter;
D.HttpStatusCode = Us;
D.default = D;
const {
  Axios: rd,
  AxiosError: od,
  CanceledError: ad,
  isCancel: ld,
  CancelToken: cd,
  VERSION: hd,
  all: ud,
  Cancel: fd,
  isAxiosError: dd,
  spread: pd,
  toFormData: md,
  AxiosHeaders: gd,
  HttpStatusCode: wd,
  formToJSON: bd,
  getAdapter: yd,
  mergeConfig: Ed
} = D;
function Ul(s) {
  return s && s.__esModule && Object.prototype.hasOwnProperty.call(s, "default") ? s.default : s;
}
var ws, ji;
function Bl() {
  if (ji) return ws;
  ji = 1;
  const s = /* @__PURE__ */ new Set([
    "ENOTFOUND",
    "ENETUNREACH",
    // SSL errors from https://github.com/nodejs/node/blob/fc8e3e2cdc521978351de257030db0076d79e0ab/src/crypto/crypto_common.cc#L301-L328
    "UNABLE_TO_GET_ISSUER_CERT",
    "UNABLE_TO_GET_CRL",
    "UNABLE_TO_DECRYPT_CERT_SIGNATURE",
    "UNABLE_TO_DECRYPT_CRL_SIGNATURE",
    "UNABLE_TO_DECODE_ISSUER_PUBLIC_KEY",
    "CERT_SIGNATURE_FAILURE",
    "CRL_SIGNATURE_FAILURE",
    "CERT_NOT_YET_VALID",
    "CERT_HAS_EXPIRED",
    "CRL_NOT_YET_VALID",
    "CRL_HAS_EXPIRED",
    "ERROR_IN_CERT_NOT_BEFORE_FIELD",
    "ERROR_IN_CERT_NOT_AFTER_FIELD",
    "ERROR_IN_CRL_LAST_UPDATE_FIELD",
    "ERROR_IN_CRL_NEXT_UPDATE_FIELD",
    "OUT_OF_MEM",
    "DEPTH_ZERO_SELF_SIGNED_CERT",
    "SELF_SIGNED_CERT_IN_CHAIN",
    "UNABLE_TO_GET_ISSUER_CERT_LOCALLY",
    "UNABLE_TO_VERIFY_LEAF_SIGNATURE",
    "CERT_CHAIN_TOO_LONG",
    "CERT_REVOKED",
    "INVALID_CA",
    "PATH_LENGTH_EXCEEDED",
    "INVALID_PURPOSE",
    "CERT_UNTRUSTED",
    "CERT_REJECTED",
    "HOSTNAME_MISMATCH"
  ]);
  return ws = (e) => !s.has(e && e.code), ws;
}
var Il = Bl();
const Ml = /* @__PURE__ */ Ul(Il), ei = "axios-retry";
function ti(s) {
  const e = ["ERR_CANCELED", "ECONNABORTED"];
  return s.response || !s.code || e.includes(s.code) ? !1 : Ml(s);
}
const nr = ["get", "head", "options"], $l = nr.concat(["put", "delete"]);
function es(s) {
  return s.code !== "ECONNABORTED" && (!s.response || s.response.status === 429 || s.response.status >= 500 && s.response.status <= 599);
}
function zl(s) {
  return s.config?.method ? es(s) && nr.indexOf(s.config.method) !== -1 : !1;
}
function rr(s) {
  return s.config?.method ? es(s) && $l.indexOf(s.config.method) !== -1 : !1;
}
function or(s) {
  return ti(s) || rr(s);
}
function si(s = void 0) {
  const e = s?.response?.headers["retry-after"];
  if (!e)
    return 0;
  let t = (Number(e) || 0) * 1e3;
  return t === 0 && (t = (new Date(e).valueOf() || 0) - Date.now()), Math.max(0, t);
}
function Wl(s = 0, e = void 0) {
  return Math.max(0, si(e));
}
function ql(s = 0, e = void 0, t = 100) {
  const i = 2 ** s * t, n = Math.max(i, si(e)), r = n * 0.2 * Math.random();
  return n + r;
}
function Hl(s = 100) {
  return (e = 0, t = void 0) => {
    const i = e * s;
    return Math.max(i, si(t));
  };
}
const Gl = {
  retries: 3,
  retryCondition: or,
  retryDelay: Wl,
  shouldResetTimeout: !1,
  onRetry: () => {
  },
  onMaxRetryTimesExceeded: () => {
  },
  validateResponse: null
};
function Jl(s, e) {
  return { ...Gl, ...e, ...s[ei] };
}
function Ui(s, e, t = !1) {
  const i = Jl(s, e || {});
  return i.retryCount = i.retryCount || 0, (!i.lastRequestTime || t) && (i.lastRequestTime = Date.now()), s[ei] = i, i;
}
function Kl(s, e) {
  s.defaults.agent === e.agent && delete e.agent, s.defaults.httpAgent === e.httpAgent && delete e.httpAgent, s.defaults.httpsAgent === e.httpsAgent && delete e.httpsAgent;
}
async function Vl(s, e) {
  const { retries: t, retryCondition: i } = s, n = (s.retryCount || 0) < t && i(e);
  if (typeof n == "object")
    try {
      return await n !== !1;
    } catch {
      return !1;
    }
  return n;
}
async function Xl(s, e, t, i) {
  e.retryCount += 1;
  const { retryDelay: n, shouldResetTimeout: r, onRetry: o } = e, a = n(e.retryCount, t);
  if (Kl(s, i), !r && i.timeout && e.lastRequestTime) {
    const l = Date.now() - e.lastRequestTime, c = i.timeout - l - a;
    if (c <= 0)
      return Promise.reject(t);
    i.timeout = c;
  }
  return i.transformRequest = [(l) => l], await o(e.retryCount, t, i), i.signal?.aborted ? Promise.resolve(s(i)) : new Promise((l) => {
    const c = () => {
      clearTimeout(h), l(s(i));
    }, h = setTimeout(() => {
      l(s(i)), i.signal?.removeEventListener && i.signal.removeEventListener("abort", c);
    }, a);
    i.signal?.addEventListener && i.signal.addEventListener("abort", c, { once: !0 });
  });
}
async function Zl(s, e) {
  s.retryCount >= s.retries && await s.onMaxRetryTimesExceeded(e, s.retryCount);
}
const Re = (s, e) => {
  const t = s.interceptors.request.use((n) => (Ui(n, e, !0), n[ei]?.validateResponse && (n.validateStatus = () => !1), n)), i = s.interceptors.response.use(null, async (n) => {
    const { config: r } = n;
    if (!r)
      return Promise.reject(n);
    const o = Ui(r, e);
    return n.response && o.validateResponse?.(n.response) ? n.response : await Vl(o, n) ? Xl(s, o, n, r) : (await Zl(o, n), Promise.reject(n));
  });
  return { requestInterceptorId: t, responseInterceptorId: i };
};
Re.isNetworkError = ti;
Re.isSafeRequestError = zl;
Re.isIdempotentRequestError = rr;
Re.isNetworkOrIdempotentRequestError = or;
Re.exponentialDelay = ql;
Re.linearDelay = Hl;
Re.isRetryableError = es;
const bs = 10, Bi = (s = 0) => (e) => `\x1B[${e + s}m`, Ii = (s = 0) => (e) => `\x1B[${38 + s};5;${e}m`, Mi = (s = 0) => (e, t, i) => `\x1B[${38 + s};2;${e};${t};${i}m`, _ = {
  modifier: {
    reset: [0, 0],
    // 21 isn't widely supported and 22 does the same thing
    bold: [1, 22],
    dim: [2, 22],
    italic: [3, 23],
    underline: [4, 24],
    overline: [53, 55],
    inverse: [7, 27],
    hidden: [8, 28],
    strikethrough: [9, 29]
  },
  color: {
    black: [30, 39],
    red: [31, 39],
    green: [32, 39],
    yellow: [33, 39],
    blue: [34, 39],
    magenta: [35, 39],
    cyan: [36, 39],
    white: [37, 39],
    // Bright color
    blackBright: [90, 39],
    gray: [90, 39],
    // Alias of `blackBright`
    grey: [90, 39],
    // Alias of `blackBright`
    redBright: [91, 39],
    greenBright: [92, 39],
    yellowBright: [93, 39],
    blueBright: [94, 39],
    magentaBright: [95, 39],
    cyanBright: [96, 39],
    whiteBright: [97, 39]
  },
  bgColor: {
    bgBlack: [40, 49],
    bgRed: [41, 49],
    bgGreen: [42, 49],
    bgYellow: [43, 49],
    bgBlue: [44, 49],
    bgMagenta: [45, 49],
    bgCyan: [46, 49],
    bgWhite: [47, 49],
    // Bright color
    bgBlackBright: [100, 49],
    bgGray: [100, 49],
    // Alias of `bgBlackBright`
    bgGrey: [100, 49],
    // Alias of `bgBlackBright`
    bgRedBright: [101, 49],
    bgGreenBright: [102, 49],
    bgYellowBright: [103, 49],
    bgBlueBright: [104, 49],
    bgMagentaBright: [105, 49],
    bgCyanBright: [106, 49],
    bgWhiteBright: [107, 49]
  }
};
Object.keys(_.modifier);
const Yl = Object.keys(_.color), Ql = Object.keys(_.bgColor);
[...Yl, ...Ql];
function ec() {
  const s = /* @__PURE__ */ new Map();
  for (const [e, t] of Object.entries(_)) {
    for (const [i, n] of Object.entries(t))
      _[i] = {
        open: `\x1B[${n[0]}m`,
        close: `\x1B[${n[1]}m`
      }, t[i] = _[i], s.set(n[0], n[1]);
    Object.defineProperty(_, e, {
      value: t,
      enumerable: !1
    });
  }
  return Object.defineProperty(_, "codes", {
    value: s,
    enumerable: !1
  }), _.color.close = "\x1B[39m", _.bgColor.close = "\x1B[49m", _.color.ansi = Bi(), _.color.ansi256 = Ii(), _.color.ansi16m = Mi(), _.bgColor.ansi = Bi(bs), _.bgColor.ansi256 = Ii(bs), _.bgColor.ansi16m = Mi(bs), Object.defineProperties(_, {
    rgbToAnsi256: {
      value(e, t, i) {
        return e === t && t === i ? e < 8 ? 16 : e > 248 ? 231 : Math.round((e - 8) / 247 * 24) + 232 : 16 + 36 * Math.round(e / 255 * 5) + 6 * Math.round(t / 255 * 5) + Math.round(i / 255 * 5);
      },
      enumerable: !1
    },
    hexToRgb: {
      value(e) {
        const t = /[a-f\d]{6}|[a-f\d]{3}/i.exec(e.toString(16));
        if (!t)
          return [0, 0, 0];
        let [i] = t;
        i.length === 3 && (i = [...i].map((r) => r + r).join(""));
        const n = Number.parseInt(i, 16);
        return [
          /* eslint-disable no-bitwise */
          n >> 16 & 255,
          n >> 8 & 255,
          n & 255
          /* eslint-enable no-bitwise */
        ];
      },
      enumerable: !1
    },
    hexToAnsi256: {
      value: (e) => _.rgbToAnsi256(..._.hexToRgb(e)),
      enumerable: !1
    },
    ansi256ToAnsi: {
      value(e) {
        if (e < 8)
          return 30 + e;
        if (e < 16)
          return 90 + (e - 8);
        let t, i, n;
        if (e >= 232)
          t = ((e - 232) * 10 + 8) / 255, i = t, n = t;
        else {
          e -= 16;
          const a = e % 36;
          t = Math.floor(e / 36) / 5, i = Math.floor(a / 6) / 5, n = a % 6 / 5;
        }
        const r = Math.max(t, i, n) * 2;
        if (r === 0)
          return 30;
        let o = 30 + (Math.round(n) << 2 | Math.round(i) << 1 | Math.round(t));
        return r === 2 && (o += 60), o;
      },
      enumerable: !1
    },
    rgbToAnsi: {
      value: (e, t, i) => _.ansi256ToAnsi(_.rgbToAnsi256(e, t, i)),
      enumerable: !1
    },
    hexToAnsi: {
      value: (e) => _.ansi256ToAnsi(_.hexToAnsi256(e)),
      enumerable: !1
    }
  }), _;
}
const he = ec(), $i = (() => {
  if (!("navigator" in globalThis))
    return 0;
  if (globalThis.navigator.userAgentData) {
    const s = navigator.userAgentData.brands.find(({ brand: e }) => e === "Chromium");
    if (s && s.version > 93)
      return 3;
  }
  return /\b(Chrome|Chromium)\//.test(globalThis.navigator.userAgent) ? 1 : 0;
})(), zi = $i !== 0 && {
  level: $i
}, tc = {
  stdout: zi,
  stderr: zi
};
function sc(s, e, t) {
  let i = s.indexOf(e);
  if (i === -1)
    return s;
  const n = e.length;
  let r = 0, o = "";
  do
    o += s.slice(r, i) + e + t, r = i + n, i = s.indexOf(e, r);
  while (i !== -1);
  return o += s.slice(r), o;
}
function ic(s, e, t, i) {
  let n = 0, r = "";
  do {
    const o = s[i - 1] === "\r";
    r += s.slice(n, o ? i - 1 : i) + e + (o ? `\r
` : `
`) + t, n = i + 1, i = s.indexOf(`
`, n);
  } while (i !== -1);
  return r += s.slice(n), r;
}
const { stdout: Wi, stderr: qi } = tc, Bs = Symbol("GENERATOR"), $e = Symbol("STYLER"), lt = Symbol("IS_EMPTY"), Hi = [
  "ansi",
  "ansi",
  "ansi256",
  "ansi16m"
], ze = /* @__PURE__ */ Object.create(null), nc = (s, e = {}) => {
  if (e.level && !(Number.isInteger(e.level) && e.level >= 0 && e.level <= 3))
    throw new Error("The `level` option should be an integer from 0 to 3");
  const t = Wi ? Wi.level : 0;
  s.level = e.level === void 0 ? t : e.level;
}, rc = (s) => {
  const e = (...t) => t.join(" ");
  return nc(e, s), Object.setPrototypeOf(e, pt.prototype), e;
};
function pt(s) {
  return rc(s);
}
Object.setPrototypeOf(pt.prototype, Function.prototype);
for (const [s, e] of Object.entries(he))
  ze[s] = {
    get() {
      const t = Mt(this, Ms(e.open, e.close, this[$e]), this[lt]);
      return Object.defineProperty(this, s, { value: t }), t;
    }
  };
ze.visible = {
  get() {
    const s = Mt(this, this[$e], !0);
    return Object.defineProperty(this, "visible", { value: s }), s;
  }
};
const Is = (s, e, t, ...i) => s === "rgb" ? e === "ansi16m" ? he[t].ansi16m(...i) : e === "ansi256" ? he[t].ansi256(he.rgbToAnsi256(...i)) : he[t].ansi(he.rgbToAnsi(...i)) : s === "hex" ? Is("rgb", e, t, ...he.hexToRgb(...i)) : he[t][s](...i), oc = ["rgb", "hex", "ansi256"];
for (const s of oc) {
  ze[s] = {
    get() {
      const { level: t } = this;
      return function(...i) {
        const n = Ms(Is(s, Hi[t], "color", ...i), he.color.close, this[$e]);
        return Mt(this, n, this[lt]);
      };
    }
  };
  const e = "bg" + s[0].toUpperCase() + s.slice(1);
  ze[e] = {
    get() {
      const { level: t } = this;
      return function(...i) {
        const n = Ms(Is(s, Hi[t], "bgColor", ...i), he.bgColor.close, this[$e]);
        return Mt(this, n, this[lt]);
      };
    }
  };
}
const ac = Object.defineProperties(() => {
}, {
  ...ze,
  level: {
    enumerable: !0,
    get() {
      return this[Bs].level;
    },
    set(s) {
      this[Bs].level = s;
    }
  }
}), Ms = (s, e, t) => {
  let i, n;
  return t === void 0 ? (i = s, n = e) : (i = t.openAll + s, n = e + t.closeAll), {
    open: s,
    close: e,
    openAll: i,
    closeAll: n,
    parent: t
  };
}, Mt = (s, e, t) => {
  const i = (...n) => lc(i, n.length === 1 ? "" + n[0] : n.join(" "));
  return Object.setPrototypeOf(i, ac), i[Bs] = s, i[$e] = e, i[lt] = t, i;
}, lc = (s, e) => {
  if (s.level <= 0 || !e)
    return s[lt] ? "" : e;
  let t = s[$e];
  if (t === void 0)
    return e;
  const { openAll: i, closeAll: n } = t;
  if (e.includes("\x1B"))
    for (; t !== void 0; )
      e = sc(e, t.close, t.open), t = t.parent;
  const r = e.indexOf(`
`);
  return r !== -1 && (e = ic(e, n, i, r)), i + e + n;
};
Object.defineProperties(pt.prototype, ze);
const me = pt();
pt({ level: qi ? qi.level : 0 });
class He extends Error {
  code;
  constructor({ code: e, message: t }) {
    super(t), this.code = e, this.name = "BaseError";
  }
  toString() {
    return `${me.bold.redBright(this.name + ":")}${ge}${me.cyan(`[${this.code}]`)} ${me.yellow(`${this.message}`)}`;
  }
}
class cc extends He {
  details;
  request;
  constructor({ code: e, message: t, details: i, request: n }) {
    super({
      code: e ?? "SDK-9001",
      message: t ?? "An error occurred while making the request. Please verify and try again."
    }), this.details = i, this.request = n, this.name = "RequestError";
  }
  toString() {
    return `${me.bold.redBright(this.name + ":")}${ge}${me.cyan(`[${this.code}]`)} ${me.yellow(`${this.message}${ge}`)}Details: ${this.details}`;
  }
}
class we extends He {
  status;
  body;
  constructor({ code: e, message: t, status: i, body: n }) {
    super({
      code: e ?? "SDK-9002",
      message: t ?? "Bad response received from server. Please verify and try again."
    }), this.status = i, this.body = n, this.name = "ResponseError";
  }
  toString() {
    return `${me.bold.redBright(this.name + ":")}${ge}${me.cyan(`[${this.code}](${this.status})`)} ${me.yellow(`${this.message}${ge}`)}Body: ${JSON.stringify(this.body, null, 2)}`;
  }
}
class hc extends we {
  constructor(e) {
    super({
      code: "SDK::0007",
      message: "Asset not found. Please verify the asset ID and try again.",
      status: 404,
      body: e
    }), this.name = "AssetNotFoundError";
  }
}
class uc extends we {
  constructor(e) {
    super({
      code: "SDK::0001",
      message: "Token is invalid or not present. Please verify and try again.",
      status: 401,
      body: e
    }), this.name = "AuthenticationError";
  }
}
class ar extends He {
  constructor(e) {
    super({
      code: "SDK::0008",
      message: `File extension not allowed. Allowed extensions: ${e}.`
    }), this.name = "FileExtensionNotAllowedError";
  }
}
class Ee extends He {
  constructor(e) {
    super({
      code: "SDK::0009",
      message: `Folder not allowed. ${e}.`
    }), this.name = "FolderNotAllowedError";
  }
}
class fc extends He {
  constructor() {
    super({
      code: "SDK::0010",
      message: 'The filename contains invalid characters. Only letters, numbers, ".", "_" and "-" are allowed.'
    }), this.name = "InvalidFilenameError";
  }
}
class dc extends we {
  constructor(e) {
    super({
      code: "SDK::0004",
      message: "Resource not found. Please verify and try again.",
      status: 404,
      body: e
    }), this.name = "NotFoundError";
  }
}
class pc extends we {
  constructor(e) {
    super({
      code: "SDK::0005",
      message: "Server error. Please try again.",
      status: 500,
      body: e
    }), this.name = "ServerError";
  }
}
class mc extends we {
  constructor(e) {
    super({
      code: "SDK::0006",
      message: "Theme not found. Please verify the theme ID and try again.",
      status: 404,
      body: e
    }), this.name = "ThemeNotFoundError";
  }
}
class gc extends we {
  constructor(e) {
    super({
      code: "SDK::0003",
      message: "Request timed out. Please try again.",
      status: 408,
      body: e
    }), this.name = "TimeoutError";
  }
}
class wc extends He {
  constructor(e) {
    super({
      code: "SDK::9999",
      message: e ?? "Unable to process request. Please try again in few moments."
    }), this.name = "UnknownError";
  }
}
class bc extends we {
  constructor(e) {
    super({
      code: "SDK::0002",
      message: "Provided data was invalid or contain errors. Please verify and try again.",
      status: 422,
      body: e
    }), this.name = "ValidationError";
  }
}
class yc {
  axios;
  constructor(e, t) {
    this.axios = D.create(e), this.setupAxiosRetry(), this.setInterceptors(t);
  }
  setupAxiosRetry() {
    Re(this.axios, {
      retries: 5,
      retryCondition(e) {
        return ti(e) || es(e);
      }
    });
  }
  setInterceptors(e) {
    this.axios.interceptors.request.clear(), this.axios.interceptors.request.use((t) => (t = e(t), t)), this.axios.interceptors.response.use(
      (t) => t,
      (t) => t.response ? Promise.reject(
        new we({
          status: t.response.status,
          body: t.response.data
        })
      ) : t.request ? Promise.reject(
        new cc({
          details: t.cause?.message ?? t.message,
          request: t.request
        })
      ) : Promise.reject(t)
    );
  }
  async get(e, t) {
    return this.axios.get(e, {
      params: t?.params,
      headers: t?.headers
    }).then((i) => i.data);
  }
  async post(e, t, i) {
    return this.axios.post(e, t, {
      params: i?.params,
      headers: i?.headers
    }).then((n) => n.data ? n.data : void 0);
  }
  async put(e, t, i) {
    return this.axios.put(e, t, {
      params: i?.params,
      headers: i?.headers
    }).then((n) => n.data ? n.data : void 0);
  }
  async delete(e, t) {
    return this.axios.delete(e, {
      params: t?.params,
      headers: t?.headers
    }).then((i) => i.data ? i.data : void 0);
  }
}
function Ec(s) {
  return s.replace(/[^a-zA-Z0-9]+(.)/g, (e, t) => t.toUpperCase()).replace(/^[A-Z]/, (e) => e.toLowerCase());
}
function Sc(s) {
  return new Promise((e, t) => {
    /^[A-Za-z0-9._-]+$/.test(s) ? e(!0) : t(new fc());
  });
}
function Rc(s) {
  const e = [
    /* Fonts extensions */
    ".ttf",
    ".otf",
    ".eot",
    ".woff",
    ".woff2",
    /* Imagens extensions */
    ".jpg",
    ".jpeg",
    ".gif",
    ".png",
    ".svg",
    /* Styles extensions */
    ".css",
    ".scss",
    /* Other extensions */
    ".html",
    ".js",
    ".json"
  ];
  return new Promise((t, i) => {
    e.includes(s.toLowerCase()) ? t(!0) : i(new ar(e.join(", ")));
  });
}
function Tc(s) {
  const e = ["configs", "css", "elements", "img", "js", "layouts", "pages"];
  if (!s || s === "")
    return Promise.resolve(!0);
  const t = s.replace(/\\/g, "/"), i = (t.startsWith("/") ? t.substring(1) : t).split("/")[0];
  return new Promise((n, r) => {
    e.includes(i) ? n(!0) : r(new Ee(e.join(", ")));
  });
}
function Oc(s) {
  const e = ["pages", "elements", "css", "img", "js", "configs"];
  if (!s || s === "")
    return Promise.resolve(!0);
  const t = s.replace(/\\/g, "/"), i = (t.startsWith("/") ? t.substring(1) : t).split("/"), n = i[0];
  return new Promise((r, o) => {
    if (i.length > 1 && !e.includes(n))
      return o(
        new Ee(
          `${Ec(n)} does not allow subfolders. Allowed with subfolders: ${e.join(", ")}`
        )
      );
    if (n === "pages" && (i.length > 2 || i.length === 2 && i[1] !== "lp"))
      return o(
        new Ee("Pages only allows the lp subfolder with no additional levels")
      );
    if (n === "elements" && (i.length > 2 || i.length === 2 && i[1] !== "snippets"))
      return o(
        new Ee(
          "Elements only allows the snippets subfolder with no additional levels"
        )
      );
    if (n === "configs") {
      if (i.length > 3)
        return o(
          new Ee(
            "Configs only allows up to 2 levels of subfolders (editor/sections)"
          )
        );
      if (i.length >= 2 && i[1] !== "editor")
        return o(
          new Ee("Configs only allows the editor subfolder")
        );
      if (i.length === 3 && i[2] !== "sections")
        return o(
          new Ee("Configs/editor only allows the sections subfolder")
        );
    }
    r(!0);
  });
}
function xc(s, e, t) {
  if (!t || t === "")
    return Promise.resolve(!0);
  const i = t.replace(/\\/g, "/"), n = (i.startsWith("/") ? i.substring(1) : i).split("/");
  return new Promise((r, o) => {
    if (n[0] === "configs" && n.length >= 2 && n[1] === "editor") {
      if (e.toLowerCase() !== ".json")
        return o(
          new ar(".json")
        );
      if (n.length === 2 && s !== "global")
        return o(
          new Ee("Only global.json is allowed in configs/editor directory")
        );
    }
    r(!0);
  });
}
function Gi(s) {
  const { name: e, ext: t, dir: i } = Go(s);
  return Sc(e).then(() => Rc(t)).then(() => Tc(i)).then(() => Oc(i)).then(() => xc(e, t, i));
}
class ii {
  adapter;
  themeId;
  debug;
  debugFilePath = "./.debug.sdk.log";
  static create(e) {
    return new ii(e);
  }
  constructor({ token: e, themeId: t, debug: i = !1 }) {
    this.themeId = t, this.debug = i, this.adapter = new yc(
      {
        baseURL: "https://app.studio.tray.net.br/api/cli/v1",
        headers: {
          "User-Agent": "@tray-tecnologia/theme-sdk@2.0.1",
          "Content-Type": "application/json",
          Accept: "application/json"
        }
      },
      (n) => (n.headers.Authorization = `Bearer ${e}`, n)
    );
  }
  /**
   * Adds operation info log to file with debug enabled
   * @param {string} type Type of message. Allowed: Emergency, Alert, Critical, Error, Warning, Notice Info or Debug
   * @param {string} operation Operation where debug info was generated
   * @param {Object|string} data Data from operation
   * @private
   */
  generateDebugFile({ type: e, operation: t, data: i }) {
    if (this.debug) {
      const n = (/* @__PURE__ */ new Date()).toLocaleString("pt-br"), r = typeof i == "object" && i !== null ? JSON.stringify(i) : i, o = `[${n}] Type: ${e} | Operation: ${t} | Data: ${r}${ge}`;
      Vo(this.debugFilePath, o).catch(() => !1);
    }
  }
  /**
   * Get all themes
   * @returns Promise ThemeInstall.
   */
  async getThemes() {
    return this.adapter.get("/theme-installs").then((e) => (this.generateDebugFile({
      type: "Info",
      operation: "getThemes",
      data: e
    }), e)).catch((e) => {
      this.handleErrors("getThemes", e);
    });
  }
  /**
   * Get a theme by id
   * @returns Promise ThemeInstall.
   */
  async getTheme() {
    return this.adapter.get(`/theme-installs/${this.themeId}`).then((e) => (this.generateDebugFile({
      type: "Info",
      operation: "getTheme",
      data: e
    }), e)).catch((e) => {
      this.handleErrors("getTheme", e);
    });
  }
  /**
   * Create a clean theme
   * @returns Promise ThemeInstall.
   */
  async createCleanTheme() {
    return this.adapter.post("/theme-installs/create-clean").then((e) => (this.generateDebugFile({
      type: "Info",
      operation: "createCleanTheme",
      data: e
    }), e)).catch((e) => {
      this.handleErrors("createCleanTheme", e);
    });
  }
  /**
   * Delete a theme
   * @returns Promise GeneralResponse.
   */
  async deleteTheme(e) {
    return this.adapter.delete(`/theme-installs/${e}`).then((t) => (this.generateDebugFile({
      type: "Info",
      operation: "deleteTheme",
      data: t
    }), t)).catch((t) => {
      this.handleErrors("deleteTheme", t);
    });
  }
  /**
   * Get all assets from a theme
   * @returns Promise ThemeInstallAsset.
   */
  async getThemeAssets() {
    return this.adapter.get(`/theme-installs/${this.themeId}/assets`).then((e) => (this.generateDebugFile({
      type: "Info",
      operation: "getThemeAssets",
      data: e
    }), e)).catch((e) => {
      this.handleErrors("getThemeAssets", e);
    });
  }
  /**
   * Get an asset by id
   * @param id Asset id
   * @returns Promise ThemeInstallAsset.
   */
  async getThemeAsset(e) {
    return this.adapter.get(`/theme-installs/${this.themeId}/assets/${e}`).then((t) => (this.generateDebugFile({
      type: "Info",
      operation: "getThemeAsset",
      data: t
    }), t)).catch((t) => {
      this.handleErrors("getThemeAsset", t);
    });
  }
  /**
   * Delete an asset by id
   * @param id Asset id
   * @returns Promise GeneralResponse.
   */
  async deleteThemeAsset(e) {
    return this.adapter.delete(`/theme-installs/${this.themeId}/assets/${e}`).then((t) => (this.generateDebugFile({
      type: "Info",
      operation: "deleteThemeAsset",
      data: t
    }), t)).catch((t) => {
      this.handleErrors("deleteThemeAsset", t);
    });
  }
  /**
   * Create a theme asset
   * @param path Asset path
   * @param content Asset content
   * @returns Promise ThemeInstallAsset.
   */
  async createThemeAsset(e, t) {
    return Gi(e).then(async () => this.adapter.post(`/theme-installs/${this.themeId}/assets`, {
      path: e,
      content: t
    }).then((i) => (this.generateDebugFile({
      type: "Info",
      operation: "createThemeAsset",
      data: i
    }), i)).catch((i) => {
      this.handleErrors("createThemeAsset", i);
    }));
  }
  /**
   * Update a theme asset
   * @param id Asset id
   * @param content Asset content
   * @returns Promise ThemeInstallAsset.
   */
  async updateThemeAsset(e, t, i) {
    return Gi(e).then(async () => this.adapter.put(`/theme-installs/${this.themeId}/assets/${t}`, {
      id: t,
      content: i
    }).then((n) => (this.generateDebugFile({
      type: "Info",
      operation: "updateThemeAsset",
      data: n
    }), n)).catch((n) => {
      this.handleErrors("updateThemeAsset", n);
    }));
  }
  /**
   * Handle errors
   * @param operation Operation where error was generated
   * @param error Error object
   */
  handleErrors(e, t) {
    if (this.generateDebugFile({
      type: "Error",
      operation: e,
      data: t.body
    }), t instanceof we)
      switch (t.status) {
        case 401:
          throw new uc(t.body);
        case 404:
          throw ["deleteTheme", "getTheme"].includes(e) ? new mc(t.body) : ["deleteThemeAsset", "getThemeAsset", "updateThemeAsset"].includes(e) ? new hc(t.body) : new dc(t.body);
        case 422:
          throw new bc(t.body);
        case 504:
          throw new gc(t.body);
        default:
          if (t.status >= 500)
            throw new pc(t.body);
      }
    throw new wc(t.body?.data?.message);
  }
}
const lr = (s, e, t) => {
  const i = s instanceof RegExp ? Ji(s, t) : s, n = e instanceof RegExp ? Ji(e, t) : e, r = i !== null && n != null && Ac(i, n, t);
  return r && {
    start: r[0],
    end: r[1],
    pre: t.slice(0, r[0]),
    body: t.slice(r[0] + i.length, r[1]),
    post: t.slice(r[1] + n.length)
  };
}, Ji = (s, e) => {
  const t = e.match(s);
  return t ? t[0] : null;
}, Ac = (s, e, t) => {
  let i, n, r, o, a, l = t.indexOf(s), c = t.indexOf(e, l + 1), h = l;
  if (l >= 0 && c > 0) {
    if (s === e)
      return [l, c];
    for (i = [], r = t.length; h >= 0 && !a; ) {
      if (h === l)
        i.push(h), l = t.indexOf(s, h + 1);
      else if (i.length === 1) {
        const u = i.pop();
        u !== void 0 && (a = [u, c]);
      } else
        n = i.pop(), n !== void 0 && n < r && (r = n, o = c), c = t.indexOf(e, h + 1);
      h = l < c && l >= 0 ? l : c;
    }
    i.length && o !== void 0 && (a = [r, o]);
  }
  return a;
}, cr = "\0SLASH" + Math.random() + "\0", hr = "\0OPEN" + Math.random() + "\0", ni = "\0CLOSE" + Math.random() + "\0", ur = "\0COMMA" + Math.random() + "\0", fr = "\0PERIOD" + Math.random() + "\0", Cc = new RegExp(cr, "g"), vc = new RegExp(hr, "g"), Nc = new RegExp(ni, "g"), _c = new RegExp(ur, "g"), Pc = new RegExp(fr, "g"), Fc = /\\\\/g, Dc = /\\{/g, kc = /\\}/g, Lc = /\\,/g, jc = /\\./g;
function ys(s) {
  return isNaN(s) ? s.charCodeAt(0) : parseInt(s, 10);
}
function Uc(s) {
  return s.replace(Fc, cr).replace(Dc, hr).replace(kc, ni).replace(Lc, ur).replace(jc, fr);
}
function Bc(s) {
  return s.replace(Cc, "\\").replace(vc, "{").replace(Nc, "}").replace(_c, ",").replace(Pc, ".");
}
function dr(s) {
  if (!s)
    return [""];
  const e = [], t = lr("{", "}", s);
  if (!t)
    return s.split(",");
  const { pre: i, body: n, post: r } = t, o = i.split(",");
  o[o.length - 1] += "{" + n + "}";
  const a = dr(r);
  return r.length && (o[o.length - 1] += a.shift(), o.push.apply(o, a)), e.push.apply(e, o), e;
}
function Ic(s) {
  return s ? (s.slice(0, 2) === "{}" && (s = "\\{\\}" + s.slice(2)), it(Uc(s), !0).map(Bc)) : [];
}
function Mc(s) {
  return "{" + s + "}";
}
function $c(s) {
  return /^-?0\d/.test(s);
}
function zc(s, e) {
  return s <= e;
}
function Wc(s, e) {
  return s >= e;
}
function it(s, e) {
  const t = [], i = lr("{", "}", s);
  if (!i)
    return [s];
  const n = i.pre, r = i.post.length ? it(i.post, !1) : [""];
  if (/\$$/.test(i.pre))
    for (let o = 0; o < r.length; o++) {
      const a = n + "{" + i.body + "}" + r[o];
      t.push(a);
    }
  else {
    const o = /^-?\d+\.\.-?\d+(?:\.\.-?\d+)?$/.test(i.body), a = /^[a-zA-Z]\.\.[a-zA-Z](?:\.\.-?\d+)?$/.test(i.body), l = o || a, c = i.body.indexOf(",") >= 0;
    if (!l && !c)
      return i.post.match(/,(?!,).*\}/) ? (s = i.pre + "{" + i.body + ni + i.post, it(s)) : [s];
    let h;
    if (l)
      h = i.body.split(/\.\./);
    else if (h = dr(i.body), h.length === 1 && h[0] !== void 0 && (h = it(h[0], !1).map(Mc), h.length === 1))
      return r.map((p) => i.pre + h[0] + p);
    let u;
    if (l && h[0] !== void 0 && h[1] !== void 0) {
      const p = ys(h[0]), d = ys(h[1]), m = Math.max(h[0].length, h[1].length);
      let f = h.length === 3 && h[2] !== void 0 ? Math.abs(ys(h[2])) : 1, b = zc;
      d < p && (f *= -1, b = Wc);
      const E = h.some($c);
      u = [];
      for (let S = p; b(S, d); S += f) {
        let T;
        if (a)
          T = String.fromCharCode(S), T === "\\" && (T = "");
        else if (T = String(S), E) {
          const O = m - T.length;
          if (O > 0) {
            const v = new Array(O + 1).join("0");
            S < 0 ? T = "-" + v + T.slice(1) : T = v + T;
          }
        }
        u.push(T);
      }
    } else {
      u = [];
      for (let p = 0; p < h.length; p++)
        u.push.apply(u, it(h[p], !1));
    }
    for (let p = 0; p < u.length; p++)
      for (let d = 0; d < r.length; d++) {
        const m = n + u[p] + r[d];
        (!e || l || m) && t.push(m);
      }
  }
  return t;
}
const qc = 1024 * 64, $t = (s) => {
  if (typeof s != "string")
    throw new TypeError("invalid pattern");
  if (s.length > qc)
    throw new TypeError("pattern is too long");
}, Hc = {
  "[:alnum:]": ["\\p{L}\\p{Nl}\\p{Nd}", !0],
  "[:alpha:]": ["\\p{L}\\p{Nl}", !0],
  "[:ascii:]": ["\\x00-\\x7f", !1],
  "[:blank:]": ["\\p{Zs}\\t", !0],
  "[:cntrl:]": ["\\p{Cc}", !0],
  "[:digit:]": ["\\p{Nd}", !0],
  "[:graph:]": ["\\p{Z}\\p{C}", !0, !0],
  "[:lower:]": ["\\p{Ll}", !0],
  "[:print:]": ["\\p{C}", !0],
  "[:punct:]": ["\\p{P}", !0],
  "[:space:]": ["\\p{Z}\\t\\r\\n\\v\\f", !0],
  "[:upper:]": ["\\p{Lu}", !0],
  "[:word:]": ["\\p{L}\\p{Nl}\\p{Nd}\\p{Pc}", !0],
  "[:xdigit:]": ["A-Fa-f0-9", !1]
}, Ve = (s) => s.replace(/[[\]\\-]/g, "\\$&"), Gc = (s) => s.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), Ki = (s) => s.join(""), Jc = (s, e) => {
  const t = e;
  if (s.charAt(t) !== "[")
    throw new Error("not in a brace expression");
  const i = [], n = [];
  let r = t + 1, o = !1, a = !1, l = !1, c = !1, h = t, u = "";
  e: for (; r < s.length; ) {
    const f = s.charAt(r);
    if ((f === "!" || f === "^") && r === t + 1) {
      c = !0, r++;
      continue;
    }
    if (f === "]" && o && !l) {
      h = r + 1;
      break;
    }
    if (o = !0, f === "\\" && !l) {
      l = !0, r++;
      continue;
    }
    if (f === "[" && !l) {
      for (const [b, [y, E, S]] of Object.entries(Hc))
        if (s.startsWith(b, r)) {
          if (u)
            return ["$.", !1, s.length - t, !0];
          r += b.length, S ? n.push(y) : i.push(y), a = a || E;
          continue e;
        }
    }
    if (l = !1, u) {
      f > u ? i.push(Ve(u) + "-" + Ve(f)) : f === u && i.push(Ve(f)), u = "", r++;
      continue;
    }
    if (s.startsWith("-]", r + 1)) {
      i.push(Ve(f + "-")), r += 2;
      continue;
    }
    if (s.startsWith("-", r + 1)) {
      u = f, r += 2;
      continue;
    }
    i.push(Ve(f)), r++;
  }
  if (h < r)
    return ["", !1, 0, !1];
  if (!i.length && !n.length)
    return ["$.", !1, s.length - t, !0];
  if (n.length === 0 && i.length === 1 && /^\\?.$/.test(i[0]) && !c) {
    const f = i[0].length === 2 ? i[0].slice(-1) : i[0];
    return [Gc(f), !1, h - t, !1];
  }
  const p = "[" + (c ? "^" : "") + Ki(i) + "]", d = "[" + (c ? "" : "^") + Ki(n) + "]";
  return [i.length && n.length ? "(" + p + "|" + d + ")" : i.length ? p : d, a, h - t, !0];
}, Be = (s, { windowsPathsNoEscape: e = !1 } = {}) => e ? s.replace(/\[([^\/\\])\]/g, "$1") : s.replace(/((?!\\).|^)\[([^\/\\])\]/g, "$1$2").replace(/\\([^\/])/g, "$1"), Kc = /* @__PURE__ */ new Set(["!", "?", "+", "*", "@"]), Vi = (s) => Kc.has(s), Vc = "(?!(?:^|/)\\.\\.?(?:$|/))", St = "(?!\\.)", Xc = /* @__PURE__ */ new Set(["[", "."]), Zc = /* @__PURE__ */ new Set(["..", "."]), Yc = new Set("().*{}+?[]^$\\!"), Qc = (s) => s.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&"), ri = "[^/]", Xi = ri + "*?", Zi = ri + "+?";
class q {
  type;
  #e;
  #t;
  #s = !1;
  #r = [];
  #o;
  #E;
  #w;
  #a = !1;
  #u;
  #l;
  // set to true if it's an extglob with no children
  // (which really means one child of '')
  #c = !1;
  constructor(e, t, i = {}) {
    this.type = e, e && (this.#t = !0), this.#o = t, this.#e = this.#o ? this.#o.#e : this, this.#u = this.#e === this ? i : this.#e.#u, this.#w = this.#e === this ? [] : this.#e.#w, e === "!" && !this.#e.#a && this.#w.push(this), this.#E = this.#o ? this.#o.#r.length : 0;
  }
  get hasMagic() {
    if (this.#t !== void 0)
      return this.#t;
    for (const e of this.#r)
      if (typeof e != "string" && (e.type || e.hasMagic))
        return this.#t = !0;
    return this.#t;
  }
  // reconstructs the pattern
  toString() {
    return this.#l !== void 0 ? this.#l : this.type ? this.#l = this.type + "(" + this.#r.map((e) => String(e)).join("|") + ")" : this.#l = this.#r.map((e) => String(e)).join("");
  }
  #i() {
    if (this !== this.#e)
      throw new Error("should only call on root");
    if (this.#a)
      return this;
    this.toString(), this.#a = !0;
    let e;
    for (; e = this.#w.pop(); ) {
      if (e.type !== "!")
        continue;
      let t = e, i = t.#o;
      for (; i; ) {
        for (let n = t.#E + 1; !i.type && n < i.#r.length; n++)
          for (const r of e.#r) {
            if (typeof r == "string")
              throw new Error("string part in extglob AST??");
            r.copyIn(i.#r[n]);
          }
        t = i, i = t.#o;
      }
    }
    return this;
  }
  push(...e) {
    for (const t of e)
      if (t !== "") {
        if (typeof t != "string" && !(t instanceof q && t.#o === this))
          throw new Error("invalid part: " + t);
        this.#r.push(t);
      }
  }
  toJSON() {
    const e = this.type === null ? this.#r.slice().map((t) => typeof t == "string" ? t : t.toJSON()) : [this.type, ...this.#r.map((t) => t.toJSON())];
    return this.isStart() && !this.type && e.unshift([]), this.isEnd() && (this === this.#e || this.#e.#a && this.#o?.type === "!") && e.push({}), e;
  }
  isStart() {
    if (this.#e === this)
      return !0;
    if (!this.#o?.isStart())
      return !1;
    if (this.#E === 0)
      return !0;
    const e = this.#o;
    for (let t = 0; t < this.#E; t++) {
      const i = e.#r[t];
      if (!(i instanceof q && i.type === "!"))
        return !1;
    }
    return !0;
  }
  isEnd() {
    if (this.#e === this || this.#o?.type === "!")
      return !0;
    if (!this.#o?.isEnd())
      return !1;
    if (!this.type)
      return this.#o?.isEnd();
    const e = this.#o ? this.#o.#r.length : 0;
    return this.#E === e - 1;
  }
  copyIn(e) {
    typeof e == "string" ? this.push(e) : this.push(e.clone(this));
  }
  clone(e) {
    const t = new q(this.type, e);
    for (const i of this.#r)
      t.copyIn(i);
    return t;
  }
  static #f(e, t, i, n) {
    let r = !1, o = !1, a = -1, l = !1;
    if (t.type === null) {
      let d = i, m = "";
      for (; d < e.length; ) {
        const f = e.charAt(d++);
        if (r || f === "\\") {
          r = !r, m += f;
          continue;
        }
        if (o) {
          d === a + 1 ? (f === "^" || f === "!") && (l = !0) : f === "]" && !(d === a + 2 && l) && (o = !1), m += f;
          continue;
        } else if (f === "[") {
          o = !0, a = d, l = !1, m += f;
          continue;
        }
        if (!n.noext && Vi(f) && e.charAt(d) === "(") {
          t.push(m), m = "";
          const b = new q(f, t);
          d = q.#f(e, b, d, n), t.push(b);
          continue;
        }
        m += f;
      }
      return t.push(m), d;
    }
    let c = i + 1, h = new q(null, t);
    const u = [];
    let p = "";
    for (; c < e.length; ) {
      const d = e.charAt(c++);
      if (r || d === "\\") {
        r = !r, p += d;
        continue;
      }
      if (o) {
        c === a + 1 ? (d === "^" || d === "!") && (l = !0) : d === "]" && !(c === a + 2 && l) && (o = !1), p += d;
        continue;
      } else if (d === "[") {
        o = !0, a = c, l = !1, p += d;
        continue;
      }
      if (Vi(d) && e.charAt(c) === "(") {
        h.push(p), p = "";
        const m = new q(d, h);
        h.push(m), c = q.#f(e, m, c, n);
        continue;
      }
      if (d === "|") {
        h.push(p), p = "", u.push(h), h = new q(null, t);
        continue;
      }
      if (d === ")")
        return p === "" && t.#r.length === 0 && (t.#c = !0), h.push(p), p = "", t.push(...u, h), c;
      p += d;
    }
    return t.type = null, t.#t = void 0, t.#r = [e.substring(i - 1)], c;
  }
  static fromGlob(e, t = {}) {
    const i = new q(null, void 0, t);
    return q.#f(e, i, 0, t), i;
  }
  // returns the regular expression if there's magic, or the unescaped
  // string if not.
  toMMPattern() {
    if (this !== this.#e)
      return this.#e.toMMPattern();
    const e = this.toString(), [t, i, n, r] = this.toRegExpSource();
    if (!(n || this.#t || this.#u.nocase && !this.#u.nocaseMagicOnly && e.toUpperCase() !== e.toLowerCase()))
      return i;
    const a = (this.#u.nocase ? "i" : "") + (r ? "u" : "");
    return Object.assign(new RegExp(`^${t}$`, a), {
      _src: t,
      _glob: e
    });
  }
  get options() {
    return this.#u;
  }
  // returns the string match, the regexp source, whether there's magic
  // in the regexp (so a regular expression is required) and whether or
  // not the uflag is needed for the regular expression (for posix classes)
  // TODO: instead of injecting the start/end at this point, just return
  // the BODY of the regexp, along with the start/end portions suitable
  // for binding the start/end in either a joined full-path makeRe context
  // (where we bind to (^|/), or a standalone matchPart context (where
  // we bind to ^, and not /).  Otherwise slashes get duped!
  //
  // In part-matching mode, the start is:
  // - if not isStart: nothing
  // - if traversal possible, but not allowed: ^(?!\.\.?$)
  // - if dots allowed or not possible: ^
  // - if dots possible and not allowed: ^(?!\.)
  // end is:
  // - if not isEnd(): nothing
  // - else: $
  //
  // In full-path matching mode, we put the slash at the START of the
  // pattern, so start is:
  // - if first pattern: same as part-matching mode
  // - if not isStart(): nothing
  // - if traversal possible, but not allowed: /(?!\.\.?(?:$|/))
  // - if dots allowed or not possible: /
  // - if dots possible and not allowed: /(?!\.)
  // end is:
  // - if last pattern, same as part-matching mode
  // - else nothing
  //
  // Always put the (?:$|/) on negated tails, though, because that has to be
  // there to bind the end of the negated pattern portion, and it's easier to
  // just stick it in now rather than try to inject it later in the middle of
  // the pattern.
  //
  // We can just always return the same end, and leave it up to the caller
  // to know whether it's going to be used joined or in parts.
  // And, if the start is adjusted slightly, can do the same there:
  // - if not isStart: nothing
  // - if traversal possible, but not allowed: (?:/|^)(?!\.\.?$)
  // - if dots allowed or not possible: (?:/|^)
  // - if dots possible and not allowed: (?:/|^)(?!\.)
  //
  // But it's better to have a simpler binding without a conditional, for
  // performance, so probably better to return both start options.
  //
  // Then the caller just ignores the end if it's not the first pattern,
  // and the start always gets applied.
  //
  // But that's always going to be $ if it's the ending pattern, or nothing,
  // so the caller can just attach $ at the end of the pattern when building.
  //
  // So the todo is:
  // - better detect what kind of start is needed
  // - return both flavors of starting pattern
  // - attach $ at the end of the pattern when creating the actual RegExp
  //
  // Ah, but wait, no, that all only applies to the root when the first pattern
  // is not an extglob. If the first pattern IS an extglob, then we need all
  // that dot prevention biz to live in the extglob portions, because eg
  // +(*|.x*) can match .xy but not .yx.
  //
  // So, return the two flavors if it's #root and the first child is not an
  // AST, otherwise leave it to the child AST to handle it, and there,
  // use the (?:^|/) style of start binding.
  //
  // Even simplified further:
  // - Since the start for a join is eg /(?!\.) and the start for a part
  // is ^(?!\.), we can just prepend (?!\.) to the pattern (either root
  // or start or whatever) and prepend ^ or / at the Regexp construction.
  toRegExpSource(e) {
    const t = e ?? !!this.#u.dot;
    if (this.#e === this && this.#i(), !this.type) {
      const l = this.isStart() && this.isEnd(), c = this.#r.map((d) => {
        const [m, f, b, y] = typeof d == "string" ? q.#p(d, this.#t, l) : d.toRegExpSource(e);
        return this.#t = this.#t || b, this.#s = this.#s || y, m;
      }).join("");
      let h = "";
      if (this.isStart() && typeof this.#r[0] == "string" && !(this.#r.length === 1 && Zc.has(this.#r[0]))) {
        const m = Xc, f = (
          // dots are allowed, and the pattern starts with [ or .
          t && m.has(c.charAt(0)) || // the pattern starts with \., and then [ or .
          c.startsWith("\\.") && m.has(c.charAt(2)) || // the pattern starts with \.\., and then [ or .
          c.startsWith("\\.\\.") && m.has(c.charAt(4))
        ), b = !t && !e && m.has(c.charAt(0));
        h = f ? Vc : b ? St : "";
      }
      let u = "";
      return this.isEnd() && this.#e.#a && this.#o?.type === "!" && (u = "(?:$|\\/)"), [
        h + c + u,
        Be(c),
        this.#t = !!this.#t,
        this.#s
      ];
    }
    const i = this.type === "*" || this.type === "+", n = this.type === "!" ? "(?:(?!(?:" : "(?:";
    let r = this.#b(t);
    if (this.isStart() && this.isEnd() && !r && this.type !== "!") {
      const l = this.toString();
      return this.#r = [l], this.type = null, this.#t = void 0, [l, Be(this.toString()), !1, !1];
    }
    let o = !i || e || t || !St ? "" : this.#b(!0);
    o === r && (o = ""), o && (r = `(?:${r})(?:${o})*?`);
    let a = "";
    if (this.type === "!" && this.#c)
      a = (this.isStart() && !t ? St : "") + Zi;
    else {
      const l = this.type === "!" ? (
        // !() must match something,but !(x) can match ''
        "))" + (this.isStart() && !t && !e ? St : "") + Xi + ")"
      ) : this.type === "@" ? ")" : this.type === "?" ? ")?" : this.type === "+" && o ? ")" : this.type === "*" && o ? ")?" : `)${this.type}`;
      a = n + r + l;
    }
    return [
      a,
      Be(r),
      this.#t = !!this.#t,
      this.#s
    ];
  }
  #b(e) {
    return this.#r.map((t) => {
      if (typeof t == "string")
        throw new Error("string type in extglob ast??");
      const [i, n, r, o] = t.toRegExpSource(e);
      return this.#s = this.#s || o, i;
    }).filter((t) => !(this.isStart() && this.isEnd()) || !!t).join("|");
  }
  static #p(e, t, i = !1) {
    let n = !1, r = "", o = !1;
    for (let a = 0; a < e.length; a++) {
      const l = e.charAt(a);
      if (n) {
        n = !1, r += (Yc.has(l) ? "\\" : "") + l;
        continue;
      }
      if (l === "\\") {
        a === e.length - 1 ? r += "\\\\" : n = !0;
        continue;
      }
      if (l === "[") {
        const [c, h, u, p] = Jc(e, a);
        if (u) {
          r += c, o = o || h, a += u - 1, t = t || p;
          continue;
        }
      }
      if (l === "*") {
        i && e === "*" ? r += Zi : r += Xi, t = !0;
        continue;
      }
      if (l === "?") {
        r += ri, t = !0;
        continue;
      }
      r += Qc(l);
    }
    return [r, Be(e), !!t, o];
  }
}
const pr = (s, { windowsPathsNoEscape: e = !1 } = {}) => e ? s.replace(/[?*()[\]]/g, "[$&]") : s.replace(/[?*()[\]\\]/g, "\\$&"), G = (s, e, t = {}) => ($t(e), !t.nocomment && e.charAt(0) === "#" ? !1 : new Se(e, t).match(s)), eh = /^\*+([^+@!?\*\[\(]*)$/, th = (s) => (e) => !e.startsWith(".") && e.endsWith(s), sh = (s) => (e) => e.endsWith(s), ih = (s) => (s = s.toLowerCase(), (e) => !e.startsWith(".") && e.toLowerCase().endsWith(s)), nh = (s) => (s = s.toLowerCase(), (e) => e.toLowerCase().endsWith(s)), rh = /^\*+\.\*+$/, oh = (s) => !s.startsWith(".") && s.includes("."), ah = (s) => s !== "." && s !== ".." && s.includes("."), lh = /^\.\*+$/, ch = (s) => s !== "." && s !== ".." && s.startsWith("."), hh = /^\*+$/, uh = (s) => s.length !== 0 && !s.startsWith("."), fh = (s) => s.length !== 0 && s !== "." && s !== "..", dh = /^\?+([^+@!?\*\[\(]*)?$/, ph = ([s, e = ""]) => {
  const t = mr([s]);
  return e ? (e = e.toLowerCase(), (i) => t(i) && i.toLowerCase().endsWith(e)) : t;
}, mh = ([s, e = ""]) => {
  const t = gr([s]);
  return e ? (e = e.toLowerCase(), (i) => t(i) && i.toLowerCase().endsWith(e)) : t;
}, gh = ([s, e = ""]) => {
  const t = gr([s]);
  return e ? (i) => t(i) && i.endsWith(e) : t;
}, wh = ([s, e = ""]) => {
  const t = mr([s]);
  return e ? (i) => t(i) && i.endsWith(e) : t;
}, mr = ([s]) => {
  const e = s.length;
  return (t) => t.length === e && !t.startsWith(".");
}, gr = ([s]) => {
  const e = s.length;
  return (t) => t.length === e && t !== "." && t !== "..";
}, wr = typeof process == "object" && process ? typeof process.env == "object" && process.env && process.env.__MINIMATCH_TESTING_PLATFORM__ || process.platform : "posix", Yi = {
  win32: { sep: "\\" },
  posix: { sep: "/" }
}, bh = wr === "win32" ? Yi.win32.sep : Yi.posix.sep;
G.sep = bh;
const H = Symbol("globstar **");
G.GLOBSTAR = H;
const yh = "[^/]", Eh = yh + "*?", Sh = "(?:(?!(?:\\/|^)(?:\\.{1,2})($|\\/)).)*?", Rh = "(?:(?!(?:\\/|^)\\.).)*?", Th = (s, e = {}) => (t) => G(t, s, e);
G.filter = Th;
const ee = (s, e = {}) => Object.assign({}, s, e), Oh = (s) => {
  if (!s || typeof s != "object" || !Object.keys(s).length)
    return G;
  const e = G;
  return Object.assign((i, n, r = {}) => e(i, n, ee(s, r)), {
    Minimatch: class extends e.Minimatch {
      constructor(n, r = {}) {
        super(n, ee(s, r));
      }
      static defaults(n) {
        return e.defaults(ee(s, n)).Minimatch;
      }
    },
    AST: class extends e.AST {
      /* c8 ignore start */
      constructor(n, r, o = {}) {
        super(n, r, ee(s, o));
      }
      /* c8 ignore stop */
      static fromGlob(n, r = {}) {
        return e.AST.fromGlob(n, ee(s, r));
      }
    },
    unescape: (i, n = {}) => e.unescape(i, ee(s, n)),
    escape: (i, n = {}) => e.escape(i, ee(s, n)),
    filter: (i, n = {}) => e.filter(i, ee(s, n)),
    defaults: (i) => e.defaults(ee(s, i)),
    makeRe: (i, n = {}) => e.makeRe(i, ee(s, n)),
    braceExpand: (i, n = {}) => e.braceExpand(i, ee(s, n)),
    match: (i, n, r = {}) => e.match(i, n, ee(s, r)),
    sep: e.sep,
    GLOBSTAR: H
  });
};
G.defaults = Oh;
const br = (s, e = {}) => ($t(s), e.nobrace || !/\{(?:(?!\{).)*\}/.test(s) ? [s] : Ic(s));
G.braceExpand = br;
const xh = (s, e = {}) => new Se(s, e).makeRe();
G.makeRe = xh;
const Ah = (s, e, t = {}) => {
  const i = new Se(e, t);
  return s = s.filter((n) => i.match(n)), i.options.nonull && !s.length && s.push(e), s;
};
G.match = Ah;
const Qi = /[?*]|[+@!]\(.*?\)|\[|\]/, Ch = (s) => s.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&");
class Se {
  options;
  set;
  pattern;
  windowsPathsNoEscape;
  nonegate;
  negate;
  comment;
  empty;
  preserveMultipleSlashes;
  partial;
  globSet;
  globParts;
  nocase;
  isWindows;
  platform;
  windowsNoMagicRoot;
  regexp;
  constructor(e, t = {}) {
    $t(e), t = t || {}, this.options = t, this.pattern = e, this.platform = t.platform || wr, this.isWindows = this.platform === "win32", this.windowsPathsNoEscape = !!t.windowsPathsNoEscape || t.allowWindowsEscape === !1, this.windowsPathsNoEscape && (this.pattern = this.pattern.replace(/\\/g, "/")), this.preserveMultipleSlashes = !!t.preserveMultipleSlashes, this.regexp = null, this.negate = !1, this.nonegate = !!t.nonegate, this.comment = !1, this.empty = !1, this.partial = !!t.partial, this.nocase = !!this.options.nocase, this.windowsNoMagicRoot = t.windowsNoMagicRoot !== void 0 ? t.windowsNoMagicRoot : !!(this.isWindows && this.nocase), this.globSet = [], this.globParts = [], this.set = [], this.make();
  }
  hasMagic() {
    if (this.options.magicalBraces && this.set.length > 1)
      return !0;
    for (const e of this.set)
      for (const t of e)
        if (typeof t != "string")
          return !0;
    return !1;
  }
  debug(...e) {
  }
  make() {
    const e = this.pattern, t = this.options;
    if (!t.nocomment && e.charAt(0) === "#") {
      this.comment = !0;
      return;
    }
    if (!e) {
      this.empty = !0;
      return;
    }
    this.parseNegate(), this.globSet = [...new Set(this.braceExpand())], t.debug && (this.debug = (...r) => console.error(...r)), this.debug(this.pattern, this.globSet);
    const i = this.globSet.map((r) => this.slashSplit(r));
    this.globParts = this.preprocess(i), this.debug(this.pattern, this.globParts);
    let n = this.globParts.map((r, o, a) => {
      if (this.isWindows && this.windowsNoMagicRoot) {
        const l = r[0] === "" && r[1] === "" && (r[2] === "?" || !Qi.test(r[2])) && !Qi.test(r[3]), c = /^[a-z]:/i.test(r[0]);
        if (l)
          return [...r.slice(0, 4), ...r.slice(4).map((h) => this.parse(h))];
        if (c)
          return [r[0], ...r.slice(1).map((h) => this.parse(h))];
      }
      return r.map((l) => this.parse(l));
    });
    if (this.debug(this.pattern, n), this.set = n.filter((r) => r.indexOf(!1) === -1), this.isWindows)
      for (let r = 0; r < this.set.length; r++) {
        const o = this.set[r];
        o[0] === "" && o[1] === "" && this.globParts[r][2] === "?" && typeof o[3] == "string" && /^[a-z]:$/i.test(o[3]) && (o[2] = "?");
      }
    this.debug(this.pattern, this.set);
  }
  // various transforms to equivalent pattern sets that are
  // faster to process in a filesystem walk.  The goal is to
  // eliminate what we can, and push all ** patterns as far
  // to the right as possible, even if it increases the number
  // of patterns that we have to process.
  preprocess(e) {
    if (this.options.noglobstar)
      for (let i = 0; i < e.length; i++)
        for (let n = 0; n < e[i].length; n++)
          e[i][n] === "**" && (e[i][n] = "*");
    const { optimizationLevel: t = 1 } = this.options;
    return t >= 2 ? (e = this.firstPhasePreProcess(e), e = this.secondPhasePreProcess(e)) : t >= 1 ? e = this.levelOneOptimize(e) : e = this.adjascentGlobstarOptimize(e), e;
  }
  // just get rid of adjascent ** portions
  adjascentGlobstarOptimize(e) {
    return e.map((t) => {
      let i = -1;
      for (; (i = t.indexOf("**", i + 1)) !== -1; ) {
        let n = i;
        for (; t[n + 1] === "**"; )
          n++;
        n !== i && t.splice(i, n - i);
      }
      return t;
    });
  }
  // get rid of adjascent ** and resolve .. portions
  levelOneOptimize(e) {
    return e.map((t) => (t = t.reduce((i, n) => {
      const r = i[i.length - 1];
      return n === "**" && r === "**" ? i : n === ".." && r && r !== ".." && r !== "." && r !== "**" ? (i.pop(), i) : (i.push(n), i);
    }, []), t.length === 0 ? [""] : t));
  }
  levelTwoFileOptimize(e) {
    Array.isArray(e) || (e = this.slashSplit(e));
    let t = !1;
    do {
      if (t = !1, !this.preserveMultipleSlashes) {
        for (let n = 1; n < e.length - 1; n++) {
          const r = e[n];
          n === 1 && r === "" && e[0] === "" || (r === "." || r === "") && (t = !0, e.splice(n, 1), n--);
        }
        e[0] === "." && e.length === 2 && (e[1] === "." || e[1] === "") && (t = !0, e.pop());
      }
      let i = 0;
      for (; (i = e.indexOf("..", i + 1)) !== -1; ) {
        const n = e[i - 1];
        n && n !== "." && n !== ".." && n !== "**" && (t = !0, e.splice(i - 1, 2), i -= 2);
      }
    } while (t);
    return e.length === 0 ? [""] : e;
  }
  // First phase: single-pattern processing
  // <pre> is 1 or more portions
  // <rest> is 1 or more portions
  // <p> is any portion other than ., .., '', or **
  // <e> is . or ''
  //
  // **/.. is *brutal* for filesystem walking performance, because
  // it effectively resets the recursive walk each time it occurs,
  // and ** cannot be reduced out by a .. pattern part like a regexp
  // or most strings (other than .., ., and '') can be.
  //
  // <pre>/**/../<p>/<p>/<rest> -> {<pre>/../<p>/<p>/<rest>,<pre>/**/<p>/<p>/<rest>}
  // <pre>/<e>/<rest> -> <pre>/<rest>
  // <pre>/<p>/../<rest> -> <pre>/<rest>
  // **/**/<rest> -> **/<rest>
  //
  // **/*/<rest> -> */**/<rest> <== not valid because ** doesn't follow
  // this WOULD be allowed if ** did follow symlinks, or * didn't
  firstPhasePreProcess(e) {
    let t = !1;
    do {
      t = !1;
      for (let i of e) {
        let n = -1;
        for (; (n = i.indexOf("**", n + 1)) !== -1; ) {
          let o = n;
          for (; i[o + 1] === "**"; )
            o++;
          o > n && i.splice(n + 1, o - n);
          let a = i[n + 1];
          const l = i[n + 2], c = i[n + 3];
          if (a !== ".." || !l || l === "." || l === ".." || !c || c === "." || c === "..")
            continue;
          t = !0, i.splice(n, 1);
          const h = i.slice(0);
          h[n] = "**", e.push(h), n--;
        }
        if (!this.preserveMultipleSlashes) {
          for (let o = 1; o < i.length - 1; o++) {
            const a = i[o];
            o === 1 && a === "" && i[0] === "" || (a === "." || a === "") && (t = !0, i.splice(o, 1), o--);
          }
          i[0] === "." && i.length === 2 && (i[1] === "." || i[1] === "") && (t = !0, i.pop());
        }
        let r = 0;
        for (; (r = i.indexOf("..", r + 1)) !== -1; ) {
          const o = i[r - 1];
          if (o && o !== "." && o !== ".." && o !== "**") {
            t = !0;
            const l = r === 1 && i[r + 1] === "**" ? ["."] : [];
            i.splice(r - 1, 2, ...l), i.length === 0 && i.push(""), r -= 2;
          }
        }
      }
    } while (t);
    return e;
  }
  // second phase: multi-pattern dedupes
  // {<pre>/*/<rest>,<pre>/<p>/<rest>} -> <pre>/*/<rest>
  // {<pre>/<rest>,<pre>/<rest>} -> <pre>/<rest>
  // {<pre>/**/<rest>,<pre>/<rest>} -> <pre>/**/<rest>
  //
  // {<pre>/**/<rest>,<pre>/**/<p>/<rest>} -> <pre>/**/<rest>
  // ^-- not valid because ** doens't follow symlinks
  secondPhasePreProcess(e) {
    for (let t = 0; t < e.length - 1; t++)
      for (let i = t + 1; i < e.length; i++) {
        const n = this.partsMatch(e[t], e[i], !this.preserveMultipleSlashes);
        if (n) {
          e[t] = [], e[i] = n;
          break;
        }
      }
    return e.filter((t) => t.length);
  }
  partsMatch(e, t, i = !1) {
    let n = 0, r = 0, o = [], a = "";
    for (; n < e.length && r < t.length; )
      if (e[n] === t[r])
        o.push(a === "b" ? t[r] : e[n]), n++, r++;
      else if (i && e[n] === "**" && t[r] === e[n + 1])
        o.push(e[n]), n++;
      else if (i && t[r] === "**" && e[n] === t[r + 1])
        o.push(t[r]), r++;
      else if (e[n] === "*" && t[r] && (this.options.dot || !t[r].startsWith(".")) && t[r] !== "**") {
        if (a === "b")
          return !1;
        a = "a", o.push(e[n]), n++, r++;
      } else if (t[r] === "*" && e[n] && (this.options.dot || !e[n].startsWith(".")) && e[n] !== "**") {
        if (a === "a")
          return !1;
        a = "b", o.push(t[r]), n++, r++;
      } else
        return !1;
    return e.length === t.length && o;
  }
  parseNegate() {
    if (this.nonegate)
      return;
    const e = this.pattern;
    let t = !1, i = 0;
    for (let n = 0; n < e.length && e.charAt(n) === "!"; n++)
      t = !t, i++;
    i && (this.pattern = e.slice(i)), this.negate = t;
  }
  // set partial to true to test if, for example,
  // "/a/b" matches the start of "/*/b/*/d"
  // Partial means, if you run out of file before you run
  // out of pattern, then that's fine, as long as all
  // the parts match.
  matchOne(e, t, i = !1) {
    const n = this.options;
    if (this.isWindows) {
      const f = typeof e[0] == "string" && /^[a-z]:$/i.test(e[0]), b = !f && e[0] === "" && e[1] === "" && e[2] === "?" && /^[a-z]:$/i.test(e[3]), y = typeof t[0] == "string" && /^[a-z]:$/i.test(t[0]), E = !y && t[0] === "" && t[1] === "" && t[2] === "?" && typeof t[3] == "string" && /^[a-z]:$/i.test(t[3]), S = b ? 3 : f ? 0 : void 0, T = E ? 3 : y ? 0 : void 0;
      if (typeof S == "number" && typeof T == "number") {
        const [O, v] = [e[S], t[T]];
        O.toLowerCase() === v.toLowerCase() && (t[T] = O, T > S ? t = t.slice(T) : S > T && (e = e.slice(S)));
      }
    }
    const { optimizationLevel: r = 1 } = this.options;
    r >= 2 && (e = this.levelTwoFileOptimize(e)), this.debug("matchOne", this, { file: e, pattern: t }), this.debug("matchOne", e.length, t.length);
    for (var o = 0, a = 0, l = e.length, c = t.length; o < l && a < c; o++, a++) {
      this.debug("matchOne loop");
      var h = t[a], u = e[o];
      if (this.debug(t, h, u), h === !1)
        return !1;
      if (h === H) {
        this.debug("GLOBSTAR", [t, h, u]);
        var p = o, d = a + 1;
        if (d === c) {
          for (this.debug("** at the end"); o < l; o++)
            if (e[o] === "." || e[o] === ".." || !n.dot && e[o].charAt(0) === ".")
              return !1;
          return !0;
        }
        for (; p < l; ) {
          var m = e[p];
          if (this.debug(`
globstar while`, e, p, t, d, m), this.matchOne(e.slice(p), t.slice(d), i))
            return this.debug("globstar found match!", p, l, m), !0;
          if (m === "." || m === ".." || !n.dot && m.charAt(0) === ".") {
            this.debug("dot detected!", e, p, t, d);
            break;
          }
          this.debug("globstar swallow a segment, and continue"), p++;
        }
        return !!(i && (this.debug(`
>>> no match, partial?`, e, p, t, d), p === l));
      }
      let f;
      if (typeof h == "string" ? (f = u === h, this.debug("string match", h, u, f)) : (f = h.test(u), this.debug("pattern match", h, u, f)), !f)
        return !1;
    }
    if (o === l && a === c)
      return !0;
    if (o === l)
      return i;
    if (a === c)
      return o === l - 1 && e[o] === "";
    throw new Error("wtf?");
  }
  braceExpand() {
    return br(this.pattern, this.options);
  }
  parse(e) {
    $t(e);
    const t = this.options;
    if (e === "**")
      return H;
    if (e === "")
      return "";
    let i, n = null;
    (i = e.match(hh)) ? n = t.dot ? fh : uh : (i = e.match(eh)) ? n = (t.nocase ? t.dot ? nh : ih : t.dot ? sh : th)(i[1]) : (i = e.match(dh)) ? n = (t.nocase ? t.dot ? mh : ph : t.dot ? gh : wh)(i) : (i = e.match(rh)) ? n = t.dot ? ah : oh : (i = e.match(lh)) && (n = ch);
    const r = q.fromGlob(e, this.options).toMMPattern();
    return n && typeof r == "object" && Reflect.defineProperty(r, "test", { value: n }), r;
  }
  makeRe() {
    if (this.regexp || this.regexp === !1)
      return this.regexp;
    const e = this.set;
    if (!e.length)
      return this.regexp = !1, this.regexp;
    const t = this.options, i = t.noglobstar ? Eh : t.dot ? Sh : Rh, n = new Set(t.nocase ? ["i"] : []);
    let r = e.map((l) => {
      const c = l.map((h) => {
        if (h instanceof RegExp)
          for (const u of h.flags.split(""))
            n.add(u);
        return typeof h == "string" ? Ch(h) : h === H ? H : h._src;
      });
      return c.forEach((h, u) => {
        const p = c[u + 1], d = c[u - 1];
        h !== H || d === H || (d === void 0 ? p !== void 0 && p !== H ? c[u + 1] = "(?:\\/|" + i + "\\/)?" + p : c[u] = i : p === void 0 ? c[u - 1] = d + "(?:\\/|" + i + ")?" : p !== H && (c[u - 1] = d + "(?:\\/|\\/" + i + "\\/)" + p, c[u + 1] = H));
      }), c.filter((h) => h !== H).join("/");
    }).join("|");
    const [o, a] = e.length > 1 ? ["(?:", ")"] : ["", ""];
    r = "^" + o + r + a + "$", this.negate && (r = "^(?!" + r + ").+$");
    try {
      this.regexp = new RegExp(r, [...n].join(""));
    } catch {
      this.regexp = !1;
    }
    return this.regexp;
  }
  slashSplit(e) {
    return this.preserveMultipleSlashes ? e.split("/") : this.isWindows && /^\/\/[^\/]+/.test(e) ? ["", ...e.split(/\/+/)] : e.split(/\/+/);
  }
  match(e, t = this.partial) {
    if (this.debug("match", e, this.pattern), this.comment)
      return !1;
    if (this.empty)
      return e === "";
    if (e === "/" && t)
      return !0;
    const i = this.options;
    this.isWindows && (e = e.split("\\").join("/"));
    const n = this.slashSplit(e);
    this.debug(this.pattern, "split", n);
    const r = this.set;
    this.debug(this.pattern, "set", r);
    let o = n[n.length - 1];
    if (!o)
      for (let a = n.length - 2; !o && a >= 0; a--)
        o = n[a];
    for (let a = 0; a < r.length; a++) {
      const l = r[a];
      let c = n;
      if (i.matchBase && l.length === 1 && (c = [o]), this.matchOne(c, l, t))
        return i.flipNegate ? !0 : !this.negate;
    }
    return i.flipNegate ? !1 : this.negate;
  }
  static defaults(e) {
    return G.defaults(e).Minimatch;
  }
}
G.AST = q;
G.Minimatch = Se;
G.escape = pr;
G.unescape = Be;
const Le = typeof performance == "object" && performance && typeof performance.now == "function" ? performance : Date, yr = /* @__PURE__ */ new Set(), $s = typeof process == "object" && process ? process : {}, Er = (s, e, t, i) => {
  typeof $s.emitWarning == "function" ? $s.emitWarning(s, e, t, i) : console.error(`[${t}] ${e}: ${s}`);
};
let zt = globalThis.AbortController, en = globalThis.AbortSignal;
if (typeof zt > "u") {
  en = class {
    onabort;
    _onabort = [];
    reason;
    aborted = !1;
    addEventListener(i, n) {
      this._onabort.push(n);
    }
  }, zt = class {
    constructor() {
      e();
    }
    signal = new en();
    abort(i) {
      if (!this.signal.aborted) {
        this.signal.reason = i, this.signal.aborted = !0;
        for (const n of this.signal._onabort)
          n(i);
        this.signal.onabort?.(i);
      }
    }
  };
  let s = $s.env?.LRU_CACHE_IGNORE_AC_WARNING !== "1";
  const e = () => {
    s && (s = !1, Er("AbortController is not defined. If using lru-cache in node 14, load an AbortController polyfill from the `node-abort-controller` package. A minimal polyfill is provided for use by LRUCache.fetch(), but it should not be relied upon in other contexts (eg, passing it to other APIs that use AbortController/AbortSignal might have undesirable effects). You may disable this with LRU_CACHE_IGNORE_AC_WARNING=1 in the env.", "NO_ABORT_CONTROLLER", "ENOTSUP", e));
  };
}
const vh = (s) => !yr.has(s), ye = (s) => s && s === Math.floor(s) && s > 0 && isFinite(s), Sr = (s) => ye(s) ? s <= Math.pow(2, 8) ? Uint8Array : s <= Math.pow(2, 16) ? Uint16Array : s <= Math.pow(2, 32) ? Uint32Array : s <= Number.MAX_SAFE_INTEGER ? Dt : null : null;
class Dt extends Array {
  constructor(e) {
    super(e), this.fill(0);
  }
}
class Ie {
  heap;
  length;
  // private constructor
  static #e = !1;
  static create(e) {
    const t = Sr(e);
    if (!t)
      return [];
    Ie.#e = !0;
    const i = new Ie(e, t);
    return Ie.#e = !1, i;
  }
  constructor(e, t) {
    if (!Ie.#e)
      throw new TypeError("instantiate Stack using Stack.create(n)");
    this.heap = new t(e), this.length = 0;
  }
  push(e) {
    this.heap[this.length++] = e;
  }
  pop() {
    return this.heap[--this.length];
  }
}
class ts {
  // options that cannot be changed without disaster
  #e;
  #t;
  #s;
  #r;
  #o;
  #E;
  #w;
  /**
   * {@link LRUCache.OptionsBase.ttl}
   */
  ttl;
  /**
   * {@link LRUCache.OptionsBase.ttlResolution}
   */
  ttlResolution;
  /**
   * {@link LRUCache.OptionsBase.ttlAutopurge}
   */
  ttlAutopurge;
  /**
   * {@link LRUCache.OptionsBase.updateAgeOnGet}
   */
  updateAgeOnGet;
  /**
   * {@link LRUCache.OptionsBase.updateAgeOnHas}
   */
  updateAgeOnHas;
  /**
   * {@link LRUCache.OptionsBase.allowStale}
   */
  allowStale;
  /**
   * {@link LRUCache.OptionsBase.noDisposeOnSet}
   */
  noDisposeOnSet;
  /**
   * {@link LRUCache.OptionsBase.noUpdateTTL}
   */
  noUpdateTTL;
  /**
   * {@link LRUCache.OptionsBase.maxEntrySize}
   */
  maxEntrySize;
  /**
   * {@link LRUCache.OptionsBase.sizeCalculation}
   */
  sizeCalculation;
  /**
   * {@link LRUCache.OptionsBase.noDeleteOnFetchRejection}
   */
  noDeleteOnFetchRejection;
  /**
   * {@link LRUCache.OptionsBase.noDeleteOnStaleGet}
   */
  noDeleteOnStaleGet;
  /**
   * {@link LRUCache.OptionsBase.allowStaleOnFetchAbort}
   */
  allowStaleOnFetchAbort;
  /**
   * {@link LRUCache.OptionsBase.allowStaleOnFetchRejection}
   */
  allowStaleOnFetchRejection;
  /**
   * {@link LRUCache.OptionsBase.ignoreFetchAbort}
   */
  ignoreFetchAbort;
  // computed properties
  #a;
  #u;
  #l;
  #c;
  #i;
  #f;
  #b;
  #p;
  #m;
  #C;
  #g;
  #v;
  #R;
  #S;
  #y;
  #x;
  #d;
  #_;
  /**
   * Do not call this method unless you need to inspect the
   * inner workings of the cache.  If anything returned by this
   * object is modified in any way, strange breakage may occur.
   *
   * These fields are private for a reason!
   *
   * @internal
   */
  static unsafeExposeInternals(e) {
    return {
      // properties
      starts: e.#R,
      ttls: e.#S,
      sizes: e.#v,
      keyMap: e.#l,
      keyList: e.#c,
      valList: e.#i,
      next: e.#f,
      prev: e.#b,
      get head() {
        return e.#p;
      },
      get tail() {
        return e.#m;
      },
      free: e.#C,
      // methods
      isBackgroundFetch: (t) => e.#h(t),
      backgroundFetch: (t, i, n, r) => e.#L(t, i, n, r),
      moveToTail: (t) => e.#B(t),
      indexes: (t) => e.#A(t),
      rindexes: (t) => e.#N(t),
      isStale: (t) => e.#O(t)
    };
  }
  // Protected read-only members
  /**
   * {@link LRUCache.OptionsBase.max} (read-only)
   */
  get max() {
    return this.#e;
  }
  /**
   * {@link LRUCache.OptionsBase.maxSize} (read-only)
   */
  get maxSize() {
    return this.#t;
  }
  /**
   * The total computed size of items in the cache (read-only)
   */
  get calculatedSize() {
    return this.#u;
  }
  /**
   * The number of items stored in the cache (read-only)
   */
  get size() {
    return this.#a;
  }
  /**
   * {@link LRUCache.OptionsBase.fetchMethod} (read-only)
   */
  get fetchMethod() {
    return this.#E;
  }
  get memoMethod() {
    return this.#w;
  }
  /**
   * {@link LRUCache.OptionsBase.dispose} (read-only)
   */
  get dispose() {
    return this.#s;
  }
  /**
   * {@link LRUCache.OptionsBase.onInsert} (read-only)
   */
  get onInsert() {
    return this.#r;
  }
  /**
   * {@link LRUCache.OptionsBase.disposeAfter} (read-only)
   */
  get disposeAfter() {
    return this.#o;
  }
  constructor(e) {
    const { max: t = 0, ttl: i, ttlResolution: n = 1, ttlAutopurge: r, updateAgeOnGet: o, updateAgeOnHas: a, allowStale: l, dispose: c, onInsert: h, disposeAfter: u, noDisposeOnSet: p, noUpdateTTL: d, maxSize: m = 0, maxEntrySize: f = 0, sizeCalculation: b, fetchMethod: y, memoMethod: E, noDeleteOnFetchRejection: S, noDeleteOnStaleGet: T, allowStaleOnFetchRejection: O, allowStaleOnFetchAbort: v, ignoreFetchAbort: P } = e;
    if (t !== 0 && !ye(t))
      throw new TypeError("max option must be a nonnegative integer");
    const B = t ? Sr(t) : Array;
    if (!B)
      throw new Error("invalid max value: " + t);
    if (this.#e = t, this.#t = m, this.maxEntrySize = f || this.#t, this.sizeCalculation = b, this.sizeCalculation) {
      if (!this.#t && !this.maxEntrySize)
        throw new TypeError("cannot set sizeCalculation without setting maxSize or maxEntrySize");
      if (typeof this.sizeCalculation != "function")
        throw new TypeError("sizeCalculation set to non-function");
    }
    if (E !== void 0 && typeof E != "function")
      throw new TypeError("memoMethod must be a function if defined");
    if (this.#w = E, y !== void 0 && typeof y != "function")
      throw new TypeError("fetchMethod must be a function if specified");
    if (this.#E = y, this.#x = !!y, this.#l = /* @__PURE__ */ new Map(), this.#c = new Array(t).fill(void 0), this.#i = new Array(t).fill(void 0), this.#f = new B(t), this.#b = new B(t), this.#p = 0, this.#m = 0, this.#C = Ie.create(t), this.#a = 0, this.#u = 0, typeof c == "function" && (this.#s = c), typeof h == "function" && (this.#r = h), typeof u == "function" ? (this.#o = u, this.#g = []) : (this.#o = void 0, this.#g = void 0), this.#y = !!this.#s, this.#_ = !!this.#r, this.#d = !!this.#o, this.noDisposeOnSet = !!p, this.noUpdateTTL = !!d, this.noDeleteOnFetchRejection = !!S, this.allowStaleOnFetchRejection = !!O, this.allowStaleOnFetchAbort = !!v, this.ignoreFetchAbort = !!P, this.maxEntrySize !== 0) {
      if (this.#t !== 0 && !ye(this.#t))
        throw new TypeError("maxSize must be a positive integer if specified");
      if (!ye(this.maxEntrySize))
        throw new TypeError("maxEntrySize must be a positive integer if specified");
      this.#M();
    }
    if (this.allowStale = !!l, this.noDeleteOnStaleGet = !!T, this.updateAgeOnGet = !!o, this.updateAgeOnHas = !!a, this.ttlResolution = ye(n) || n === 0 ? n : 1, this.ttlAutopurge = !!r, this.ttl = i || 0, this.ttl) {
      if (!ye(this.ttl))
        throw new TypeError("ttl must be a positive integer if specified");
      this.#n();
    }
    if (this.#e === 0 && this.ttl === 0 && this.#t === 0)
      throw new TypeError("At least one of max, maxSize, or ttl is required");
    if (!this.ttlAutopurge && !this.#e && !this.#t) {
      const z = "LRU_CACHE_UNBOUNDED";
      vh(z) && (yr.add(z), Er("TTL caching without ttlAutopurge, max, or maxSize can result in unbounded memory consumption.", "UnboundedCacheWarning", z, ts));
    }
  }
  /**
   * Return the number of ms left in the item's TTL. If item is not in cache,
   * returns `0`. Returns `Infinity` if item is in cache without a defined TTL.
   */
  getRemainingTTL(e) {
    return this.#l.has(e) ? 1 / 0 : 0;
  }
  #n() {
    const e = new Dt(this.#e), t = new Dt(this.#e);
    this.#S = e, this.#R = t, this.#P = (r, o, a = Le.now()) => {
      if (t[r] = o !== 0 ? a : 0, e[r] = o, o !== 0 && this.ttlAutopurge) {
        const l = setTimeout(() => {
          this.#O(r) && this.#F(this.#c[r], "expire");
        }, o + 1);
        l.unref && l.unref();
      }
    }, this.#D = (r) => {
      t[r] = e[r] !== 0 ? Le.now() : 0;
    }, this.#T = (r, o) => {
      if (e[o]) {
        const a = e[o], l = t[o];
        if (!a || !l)
          return;
        r.ttl = a, r.start = l, r.now = i || n();
        const c = r.now - l;
        r.remainingTTL = a - c;
      }
    };
    let i = 0;
    const n = () => {
      const r = Le.now();
      if (this.ttlResolution > 0) {
        i = r;
        const o = setTimeout(() => i = 0, this.ttlResolution);
        o.unref && o.unref();
      }
      return r;
    };
    this.getRemainingTTL = (r) => {
      const o = this.#l.get(r);
      if (o === void 0)
        return 0;
      const a = e[o], l = t[o];
      if (!a || !l)
        return 1 / 0;
      const c = (i || n()) - l;
      return a - c;
    }, this.#O = (r) => {
      const o = t[r], a = e[r];
      return !!a && !!o && (i || n()) - o > a;
    };
  }
  // conditionally set private methods related to TTL
  #D = () => {
  };
  #T = () => {
  };
  #P = () => {
  };
  /* c8 ignore stop */
  #O = () => !1;
  #M() {
    const e = new Dt(this.#e);
    this.#u = 0, this.#v = e, this.#k = (t) => {
      this.#u -= e[t], e[t] = 0;
    }, this.#$ = (t, i, n, r) => {
      if (this.#h(i))
        return 0;
      if (!ye(n))
        if (r) {
          if (typeof r != "function")
            throw new TypeError("sizeCalculation must be a function");
          if (n = r(i, t), !ye(n))
            throw new TypeError("sizeCalculation return invalid (expect positive integer)");
        } else
          throw new TypeError("invalid size value (must be positive integer). When maxSize or maxEntrySize is used, sizeCalculation or size must be set.");
      return n;
    }, this.#j = (t, i, n) => {
      if (e[t] = i, this.#t) {
        const r = this.#t - e[t];
        for (; this.#u > r; )
          this.#U(!0);
      }
      this.#u += e[t], n && (n.entrySize = i, n.totalCalculatedSize = this.#u);
    };
  }
  #k = (e) => {
  };
  #j = (e, t, i) => {
  };
  #$ = (e, t, i, n) => {
    if (i || n)
      throw new TypeError("cannot set size without setting maxSize or maxEntrySize on cache");
    return 0;
  };
  *#A({ allowStale: e = this.allowStale } = {}) {
    if (this.#a)
      for (let t = this.#m; !(!this.#z(t) || ((e || !this.#O(t)) && (yield t), t === this.#p)); )
        t = this.#b[t];
  }
  *#N({ allowStale: e = this.allowStale } = {}) {
    if (this.#a)
      for (let t = this.#p; !(!this.#z(t) || ((e || !this.#O(t)) && (yield t), t === this.#m)); )
        t = this.#f[t];
  }
  #z(e) {
    return e !== void 0 && this.#l.get(this.#c[e]) === e;
  }
  /**
   * Return a generator yielding `[key, value]` pairs,
   * in order from most recently used to least recently used.
   */
  *entries() {
    for (const e of this.#A())
      this.#i[e] !== void 0 && this.#c[e] !== void 0 && !this.#h(this.#i[e]) && (yield [this.#c[e], this.#i[e]]);
  }
  /**
   * Inverse order version of {@link LRUCache.entries}
   *
   * Return a generator yielding `[key, value]` pairs,
   * in order from least recently used to most recently used.
   */
  *rentries() {
    for (const e of this.#N())
      this.#i[e] !== void 0 && this.#c[e] !== void 0 && !this.#h(this.#i[e]) && (yield [this.#c[e], this.#i[e]]);
  }
  /**
   * Return a generator yielding the keys in the cache,
   * in order from most recently used to least recently used.
   */
  *keys() {
    for (const e of this.#A()) {
      const t = this.#c[e];
      t !== void 0 && !this.#h(this.#i[e]) && (yield t);
    }
  }
  /**
   * Inverse order version of {@link LRUCache.keys}
   *
   * Return a generator yielding the keys in the cache,
   * in order from least recently used to most recently used.
   */
  *rkeys() {
    for (const e of this.#N()) {
      const t = this.#c[e];
      t !== void 0 && !this.#h(this.#i[e]) && (yield t);
    }
  }
  /**
   * Return a generator yielding the values in the cache,
   * in order from most recently used to least recently used.
   */
  *values() {
    for (const e of this.#A())
      this.#i[e] !== void 0 && !this.#h(this.#i[e]) && (yield this.#i[e]);
  }
  /**
   * Inverse order version of {@link LRUCache.values}
   *
   * Return a generator yielding the values in the cache,
   * in order from least recently used to most recently used.
   */
  *rvalues() {
    for (const e of this.#N())
      this.#i[e] !== void 0 && !this.#h(this.#i[e]) && (yield this.#i[e]);
  }
  /**
   * Iterating over the cache itself yields the same results as
   * {@link LRUCache.entries}
   */
  [Symbol.iterator]() {
    return this.entries();
  }
  /**
   * A String value that is used in the creation of the default string
   * description of an object. Called by the built-in method
   * `Object.prototype.toString`.
   */
  [Symbol.toStringTag] = "LRUCache";
  /**
   * Find a value for which the supplied fn method returns a truthy value,
   * similar to `Array.find()`. fn is called as `fn(value, key, cache)`.
   */
  find(e, t = {}) {
    for (const i of this.#A()) {
      const n = this.#i[i], r = this.#h(n) ? n.__staleWhileFetching : n;
      if (r !== void 0 && e(r, this.#c[i], this))
        return this.get(this.#c[i], t);
    }
  }
  /**
   * Call the supplied function on each item in the cache, in order from most
   * recently used to least recently used.
   *
   * `fn` is called as `fn(value, key, cache)`.
   *
   * If `thisp` is provided, function will be called in the `this`-context of
   * the provided object, or the cache if no `thisp` object is provided.
   *
   * Does not update age or recenty of use, or iterate over stale values.
   */
  forEach(e, t = this) {
    for (const i of this.#A()) {
      const n = this.#i[i], r = this.#h(n) ? n.__staleWhileFetching : n;
      r !== void 0 && e.call(t, r, this.#c[i], this);
    }
  }
  /**
   * The same as {@link LRUCache.forEach} but items are iterated over in
   * reverse order.  (ie, less recently used items are iterated over first.)
   */
  rforEach(e, t = this) {
    for (const i of this.#N()) {
      const n = this.#i[i], r = this.#h(n) ? n.__staleWhileFetching : n;
      r !== void 0 && e.call(t, r, this.#c[i], this);
    }
  }
  /**
   * Delete any stale entries. Returns true if anything was removed,
   * false otherwise.
   */
  purgeStale() {
    let e = !1;
    for (const t of this.#N({ allowStale: !0 }))
      this.#O(t) && (this.#F(this.#c[t], "expire"), e = !0);
    return e;
  }
  /**
   * Get the extended info about a given entry, to get its value, size, and
   * TTL info simultaneously. Returns `undefined` if the key is not present.
   *
   * Unlike {@link LRUCache#dump}, which is designed to be portable and survive
   * serialization, the `start` value is always the current timestamp, and the
   * `ttl` is a calculated remaining time to live (negative if expired).
   *
   * Always returns stale values, if their info is found in the cache, so be
   * sure to check for expirations (ie, a negative {@link LRUCache.Entry#ttl})
   * if relevant.
   */
  info(e) {
    const t = this.#l.get(e);
    if (t === void 0)
      return;
    const i = this.#i[t], n = this.#h(i) ? i.__staleWhileFetching : i;
    if (n === void 0)
      return;
    const r = { value: n };
    if (this.#S && this.#R) {
      const o = this.#S[t], a = this.#R[t];
      if (o && a) {
        const l = o - (Le.now() - a);
        r.ttl = l, r.start = Date.now();
      }
    }
    return this.#v && (r.size = this.#v[t]), r;
  }
  /**
   * Return an array of [key, {@link LRUCache.Entry}] tuples which can be
   * passed to {@link LRUCache#load}.
   *
   * The `start` fields are calculated relative to a portable `Date.now()`
   * timestamp, even if `performance.now()` is available.
   *
   * Stale entries are always included in the `dump`, even if
   * {@link LRUCache.OptionsBase.allowStale} is false.
   *
   * Note: this returns an actual array, not a generator, so it can be more
   * easily passed around.
   */
  dump() {
    const e = [];
    for (const t of this.#A({ allowStale: !0 })) {
      const i = this.#c[t], n = this.#i[t], r = this.#h(n) ? n.__staleWhileFetching : n;
      if (r === void 0 || i === void 0)
        continue;
      const o = { value: r };
      if (this.#S && this.#R) {
        o.ttl = this.#S[t];
        const a = Le.now() - this.#R[t];
        o.start = Math.floor(Date.now() - a);
      }
      this.#v && (o.size = this.#v[t]), e.unshift([i, o]);
    }
    return e;
  }
  /**
   * Reset the cache and load in the items in entries in the order listed.
   *
   * The shape of the resulting cache may be different if the same options are
   * not used in both caches.
   *
   * The `start` fields are assumed to be calculated relative to a portable
   * `Date.now()` timestamp, even if `performance.now()` is available.
   */
  load(e) {
    this.clear();
    for (const [t, i] of e) {
      if (i.start) {
        const n = Date.now() - i.start;
        i.start = Le.now() - n;
      }
      this.set(t, i.value, i);
    }
  }
  /**
   * Add a value to the cache.
   *
   * Note: if `undefined` is specified as a value, this is an alias for
   * {@link LRUCache#delete}
   *
   * Fields on the {@link LRUCache.SetOptions} options param will override
   * their corresponding values in the constructor options for the scope
   * of this single `set()` operation.
   *
   * If `start` is provided, then that will set the effective start
   * time for the TTL calculation. Note that this must be a previous
   * value of `performance.now()` if supported, or a previous value of
   * `Date.now()` if not.
   *
   * Options object may also include `size`, which will prevent
   * calling the `sizeCalculation` function and just use the specified
   * number if it is a positive integer, and `noDisposeOnSet` which
   * will prevent calling a `dispose` function in the case of
   * overwrites.
   *
   * If the `size` (or return value of `sizeCalculation`) for a given
   * entry is greater than `maxEntrySize`, then the item will not be
   * added to the cache.
   *
   * Will update the recency of the entry.
   *
   * If the value is `undefined`, then this is an alias for
   * `cache.delete(key)`. `undefined` is never stored in the cache.
   */
  set(e, t, i = {}) {
    if (t === void 0)
      return this.delete(e), this;
    const { ttl: n = this.ttl, start: r, noDisposeOnSet: o = this.noDisposeOnSet, sizeCalculation: a = this.sizeCalculation, status: l } = i;
    let { noUpdateTTL: c = this.noUpdateTTL } = i;
    const h = this.#$(e, t, i.size || 0, a);
    if (this.maxEntrySize && h > this.maxEntrySize)
      return l && (l.set = "miss", l.maxEntrySizeExceeded = !0), this.#F(e, "set"), this;
    let u = this.#a === 0 ? void 0 : this.#l.get(e);
    if (u === void 0)
      u = this.#a === 0 ? this.#m : this.#C.length !== 0 ? this.#C.pop() : this.#a === this.#e ? this.#U(!1) : this.#a, this.#c[u] = e, this.#i[u] = t, this.#l.set(e, u), this.#f[this.#m] = u, this.#b[u] = this.#m, this.#m = u, this.#a++, this.#j(u, h, l), l && (l.set = "add"), c = !1, this.#_ && this.#r?.(t, e, "add");
    else {
      this.#B(u);
      const p = this.#i[u];
      if (t !== p) {
        if (this.#x && this.#h(p)) {
          p.__abortController.abort(new Error("replaced"));
          const { __staleWhileFetching: d } = p;
          d !== void 0 && !o && (this.#y && this.#s?.(d, e, "set"), this.#d && this.#g?.push([d, e, "set"]));
        } else o || (this.#y && this.#s?.(p, e, "set"), this.#d && this.#g?.push([p, e, "set"]));
        if (this.#k(u), this.#j(u, h, l), this.#i[u] = t, l) {
          l.set = "replace";
          const d = p && this.#h(p) ? p.__staleWhileFetching : p;
          d !== void 0 && (l.oldValue = d);
        }
      } else l && (l.set = "update");
      this.#_ && this.onInsert?.(t, e, t === p ? "update" : "replace");
    }
    if (n !== 0 && !this.#S && this.#n(), this.#S && (c || this.#P(u, n, r), l && this.#T(l, u)), !o && this.#d && this.#g) {
      const p = this.#g;
      let d;
      for (; d = p?.shift(); )
        this.#o?.(...d);
    }
    return this;
  }
  /**
   * Evict the least recently used item, returning its value or
   * `undefined` if cache is empty.
   */
  pop() {
    try {
      for (; this.#a; ) {
        const e = this.#i[this.#p];
        if (this.#U(!0), this.#h(e)) {
          if (e.__staleWhileFetching)
            return e.__staleWhileFetching;
        } else if (e !== void 0)
          return e;
      }
    } finally {
      if (this.#d && this.#g) {
        const e = this.#g;
        let t;
        for (; t = e?.shift(); )
          this.#o?.(...t);
      }
    }
  }
  #U(e) {
    const t = this.#p, i = this.#c[t], n = this.#i[t];
    return this.#x && this.#h(n) ? n.__abortController.abort(new Error("evicted")) : (this.#y || this.#d) && (this.#y && this.#s?.(n, i, "evict"), this.#d && this.#g?.push([n, i, "evict"])), this.#k(t), e && (this.#c[t] = void 0, this.#i[t] = void 0, this.#C.push(t)), this.#a === 1 ? (this.#p = this.#m = 0, this.#C.length = 0) : this.#p = this.#f[t], this.#l.delete(i), this.#a--, t;
  }
  /**
   * Check if a key is in the cache, without updating the recency of use.
   * Will return false if the item is stale, even though it is technically
   * in the cache.
   *
   * Check if a key is in the cache, without updating the recency of
   * use. Age is updated if {@link LRUCache.OptionsBase.updateAgeOnHas} is set
   * to `true` in either the options or the constructor.
   *
   * Will return `false` if the item is stale, even though it is technically in
   * the cache. The difference can be determined (if it matters) by using a
   * `status` argument, and inspecting the `has` field.
   *
   * Will not update item age unless
   * {@link LRUCache.OptionsBase.updateAgeOnHas} is set.
   */
  has(e, t = {}) {
    const { updateAgeOnHas: i = this.updateAgeOnHas, status: n } = t, r = this.#l.get(e);
    if (r !== void 0) {
      const o = this.#i[r];
      if (this.#h(o) && o.__staleWhileFetching === void 0)
        return !1;
      if (this.#O(r))
        n && (n.has = "stale", this.#T(n, r));
      else return i && this.#D(r), n && (n.has = "hit", this.#T(n, r)), !0;
    } else n && (n.has = "miss");
    return !1;
  }
  /**
   * Like {@link LRUCache#get} but doesn't update recency or delete stale
   * items.
   *
   * Returns `undefined` if the item is stale, unless
   * {@link LRUCache.OptionsBase.allowStale} is set.
   */
  peek(e, t = {}) {
    const { allowStale: i = this.allowStale } = t, n = this.#l.get(e);
    if (n === void 0 || !i && this.#O(n))
      return;
    const r = this.#i[n];
    return this.#h(r) ? r.__staleWhileFetching : r;
  }
  #L(e, t, i, n) {
    const r = t === void 0 ? void 0 : this.#i[t];
    if (this.#h(r))
      return r;
    const o = new zt(), { signal: a } = i;
    a?.addEventListener("abort", () => o.abort(a.reason), {
      signal: o.signal
    });
    const l = {
      signal: o.signal,
      options: i,
      context: n
    }, c = (f, b = !1) => {
      const { aborted: y } = o.signal, E = i.ignoreFetchAbort && f !== void 0;
      if (i.status && (y && !b ? (i.status.fetchAborted = !0, i.status.fetchError = o.signal.reason, E && (i.status.fetchAbortIgnored = !0)) : i.status.fetchResolved = !0), y && !E && !b)
        return u(o.signal.reason);
      const S = d;
      return this.#i[t] === d && (f === void 0 ? S.__staleWhileFetching ? this.#i[t] = S.__staleWhileFetching : this.#F(e, "fetch") : (i.status && (i.status.fetchUpdated = !0), this.set(e, f, l.options))), f;
    }, h = (f) => (i.status && (i.status.fetchRejected = !0, i.status.fetchError = f), u(f)), u = (f) => {
      const { aborted: b } = o.signal, y = b && i.allowStaleOnFetchAbort, E = y || i.allowStaleOnFetchRejection, S = E || i.noDeleteOnFetchRejection, T = d;
      if (this.#i[t] === d && (!S || T.__staleWhileFetching === void 0 ? this.#F(e, "fetch") : y || (this.#i[t] = T.__staleWhileFetching)), E)
        return i.status && T.__staleWhileFetching !== void 0 && (i.status.returnedStale = !0), T.__staleWhileFetching;
      if (T.__returned === T)
        throw f;
    }, p = (f, b) => {
      const y = this.#E?.(e, r, l);
      y && y instanceof Promise && y.then((E) => f(E === void 0 ? void 0 : E), b), o.signal.addEventListener("abort", () => {
        (!i.ignoreFetchAbort || i.allowStaleOnFetchAbort) && (f(void 0), i.allowStaleOnFetchAbort && (f = (E) => c(E, !0)));
      });
    };
    i.status && (i.status.fetchDispatched = !0);
    const d = new Promise(p).then(c, h), m = Object.assign(d, {
      __abortController: o,
      __staleWhileFetching: r,
      __returned: void 0
    });
    return t === void 0 ? (this.set(e, m, { ...l.options, status: void 0 }), t = this.#l.get(e)) : this.#i[t] = m, m;
  }
  #h(e) {
    if (!this.#x)
      return !1;
    const t = e;
    return !!t && t instanceof Promise && t.hasOwnProperty("__staleWhileFetching") && t.__abortController instanceof zt;
  }
  async fetch(e, t = {}) {
    const {
      // get options
      allowStale: i = this.allowStale,
      updateAgeOnGet: n = this.updateAgeOnGet,
      noDeleteOnStaleGet: r = this.noDeleteOnStaleGet,
      // set options
      ttl: o = this.ttl,
      noDisposeOnSet: a = this.noDisposeOnSet,
      size: l = 0,
      sizeCalculation: c = this.sizeCalculation,
      noUpdateTTL: h = this.noUpdateTTL,
      // fetch exclusive options
      noDeleteOnFetchRejection: u = this.noDeleteOnFetchRejection,
      allowStaleOnFetchRejection: p = this.allowStaleOnFetchRejection,
      ignoreFetchAbort: d = this.ignoreFetchAbort,
      allowStaleOnFetchAbort: m = this.allowStaleOnFetchAbort,
      context: f,
      forceRefresh: b = !1,
      status: y,
      signal: E
    } = t;
    if (!this.#x)
      return y && (y.fetch = "get"), this.get(e, {
        allowStale: i,
        updateAgeOnGet: n,
        noDeleteOnStaleGet: r,
        status: y
      });
    const S = {
      allowStale: i,
      updateAgeOnGet: n,
      noDeleteOnStaleGet: r,
      ttl: o,
      noDisposeOnSet: a,
      size: l,
      sizeCalculation: c,
      noUpdateTTL: h,
      noDeleteOnFetchRejection: u,
      allowStaleOnFetchRejection: p,
      allowStaleOnFetchAbort: m,
      ignoreFetchAbort: d,
      status: y,
      signal: E
    };
    let T = this.#l.get(e);
    if (T === void 0) {
      y && (y.fetch = "miss");
      const O = this.#L(e, T, S, f);
      return O.__returned = O;
    } else {
      const O = this.#i[T];
      if (this.#h(O)) {
        const Te = i && O.__staleWhileFetching !== void 0;
        return y && (y.fetch = "inflight", Te && (y.returnedStale = !0)), Te ? O.__staleWhileFetching : O.__returned = O;
      }
      const v = this.#O(T);
      if (!b && !v)
        return y && (y.fetch = "hit"), this.#B(T), n && this.#D(T), y && this.#T(y, T), O;
      const P = this.#L(e, T, S, f), z = P.__staleWhileFetching !== void 0 && i;
      return y && (y.fetch = v ? "stale" : "refresh", z && v && (y.returnedStale = !0)), z ? P.__staleWhileFetching : P.__returned = P;
    }
  }
  async forceFetch(e, t = {}) {
    const i = await this.fetch(e, t);
    if (i === void 0)
      throw new Error("fetch() returned undefined");
    return i;
  }
  memo(e, t = {}) {
    const i = this.#w;
    if (!i)
      throw new Error("no memoMethod provided to constructor");
    const { context: n, forceRefresh: r, ...o } = t, a = this.get(e, o);
    if (!r && a !== void 0)
      return a;
    const l = i(e, a, {
      options: o,
      context: n
    });
    return this.set(e, l, o), l;
  }
  /**
   * Return a value from the cache. Will update the recency of the cache
   * entry found.
   *
   * If the key is not found, get() will return `undefined`.
   */
  get(e, t = {}) {
    const { allowStale: i = this.allowStale, updateAgeOnGet: n = this.updateAgeOnGet, noDeleteOnStaleGet: r = this.noDeleteOnStaleGet, status: o } = t, a = this.#l.get(e);
    if (a !== void 0) {
      const l = this.#i[a], c = this.#h(l);
      return o && this.#T(o, a), this.#O(a) ? (o && (o.get = "stale"), c ? (o && i && l.__staleWhileFetching !== void 0 && (o.returnedStale = !0), i ? l.__staleWhileFetching : void 0) : (r || this.#F(e, "expire"), o && i && (o.returnedStale = !0), i ? l : void 0)) : (o && (o.get = "hit"), c ? l.__staleWhileFetching : (this.#B(a), n && this.#D(a), l));
    } else o && (o.get = "miss");
  }
  #W(e, t) {
    this.#b[t] = e, this.#f[e] = t;
  }
  #B(e) {
    e !== this.#m && (e === this.#p ? this.#p = this.#f[e] : this.#W(this.#b[e], this.#f[e]), this.#W(this.#m, e), this.#m = e);
  }
  /**
   * Deletes a key out of the cache.
   *
   * Returns true if the key was deleted, false otherwise.
   */
  delete(e) {
    return this.#F(e, "delete");
  }
  #F(e, t) {
    let i = !1;
    if (this.#a !== 0) {
      const n = this.#l.get(e);
      if (n !== void 0)
        if (i = !0, this.#a === 1)
          this.#I(t);
        else {
          this.#k(n);
          const r = this.#i[n];
          if (this.#h(r) ? r.__abortController.abort(new Error("deleted")) : (this.#y || this.#d) && (this.#y && this.#s?.(r, e, t), this.#d && this.#g?.push([r, e, t])), this.#l.delete(e), this.#c[n] = void 0, this.#i[n] = void 0, n === this.#m)
            this.#m = this.#b[n];
          else if (n === this.#p)
            this.#p = this.#f[n];
          else {
            const o = this.#b[n];
            this.#f[o] = this.#f[n];
            const a = this.#f[n];
            this.#b[a] = this.#b[n];
          }
          this.#a--, this.#C.push(n);
        }
    }
    if (this.#d && this.#g?.length) {
      const n = this.#g;
      let r;
      for (; r = n?.shift(); )
        this.#o?.(...r);
    }
    return i;
  }
  /**
   * Clear the cache entirely, throwing away all values.
   */
  clear() {
    return this.#I("delete");
  }
  #I(e) {
    for (const t of this.#N({ allowStale: !0 })) {
      const i = this.#i[t];
      if (this.#h(i))
        i.__abortController.abort(new Error("deleted"));
      else {
        const n = this.#c[t];
        this.#y && this.#s?.(i, n, e), this.#d && this.#g?.push([i, n, e]);
      }
    }
    if (this.#l.clear(), this.#i.fill(void 0), this.#c.fill(void 0), this.#S && this.#R && (this.#S.fill(0), this.#R.fill(0)), this.#v && this.#v.fill(0), this.#p = 0, this.#m = 0, this.#C.length = 0, this.#u = 0, this.#a = 0, this.#d && this.#g) {
      const t = this.#g;
      let i;
      for (; i = t?.shift(); )
        this.#o?.(...i);
    }
  }
}
const tn = typeof process == "object" && process ? process : {
  stdout: null,
  stderr: null
}, Nh = (s) => !!s && typeof s == "object" && (s instanceof Wt || s instanceof Fn || _h(s) || Ph(s)), _h = (s) => !!s && typeof s == "object" && s instanceof Xs && typeof s.pipe == "function" && // node core Writable streams have a pipe() method, but it throws
s.pipe !== Fn.Writable.prototype.pipe, Ph = (s) => !!s && typeof s == "object" && s instanceof Xs && typeof s.write == "function" && typeof s.end == "function", de = Symbol("EOF"), pe = Symbol("maybeEmitEnd"), be = Symbol("emittedEnd"), Rt = Symbol("emittingEnd"), Xe = Symbol("emittedError"), Tt = Symbol("closed"), sn = Symbol("read"), Ot = Symbol("flush"), nn = Symbol("flushChunk"), ie = Symbol("encoding"), je = Symbol("decoder"), L = Symbol("flowing"), Ze = Symbol("paused"), Ue = Symbol("resume"), j = Symbol("buffer"), W = Symbol("pipes"), U = Symbol("bufferLength"), Es = Symbol("bufferPush"), xt = Symbol("bufferShift"), I = Symbol("objectMode"), F = Symbol("destroyed"), Ss = Symbol("error"), Rs = Symbol("emitData"), rn = Symbol("emitEnd"), Ts = Symbol("emitEnd2"), le = Symbol("async"), Os = Symbol("abort"), At = Symbol("aborted"), Ye = Symbol("signal"), Oe = Symbol("dataListeners"), Q = Symbol("discarded"), Qe = (s) => Promise.resolve().then(s), Fh = (s) => s(), Dh = (s) => s === "end" || s === "finish" || s === "prefinish", kh = (s) => s instanceof ArrayBuffer || !!s && typeof s == "object" && s.constructor && s.constructor.name === "ArrayBuffer" && s.byteLength >= 0, Lh = (s) => !Buffer.isBuffer(s) && ArrayBuffer.isView(s);
class Rr {
  src;
  dest;
  opts;
  ondrain;
  constructor(e, t, i) {
    this.src = e, this.dest = t, this.opts = i, this.ondrain = () => e[Ue](), this.dest.on("drain", this.ondrain);
  }
  unpipe() {
    this.dest.removeListener("drain", this.ondrain);
  }
  // only here for the prototype
  /* c8 ignore start */
  proxyErrors(e) {
  }
  /* c8 ignore stop */
  end() {
    this.unpipe(), this.opts.end && this.dest.end();
  }
}
class jh extends Rr {
  unpipe() {
    this.src.removeListener("error", this.proxyErrors), super.unpipe();
  }
  constructor(e, t, i) {
    super(e, t, i), this.proxyErrors = (n) => t.emit("error", n), e.on("error", this.proxyErrors);
  }
}
const Uh = (s) => !!s.objectMode, Bh = (s) => !s.objectMode && !!s.encoding && s.encoding !== "buffer";
class Wt extends Xs {
  [L] = !1;
  [Ze] = !1;
  [W] = [];
  [j] = [];
  [I];
  [ie];
  [le];
  [je];
  [de] = !1;
  [be] = !1;
  [Rt] = !1;
  [Tt] = !1;
  [Xe] = null;
  [U] = 0;
  [F] = !1;
  [Ye];
  [At] = !1;
  [Oe] = 0;
  [Q] = !1;
  /**
   * true if the stream can be written
   */
  writable = !0;
  /**
   * true if the stream can be read
   */
  readable = !0;
  /**
   * If `RType` is Buffer, then options do not need to be provided.
   * Otherwise, an options object must be provided to specify either
   * {@link Minipass.SharedOptions.objectMode} or
   * {@link Minipass.SharedOptions.encoding}, as appropriate.
   */
  constructor(...e) {
    const t = e[0] || {};
    if (super(), t.objectMode && typeof t.encoding == "string")
      throw new TypeError("Encoding and objectMode may not be used together");
    Uh(t) ? (this[I] = !0, this[ie] = null) : Bh(t) ? (this[ie] = t.encoding, this[I] = !1) : (this[I] = !1, this[ie] = null), this[le] = !!t.async, this[je] = this[ie] ? new oa(this[ie]) : null, t && t.debugExposeBuffer === !0 && Object.defineProperty(this, "buffer", { get: () => this[j] }), t && t.debugExposePipes === !0 && Object.defineProperty(this, "pipes", { get: () => this[W] });
    const { signal: i } = t;
    i && (this[Ye] = i, i.aborted ? this[Os]() : i.addEventListener("abort", () => this[Os]()));
  }
  /**
   * The amount of data stored in the buffer waiting to be read.
   *
   * For Buffer strings, this will be the total byte length.
   * For string encoding streams, this will be the string character length,
   * according to JavaScript's `string.length` logic.
   * For objectMode streams, this is a count of the items waiting to be
   * emitted.
   */
  get bufferLength() {
    return this[U];
  }
  /**
   * The `BufferEncoding` currently in use, or `null`
   */
  get encoding() {
    return this[ie];
  }
  /**
   * @deprecated - This is a read only property
   */
  set encoding(e) {
    throw new Error("Encoding must be set at instantiation time");
  }
  /**
   * @deprecated - Encoding may only be set at instantiation time
   */
  setEncoding(e) {
    throw new Error("Encoding must be set at instantiation time");
  }
  /**
   * True if this is an objectMode stream
   */
  get objectMode() {
    return this[I];
  }
  /**
   * @deprecated - This is a read-only property
   */
  set objectMode(e) {
    throw new Error("objectMode must be set at instantiation time");
  }
  /**
   * true if this is an async stream
   */
  get async() {
    return this[le];
  }
  /**
   * Set to true to make this stream async.
   *
   * Once set, it cannot be unset, as this would potentially cause incorrect
   * behavior.  Ie, a sync stream can be made async, but an async stream
   * cannot be safely made sync.
   */
  set async(e) {
    this[le] = this[le] || !!e;
  }
  // drop everything and get out of the flow completely
  [Os]() {
    this[At] = !0, this.emit("abort", this[Ye]?.reason), this.destroy(this[Ye]?.reason);
  }
  /**
   * True if the stream has been aborted.
   */
  get aborted() {
    return this[At];
  }
  /**
   * No-op setter. Stream aborted status is set via the AbortSignal provided
   * in the constructor options.
   */
  set aborted(e) {
  }
  write(e, t, i) {
    if (this[At])
      return !1;
    if (this[de])
      throw new Error("write after end");
    if (this[F])
      return this.emit("error", Object.assign(new Error("Cannot call write after a stream was destroyed"), { code: "ERR_STREAM_DESTROYED" })), !0;
    typeof t == "function" && (i = t, t = "utf8"), t || (t = "utf8");
    const n = this[le] ? Qe : Fh;
    if (!this[I] && !Buffer.isBuffer(e)) {
      if (Lh(e))
        e = Buffer.from(e.buffer, e.byteOffset, e.byteLength);
      else if (kh(e))
        e = Buffer.from(e);
      else if (typeof e != "string")
        throw new Error("Non-contiguous data written to non-objectMode stream");
    }
    return this[I] ? (this[L] && this[U] !== 0 && this[Ot](!0), this[L] ? this.emit("data", e) : this[Es](e), this[U] !== 0 && this.emit("readable"), i && n(i), this[L]) : e.length ? (typeof e == "string" && // unless it is a string already ready for us to use
    !(t === this[ie] && !this[je]?.lastNeed) && (e = Buffer.from(e, t)), Buffer.isBuffer(e) && this[ie] && (e = this[je].write(e)), this[L] && this[U] !== 0 && this[Ot](!0), this[L] ? this.emit("data", e) : this[Es](e), this[U] !== 0 && this.emit("readable"), i && n(i), this[L]) : (this[U] !== 0 && this.emit("readable"), i && n(i), this[L]);
  }
  /**
   * Low-level explicit read method.
   *
   * In objectMode, the argument is ignored, and one item is returned if
   * available.
   *
   * `n` is the number of bytes (or in the case of encoding streams,
   * characters) to consume. If `n` is not provided, then the entire buffer
   * is returned, or `null` is returned if no data is available.
   *
   * If `n` is greater that the amount of data in the internal buffer,
   * then `null` is returned.
   */
  read(e) {
    if (this[F])
      return null;
    if (this[Q] = !1, this[U] === 0 || e === 0 || e && e > this[U])
      return this[pe](), null;
    this[I] && (e = null), this[j].length > 1 && !this[I] && (this[j] = [
      this[ie] ? this[j].join("") : Buffer.concat(this[j], this[U])
    ]);
    const t = this[sn](e || null, this[j][0]);
    return this[pe](), t;
  }
  [sn](e, t) {
    if (this[I])
      this[xt]();
    else {
      const i = t;
      e === i.length || e === null ? this[xt]() : typeof i == "string" ? (this[j][0] = i.slice(e), t = i.slice(0, e), this[U] -= e) : (this[j][0] = i.subarray(e), t = i.subarray(0, e), this[U] -= e);
    }
    return this.emit("data", t), !this[j].length && !this[de] && this.emit("drain"), t;
  }
  end(e, t, i) {
    return typeof e == "function" && (i = e, e = void 0), typeof t == "function" && (i = t, t = "utf8"), e !== void 0 && this.write(e, t), i && this.once("end", i), this[de] = !0, this.writable = !1, (this[L] || !this[Ze]) && this[pe](), this;
  }
  // don't let the internal resume be overwritten
  [Ue]() {
    this[F] || (!this[Oe] && !this[W].length && (this[Q] = !0), this[Ze] = !1, this[L] = !0, this.emit("resume"), this[j].length ? this[Ot]() : this[de] ? this[pe]() : this.emit("drain"));
  }
  /**
   * Resume the stream if it is currently in a paused state
   *
   * If called when there are no pipe destinations or `data` event listeners,
   * this will place the stream in a "discarded" state, where all data will
   * be thrown away. The discarded state is removed if a pipe destination or
   * data handler is added, if pause() is called, or if any synchronous or
   * asynchronous iteration is started.
   */
  resume() {
    return this[Ue]();
  }
  /**
   * Pause the stream
   */
  pause() {
    this[L] = !1, this[Ze] = !0, this[Q] = !1;
  }
  /**
   * true if the stream has been forcibly destroyed
   */
  get destroyed() {
    return this[F];
  }
  /**
   * true if the stream is currently in a flowing state, meaning that
   * any writes will be immediately emitted.
   */
  get flowing() {
    return this[L];
  }
  /**
   * true if the stream is currently in a paused state
   */
  get paused() {
    return this[Ze];
  }
  [Es](e) {
    this[I] ? this[U] += 1 : this[U] += e.length, this[j].push(e);
  }
  [xt]() {
    return this[I] ? this[U] -= 1 : this[U] -= this[j][0].length, this[j].shift();
  }
  [Ot](e = !1) {
    do
      ;
    while (this[nn](this[xt]()) && this[j].length);
    !e && !this[j].length && !this[de] && this.emit("drain");
  }
  [nn](e) {
    return this.emit("data", e), this[L];
  }
  /**
   * Pipe all data emitted by this stream into the destination provided.
   *
   * Triggers the flow of data.
   */
  pipe(e, t) {
    if (this[F])
      return e;
    this[Q] = !1;
    const i = this[be];
    return t = t || {}, e === tn.stdout || e === tn.stderr ? t.end = !1 : t.end = t.end !== !1, t.proxyErrors = !!t.proxyErrors, i ? t.end && e.end() : (this[W].push(t.proxyErrors ? new jh(this, e, t) : new Rr(this, e, t)), this[le] ? Qe(() => this[Ue]()) : this[Ue]()), e;
  }
  /**
   * Fully unhook a piped destination stream.
   *
   * If the destination stream was the only consumer of this stream (ie,
   * there are no other piped destinations or `'data'` event listeners)
   * then the flow of data will stop until there is another consumer or
   * {@link Minipass#resume} is explicitly called.
   */
  unpipe(e) {
    const t = this[W].find((i) => i.dest === e);
    t && (this[W].length === 1 ? (this[L] && this[Oe] === 0 && (this[L] = !1), this[W] = []) : this[W].splice(this[W].indexOf(t), 1), t.unpipe());
  }
  /**
   * Alias for {@link Minipass#on}
   */
  addListener(e, t) {
    return this.on(e, t);
  }
  /**
   * Mostly identical to `EventEmitter.on`, with the following
   * behavior differences to prevent data loss and unnecessary hangs:
   *
   * - Adding a 'data' event handler will trigger the flow of data
   *
   * - Adding a 'readable' event handler when there is data waiting to be read
   *   will cause 'readable' to be emitted immediately.
   *
   * - Adding an 'endish' event handler ('end', 'finish', etc.) which has
   *   already passed will cause the event to be emitted immediately and all
   *   handlers removed.
   *
   * - Adding an 'error' event handler after an error has been emitted will
   *   cause the event to be re-emitted immediately with the error previously
   *   raised.
   */
  on(e, t) {
    const i = super.on(e, t);
    if (e === "data")
      this[Q] = !1, this[Oe]++, !this[W].length && !this[L] && this[Ue]();
    else if (e === "readable" && this[U] !== 0)
      super.emit("readable");
    else if (Dh(e) && this[be])
      super.emit(e), this.removeAllListeners(e);
    else if (e === "error" && this[Xe]) {
      const n = t;
      this[le] ? Qe(() => n.call(this, this[Xe])) : n.call(this, this[Xe]);
    }
    return i;
  }
  /**
   * Alias for {@link Minipass#off}
   */
  removeListener(e, t) {
    return this.off(e, t);
  }
  /**
   * Mostly identical to `EventEmitter.off`
   *
   * If a 'data' event handler is removed, and it was the last consumer
   * (ie, there are no pipe destinations or other 'data' event listeners),
   * then the flow of data will stop until there is another consumer or
   * {@link Minipass#resume} is explicitly called.
   */
  off(e, t) {
    const i = super.off(e, t);
    return e === "data" && (this[Oe] = this.listeners("data").length, this[Oe] === 0 && !this[Q] && !this[W].length && (this[L] = !1)), i;
  }
  /**
   * Mostly identical to `EventEmitter.removeAllListeners`
   *
   * If all 'data' event handlers are removed, and they were the last consumer
   * (ie, there are no pipe destinations), then the flow of data will stop
   * until there is another consumer or {@link Minipass#resume} is explicitly
   * called.
   */
  removeAllListeners(e) {
    const t = super.removeAllListeners(e);
    return (e === "data" || e === void 0) && (this[Oe] = 0, !this[Q] && !this[W].length && (this[L] = !1)), t;
  }
  /**
   * true if the 'end' event has been emitted
   */
  get emittedEnd() {
    return this[be];
  }
  [pe]() {
    !this[Rt] && !this[be] && !this[F] && this[j].length === 0 && this[de] && (this[Rt] = !0, this.emit("end"), this.emit("prefinish"), this.emit("finish"), this[Tt] && this.emit("close"), this[Rt] = !1);
  }
  /**
   * Mostly identical to `EventEmitter.emit`, with the following
   * behavior differences to prevent data loss and unnecessary hangs:
   *
   * If the stream has been destroyed, and the event is something other
   * than 'close' or 'error', then `false` is returned and no handlers
   * are called.
   *
   * If the event is 'end', and has already been emitted, then the event
   * is ignored. If the stream is in a paused or non-flowing state, then
   * the event will be deferred until data flow resumes. If the stream is
   * async, then handlers will be called on the next tick rather than
   * immediately.
   *
   * If the event is 'close', and 'end' has not yet been emitted, then
   * the event will be deferred until after 'end' is emitted.
   *
   * If the event is 'error', and an AbortSignal was provided for the stream,
   * and there are no listeners, then the event is ignored, matching the
   * behavior of node core streams in the presense of an AbortSignal.
   *
   * If the event is 'finish' or 'prefinish', then all listeners will be
   * removed after emitting the event, to prevent double-firing.
   */
  emit(e, ...t) {
    const i = t[0];
    if (e !== "error" && e !== "close" && e !== F && this[F])
      return !1;
    if (e === "data")
      return !this[I] && !i ? !1 : this[le] ? (Qe(() => this[Rs](i)), !0) : this[Rs](i);
    if (e === "end")
      return this[rn]();
    if (e === "close") {
      if (this[Tt] = !0, !this[be] && !this[F])
        return !1;
      const r = super.emit("close");
      return this.removeAllListeners("close"), r;
    } else if (e === "error") {
      this[Xe] = i, super.emit(Ss, i);
      const r = !this[Ye] || this.listeners("error").length ? super.emit("error", i) : !1;
      return this[pe](), r;
    } else if (e === "resume") {
      const r = super.emit("resume");
      return this[pe](), r;
    } else if (e === "finish" || e === "prefinish") {
      const r = super.emit(e);
      return this.removeAllListeners(e), r;
    }
    const n = super.emit(e, ...t);
    return this[pe](), n;
  }
  [Rs](e) {
    for (const i of this[W])
      i.dest.write(e) === !1 && this.pause();
    const t = this[Q] ? !1 : super.emit("data", e);
    return this[pe](), t;
  }
  [rn]() {
    return this[be] ? !1 : (this[be] = !0, this.readable = !1, this[le] ? (Qe(() => this[Ts]()), !0) : this[Ts]());
  }
  [Ts]() {
    if (this[je]) {
      const t = this[je].end();
      if (t) {
        for (const i of this[W])
          i.dest.write(t);
        this[Q] || super.emit("data", t);
      }
    }
    for (const t of this[W])
      t.end();
    const e = super.emit("end");
    return this.removeAllListeners("end"), e;
  }
  /**
   * Return a Promise that resolves to an array of all emitted data once
   * the stream ends.
   */
  async collect() {
    const e = Object.assign([], {
      dataLength: 0
    });
    this[I] || (e.dataLength = 0);
    const t = this.promise();
    return this.on("data", (i) => {
      e.push(i), this[I] || (e.dataLength += i.length);
    }), await t, e;
  }
  /**
   * Return a Promise that resolves to the concatenation of all emitted data
   * once the stream ends.
   *
   * Not allowed on objectMode streams.
   */
  async concat() {
    if (this[I])
      throw new Error("cannot concat in objectMode");
    const e = await this.collect();
    return this[ie] ? e.join("") : Buffer.concat(e, e.dataLength);
  }
  /**
   * Return a void Promise that resolves once the stream ends.
   */
  async promise() {
    return new Promise((e, t) => {
      this.on(F, () => t(new Error("stream destroyed"))), this.on("error", (i) => t(i)), this.on("end", () => e());
    });
  }
  /**
   * Asynchronous `for await of` iteration.
   *
   * This will continue emitting all chunks until the stream terminates.
   */
  [Symbol.asyncIterator]() {
    this[Q] = !1;
    let e = !1;
    const t = async () => (this.pause(), e = !0, { value: void 0, done: !0 });
    return {
      next: () => {
        if (e)
          return t();
        const n = this.read();
        if (n !== null)
          return Promise.resolve({ done: !1, value: n });
        if (this[de])
          return t();
        let r, o;
        const a = (u) => {
          this.off("data", l), this.off("end", c), this.off(F, h), t(), o(u);
        }, l = (u) => {
          this.off("error", a), this.off("end", c), this.off(F, h), this.pause(), r({ value: u, done: !!this[de] });
        }, c = () => {
          this.off("error", a), this.off("data", l), this.off(F, h), t(), r({ done: !0, value: void 0 });
        }, h = () => a(new Error("stream destroyed"));
        return new Promise((u, p) => {
          o = p, r = u, this.once(F, h), this.once("error", a), this.once("end", c), this.once("data", l);
        });
      },
      throw: t,
      return: t,
      [Symbol.asyncIterator]() {
        return this;
      }
    };
  }
  /**
   * Synchronous `for of` iteration.
   *
   * The iteration will terminate when the internal buffer runs out, even
   * if the stream has not yet terminated.
   */
  [Symbol.iterator]() {
    this[Q] = !1;
    let e = !1;
    const t = () => (this.pause(), this.off(Ss, t), this.off(F, t), this.off("end", t), e = !0, { done: !0, value: void 0 }), i = () => {
      if (e)
        return t();
      const n = this.read();
      return n === null ? t() : { done: !1, value: n };
    };
    return this.once("end", t), this.once(Ss, t), this.once(F, t), {
      next: i,
      throw: t,
      return: t,
      [Symbol.iterator]() {
        return this;
      }
    };
  }
  /**
   * Destroy a stream, preventing it from being used for any further purpose.
   *
   * If the stream has a `close()` method, then it will be called on
   * destruction.
   *
   * After destruction, any attempt to write data, read data, or emit most
   * events will be ignored.
   *
   * If an error argument is provided, then it will be emitted in an
   * 'error' event.
   */
  destroy(e) {
    if (this[F])
      return e ? this.emit("error", e) : this.emit(F), this;
    this[F] = !0, this[Q] = !0, this[j].length = 0, this[U] = 0;
    const t = this;
    return typeof t.close == "function" && !this[Tt] && t.close(), e ? this.emit("error", e) : this.emit(F), this;
  }
  /**
   * Alias for {@link isStream}
   *
   * Former export location, maintained for backwards compatibility.
   *
   * @deprecated
   */
  static get isStream() {
    return Nh;
  }
}
const Ih = ea.native, nt = {
  lstatSync: na,
  readdir: ia,
  readdirSync: sa,
  readlinkSync: ta,
  realpathSync: Ih,
  promises: {
    lstat: Qo,
    readdir: Yo,
    readlink: Zo,
    realpath: Xo
  }
}, Tr = (s) => !s || s === nt || s === ra ? nt : {
  ...nt,
  ...s,
  promises: {
    ...nt.promises,
    ...s.promises || {}
  }
}, Or = /^\\\\\?\\([a-z]:)\\?$/i, Mh = (s) => s.replace(/\//g, "\\").replace(Or, "$1\\"), $h = /[\\\/]/, se = 0, xr = 1, Ar = 2, ue = 4, Cr = 6, vr = 8, xe = 10, Nr = 12, te = 15, et = ~te, xs = 16, on = 32, rt = 64, ne = 128, Ct = 256, kt = 512, an = rt | ne | kt, zh = 1023, As = (s) => s.isFile() ? vr : s.isDirectory() ? ue : s.isSymbolicLink() ? xe : s.isCharacterDevice() ? Ar : s.isBlockDevice() ? Cr : s.isSocket() ? Nr : s.isFIFO() ? xr : se, ln = /* @__PURE__ */ new Map(), ot = (s) => {
  const e = ln.get(s);
  if (e)
    return e;
  const t = s.normalize("NFKD");
  return ln.set(s, t), t;
}, cn = /* @__PURE__ */ new Map(), vt = (s) => {
  const e = cn.get(s);
  if (e)
    return e;
  const t = ot(s.toLowerCase());
  return cn.set(s, t), t;
};
class hn extends ts {
  constructor() {
    super({ max: 256 });
  }
}
class Wh extends ts {
  constructor(e = 16 * 1024) {
    super({
      maxSize: e,
      // parent + children
      sizeCalculation: (t) => t.length + 1
    });
  }
}
const _r = Symbol("PathScurry setAsCwd");
class K {
  /**
   * the basename of this path
   *
   * **Important**: *always* test the path name against any test string
   * usingthe {@link isNamed} method, and not by directly comparing this
   * string. Otherwise, unicode path strings that the system sees as identical
   * will not be properly treated as the same path, leading to incorrect
   * behavior and possible security issues.
   */
  name;
  /**
   * the Path entry corresponding to the path root.
   *
   * @internal
   */
  root;
  /**
   * All roots found within the current PathScurry family
   *
   * @internal
   */
  roots;
  /**
   * a reference to the parent path, or undefined in the case of root entries
   *
   * @internal
   */
  parent;
  /**
   * boolean indicating whether paths are compared case-insensitively
   * @internal
   */
  nocase;
  /**
   * boolean indicating that this path is the current working directory
   * of the PathScurry collection that contains it.
   */
  isCWD = !1;
  // potential default fs override
  #e;
  // Stats fields
  #t;
  get dev() {
    return this.#t;
  }
  #s;
  get mode() {
    return this.#s;
  }
  #r;
  get nlink() {
    return this.#r;
  }
  #o;
  get uid() {
    return this.#o;
  }
  #E;
  get gid() {
    return this.#E;
  }
  #w;
  get rdev() {
    return this.#w;
  }
  #a;
  get blksize() {
    return this.#a;
  }
  #u;
  get ino() {
    return this.#u;
  }
  #l;
  get size() {
    return this.#l;
  }
  #c;
  get blocks() {
    return this.#c;
  }
  #i;
  get atimeMs() {
    return this.#i;
  }
  #f;
  get mtimeMs() {
    return this.#f;
  }
  #b;
  get ctimeMs() {
    return this.#b;
  }
  #p;
  get birthtimeMs() {
    return this.#p;
  }
  #m;
  get atime() {
    return this.#m;
  }
  #C;
  get mtime() {
    return this.#C;
  }
  #g;
  get ctime() {
    return this.#g;
  }
  #v;
  get birthtime() {
    return this.#v;
  }
  #R;
  #S;
  #y;
  #x;
  #d;
  #_;
  #n;
  #D;
  #T;
  #P;
  /**
   * This property is for compatibility with the Dirent class as of
   * Node v20, where Dirent['parentPath'] refers to the path of the
   * directory that was passed to readdir. For root entries, it's the path
   * to the entry itself.
   */
  get parentPath() {
    return (this.parent || this).fullpath();
  }
  /**
   * Deprecated alias for Dirent['parentPath'] Somewhat counterintuitively,
   * this property refers to the *parent* path, not the path object itself.
   *
   * @deprecated
   */
  get path() {
    return this.parentPath;
  }
  /**
   * Do not create new Path objects directly.  They should always be accessed
   * via the PathScurry class or other methods on the Path class.
   *
   * @internal
   */
  constructor(e, t = se, i, n, r, o, a) {
    this.name = e, this.#R = r ? vt(e) : ot(e), this.#n = t & zh, this.nocase = r, this.roots = n, this.root = i || this, this.#D = o, this.#y = a.fullpath, this.#d = a.relative, this.#_ = a.relativePosix, this.parent = a.parent, this.parent ? this.#e = this.parent.#e : this.#e = Tr(a.fs);
  }
  /**
   * Returns the depth of the Path object from its root.
   *
   * For example, a path at `/foo/bar` would have a depth of 2.
   */
  depth() {
    return this.#S !== void 0 ? this.#S : this.parent ? this.#S = this.parent.depth() + 1 : this.#S = 0;
  }
  /**
   * @internal
   */
  childrenCache() {
    return this.#D;
  }
  /**
   * Get the Path object referenced by the string path, resolved from this Path
   */
  resolve(e) {
    if (!e)
      return this;
    const t = this.getRootString(e), n = e.substring(t.length).split(this.splitSep);
    return t ? this.getRoot(t).#O(n) : this.#O(n);
  }
  #O(e) {
    let t = this;
    for (const i of e)
      t = t.child(i);
    return t;
  }
  /**
   * Returns the cached children Path objects, if still available.  If they
   * have fallen out of the cache, then returns an empty array, and resets the
   * READDIR_CALLED bit, so that future calls to readdir() will require an fs
   * lookup.
   *
   * @internal
   */
  children() {
    const e = this.#D.get(this);
    if (e)
      return e;
    const t = Object.assign([], { provisional: 0 });
    return this.#D.set(this, t), this.#n &= ~xs, t;
  }
  /**
   * Resolves a path portion and returns or creates the child Path.
   *
   * Returns `this` if pathPart is `''` or `'.'`, or `parent` if pathPart is
   * `'..'`.
   *
   * This should not be called directly.  If `pathPart` contains any path
   * separators, it will lead to unsafe undefined behavior.
   *
   * Use `Path.resolve()` instead.
   *
   * @internal
   */
  child(e, t) {
    if (e === "" || e === ".")
      return this;
    if (e === "..")
      return this.parent || this;
    const i = this.children(), n = this.nocase ? vt(e) : ot(e);
    for (const l of i)
      if (l.#R === n)
        return l;
    const r = this.parent ? this.sep : "", o = this.#y ? this.#y + r + e : void 0, a = this.newChild(e, se, {
      ...t,
      parent: this,
      fullpath: o
    });
    return this.canReaddir() || (a.#n |= ne), i.push(a), a;
  }
  /**
   * The relative path from the cwd. If it does not share an ancestor with
   * the cwd, then this ends up being equivalent to the fullpath()
   */
  relative() {
    if (this.isCWD)
      return "";
    if (this.#d !== void 0)
      return this.#d;
    const e = this.name, t = this.parent;
    if (!t)
      return this.#d = this.name;
    const i = t.relative();
    return i + (!i || !t.parent ? "" : this.sep) + e;
  }
  /**
   * The relative path from the cwd, using / as the path separator.
   * If it does not share an ancestor with
   * the cwd, then this ends up being equivalent to the fullpathPosix()
   * On posix systems, this is identical to relative().
   */
  relativePosix() {
    if (this.sep === "/")
      return this.relative();
    if (this.isCWD)
      return "";
    if (this.#_ !== void 0)
      return this.#_;
    const e = this.name, t = this.parent;
    if (!t)
      return this.#_ = this.fullpathPosix();
    const i = t.relativePosix();
    return i + (!i || !t.parent ? "" : "/") + e;
  }
  /**
   * The fully resolved path string for this Path entry
   */
  fullpath() {
    if (this.#y !== void 0)
      return this.#y;
    const e = this.name, t = this.parent;
    if (!t)
      return this.#y = this.name;
    const n = t.fullpath() + (t.parent ? this.sep : "") + e;
    return this.#y = n;
  }
  /**
   * On platforms other than windows, this is identical to fullpath.
   *
   * On windows, this is overridden to return the forward-slash form of the
   * full UNC path.
   */
  fullpathPosix() {
    if (this.#x !== void 0)
      return this.#x;
    if (this.sep === "/")
      return this.#x = this.fullpath();
    if (!this.parent) {
      const n = this.fullpath().replace(/\\/g, "/");
      return /^[a-z]:\//i.test(n) ? this.#x = `//?/${n}` : this.#x = n;
    }
    const e = this.parent, t = e.fullpathPosix(), i = t + (!t || !e.parent ? "" : "/") + this.name;
    return this.#x = i;
  }
  /**
   * Is the Path of an unknown type?
   *
   * Note that we might know *something* about it if there has been a previous
   * filesystem operation, for example that it does not exist, or is not a
   * link, or whether it has child entries.
   */
  isUnknown() {
    return (this.#n & te) === se;
  }
  isType(e) {
    return this[`is${e}`]();
  }
  getType() {
    return this.isUnknown() ? "Unknown" : this.isDirectory() ? "Directory" : this.isFile() ? "File" : this.isSymbolicLink() ? "SymbolicLink" : this.isFIFO() ? "FIFO" : this.isCharacterDevice() ? "CharacterDevice" : this.isBlockDevice() ? "BlockDevice" : (
      /* c8 ignore start */
      this.isSocket() ? "Socket" : "Unknown"
    );
  }
  /**
   * Is the Path a regular file?
   */
  isFile() {
    return (this.#n & te) === vr;
  }
  /**
   * Is the Path a directory?
   */
  isDirectory() {
    return (this.#n & te) === ue;
  }
  /**
   * Is the path a character device?
   */
  isCharacterDevice() {
    return (this.#n & te) === Ar;
  }
  /**
   * Is the path a block device?
   */
  isBlockDevice() {
    return (this.#n & te) === Cr;
  }
  /**
   * Is the path a FIFO pipe?
   */
  isFIFO() {
    return (this.#n & te) === xr;
  }
  /**
   * Is the path a socket?
   */
  isSocket() {
    return (this.#n & te) === Nr;
  }
  /**
   * Is the path a symbolic link?
   */
  isSymbolicLink() {
    return (this.#n & xe) === xe;
  }
  /**
   * Return the entry if it has been subject of a successful lstat, or
   * undefined otherwise.
   *
   * Does not read the filesystem, so an undefined result *could* simply
   * mean that we haven't called lstat on it.
   */
  lstatCached() {
    return this.#n & on ? this : void 0;
  }
  /**
   * Return the cached link target if the entry has been the subject of a
   * successful readlink, or undefined otherwise.
   *
   * Does not read the filesystem, so an undefined result *could* just mean we
   * don't have any cached data. Only use it if you are very sure that a
   * readlink() has been called at some point.
   */
  readlinkCached() {
    return this.#T;
  }
  /**
   * Returns the cached realpath target if the entry has been the subject
   * of a successful realpath, or undefined otherwise.
   *
   * Does not read the filesystem, so an undefined result *could* just mean we
   * don't have any cached data. Only use it if you are very sure that a
   * realpath() has been called at some point.
   */
  realpathCached() {
    return this.#P;
  }
  /**
   * Returns the cached child Path entries array if the entry has been the
   * subject of a successful readdir(), or [] otherwise.
   *
   * Does not read the filesystem, so an empty array *could* just mean we
   * don't have any cached data. Only use it if you are very sure that a
   * readdir() has been called recently enough to still be valid.
   */
  readdirCached() {
    const e = this.children();
    return e.slice(0, e.provisional);
  }
  /**
   * Return true if it's worth trying to readlink.  Ie, we don't (yet) have
   * any indication that readlink will definitely fail.
   *
   * Returns false if the path is known to not be a symlink, if a previous
   * readlink failed, or if the entry does not exist.
   */
  canReadlink() {
    if (this.#T)
      return !0;
    if (!this.parent)
      return !1;
    const e = this.#n & te;
    return !(e !== se && e !== xe || this.#n & Ct || this.#n & ne);
  }
  /**
   * Return true if readdir has previously been successfully called on this
   * path, indicating that cachedReaddir() is likely valid.
   */
  calledReaddir() {
    return !!(this.#n & xs);
  }
  /**
   * Returns true if the path is known to not exist. That is, a previous lstat
   * or readdir failed to verify its existence when that would have been
   * expected, or a parent entry was marked either enoent or enotdir.
   */
  isENOENT() {
    return !!(this.#n & ne);
  }
  /**
   * Return true if the path is a match for the given path name.  This handles
   * case sensitivity and unicode normalization.
   *
   * Note: even on case-sensitive systems, it is **not** safe to test the
   * equality of the `.name` property to determine whether a given pathname
   * matches, due to unicode normalization mismatches.
   *
   * Always use this method instead of testing the `path.name` property
   * directly.
   */
  isNamed(e) {
    return this.nocase ? this.#R === vt(e) : this.#R === ot(e);
  }
  /**
   * Return the Path object corresponding to the target of a symbolic link.
   *
   * If the Path is not a symbolic link, or if the readlink call fails for any
   * reason, `undefined` is returned.
   *
   * Result is cached, and thus may be outdated if the filesystem is mutated.
   */
  async readlink() {
    const e = this.#T;
    if (e)
      return e;
    if (this.canReadlink() && this.parent)
      try {
        const t = await this.#e.promises.readlink(this.fullpath()), i = (await this.parent.realpath())?.resolve(t);
        if (i)
          return this.#T = i;
      } catch (t) {
        this.#U(t.code);
        return;
      }
  }
  /**
   * Synchronous {@link PathBase.readlink}
   */
  readlinkSync() {
    const e = this.#T;
    if (e)
      return e;
    if (this.canReadlink() && this.parent)
      try {
        const t = this.#e.readlinkSync(this.fullpath()), i = this.parent.realpathSync()?.resolve(t);
        if (i)
          return this.#T = i;
      } catch (t) {
        this.#U(t.code);
        return;
      }
  }
  #M(e) {
    this.#n |= xs;
    for (let t = e.provisional; t < e.length; t++) {
      const i = e[t];
      i && i.#k();
    }
  }
  #k() {
    this.#n & ne || (this.#n = (this.#n | ne) & et, this.#j());
  }
  #j() {
    const e = this.children();
    e.provisional = 0;
    for (const t of e)
      t.#k();
  }
  #$() {
    this.#n |= kt, this.#A();
  }
  // save the information when we know the entry is not a dir
  #A() {
    if (this.#n & rt)
      return;
    let e = this.#n;
    (e & te) === ue && (e &= et), this.#n = e | rt, this.#j();
  }
  #N(e = "") {
    e === "ENOTDIR" || e === "EPERM" ? this.#A() : e === "ENOENT" ? this.#k() : this.children().provisional = 0;
  }
  #z(e = "") {
    e === "ENOTDIR" ? this.parent.#A() : e === "ENOENT" && this.#k();
  }
  #U(e = "") {
    let t = this.#n;
    t |= Ct, e === "ENOENT" && (t |= ne), (e === "EINVAL" || e === "UNKNOWN") && (t &= et), this.#n = t, e === "ENOTDIR" && this.parent && this.parent.#A();
  }
  #L(e, t) {
    return this.#W(e, t) || this.#h(e, t);
  }
  #h(e, t) {
    const i = As(e), n = this.newChild(e.name, i, { parent: this }), r = n.#n & te;
    return r !== ue && r !== xe && r !== se && (n.#n |= rt), t.unshift(n), t.provisional++, n;
  }
  #W(e, t) {
    for (let i = t.provisional; i < t.length; i++) {
      const n = t[i];
      if ((this.nocase ? vt(e.name) : ot(e.name)) === n.#R)
        return this.#B(e, n, i, t);
    }
  }
  #B(e, t, i, n) {
    const r = t.name;
    return t.#n = t.#n & et | As(e), r !== e.name && (t.name = e.name), i !== n.provisional && (i === n.length - 1 ? n.pop() : n.splice(i, 1), n.unshift(t)), n.provisional++, t;
  }
  /**
   * Call lstat() on this Path, and update all known information that can be
   * determined.
   *
   * Note that unlike `fs.lstat()`, the returned value does not contain some
   * information, such as `mode`, `dev`, `nlink`, and `ino`.  If that
   * information is required, you will need to call `fs.lstat` yourself.
   *
   * If the Path refers to a nonexistent file, or if the lstat call fails for
   * any reason, `undefined` is returned.  Otherwise the updated Path object is
   * returned.
   *
   * Results are cached, and thus may be out of date if the filesystem is
   * mutated.
   */
  async lstat() {
    if ((this.#n & ne) === 0)
      try {
        return this.#F(await this.#e.promises.lstat(this.fullpath())), this;
      } catch (e) {
        this.#z(e.code);
      }
  }
  /**
   * synchronous {@link PathBase.lstat}
   */
  lstatSync() {
    if ((this.#n & ne) === 0)
      try {
        return this.#F(this.#e.lstatSync(this.fullpath())), this;
      } catch (e) {
        this.#z(e.code);
      }
  }
  #F(e) {
    const { atime: t, atimeMs: i, birthtime: n, birthtimeMs: r, blksize: o, blocks: a, ctime: l, ctimeMs: c, dev: h, gid: u, ino: p, mode: d, mtime: m, mtimeMs: f, nlink: b, rdev: y, size: E, uid: S } = e;
    this.#m = t, this.#i = i, this.#v = n, this.#p = r, this.#a = o, this.#c = a, this.#g = l, this.#b = c, this.#t = h, this.#E = u, this.#u = p, this.#s = d, this.#C = m, this.#f = f, this.#r = b, this.#w = y, this.#l = E, this.#o = S;
    const T = As(e);
    this.#n = this.#n & et | T | on, T !== se && T !== ue && T !== xe && (this.#n |= rt);
  }
  #I = [];
  #H = !1;
  #G(e) {
    this.#H = !1;
    const t = this.#I.slice();
    this.#I.length = 0, t.forEach((i) => i(null, e));
  }
  /**
   * Standard node-style callback interface to get list of directory entries.
   *
   * If the Path cannot or does not contain any children, then an empty array
   * is returned.
   *
   * Results are cached, and thus may be out of date if the filesystem is
   * mutated.
   *
   * @param cb The callback called with (er, entries).  Note that the `er`
   * param is somewhat extraneous, as all readdir() errors are handled and
   * simply result in an empty set of entries being returned.
   * @param allowZalgo Boolean indicating that immediately known results should
   * *not* be deferred with `queueMicrotask`. Defaults to `false`. Release
   * zalgo at your peril, the dark pony lord is devious and unforgiving.
   */
  readdirCB(e, t = !1) {
    if (!this.canReaddir()) {
      t ? e(null, []) : queueMicrotask(() => e(null, []));
      return;
    }
    const i = this.children();
    if (this.calledReaddir()) {
      const r = i.slice(0, i.provisional);
      t ? e(null, r) : queueMicrotask(() => e(null, r));
      return;
    }
    if (this.#I.push(e), this.#H)
      return;
    this.#H = !0;
    const n = this.fullpath();
    this.#e.readdir(n, { withFileTypes: !0 }, (r, o) => {
      if (r)
        this.#N(r.code), i.provisional = 0;
      else {
        for (const a of o)
          this.#L(a, i);
        this.#M(i);
      }
      this.#G(i.slice(0, i.provisional));
    });
  }
  #q;
  /**
   * Return an array of known child entries.
   *
   * If the Path cannot or does not contain any children, then an empty array
   * is returned.
   *
   * Results are cached, and thus may be out of date if the filesystem is
   * mutated.
   */
  async readdir() {
    if (!this.canReaddir())
      return [];
    const e = this.children();
    if (this.calledReaddir())
      return e.slice(0, e.provisional);
    const t = this.fullpath();
    if (this.#q)
      await this.#q;
    else {
      let i = () => {
      };
      this.#q = new Promise((n) => i = n);
      try {
        for (const n of await this.#e.promises.readdir(t, {
          withFileTypes: !0
        }))
          this.#L(n, e);
        this.#M(e);
      } catch (n) {
        this.#N(n.code), e.provisional = 0;
      }
      this.#q = void 0, i();
    }
    return e.slice(0, e.provisional);
  }
  /**
   * synchronous {@link PathBase.readdir}
   */
  readdirSync() {
    if (!this.canReaddir())
      return [];
    const e = this.children();
    if (this.calledReaddir())
      return e.slice(0, e.provisional);
    const t = this.fullpath();
    try {
      for (const i of this.#e.readdirSync(t, {
        withFileTypes: !0
      }))
        this.#L(i, e);
      this.#M(e);
    } catch (i) {
      this.#N(i.code), e.provisional = 0;
    }
    return e.slice(0, e.provisional);
  }
  canReaddir() {
    if (this.#n & an)
      return !1;
    const e = te & this.#n;
    return e === se || e === ue || e === xe;
  }
  shouldWalk(e, t) {
    return (this.#n & ue) === ue && !(this.#n & an) && !e.has(this) && (!t || t(this));
  }
  /**
   * Return the Path object corresponding to path as resolved
   * by realpath(3).
   *
   * If the realpath call fails for any reason, `undefined` is returned.
   *
   * Result is cached, and thus may be outdated if the filesystem is mutated.
   * On success, returns a Path object.
   */
  async realpath() {
    if (this.#P)
      return this.#P;
    if (!((kt | Ct | ne) & this.#n))
      try {
        const e = await this.#e.promises.realpath(this.fullpath());
        return this.#P = this.resolve(e);
      } catch {
        this.#$();
      }
  }
  /**
   * Synchronous {@link realpath}
   */
  realpathSync() {
    if (this.#P)
      return this.#P;
    if (!((kt | Ct | ne) & this.#n))
      try {
        const e = this.#e.realpathSync(this.fullpath());
        return this.#P = this.resolve(e);
      } catch {
        this.#$();
      }
  }
  /**
   * Internal method to mark this Path object as the scurry cwd,
   * called by {@link PathScurry#chdir}
   *
   * @internal
   */
  [_r](e) {
    if (e === this)
      return;
    e.isCWD = !1, this.isCWD = !0;
    const t = /* @__PURE__ */ new Set([]);
    let i = [], n = this;
    for (; n && n.parent; )
      t.add(n), n.#d = i.join(this.sep), n.#_ = i.join("/"), n = n.parent, i.push("..");
    for (n = e; n && n.parent && !t.has(n); )
      n.#d = void 0, n.#_ = void 0, n = n.parent;
  }
}
class ss extends K {
  /**
   * Separator for generating path strings.
   */
  sep = "\\";
  /**
   * Separator for parsing path strings.
   */
  splitSep = $h;
  /**
   * Do not create new Path objects directly.  They should always be accessed
   * via the PathScurry class or other methods on the Path class.
   *
   * @internal
   */
  constructor(e, t = se, i, n, r, o, a) {
    super(e, t, i, n, r, o, a);
  }
  /**
   * @internal
   */
  newChild(e, t = se, i = {}) {
    return new ss(e, t, this.root, this.roots, this.nocase, this.childrenCache(), i);
  }
  /**
   * @internal
   */
  getRootString(e) {
    return Ps.parse(e).root;
  }
  /**
   * @internal
   */
  getRoot(e) {
    if (e = Mh(e.toUpperCase()), e === this.root.name)
      return this.root;
    for (const [t, i] of Object.entries(this.roots))
      if (this.sameRoot(e, t))
        return this.roots[e] = i;
    return this.roots[e] = new oi(e, this).root;
  }
  /**
   * @internal
   */
  sameRoot(e, t = this.root.name) {
    return e = e.toUpperCase().replace(/\//g, "\\").replace(Or, "$1\\"), e === t;
  }
}
class is extends K {
  /**
   * separator for parsing path strings
   */
  splitSep = "/";
  /**
   * separator for generating path strings
   */
  sep = "/";
  /**
   * Do not create new Path objects directly.  They should always be accessed
   * via the PathScurry class or other methods on the Path class.
   *
   * @internal
   */
  constructor(e, t = se, i, n, r, o, a) {
    super(e, t, i, n, r, o, a);
  }
  /**
   * @internal
   */
  getRootString(e) {
    return e.startsWith("/") ? "/" : "";
  }
  /**
   * @internal
   */
  getRoot(e) {
    return this.root;
  }
  /**
   * @internal
   */
  newChild(e, t = se, i = {}) {
    return new is(e, t, this.root, this.roots, this.nocase, this.childrenCache(), i);
  }
}
class Pr {
  /**
   * The root Path entry for the current working directory of this Scurry
   */
  root;
  /**
   * The string path for the root of this Scurry's current working directory
   */
  rootPath;
  /**
   * A collection of all roots encountered, referenced by rootPath
   */
  roots;
  /**
   * The Path entry corresponding to this PathScurry's current working directory.
   */
  cwd;
  #e;
  #t;
  #s;
  /**
   * Perform path comparisons case-insensitively.
   *
   * Defaults true on Darwin and Windows systems, false elsewhere.
   */
  nocase;
  #r;
  /**
   * This class should not be instantiated directly.
   *
   * Use PathScurryWin32, PathScurryDarwin, PathScurryPosix, or PathScurry
   *
   * @internal
   */
  constructor(e = process.cwd(), t, i, { nocase: n, childrenCacheSize: r = 16 * 1024, fs: o = nt } = {}) {
    this.#r = Tr(o), (e instanceof URL || e.startsWith("file://")) && (e = Pn(e));
    const a = t.resolve(e);
    this.roots = /* @__PURE__ */ Object.create(null), this.rootPath = this.parseRootPath(a), this.#e = new hn(), this.#t = new hn(), this.#s = new Wh(r);
    const l = a.substring(this.rootPath.length).split(i);
    if (l.length === 1 && !l[0] && l.pop(), n === void 0)
      throw new TypeError("must provide nocase setting to PathScurryBase ctor");
    this.nocase = n, this.root = this.newRoot(this.#r), this.roots[this.rootPath] = this.root;
    let c = this.root, h = l.length - 1;
    const u = t.sep;
    let p = this.rootPath, d = !1;
    for (const m of l) {
      const f = h--;
      c = c.child(m, {
        relative: new Array(f).fill("..").join(u),
        relativePosix: new Array(f).fill("..").join("/"),
        fullpath: p += (d ? "" : u) + m
      }), d = !0;
    }
    this.cwd = c;
  }
  /**
   * Get the depth of a provided path, string, or the cwd
   */
  depth(e = this.cwd) {
    return typeof e == "string" && (e = this.cwd.resolve(e)), e.depth();
  }
  /**
   * Return the cache of child entries.  Exposed so subclasses can create
   * child Path objects in a platform-specific way.
   *
   * @internal
   */
  childrenCache() {
    return this.#s;
  }
  /**
   * Resolve one or more path strings to a resolved string
   *
   * Same interface as require('path').resolve.
   *
   * Much faster than path.resolve() when called multiple times for the same
   * path, because the resolved Path objects are cached.  Much slower
   * otherwise.
   */
  resolve(...e) {
    let t = "";
    for (let r = e.length - 1; r >= 0; r--) {
      const o = e[r];
      if (!(!o || o === ".") && (t = t ? `${o}/${t}` : o, this.isAbsolute(o)))
        break;
    }
    const i = this.#e.get(t);
    if (i !== void 0)
      return i;
    const n = this.cwd.resolve(t).fullpath();
    return this.#e.set(t, n), n;
  }
  /**
   * Resolve one or more path strings to a resolved string, returning
   * the posix path.  Identical to .resolve() on posix systems, but on
   * windows will return a forward-slash separated UNC path.
   *
   * Same interface as require('path').resolve.
   *
   * Much faster than path.resolve() when called multiple times for the same
   * path, because the resolved Path objects are cached.  Much slower
   * otherwise.
   */
  resolvePosix(...e) {
    let t = "";
    for (let r = e.length - 1; r >= 0; r--) {
      const o = e[r];
      if (!(!o || o === ".") && (t = t ? `${o}/${t}` : o, this.isAbsolute(o)))
        break;
    }
    const i = this.#t.get(t);
    if (i !== void 0)
      return i;
    const n = this.cwd.resolve(t).fullpathPosix();
    return this.#t.set(t, n), n;
  }
  /**
   * find the relative path from the cwd to the supplied path string or entry
   */
  relative(e = this.cwd) {
    return typeof e == "string" && (e = this.cwd.resolve(e)), e.relative();
  }
  /**
   * find the relative path from the cwd to the supplied path string or
   * entry, using / as the path delimiter, even on Windows.
   */
  relativePosix(e = this.cwd) {
    return typeof e == "string" && (e = this.cwd.resolve(e)), e.relativePosix();
  }
  /**
   * Return the basename for the provided string or Path object
   */
  basename(e = this.cwd) {
    return typeof e == "string" && (e = this.cwd.resolve(e)), e.name;
  }
  /**
   * Return the dirname for the provided string or Path object
   */
  dirname(e = this.cwd) {
    return typeof e == "string" && (e = this.cwd.resolve(e)), (e.parent || e).fullpath();
  }
  async readdir(e = this.cwd, t = {
    withFileTypes: !0
  }) {
    typeof e == "string" ? e = this.cwd.resolve(e) : e instanceof K || (t = e, e = this.cwd);
    const { withFileTypes: i } = t;
    if (e.canReaddir()) {
      const n = await e.readdir();
      return i ? n : n.map((r) => r.name);
    } else
      return [];
  }
  readdirSync(e = this.cwd, t = {
    withFileTypes: !0
  }) {
    typeof e == "string" ? e = this.cwd.resolve(e) : e instanceof K || (t = e, e = this.cwd);
    const { withFileTypes: i = !0 } = t;
    return e.canReaddir() ? i ? e.readdirSync() : e.readdirSync().map((n) => n.name) : [];
  }
  /**
   * Call lstat() on the string or Path object, and update all known
   * information that can be determined.
   *
   * Note that unlike `fs.lstat()`, the returned value does not contain some
   * information, such as `mode`, `dev`, `nlink`, and `ino`.  If that
   * information is required, you will need to call `fs.lstat` yourself.
   *
   * If the Path refers to a nonexistent file, or if the lstat call fails for
   * any reason, `undefined` is returned.  Otherwise the updated Path object is
   * returned.
   *
   * Results are cached, and thus may be out of date if the filesystem is
   * mutated.
   */
  async lstat(e = this.cwd) {
    return typeof e == "string" && (e = this.cwd.resolve(e)), e.lstat();
  }
  /**
   * synchronous {@link PathScurryBase.lstat}
   */
  lstatSync(e = this.cwd) {
    return typeof e == "string" && (e = this.cwd.resolve(e)), e.lstatSync();
  }
  async readlink(e = this.cwd, { withFileTypes: t } = {
    withFileTypes: !1
  }) {
    typeof e == "string" ? e = this.cwd.resolve(e) : e instanceof K || (t = e.withFileTypes, e = this.cwd);
    const i = await e.readlink();
    return t ? i : i?.fullpath();
  }
  readlinkSync(e = this.cwd, { withFileTypes: t } = {
    withFileTypes: !1
  }) {
    typeof e == "string" ? e = this.cwd.resolve(e) : e instanceof K || (t = e.withFileTypes, e = this.cwd);
    const i = e.readlinkSync();
    return t ? i : i?.fullpath();
  }
  async realpath(e = this.cwd, { withFileTypes: t } = {
    withFileTypes: !1
  }) {
    typeof e == "string" ? e = this.cwd.resolve(e) : e instanceof K || (t = e.withFileTypes, e = this.cwd);
    const i = await e.realpath();
    return t ? i : i?.fullpath();
  }
  realpathSync(e = this.cwd, { withFileTypes: t } = {
    withFileTypes: !1
  }) {
    typeof e == "string" ? e = this.cwd.resolve(e) : e instanceof K || (t = e.withFileTypes, e = this.cwd);
    const i = e.realpathSync();
    return t ? i : i?.fullpath();
  }
  async walk(e = this.cwd, t = {}) {
    typeof e == "string" ? e = this.cwd.resolve(e) : e instanceof K || (t = e, e = this.cwd);
    const { withFileTypes: i = !0, follow: n = !1, filter: r, walkFilter: o } = t, a = [];
    (!r || r(e)) && a.push(i ? e : e.fullpath());
    const l = /* @__PURE__ */ new Set(), c = (u, p) => {
      l.add(u), u.readdirCB((d, m) => {
        if (d)
          return p(d);
        let f = m.length;
        if (!f)
          return p();
        const b = () => {
          --f === 0 && p();
        };
        for (const y of m)
          (!r || r(y)) && a.push(i ? y : y.fullpath()), n && y.isSymbolicLink() ? y.realpath().then((E) => E?.isUnknown() ? E.lstat() : E).then((E) => E?.shouldWalk(l, o) ? c(E, b) : b()) : y.shouldWalk(l, o) ? c(y, b) : b();
      }, !0);
    }, h = e;
    return new Promise((u, p) => {
      c(h, (d) => {
        if (d)
          return p(d);
        u(a);
      });
    });
  }
  walkSync(e = this.cwd, t = {}) {
    typeof e == "string" ? e = this.cwd.resolve(e) : e instanceof K || (t = e, e = this.cwd);
    const { withFileTypes: i = !0, follow: n = !1, filter: r, walkFilter: o } = t, a = [];
    (!r || r(e)) && a.push(i ? e : e.fullpath());
    const l = /* @__PURE__ */ new Set([e]);
    for (const c of l) {
      const h = c.readdirSync();
      for (const u of h) {
        (!r || r(u)) && a.push(i ? u : u.fullpath());
        let p = u;
        if (u.isSymbolicLink()) {
          if (!(n && (p = u.realpathSync())))
            continue;
          p.isUnknown() && p.lstatSync();
        }
        p.shouldWalk(l, o) && l.add(p);
      }
    }
    return a;
  }
  /**
   * Support for `for await`
   *
   * Alias for {@link PathScurryBase.iterate}
   *
   * Note: As of Node 19, this is very slow, compared to other methods of
   * walking.  Consider using {@link PathScurryBase.stream} if memory overhead
   * and backpressure are concerns, or {@link PathScurryBase.walk} if not.
   */
  [Symbol.asyncIterator]() {
    return this.iterate();
  }
  iterate(e = this.cwd, t = {}) {
    return typeof e == "string" ? e = this.cwd.resolve(e) : e instanceof K || (t = e, e = this.cwd), this.stream(e, t)[Symbol.asyncIterator]();
  }
  /**
   * Iterating over a PathScurry performs a synchronous walk.
   *
   * Alias for {@link PathScurryBase.iterateSync}
   */
  [Symbol.iterator]() {
    return this.iterateSync();
  }
  *iterateSync(e = this.cwd, t = {}) {
    typeof e == "string" ? e = this.cwd.resolve(e) : e instanceof K || (t = e, e = this.cwd);
    const { withFileTypes: i = !0, follow: n = !1, filter: r, walkFilter: o } = t;
    (!r || r(e)) && (yield i ? e : e.fullpath());
    const a = /* @__PURE__ */ new Set([e]);
    for (const l of a) {
      const c = l.readdirSync();
      for (const h of c) {
        (!r || r(h)) && (yield i ? h : h.fullpath());
        let u = h;
        if (h.isSymbolicLink()) {
          if (!(n && (u = h.realpathSync())))
            continue;
          u.isUnknown() && u.lstatSync();
        }
        u.shouldWalk(a, o) && a.add(u);
      }
    }
  }
  stream(e = this.cwd, t = {}) {
    typeof e == "string" ? e = this.cwd.resolve(e) : e instanceof K || (t = e, e = this.cwd);
    const { withFileTypes: i = !0, follow: n = !1, filter: r, walkFilter: o } = t, a = new Wt({ objectMode: !0 });
    (!r || r(e)) && a.write(i ? e : e.fullpath());
    const l = /* @__PURE__ */ new Set(), c = [e];
    let h = 0;
    const u = () => {
      let p = !1;
      for (; !p; ) {
        const d = c.shift();
        if (!d) {
          h === 0 && a.end();
          return;
        }
        h++, l.add(d);
        const m = (b, y, E = !1) => {
          if (b)
            return a.emit("error", b);
          if (n && !E) {
            const S = [];
            for (const T of y)
              T.isSymbolicLink() && S.push(T.realpath().then((O) => O?.isUnknown() ? O.lstat() : O));
            if (S.length) {
              Promise.all(S).then(() => m(null, y, !0));
              return;
            }
          }
          for (const S of y)
            S && (!r || r(S)) && (a.write(i ? S : S.fullpath()) || (p = !0));
          h--;
          for (const S of y) {
            const T = S.realpathCached() || S;
            T.shouldWalk(l, o) && c.push(T);
          }
          p && !a.flowing ? a.once("drain", u) : f || u();
        };
        let f = !0;
        d.readdirCB(m, !0), f = !1;
      }
    };
    return u(), a;
  }
  streamSync(e = this.cwd, t = {}) {
    typeof e == "string" ? e = this.cwd.resolve(e) : e instanceof K || (t = e, e = this.cwd);
    const { withFileTypes: i = !0, follow: n = !1, filter: r, walkFilter: o } = t, a = new Wt({ objectMode: !0 }), l = /* @__PURE__ */ new Set();
    (!r || r(e)) && a.write(i ? e : e.fullpath());
    const c = [e];
    let h = 0;
    const u = () => {
      let p = !1;
      for (; !p; ) {
        const d = c.shift();
        if (!d) {
          h === 0 && a.end();
          return;
        }
        h++, l.add(d);
        const m = d.readdirSync();
        for (const f of m)
          (!r || r(f)) && (a.write(i ? f : f.fullpath()) || (p = !0));
        h--;
        for (const f of m) {
          let b = f;
          if (f.isSymbolicLink()) {
            if (!(n && (b = f.realpathSync())))
              continue;
            b.isUnknown() && b.lstatSync();
          }
          b.shouldWalk(l, o) && c.push(b);
        }
      }
      p && !a.flowing && a.once("drain", u);
    };
    return u(), a;
  }
  chdir(e = this.cwd) {
    const t = this.cwd;
    this.cwd = typeof e == "string" ? this.cwd.resolve(e) : e, this.cwd[_r](t);
  }
}
class oi extends Pr {
  /**
   * separator for generating path strings
   */
  sep = "\\";
  constructor(e = process.cwd(), t = {}) {
    const { nocase: i = !0 } = t;
    super(e, Ps, "\\", { ...t, nocase: i }), this.nocase = i;
    for (let n = this.cwd; n; n = n.parent)
      n.nocase = this.nocase;
  }
  /**
   * @internal
   */
  parseRootPath(e) {
    return Ps.parse(e).root.toUpperCase();
  }
  /**
   * @internal
   */
  newRoot(e) {
    return new ss(this.rootPath, ue, void 0, this.roots, this.nocase, this.childrenCache(), { fs: e });
  }
  /**
   * Return true if the provided path string is an absolute path
   */
  isAbsolute(e) {
    return e.startsWith("/") || e.startsWith("\\") || /^[a-z]:(\/|\\)/i.test(e);
  }
}
class ai extends Pr {
  /**
   * separator for generating path strings
   */
  sep = "/";
  constructor(e = process.cwd(), t = {}) {
    const { nocase: i = !1 } = t;
    super(e, Jo, "/", { ...t, nocase: i }), this.nocase = i;
  }
  /**
   * @internal
   */
  parseRootPath(e) {
    return "/";
  }
  /**
   * @internal
   */
  newRoot(e) {
    return new is(this.rootPath, ue, void 0, this.roots, this.nocase, this.childrenCache(), { fs: e });
  }
  /**
   * Return true if the provided path string is an absolute path
   */
  isAbsolute(e) {
    return e.startsWith("/");
  }
}
class Fr extends ai {
  constructor(e = process.cwd(), t = {}) {
    const { nocase: i = !0 } = t;
    super(e, { ...t, nocase: i });
  }
}
process.platform;
const qh = process.platform === "win32" ? oi : process.platform === "darwin" ? Fr : ai, Hh = (s) => s.length >= 1, Gh = (s) => s.length >= 1;
class ns {
  #e;
  #t;
  #s;
  length;
  #r;
  #o;
  #E;
  #w;
  #a;
  #u;
  #l = !0;
  constructor(e, t, i, n) {
    if (!Hh(e))
      throw new TypeError("empty pattern list");
    if (!Gh(t))
      throw new TypeError("empty glob list");
    if (t.length !== e.length)
      throw new TypeError("mismatched pattern list and glob list lengths");
    if (this.length = e.length, i < 0 || i >= this.length)
      throw new TypeError("index out of range");
    if (this.#e = e, this.#t = t, this.#s = i, this.#r = n, this.#s === 0) {
      if (this.isUNC()) {
        const [r, o, a, l, ...c] = this.#e, [h, u, p, d, ...m] = this.#t;
        c[0] === "" && (c.shift(), m.shift());
        const f = [r, o, a, l, ""].join("/"), b = [h, u, p, d, ""].join("/");
        this.#e = [f, ...c], this.#t = [b, ...m], this.length = this.#e.length;
      } else if (this.isDrive() || this.isAbsolute()) {
        const [r, ...o] = this.#e, [a, ...l] = this.#t;
        o[0] === "" && (o.shift(), l.shift());
        const c = r + "/", h = a + "/";
        this.#e = [c, ...o], this.#t = [h, ...l], this.length = this.#e.length;
      }
    }
  }
  /**
   * The first entry in the parsed list of patterns
   */
  pattern() {
    return this.#e[this.#s];
  }
  /**
   * true of if pattern() returns a string
   */
  isString() {
    return typeof this.#e[this.#s] == "string";
  }
  /**
   * true of if pattern() returns GLOBSTAR
   */
  isGlobstar() {
    return this.#e[this.#s] === H;
  }
  /**
   * true if pattern() returns a regexp
   */
  isRegExp() {
    return this.#e[this.#s] instanceof RegExp;
  }
  /**
   * The /-joined set of glob parts that make up this pattern
   */
  globString() {
    return this.#E = this.#E || (this.#s === 0 ? this.isAbsolute() ? this.#t[0] + this.#t.slice(1).join("/") : this.#t.join("/") : this.#t.slice(this.#s).join("/"));
  }
  /**
   * true if there are more pattern parts after this one
   */
  hasMore() {
    return this.length > this.#s + 1;
  }
  /**
   * The rest of the pattern after this part, or null if this is the end
   */
  rest() {
    return this.#o !== void 0 ? this.#o : this.hasMore() ? (this.#o = new ns(this.#e, this.#t, this.#s + 1, this.#r), this.#o.#u = this.#u, this.#o.#a = this.#a, this.#o.#w = this.#w, this.#o) : this.#o = null;
  }
  /**
   * true if the pattern represents a //unc/path/ on windows
   */
  isUNC() {
    const e = this.#e;
    return this.#a !== void 0 ? this.#a : this.#a = this.#r === "win32" && this.#s === 0 && e[0] === "" && e[1] === "" && typeof e[2] == "string" && !!e[2] && typeof e[3] == "string" && !!e[3];
  }
  // pattern like C:/...
  // split = ['C:', ...]
  // XXX: would be nice to handle patterns like `c:*` to test the cwd
  // in c: for *, but I don't know of a way to even figure out what that
  // cwd is without actually chdir'ing into it?
  /**
   * True if the pattern starts with a drive letter on Windows
   */
  isDrive() {
    const e = this.#e;
    return this.#w !== void 0 ? this.#w : this.#w = this.#r === "win32" && this.#s === 0 && this.length > 1 && typeof e[0] == "string" && /^[a-z]:$/i.test(e[0]);
  }
  // pattern = '/' or '/...' or '/x/...'
  // split = ['', ''] or ['', ...] or ['', 'x', ...]
  // Drive and UNC both considered absolute on windows
  /**
   * True if the pattern is rooted on an absolute path
   */
  isAbsolute() {
    const e = this.#e;
    return this.#u !== void 0 ? this.#u : this.#u = e[0] === "" && e.length > 1 || this.isDrive() || this.isUNC();
  }
  /**
   * consume the root of the pattern, and return it
   */
  root() {
    const e = this.#e[0];
    return typeof e == "string" && this.isAbsolute() && this.#s === 0 ? e : "";
  }
  /**
   * Check to see if the current globstar pattern is allowed to follow
   * a symbolic link.
   */
  checkFollowGlobstar() {
    return !(this.#s === 0 || !this.isGlobstar() || !this.#l);
  }
  /**
   * Mark that the current globstar pattern is following a symbolic link
   */
  markFollowGlobstar() {
    return this.#s === 0 || !this.isGlobstar() || !this.#l ? !1 : (this.#l = !1, !0);
  }
}
const Jh = typeof process == "object" && process && typeof process.platform == "string" ? process.platform : "linux";
class un {
  relative;
  relativeChildren;
  absolute;
  absoluteChildren;
  platform;
  mmopts;
  constructor(e, { nobrace: t, nocase: i, noext: n, noglobstar: r, platform: o = Jh }) {
    this.relative = [], this.absolute = [], this.relativeChildren = [], this.absoluteChildren = [], this.platform = o, this.mmopts = {
      dot: !0,
      nobrace: t,
      nocase: i,
      noext: n,
      noglobstar: r,
      optimizationLevel: 2,
      platform: o,
      nocomment: !0,
      nonegate: !0
    };
    for (const a of e)
      this.add(a);
  }
  add(e) {
    const t = new Se(e, this.mmopts);
    for (let i = 0; i < t.set.length; i++) {
      const n = t.set[i], r = t.globParts[i];
      if (!n || !r)
        throw new Error("invalid pattern object");
      for (; n[0] === "." && r[0] === "."; )
        n.shift(), r.shift();
      const o = new ns(n, r, 0, this.platform), a = new Se(o.globString(), this.mmopts), l = r[r.length - 1] === "**", c = o.isAbsolute();
      c ? this.absolute.push(a) : this.relative.push(a), l && (c ? this.absoluteChildren.push(a) : this.relativeChildren.push(a));
    }
  }
  ignored(e) {
    const t = e.fullpath(), i = `${t}/`, n = e.relative() || ".", r = `${n}/`;
    for (const o of this.relative)
      if (o.match(n) || o.match(r))
        return !0;
    for (const o of this.absolute)
      if (o.match(t) || o.match(i))
        return !0;
    return !1;
  }
  childrenIgnored(e) {
    const t = e.fullpath() + "/", i = (e.relative() || ".") + "/";
    for (const n of this.relativeChildren)
      if (n.match(i))
        return !0;
    for (const n of this.absoluteChildren)
      if (n.match(t))
        return !0;
    return !1;
  }
}
class li {
  store;
  constructor(e = /* @__PURE__ */ new Map()) {
    this.store = e;
  }
  copy() {
    return new li(new Map(this.store));
  }
  hasWalked(e, t) {
    return this.store.get(e.fullpath())?.has(t.globString());
  }
  storeWalked(e, t) {
    const i = e.fullpath(), n = this.store.get(i);
    n ? n.add(t.globString()) : this.store.set(i, /* @__PURE__ */ new Set([t.globString()]));
  }
}
class Kh {
  store = /* @__PURE__ */ new Map();
  add(e, t, i) {
    const n = (t ? 2 : 0) | (i ? 1 : 0), r = this.store.get(e);
    this.store.set(e, r === void 0 ? n : n & r);
  }
  // match, absolute, ifdir
  entries() {
    return [...this.store.entries()].map(([e, t]) => [
      e,
      !!(t & 2),
      !!(t & 1)
    ]);
  }
}
class Vh {
  store = /* @__PURE__ */ new Map();
  add(e, t) {
    if (!e.canReaddir())
      return;
    const i = this.store.get(e);
    i ? i.find((n) => n.globString() === t.globString()) || i.push(t) : this.store.set(e, [t]);
  }
  get(e) {
    const t = this.store.get(e);
    if (!t)
      throw new Error("attempting to walk unknown path");
    return t;
  }
  entries() {
    return this.keys().map((e) => [e, this.store.get(e)]);
  }
  keys() {
    return [...this.store.keys()].filter((e) => e.canReaddir());
  }
}
class qt {
  hasWalkedCache;
  matches = new Kh();
  subwalks = new Vh();
  patterns;
  follow;
  dot;
  opts;
  constructor(e, t) {
    this.opts = e, this.follow = !!e.follow, this.dot = !!e.dot, this.hasWalkedCache = t ? t.copy() : new li();
  }
  processPatterns(e, t) {
    this.patterns = t;
    const i = t.map((n) => [e, n]);
    for (let [n, r] of i) {
      this.hasWalkedCache.storeWalked(n, r);
      const o = r.root(), a = r.isAbsolute() && this.opts.absolute !== !1;
      if (o) {
        n = n.resolve(o === "/" && this.opts.root !== void 0 ? this.opts.root : o);
        const u = r.rest();
        if (u)
          r = u;
        else {
          this.matches.add(n, !0, !1);
          continue;
        }
      }
      if (n.isENOENT())
        continue;
      let l, c, h = !1;
      for (; typeof (l = r.pattern()) == "string" && (c = r.rest()); )
        n = n.resolve(l), r = c, h = !0;
      if (l = r.pattern(), c = r.rest(), h) {
        if (this.hasWalkedCache.hasWalked(n, r))
          continue;
        this.hasWalkedCache.storeWalked(n, r);
      }
      if (typeof l == "string") {
        const u = l === ".." || l === "" || l === ".";
        this.matches.add(n.resolve(l), a, u);
        continue;
      } else if (l === H) {
        (!n.isSymbolicLink() || this.follow || r.checkFollowGlobstar()) && this.subwalks.add(n, r);
        const u = c?.pattern(), p = c?.rest();
        if (!c || (u === "" || u === ".") && !p)
          this.matches.add(n, a, u === "" || u === ".");
        else if (u === "..") {
          const d = n.parent || n;
          p ? this.hasWalkedCache.hasWalked(d, p) || this.subwalks.add(d, p) : this.matches.add(d, a, !0);
        }
      } else l instanceof RegExp && this.subwalks.add(n, r);
    }
    return this;
  }
  subwalkTargets() {
    return this.subwalks.keys();
  }
  child() {
    return new qt(this.opts, this.hasWalkedCache);
  }
  // return a new Processor containing the subwalks for each
  // child entry, and a set of matches, and
  // a hasWalkedCache that's a copy of this one
  // then we're going to call
  filterEntries(e, t) {
    const i = this.subwalks.get(e), n = this.child();
    for (const r of t)
      for (const o of i) {
        const a = o.isAbsolute(), l = o.pattern(), c = o.rest();
        l === H ? n.testGlobstar(r, o, c, a) : l instanceof RegExp ? n.testRegExp(r, l, c, a) : n.testString(r, l, c, a);
      }
    return n;
  }
  testGlobstar(e, t, i, n) {
    if ((this.dot || !e.name.startsWith(".")) && (t.hasMore() || this.matches.add(e, n, !1), e.canReaddir() && (this.follow || !e.isSymbolicLink() ? this.subwalks.add(e, t) : e.isSymbolicLink() && (i && t.checkFollowGlobstar() ? this.subwalks.add(e, i) : t.markFollowGlobstar() && this.subwalks.add(e, t)))), i) {
      const r = i.pattern();
      if (typeof r == "string" && // dots and empty were handled already
      r !== ".." && r !== "" && r !== ".")
        this.testString(e, r, i.rest(), n);
      else if (r === "..") {
        const o = e.parent || e;
        this.subwalks.add(o, i);
      } else r instanceof RegExp && this.testRegExp(e, r, i.rest(), n);
    }
  }
  testRegExp(e, t, i, n) {
    t.test(e.name) && (i ? this.subwalks.add(e, i) : this.matches.add(e, n, !1));
  }
  testString(e, t, i, n) {
    e.isNamed(t) && (i ? this.subwalks.add(e, i) : this.matches.add(e, n, !1));
  }
}
const Xh = (s, e) => typeof s == "string" ? new un([s], e) : Array.isArray(s) ? new un(s, e) : s;
class Dr {
  path;
  patterns;
  opts;
  seen = /* @__PURE__ */ new Set();
  paused = !1;
  aborted = !1;
  #e = [];
  #t;
  #s;
  signal;
  maxDepth;
  includeChildMatches;
  constructor(e, t, i) {
    if (this.patterns = e, this.path = t, this.opts = i, this.#s = !i.posix && i.platform === "win32" ? "\\" : "/", this.includeChildMatches = i.includeChildMatches !== !1, (i.ignore || !this.includeChildMatches) && (this.#t = Xh(i.ignore ?? [], i), !this.includeChildMatches && typeof this.#t.add != "function")) {
      const n = "cannot ignore child matches, ignore lacks add() method.";
      throw new Error(n);
    }
    this.maxDepth = i.maxDepth || 1 / 0, i.signal && (this.signal = i.signal, this.signal.addEventListener("abort", () => {
      this.#e.length = 0;
    }));
  }
  #r(e) {
    return this.seen.has(e) || !!this.#t?.ignored?.(e);
  }
  #o(e) {
    return !!this.#t?.childrenIgnored?.(e);
  }
  // backpressure mechanism
  pause() {
    this.paused = !0;
  }
  resume() {
    if (this.signal?.aborted)
      return;
    this.paused = !1;
    let e;
    for (; !this.paused && (e = this.#e.shift()); )
      e();
  }
  onResume(e) {
    this.signal?.aborted || (this.paused ? this.#e.push(e) : e());
  }
  // do the requisite realpath/stat checking, and return the path
  // to add or undefined to filter it out.
  async matchCheck(e, t) {
    if (t && this.opts.nodir)
      return;
    let i;
    if (this.opts.realpath) {
      if (i = e.realpathCached() || await e.realpath(), !i)
        return;
      e = i;
    }
    const r = e.isUnknown() || this.opts.stat ? await e.lstat() : e;
    if (this.opts.follow && this.opts.nodir && r?.isSymbolicLink()) {
      const o = await r.realpath();
      o && (o.isUnknown() || this.opts.stat) && await o.lstat();
    }
    return this.matchCheckTest(r, t);
  }
  matchCheckTest(e, t) {
    return e && (this.maxDepth === 1 / 0 || e.depth() <= this.maxDepth) && (!t || e.canReaddir()) && (!this.opts.nodir || !e.isDirectory()) && (!this.opts.nodir || !this.opts.follow || !e.isSymbolicLink() || !e.realpathCached()?.isDirectory()) && !this.#r(e) ? e : void 0;
  }
  matchCheckSync(e, t) {
    if (t && this.opts.nodir)
      return;
    let i;
    if (this.opts.realpath) {
      if (i = e.realpathCached() || e.realpathSync(), !i)
        return;
      e = i;
    }
    const r = e.isUnknown() || this.opts.stat ? e.lstatSync() : e;
    if (this.opts.follow && this.opts.nodir && r?.isSymbolicLink()) {
      const o = r.realpathSync();
      o && (o?.isUnknown() || this.opts.stat) && o.lstatSync();
    }
    return this.matchCheckTest(r, t);
  }
  matchFinish(e, t) {
    if (this.#r(e))
      return;
    if (!this.includeChildMatches && this.#t?.add) {
      const r = `${e.relativePosix()}/**`;
      this.#t.add(r);
    }
    const i = this.opts.absolute === void 0 ? t : this.opts.absolute;
    this.seen.add(e);
    const n = this.opts.mark && e.isDirectory() ? this.#s : "";
    if (this.opts.withFileTypes)
      this.matchEmit(e);
    else if (i) {
      const r = this.opts.posix ? e.fullpathPosix() : e.fullpath();
      this.matchEmit(r + n);
    } else {
      const r = this.opts.posix ? e.relativePosix() : e.relative(), o = this.opts.dotRelative && !r.startsWith(".." + this.#s) ? "." + this.#s : "";
      this.matchEmit(r ? o + r + n : "." + n);
    }
  }
  async match(e, t, i) {
    const n = await this.matchCheck(e, i);
    n && this.matchFinish(n, t);
  }
  matchSync(e, t, i) {
    const n = this.matchCheckSync(e, i);
    n && this.matchFinish(n, t);
  }
  walkCB(e, t, i) {
    this.signal?.aborted && i(), this.walkCB2(e, t, new qt(this.opts), i);
  }
  walkCB2(e, t, i, n) {
    if (this.#o(e))
      return n();
    if (this.signal?.aborted && n(), this.paused) {
      this.onResume(() => this.walkCB2(e, t, i, n));
      return;
    }
    i.processPatterns(e, t);
    let r = 1;
    const o = () => {
      --r === 0 && n();
    };
    for (const [a, l, c] of i.matches.entries())
      this.#r(a) || (r++, this.match(a, l, c).then(() => o()));
    for (const a of i.subwalkTargets()) {
      if (this.maxDepth !== 1 / 0 && a.depth() >= this.maxDepth)
        continue;
      r++;
      const l = a.readdirCached();
      a.calledReaddir() ? this.walkCB3(a, l, i, o) : a.readdirCB((c, h) => this.walkCB3(a, h, i, o), !0);
    }
    o();
  }
  walkCB3(e, t, i, n) {
    i = i.filterEntries(e, t);
    let r = 1;
    const o = () => {
      --r === 0 && n();
    };
    for (const [a, l, c] of i.matches.entries())
      this.#r(a) || (r++, this.match(a, l, c).then(() => o()));
    for (const [a, l] of i.subwalks.entries())
      r++, this.walkCB2(a, l, i.child(), o);
    o();
  }
  walkCBSync(e, t, i) {
    this.signal?.aborted && i(), this.walkCB2Sync(e, t, new qt(this.opts), i);
  }
  walkCB2Sync(e, t, i, n) {
    if (this.#o(e))
      return n();
    if (this.signal?.aborted && n(), this.paused) {
      this.onResume(() => this.walkCB2Sync(e, t, i, n));
      return;
    }
    i.processPatterns(e, t);
    let r = 1;
    const o = () => {
      --r === 0 && n();
    };
    for (const [a, l, c] of i.matches.entries())
      this.#r(a) || this.matchSync(a, l, c);
    for (const a of i.subwalkTargets()) {
      if (this.maxDepth !== 1 / 0 && a.depth() >= this.maxDepth)
        continue;
      r++;
      const l = a.readdirSync();
      this.walkCB3Sync(a, l, i, o);
    }
    o();
  }
  walkCB3Sync(e, t, i, n) {
    i = i.filterEntries(e, t);
    let r = 1;
    const o = () => {
      --r === 0 && n();
    };
    for (const [a, l, c] of i.matches.entries())
      this.#r(a) || this.matchSync(a, l, c);
    for (const [a, l] of i.subwalks.entries())
      r++, this.walkCB2Sync(a, l, i.child(), o);
    o();
  }
}
class fn extends Dr {
  matches = /* @__PURE__ */ new Set();
  constructor(e, t, i) {
    super(e, t, i);
  }
  matchEmit(e) {
    this.matches.add(e);
  }
  async walk() {
    if (this.signal?.aborted)
      throw this.signal.reason;
    return this.path.isUnknown() && await this.path.lstat(), await new Promise((e, t) => {
      this.walkCB(this.path, this.patterns, () => {
        this.signal?.aborted ? t(this.signal.reason) : e(this.matches);
      });
    }), this.matches;
  }
  walkSync() {
    if (this.signal?.aborted)
      throw this.signal.reason;
    return this.path.isUnknown() && this.path.lstatSync(), this.walkCBSync(this.path, this.patterns, () => {
      if (this.signal?.aborted)
        throw this.signal.reason;
    }), this.matches;
  }
}
class dn extends Dr {
  results;
  constructor(e, t, i) {
    super(e, t, i), this.results = new Wt({
      signal: this.signal,
      objectMode: !0
    }), this.results.on("drain", () => this.resume()), this.results.on("resume", () => this.resume());
  }
  matchEmit(e) {
    this.results.write(e), this.results.flowing || this.pause();
  }
  stream() {
    const e = this.path;
    return e.isUnknown() ? e.lstat().then(() => {
      this.walkCB(e, this.patterns, () => this.results.end());
    }) : this.walkCB(e, this.patterns, () => this.results.end()), this.results;
  }
  streamSync() {
    return this.path.isUnknown() && this.path.lstatSync(), this.walkCBSync(this.path, this.patterns, () => this.results.end()), this.results;
  }
}
const Zh = typeof process == "object" && process && typeof process.platform == "string" ? process.platform : "linux";
class Fe {
  absolute;
  cwd;
  root;
  dot;
  dotRelative;
  follow;
  ignore;
  magicalBraces;
  mark;
  matchBase;
  maxDepth;
  nobrace;
  nocase;
  nodir;
  noext;
  noglobstar;
  pattern;
  platform;
  realpath;
  scurry;
  stat;
  signal;
  windowsPathsNoEscape;
  withFileTypes;
  includeChildMatches;
  /**
   * The options provided to the constructor.
   */
  opts;
  /**
   * An array of parsed immutable {@link Pattern} objects.
   */
  patterns;
  /**
   * All options are stored as properties on the `Glob` object.
   *
   * See {@link GlobOptions} for full options descriptions.
   *
   * Note that a previous `Glob` object can be passed as the
   * `GlobOptions` to another `Glob` instantiation to re-use settings
   * and caches with a new pattern.
   *
   * Traversal functions can be called multiple times to run the walk
   * again.
   */
  constructor(e, t) {
    if (!t)
      throw new TypeError("glob options required");
    if (this.withFileTypes = !!t.withFileTypes, this.signal = t.signal, this.follow = !!t.follow, this.dot = !!t.dot, this.dotRelative = !!t.dotRelative, this.nodir = !!t.nodir, this.mark = !!t.mark, t.cwd ? (t.cwd instanceof URL || t.cwd.startsWith("file://")) && (t.cwd = Pn(t.cwd)) : this.cwd = "", this.cwd = t.cwd || "", this.root = t.root, this.magicalBraces = !!t.magicalBraces, this.nobrace = !!t.nobrace, this.noext = !!t.noext, this.realpath = !!t.realpath, this.absolute = t.absolute, this.includeChildMatches = t.includeChildMatches !== !1, this.noglobstar = !!t.noglobstar, this.matchBase = !!t.matchBase, this.maxDepth = typeof t.maxDepth == "number" ? t.maxDepth : 1 / 0, this.stat = !!t.stat, this.ignore = t.ignore, this.withFileTypes && this.absolute !== void 0)
      throw new Error("cannot set absolute and withFileTypes:true");
    if (typeof e == "string" && (e = [e]), this.windowsPathsNoEscape = !!t.windowsPathsNoEscape || t.allowWindowsEscape === !1, this.windowsPathsNoEscape && (e = e.map((l) => l.replace(/\\/g, "/"))), this.matchBase) {
      if (t.noglobstar)
        throw new TypeError("base matching requires globstar");
      e = e.map((l) => l.includes("/") ? l : `./**/${l}`);
    }
    if (this.pattern = e, this.platform = t.platform || Zh, this.opts = { ...t, platform: this.platform }, t.scurry) {
      if (this.scurry = t.scurry, t.nocase !== void 0 && t.nocase !== t.scurry.nocase)
        throw new Error("nocase option contradicts provided scurry option");
    } else {
      const l = t.platform === "win32" ? oi : t.platform === "darwin" ? Fr : t.platform ? ai : qh;
      this.scurry = new l(this.cwd, {
        nocase: t.nocase,
        fs: t.fs
      });
    }
    this.nocase = this.scurry.nocase;
    const i = this.platform === "darwin" || this.platform === "win32", n = {
      // default nocase based on platform
      ...t,
      dot: this.dot,
      matchBase: this.matchBase,
      nobrace: this.nobrace,
      nocase: this.nocase,
      nocaseMagicOnly: i,
      nocomment: !0,
      noext: this.noext,
      nonegate: !0,
      optimizationLevel: 2,
      platform: this.platform,
      windowsPathsNoEscape: this.windowsPathsNoEscape,
      debug: !!this.opts.debug
    }, r = this.pattern.map((l) => new Se(l, n)), [o, a] = r.reduce((l, c) => (l[0].push(...c.set), l[1].push(...c.globParts), l), [[], []]);
    this.patterns = o.map((l, c) => {
      const h = a[c];
      if (!h)
        throw new Error("invalid pattern object");
      return new ns(l, h, 0, this.platform);
    });
  }
  async walk() {
    return [
      ...await new fn(this.patterns, this.scurry.cwd, {
        ...this.opts,
        maxDepth: this.maxDepth !== 1 / 0 ? this.maxDepth + this.scurry.cwd.depth() : 1 / 0,
        platform: this.platform,
        nocase: this.nocase,
        includeChildMatches: this.includeChildMatches
      }).walk()
    ];
  }
  walkSync() {
    return [
      ...new fn(this.patterns, this.scurry.cwd, {
        ...this.opts,
        maxDepth: this.maxDepth !== 1 / 0 ? this.maxDepth + this.scurry.cwd.depth() : 1 / 0,
        platform: this.platform,
        nocase: this.nocase,
        includeChildMatches: this.includeChildMatches
      }).walkSync()
    ];
  }
  stream() {
    return new dn(this.patterns, this.scurry.cwd, {
      ...this.opts,
      maxDepth: this.maxDepth !== 1 / 0 ? this.maxDepth + this.scurry.cwd.depth() : 1 / 0,
      platform: this.platform,
      nocase: this.nocase,
      includeChildMatches: this.includeChildMatches
    }).stream();
  }
  streamSync() {
    return new dn(this.patterns, this.scurry.cwd, {
      ...this.opts,
      maxDepth: this.maxDepth !== 1 / 0 ? this.maxDepth + this.scurry.cwd.depth() : 1 / 0,
      platform: this.platform,
      nocase: this.nocase,
      includeChildMatches: this.includeChildMatches
    }).streamSync();
  }
  /**
   * Default sync iteration function. Returns a Generator that
   * iterates over the results.
   */
  iterateSync() {
    return this.streamSync()[Symbol.iterator]();
  }
  [Symbol.iterator]() {
    return this.iterateSync();
  }
  /**
   * Default async iteration function. Returns an AsyncGenerator that
   * iterates over the results.
   */
  iterate() {
    return this.stream()[Symbol.asyncIterator]();
  }
  [Symbol.asyncIterator]() {
    return this.iterate();
  }
}
const Yh = (s, e = {}) => {
  Array.isArray(s) || (s = [s]);
  for (const t of s)
    if (new Se(t, e).hasMagic())
      return !0;
  return !1;
};
function rs(s, e = {}) {
  return new Fe(s, e).streamSync();
}
function kr(s, e = {}) {
  return new Fe(s, e).stream();
}
function Ht(s, e = {}) {
  return new Fe(s, e).walkSync();
}
async function pn(s, e = {}) {
  return new Fe(s, e).walk();
}
function os(s, e = {}) {
  return new Fe(s, e).iterateSync();
}
function Lr(s, e = {}) {
  return new Fe(s, e).iterate();
}
const Qh = rs, eu = Object.assign(kr, { sync: rs }), tu = os, su = Object.assign(Lr, {
  sync: os
}), iu = Object.assign(Ht, {
  stream: rs,
  iterate: os
}), mn = Object.assign(pn, {
  glob: pn,
  globSync: Ht,
  sync: iu,
  globStream: kr,
  stream: eu,
  globStreamSync: rs,
  streamSync: Qh,
  globIterate: Lr,
  iterate: su,
  globIterateSync: os,
  iterateSync: tu,
  Glob: Fe,
  hasMagic: Yh,
  escape: pr,
  unescape: Be
});
mn.glob = mn;
function jr(s, e) {
  return function() {
    return s.apply(e, arguments);
  };
}
const { toString: nu } = Object.prototype, { getPrototypeOf: ci } = Object, { iterator: as, toStringTag: Ur } = Symbol, ls = /* @__PURE__ */ ((s) => (e) => {
  const t = nu.call(e);
  return s[t] || (s[t] = t.slice(8, -1).toLowerCase());
})(/* @__PURE__ */ Object.create(null)), oe = (s) => (s = s.toLowerCase(), (e) => ls(e) === s), cs = (s) => (e) => typeof e === s, { isArray: Ge } = Array, ct = cs("undefined");
function mt(s) {
  return s !== null && !ct(s) && s.constructor !== null && !ct(s.constructor) && Z(s.constructor.isBuffer) && s.constructor.isBuffer(s);
}
const Br = oe("ArrayBuffer");
function ru(s) {
  let e;
  return typeof ArrayBuffer < "u" && ArrayBuffer.isView ? e = ArrayBuffer.isView(s) : e = s && s.buffer && Br(s.buffer), e;
}
const ou = cs("string"), Z = cs("function"), Ir = cs("number"), gt = (s) => s !== null && typeof s == "object", au = (s) => s === !0 || s === !1, Lt = (s) => {
  if (ls(s) !== "object")
    return !1;
  const e = ci(s);
  return (e === null || e === Object.prototype || Object.getPrototypeOf(e) === null) && !(Ur in s) && !(as in s);
}, lu = (s) => {
  if (!gt(s) || mt(s))
    return !1;
  try {
    return Object.keys(s).length === 0 && Object.getPrototypeOf(s) === Object.prototype;
  } catch {
    return !1;
  }
}, cu = oe("Date"), hu = oe("File"), uu = oe("Blob"), fu = oe("FileList"), du = (s) => gt(s) && Z(s.pipe), pu = (s) => {
  let e;
  return s && (typeof FormData == "function" && s instanceof FormData || Z(s.append) && ((e = ls(s)) === "formdata" || // detect form-data instance
  e === "object" && Z(s.toString) && s.toString() === "[object FormData]"));
}, mu = oe("URLSearchParams"), [gu, wu, bu, yu] = ["ReadableStream", "Request", "Response", "Headers"].map(oe), Eu = (s) => s.trim ? s.trim() : s.replace(/^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, "");
function wt(s, e, { allOwnKeys: t = !1 } = {}) {
  if (s === null || typeof s > "u")
    return;
  let i, n;
  if (typeof s != "object" && (s = [s]), Ge(s))
    for (i = 0, n = s.length; i < n; i++)
      e.call(null, s[i], i, s);
  else {
    if (mt(s))
      return;
    const r = t ? Object.getOwnPropertyNames(s) : Object.keys(s), o = r.length;
    let a;
    for (i = 0; i < o; i++)
      a = r[i], e.call(null, s[a], a, s);
  }
}
function Mr(s, e) {
  if (mt(s))
    return null;
  e = e.toLowerCase();
  const t = Object.keys(s);
  let i = t.length, n;
  for (; i-- > 0; )
    if (n = t[i], e === n.toLowerCase())
      return n;
  return null;
}
const Ce = typeof globalThis < "u" ? globalThis : typeof self < "u" ? self : typeof window < "u" ? window : global, $r = (s) => !ct(s) && s !== Ce;
function zs() {
  const { caseless: s } = $r(this) && this || {}, e = {}, t = (i, n) => {
    const r = s && Mr(e, n) || n;
    Lt(e[r]) && Lt(i) ? e[r] = zs(e[r], i) : Lt(i) ? e[r] = zs({}, i) : Ge(i) ? e[r] = i.slice() : e[r] = i;
  };
  for (let i = 0, n = arguments.length; i < n; i++)
    arguments[i] && wt(arguments[i], t);
  return e;
}
const Su = (s, e, t, { allOwnKeys: i } = {}) => (wt(e, (n, r) => {
  t && Z(n) ? s[r] = jr(n, t) : s[r] = n;
}, { allOwnKeys: i }), s), Ru = (s) => (s.charCodeAt(0) === 65279 && (s = s.slice(1)), s), Tu = (s, e, t, i) => {
  s.prototype = Object.create(e.prototype, i), s.prototype.constructor = s, Object.defineProperty(s, "super", {
    value: e.prototype
  }), t && Object.assign(s.prototype, t);
}, Ou = (s, e, t, i) => {
  let n, r, o;
  const a = {};
  if (e = e || {}, s == null) return e;
  do {
    for (n = Object.getOwnPropertyNames(s), r = n.length; r-- > 0; )
      o = n[r], (!i || i(o, s, e)) && !a[o] && (e[o] = s[o], a[o] = !0);
    s = t !== !1 && ci(s);
  } while (s && (!t || t(s, e)) && s !== Object.prototype);
  return e;
}, xu = (s, e, t) => {
  s = String(s), (t === void 0 || t > s.length) && (t = s.length), t -= e.length;
  const i = s.indexOf(e, t);
  return i !== -1 && i === t;
}, Au = (s) => {
  if (!s) return null;
  if (Ge(s)) return s;
  let e = s.length;
  if (!Ir(e)) return null;
  const t = new Array(e);
  for (; e-- > 0; )
    t[e] = s[e];
  return t;
}, Cu = /* @__PURE__ */ ((s) => (e) => s && e instanceof s)(typeof Uint8Array < "u" && ci(Uint8Array)), vu = (s, e) => {
  const i = (s && s[as]).call(s);
  let n;
  for (; (n = i.next()) && !n.done; ) {
    const r = n.value;
    e.call(s, r[0], r[1]);
  }
}, Nu = (s, e) => {
  let t;
  const i = [];
  for (; (t = s.exec(e)) !== null; )
    i.push(t);
  return i;
}, _u = oe("HTMLFormElement"), Pu = (s) => s.toLowerCase().replace(
  /[-_\s]([a-z\d])(\w*)/g,
  function(t, i, n) {
    return i.toUpperCase() + n;
  }
), gn = (({ hasOwnProperty: s }) => (e, t) => s.call(e, t))(Object.prototype), Fu = oe("RegExp"), zr = (s, e) => {
  const t = Object.getOwnPropertyDescriptors(s), i = {};
  wt(t, (n, r) => {
    let o;
    (o = e(n, r, s)) !== !1 && (i[r] = o || n);
  }), Object.defineProperties(s, i);
}, Du = (s) => {
  zr(s, (e, t) => {
    if (Z(s) && ["arguments", "caller", "callee"].indexOf(t) !== -1)
      return !1;
    const i = s[t];
    if (Z(i)) {
      if (e.enumerable = !1, "writable" in e) {
        e.writable = !1;
        return;
      }
      e.set || (e.set = () => {
        throw Error("Can not rewrite read-only method '" + t + "'");
      });
    }
  });
}, ku = (s, e) => {
  const t = {}, i = (n) => {
    n.forEach((r) => {
      t[r] = !0;
    });
  };
  return Ge(s) ? i(s) : i(String(s).split(e)), t;
}, Lu = () => {
}, ju = (s, e) => s != null && Number.isFinite(s = +s) ? s : e;
function Uu(s) {
  return !!(s && Z(s.append) && s[Ur] === "FormData" && s[as]);
}
const Bu = (s) => {
  const e = new Array(10), t = (i, n) => {
    if (gt(i)) {
      if (e.indexOf(i) >= 0)
        return;
      if (mt(i))
        return i;
      if (!("toJSON" in i)) {
        e[n] = i;
        const r = Ge(i) ? [] : {};
        return wt(i, (o, a) => {
          const l = t(o, n + 1);
          !ct(l) && (r[a] = l);
        }), e[n] = void 0, r;
      }
    }
    return i;
  };
  return t(s, 0);
}, Iu = oe("AsyncFunction"), Mu = (s) => s && (gt(s) || Z(s)) && Z(s.then) && Z(s.catch), Wr = ((s, e) => s ? setImmediate : e ? ((t, i) => (Ce.addEventListener("message", ({ source: n, data: r }) => {
  n === Ce && r === t && i.length && i.shift()();
}, !1), (n) => {
  i.push(n), Ce.postMessage(t, "*");
}))(`axios@${Math.random()}`, []) : (t) => setTimeout(t))(
  typeof setImmediate == "function",
  Z(Ce.postMessage)
), $u = typeof queueMicrotask < "u" ? queueMicrotask.bind(Ce) : typeof process < "u" && process.nextTick || Wr, zu = (s) => s != null && Z(s[as]), w = {
  isArray: Ge,
  isArrayBuffer: Br,
  isBuffer: mt,
  isFormData: pu,
  isArrayBufferView: ru,
  isString: ou,
  isNumber: Ir,
  isBoolean: au,
  isObject: gt,
  isPlainObject: Lt,
  isEmptyObject: lu,
  isReadableStream: gu,
  isRequest: wu,
  isResponse: bu,
  isHeaders: yu,
  isUndefined: ct,
  isDate: cu,
  isFile: hu,
  isBlob: uu,
  isRegExp: Fu,
  isFunction: Z,
  isStream: du,
  isURLSearchParams: mu,
  isTypedArray: Cu,
  isFileList: fu,
  forEach: wt,
  merge: zs,
  extend: Su,
  trim: Eu,
  stripBOM: Ru,
  inherits: Tu,
  toFlatObject: Ou,
  kindOf: ls,
  kindOfTest: oe,
  endsWith: xu,
  toArray: Au,
  forEachEntry: vu,
  matchAll: Nu,
  isHTMLForm: _u,
  hasOwnProperty: gn,
  hasOwnProp: gn,
  // an alias to avoid ESLint no-prototype-builtins detection
  reduceDescriptors: zr,
  freezeMethods: Du,
  toObjectSet: ku,
  toCamelCase: Pu,
  noop: Lu,
  toFiniteNumber: ju,
  findKey: Mr,
  global: Ce,
  isContextDefined: $r,
  isSpecCompliantForm: Uu,
  toJSONObject: Bu,
  isAsyncFn: Iu,
  isThenable: Mu,
  setImmediate: Wr,
  asap: $u,
  isIterable: zu
};
function A(s, e, t, i, n) {
  Error.call(this), Error.captureStackTrace ? Error.captureStackTrace(this, this.constructor) : this.stack = new Error().stack, this.message = s, this.name = "AxiosError", e && (this.code = e), t && (this.config = t), i && (this.request = i), n && (this.response = n, this.status = n.status ? n.status : null);
}
w.inherits(A, Error, {
  toJSON: function() {
    return {
      // Standard
      message: this.message,
      name: this.name,
      // Microsoft
      description: this.description,
      number: this.number,
      // Mozilla
      fileName: this.fileName,
      lineNumber: this.lineNumber,
      columnNumber: this.columnNumber,
      stack: this.stack,
      // Axios
      config: w.toJSONObject(this.config),
      code: this.code,
      status: this.status
    };
  }
});
const qr = A.prototype, Hr = {};
[
  "ERR_BAD_OPTION_VALUE",
  "ERR_BAD_OPTION",
  "ECONNABORTED",
  "ETIMEDOUT",
  "ERR_NETWORK",
  "ERR_FR_TOO_MANY_REDIRECTS",
  "ERR_DEPRECATED",
  "ERR_BAD_RESPONSE",
  "ERR_BAD_REQUEST",
  "ERR_CANCELED",
  "ERR_NOT_SUPPORT",
  "ERR_INVALID_URL"
  // eslint-disable-next-line func-names
].forEach((s) => {
  Hr[s] = { value: s };
});
Object.defineProperties(A, Hr);
Object.defineProperty(qr, "isAxiosError", { value: !0 });
A.from = (s, e, t, i, n, r) => {
  const o = Object.create(qr);
  return w.toFlatObject(s, o, function(l) {
    return l !== Error.prototype;
  }, (a) => a !== "isAxiosError"), A.call(o, s.message, e, t, i, n), o.cause = s, o.name = s.name, r && Object.assign(o, r), o;
};
const Wu = null;
function Ws(s) {
  return w.isPlainObject(s) || w.isArray(s);
}
function Gr(s) {
  return w.endsWith(s, "[]") ? s.slice(0, -2) : s;
}
function wn(s, e, t) {
  return s ? s.concat(e).map(function(n, r) {
    return n = Gr(n), !t && r ? "[" + n + "]" : n;
  }).join(t ? "." : "") : e;
}
function qu(s) {
  return w.isArray(s) && !s.some(Ws);
}
const Hu = w.toFlatObject(w, {}, null, function(e) {
  return /^is[A-Z]/.test(e);
});
function hs(s, e, t) {
  if (!w.isObject(s))
    throw new TypeError("target must be an object");
  e = e || new FormData(), t = w.toFlatObject(t, {
    metaTokens: !0,
    dots: !1,
    indexes: !1
  }, !1, function(f, b) {
    return !w.isUndefined(b[f]);
  });
  const i = t.metaTokens, n = t.visitor || h, r = t.dots, o = t.indexes, l = (t.Blob || typeof Blob < "u" && Blob) && w.isSpecCompliantForm(e);
  if (!w.isFunction(n))
    throw new TypeError("visitor must be a function");
  function c(m) {
    if (m === null) return "";
    if (w.isDate(m))
      return m.toISOString();
    if (w.isBoolean(m))
      return m.toString();
    if (!l && w.isBlob(m))
      throw new A("Blob is not supported. Use a Buffer instead.");
    return w.isArrayBuffer(m) || w.isTypedArray(m) ? l && typeof Blob == "function" ? new Blob([m]) : Buffer.from(m) : m;
  }
  function h(m, f, b) {
    let y = m;
    if (m && !b && typeof m == "object") {
      if (w.endsWith(f, "{}"))
        f = i ? f : f.slice(0, -2), m = JSON.stringify(m);
      else if (w.isArray(m) && qu(m) || (w.isFileList(m) || w.endsWith(f, "[]")) && (y = w.toArray(m)))
        return f = Gr(f), y.forEach(function(S, T) {
          !(w.isUndefined(S) || S === null) && e.append(
            // eslint-disable-next-line no-nested-ternary
            o === !0 ? wn([f], T, r) : o === null ? f : f + "[]",
            c(S)
          );
        }), !1;
    }
    return Ws(m) ? !0 : (e.append(wn(b, f, r), c(m)), !1);
  }
  const u = [], p = Object.assign(Hu, {
    defaultVisitor: h,
    convertValue: c,
    isVisitable: Ws
  });
  function d(m, f) {
    if (!w.isUndefined(m)) {
      if (u.indexOf(m) !== -1)
        throw Error("Circular reference detected in " + f.join("."));
      u.push(m), w.forEach(m, function(y, E) {
        (!(w.isUndefined(y) || y === null) && n.call(
          e,
          y,
          w.isString(E) ? E.trim() : E,
          f,
          p
        )) === !0 && d(y, f ? f.concat(E) : [E]);
      }), u.pop();
    }
  }
  if (!w.isObject(s))
    throw new TypeError("data must be an object");
  return d(s), e;
}
function bn(s) {
  const e = {
    "!": "%21",
    "'": "%27",
    "(": "%28",
    ")": "%29",
    "~": "%7E",
    "%20": "+",
    "%00": "\0"
  };
  return encodeURIComponent(s).replace(/[!'()~]|%20|%00/g, function(i) {
    return e[i];
  });
}
function hi(s, e) {
  this._pairs = [], s && hs(s, this, e);
}
const Jr = hi.prototype;
Jr.append = function(e, t) {
  this._pairs.push([e, t]);
};
Jr.toString = function(e) {
  const t = e ? function(i) {
    return e.call(this, i, bn);
  } : bn;
  return this._pairs.map(function(n) {
    return t(n[0]) + "=" + t(n[1]);
  }, "").join("&");
};
function Gu(s) {
  return encodeURIComponent(s).replace(/%3A/gi, ":").replace(/%24/g, "$").replace(/%2C/gi, ",").replace(/%20/g, "+").replace(/%5B/gi, "[").replace(/%5D/gi, "]");
}
function Kr(s, e, t) {
  if (!e)
    return s;
  const i = t && t.encode || Gu;
  w.isFunction(t) && (t = {
    serialize: t
  });
  const n = t && t.serialize;
  let r;
  if (n ? r = n(e, t) : r = w.isURLSearchParams(e) ? e.toString() : new hi(e, t).toString(i), r) {
    const o = s.indexOf("#");
    o !== -1 && (s = s.slice(0, o)), s += (s.indexOf("?") === -1 ? "?" : "&") + r;
  }
  return s;
}
class yn {
  constructor() {
    this.handlers = [];
  }
  /**
   * Add a new interceptor to the stack
   *
   * @param {Function} fulfilled The function to handle `then` for a `Promise`
   * @param {Function} rejected The function to handle `reject` for a `Promise`
   *
   * @return {Number} An ID used to remove interceptor later
   */
  use(e, t, i) {
    return this.handlers.push({
      fulfilled: e,
      rejected: t,
      synchronous: i ? i.synchronous : !1,
      runWhen: i ? i.runWhen : null
    }), this.handlers.length - 1;
  }
  /**
   * Remove an interceptor from the stack
   *
   * @param {Number} id The ID that was returned by `use`
   *
   * @returns {Boolean} `true` if the interceptor was removed, `false` otherwise
   */
  eject(e) {
    this.handlers[e] && (this.handlers[e] = null);
  }
  /**
   * Clear all interceptors from the stack
   *
   * @returns {void}
   */
  clear() {
    this.handlers && (this.handlers = []);
  }
  /**
   * Iterate over all the registered interceptors
   *
   * This method is particularly useful for skipping over any
   * interceptors that may have become `null` calling `eject`.
   *
   * @param {Function} fn The function to call for each interceptor
   *
   * @returns {void}
   */
  forEach(e) {
    w.forEach(this.handlers, function(i) {
      i !== null && e(i);
    });
  }
}
const Vr = {
  silentJSONParsing: !0,
  forcedJSONParsing: !0,
  clarifyTimeoutError: !1
}, Ju = typeof URLSearchParams < "u" ? URLSearchParams : hi, Ku = typeof FormData < "u" ? FormData : null, Vu = typeof Blob < "u" ? Blob : null, Xu = {
  isBrowser: !0,
  classes: {
    URLSearchParams: Ju,
    FormData: Ku,
    Blob: Vu
  },
  protocols: ["http", "https", "file", "blob", "url", "data"]
}, ui = typeof window < "u" && typeof document < "u", qs = typeof navigator == "object" && navigator || void 0, Zu = ui && (!qs || ["ReactNative", "NativeScript", "NS"].indexOf(qs.product) < 0), Yu = typeof WorkerGlobalScope < "u" && // eslint-disable-next-line no-undef
self instanceof WorkerGlobalScope && typeof self.importScripts == "function", Qu = ui && window.location.href || "http://localhost", ef = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  hasBrowserEnv: ui,
  hasStandardBrowserEnv: Zu,
  hasStandardBrowserWebWorkerEnv: Yu,
  navigator: qs,
  origin: Qu
}, Symbol.toStringTag, { value: "Module" })), $ = {
  ...ef,
  ...Xu
};
function tf(s, e) {
  return hs(s, new $.classes.URLSearchParams(), {
    visitor: function(t, i, n, r) {
      return $.isNode && w.isBuffer(t) ? (this.append(i, t.toString("base64")), !1) : r.defaultVisitor.apply(this, arguments);
    },
    ...e
  });
}
function sf(s) {
  return w.matchAll(/\w+|\[(\w*)]/g, s).map((e) => e[0] === "[]" ? "" : e[1] || e[0]);
}
function nf(s) {
  const e = {}, t = Object.keys(s);
  let i;
  const n = t.length;
  let r;
  for (i = 0; i < n; i++)
    r = t[i], e[r] = s[r];
  return e;
}
function Xr(s) {
  function e(t, i, n, r) {
    let o = t[r++];
    if (o === "__proto__") return !0;
    const a = Number.isFinite(+o), l = r >= t.length;
    return o = !o && w.isArray(n) ? n.length : o, l ? (w.hasOwnProp(n, o) ? n[o] = [n[o], i] : n[o] = i, !a) : ((!n[o] || !w.isObject(n[o])) && (n[o] = []), e(t, i, n[o], r) && w.isArray(n[o]) && (n[o] = nf(n[o])), !a);
  }
  if (w.isFormData(s) && w.isFunction(s.entries)) {
    const t = {};
    return w.forEachEntry(s, (i, n) => {
      e(sf(i), n, t, 0);
    }), t;
  }
  return null;
}
function rf(s, e, t) {
  if (w.isString(s))
    try {
      return (e || JSON.parse)(s), w.trim(s);
    } catch (i) {
      if (i.name !== "SyntaxError")
        throw i;
    }
  return (t || JSON.stringify)(s);
}
const bt = {
  transitional: Vr,
  adapter: ["xhr", "http", "fetch"],
  transformRequest: [function(e, t) {
    const i = t.getContentType() || "", n = i.indexOf("application/json") > -1, r = w.isObject(e);
    if (r && w.isHTMLForm(e) && (e = new FormData(e)), w.isFormData(e))
      return n ? JSON.stringify(Xr(e)) : e;
    if (w.isArrayBuffer(e) || w.isBuffer(e) || w.isStream(e) || w.isFile(e) || w.isBlob(e) || w.isReadableStream(e))
      return e;
    if (w.isArrayBufferView(e))
      return e.buffer;
    if (w.isURLSearchParams(e))
      return t.setContentType("application/x-www-form-urlencoded;charset=utf-8", !1), e.toString();
    let a;
    if (r) {
      if (i.indexOf("application/x-www-form-urlencoded") > -1)
        return tf(e, this.formSerializer).toString();
      if ((a = w.isFileList(e)) || i.indexOf("multipart/form-data") > -1) {
        const l = this.env && this.env.FormData;
        return hs(
          a ? { "files[]": e } : e,
          l && new l(),
          this.formSerializer
        );
      }
    }
    return r || n ? (t.setContentType("application/json", !1), rf(e)) : e;
  }],
  transformResponse: [function(e) {
    const t = this.transitional || bt.transitional, i = t && t.forcedJSONParsing, n = this.responseType === "json";
    if (w.isResponse(e) || w.isReadableStream(e))
      return e;
    if (e && w.isString(e) && (i && !this.responseType || n)) {
      const o = !(t && t.silentJSONParsing) && n;
      try {
        return JSON.parse(e);
      } catch (a) {
        if (o)
          throw a.name === "SyntaxError" ? A.from(a, A.ERR_BAD_RESPONSE, this, null, this.response) : a;
      }
    }
    return e;
  }],
  /**
   * A timeout in milliseconds to abort a request. If set to 0 (default) a
   * timeout is not created.
   */
  timeout: 0,
  xsrfCookieName: "XSRF-TOKEN",
  xsrfHeaderName: "X-XSRF-TOKEN",
  maxContentLength: -1,
  maxBodyLength: -1,
  env: {
    FormData: $.classes.FormData,
    Blob: $.classes.Blob
  },
  validateStatus: function(e) {
    return e >= 200 && e < 300;
  },
  headers: {
    common: {
      Accept: "application/json, text/plain, */*",
      "Content-Type": void 0
    }
  }
};
w.forEach(["delete", "get", "head", "post", "put", "patch"], (s) => {
  bt.headers[s] = {};
});
const of = w.toObjectSet([
  "age",
  "authorization",
  "content-length",
  "content-type",
  "etag",
  "expires",
  "from",
  "host",
  "if-modified-since",
  "if-unmodified-since",
  "last-modified",
  "location",
  "max-forwards",
  "proxy-authorization",
  "referer",
  "retry-after",
  "user-agent"
]), af = (s) => {
  const e = {};
  let t, i, n;
  return s && s.split(`
`).forEach(function(o) {
    n = o.indexOf(":"), t = o.substring(0, n).trim().toLowerCase(), i = o.substring(n + 1).trim(), !(!t || e[t] && of[t]) && (t === "set-cookie" ? e[t] ? e[t].push(i) : e[t] = [i] : e[t] = e[t] ? e[t] + ", " + i : i);
  }), e;
}, En = Symbol("internals");
function tt(s) {
  return s && String(s).trim().toLowerCase();
}
function jt(s) {
  return s === !1 || s == null ? s : w.isArray(s) ? s.map(jt) : String(s);
}
function lf(s) {
  const e = /* @__PURE__ */ Object.create(null), t = /([^\s,;=]+)\s*(?:=\s*([^,;]+))?/g;
  let i;
  for (; i = t.exec(s); )
    e[i[1]] = i[2];
  return e;
}
const cf = (s) => /^[-_a-zA-Z0-9^`|~,!#$%&'*+.]+$/.test(s.trim());
function Cs(s, e, t, i, n) {
  if (w.isFunction(i))
    return i.call(this, e, t);
  if (n && (e = t), !!w.isString(e)) {
    if (w.isString(i))
      return e.indexOf(i) !== -1;
    if (w.isRegExp(i))
      return i.test(e);
  }
}
function hf(s) {
  return s.trim().toLowerCase().replace(/([a-z\d])(\w*)/g, (e, t, i) => t.toUpperCase() + i);
}
function uf(s, e) {
  const t = w.toCamelCase(" " + e);
  ["get", "set", "has"].forEach((i) => {
    Object.defineProperty(s, i + t, {
      value: function(n, r, o) {
        return this[i].call(this, e, n, r, o);
      },
      configurable: !0
    });
  });
}
let Y = class {
  constructor(e) {
    e && this.set(e);
  }
  set(e, t, i) {
    const n = this;
    function r(a, l, c) {
      const h = tt(l);
      if (!h)
        throw new Error("header name must be a non-empty string");
      const u = w.findKey(n, h);
      (!u || n[u] === void 0 || c === !0 || c === void 0 && n[u] !== !1) && (n[u || l] = jt(a));
    }
    const o = (a, l) => w.forEach(a, (c, h) => r(c, h, l));
    if (w.isPlainObject(e) || e instanceof this.constructor)
      o(e, t);
    else if (w.isString(e) && (e = e.trim()) && !cf(e))
      o(af(e), t);
    else if (w.isObject(e) && w.isIterable(e)) {
      let a = {}, l, c;
      for (const h of e) {
        if (!w.isArray(h))
          throw TypeError("Object iterator must return a key-value pair");
        a[c = h[0]] = (l = a[c]) ? w.isArray(l) ? [...l, h[1]] : [l, h[1]] : h[1];
      }
      o(a, t);
    } else
      e != null && r(t, e, i);
    return this;
  }
  get(e, t) {
    if (e = tt(e), e) {
      const i = w.findKey(this, e);
      if (i) {
        const n = this[i];
        if (!t)
          return n;
        if (t === !0)
          return lf(n);
        if (w.isFunction(t))
          return t.call(this, n, i);
        if (w.isRegExp(t))
          return t.exec(n);
        throw new TypeError("parser must be boolean|regexp|function");
      }
    }
  }
  has(e, t) {
    if (e = tt(e), e) {
      const i = w.findKey(this, e);
      return !!(i && this[i] !== void 0 && (!t || Cs(this, this[i], i, t)));
    }
    return !1;
  }
  delete(e, t) {
    const i = this;
    let n = !1;
    function r(o) {
      if (o = tt(o), o) {
        const a = w.findKey(i, o);
        a && (!t || Cs(i, i[a], a, t)) && (delete i[a], n = !0);
      }
    }
    return w.isArray(e) ? e.forEach(r) : r(e), n;
  }
  clear(e) {
    const t = Object.keys(this);
    let i = t.length, n = !1;
    for (; i--; ) {
      const r = t[i];
      (!e || Cs(this, this[r], r, e, !0)) && (delete this[r], n = !0);
    }
    return n;
  }
  normalize(e) {
    const t = this, i = {};
    return w.forEach(this, (n, r) => {
      const o = w.findKey(i, r);
      if (o) {
        t[o] = jt(n), delete t[r];
        return;
      }
      const a = e ? hf(r) : String(r).trim();
      a !== r && delete t[r], t[a] = jt(n), i[a] = !0;
    }), this;
  }
  concat(...e) {
    return this.constructor.concat(this, ...e);
  }
  toJSON(e) {
    const t = /* @__PURE__ */ Object.create(null);
    return w.forEach(this, (i, n) => {
      i != null && i !== !1 && (t[n] = e && w.isArray(i) ? i.join(", ") : i);
    }), t;
  }
  [Symbol.iterator]() {
    return Object.entries(this.toJSON())[Symbol.iterator]();
  }
  toString() {
    return Object.entries(this.toJSON()).map(([e, t]) => e + ": " + t).join(`
`);
  }
  getSetCookie() {
    return this.get("set-cookie") || [];
  }
  get [Symbol.toStringTag]() {
    return "AxiosHeaders";
  }
  static from(e) {
    return e instanceof this ? e : new this(e);
  }
  static concat(e, ...t) {
    const i = new this(e);
    return t.forEach((n) => i.set(n)), i;
  }
  static accessor(e) {
    const i = (this[En] = this[En] = {
      accessors: {}
    }).accessors, n = this.prototype;
    function r(o) {
      const a = tt(o);
      i[a] || (uf(n, o), i[a] = !0);
    }
    return w.isArray(e) ? e.forEach(r) : r(e), this;
  }
};
Y.accessor(["Content-Type", "Content-Length", "Accept", "Accept-Encoding", "User-Agent", "Authorization"]);
w.reduceDescriptors(Y.prototype, ({ value: s }, e) => {
  let t = e[0].toUpperCase() + e.slice(1);
  return {
    get: () => s,
    set(i) {
      this[t] = i;
    }
  };
});
w.freezeMethods(Y);
function vs(s, e) {
  const t = this || bt, i = e || t, n = Y.from(i.headers);
  let r = i.data;
  return w.forEach(s, function(a) {
    r = a.call(t, r, n.normalize(), e ? e.status : void 0);
  }), n.normalize(), r;
}
function Zr(s) {
  return !!(s && s.__CANCEL__);
}
function Je(s, e, t) {
  A.call(this, s ?? "canceled", A.ERR_CANCELED, e, t), this.name = "CanceledError";
}
w.inherits(Je, A, {
  __CANCEL__: !0
});
function Yr(s, e, t) {
  const i = t.config.validateStatus;
  !t.status || !i || i(t.status) ? s(t) : e(new A(
    "Request failed with status code " + t.status,
    [A.ERR_BAD_REQUEST, A.ERR_BAD_RESPONSE][Math.floor(t.status / 100) - 4],
    t.config,
    t.request,
    t
  ));
}
function ff(s) {
  const e = /^([-+\w]{1,25})(:?\/\/|:)/.exec(s);
  return e && e[1] || "";
}
function df(s, e) {
  s = s || 10;
  const t = new Array(s), i = new Array(s);
  let n = 0, r = 0, o;
  return e = e !== void 0 ? e : 1e3, function(l) {
    const c = Date.now(), h = i[r];
    o || (o = c), t[n] = l, i[n] = c;
    let u = r, p = 0;
    for (; u !== n; )
      p += t[u++], u = u % s;
    if (n = (n + 1) % s, n === r && (r = (r + 1) % s), c - o < e)
      return;
    const d = h && c - h;
    return d ? Math.round(p * 1e3 / d) : void 0;
  };
}
function pf(s, e) {
  let t = 0, i = 1e3 / e, n, r;
  const o = (c, h = Date.now()) => {
    t = h, n = null, r && (clearTimeout(r), r = null), s(...c);
  };
  return [(...c) => {
    const h = Date.now(), u = h - t;
    u >= i ? o(c, h) : (n = c, r || (r = setTimeout(() => {
      r = null, o(n);
    }, i - u)));
  }, () => n && o(n)];
}
const Gt = (s, e, t = 3) => {
  let i = 0;
  const n = df(50, 250);
  return pf((r) => {
    const o = r.loaded, a = r.lengthComputable ? r.total : void 0, l = o - i, c = n(l), h = o <= a;
    i = o;
    const u = {
      loaded: o,
      total: a,
      progress: a ? o / a : void 0,
      bytes: l,
      rate: c || void 0,
      estimated: c && a && h ? (a - o) / c : void 0,
      event: r,
      lengthComputable: a != null,
      [e ? "download" : "upload"]: !0
    };
    s(u);
  }, t);
}, Sn = (s, e) => {
  const t = s != null;
  return [(i) => e[0]({
    lengthComputable: t,
    total: s,
    loaded: i
  }), e[1]];
}, Rn = (s) => (...e) => w.asap(() => s(...e)), mf = $.hasStandardBrowserEnv ? /* @__PURE__ */ ((s, e) => (t) => (t = new URL(t, $.origin), s.protocol === t.protocol && s.host === t.host && (e || s.port === t.port)))(
  new URL($.origin),
  $.navigator && /(msie|trident)/i.test($.navigator.userAgent)
) : () => !0, gf = $.hasStandardBrowserEnv ? (
  // Standard browser envs support document.cookie
  {
    write(s, e, t, i, n, r) {
      const o = [s + "=" + encodeURIComponent(e)];
      w.isNumber(t) && o.push("expires=" + new Date(t).toGMTString()), w.isString(i) && o.push("path=" + i), w.isString(n) && o.push("domain=" + n), r === !0 && o.push("secure"), document.cookie = o.join("; ");
    },
    read(s) {
      const e = document.cookie.match(new RegExp("(^|;\\s*)(" + s + ")=([^;]*)"));
      return e ? decodeURIComponent(e[3]) : null;
    },
    remove(s) {
      this.write(s, "", Date.now() - 864e5);
    }
  }
) : (
  // Non-standard browser env (web workers, react-native) lack needed support.
  {
    write() {
    },
    read() {
      return null;
    },
    remove() {
    }
  }
);
function wf(s) {
  return /^([a-z][a-z\d+\-.]*:)?\/\//i.test(s);
}
function bf(s, e) {
  return e ? s.replace(/\/?\/$/, "") + "/" + e.replace(/^\/+/, "") : s;
}
function Qr(s, e, t) {
  let i = !wf(e);
  return s && (i || t == !1) ? bf(s, e) : e;
}
const Tn = (s) => s instanceof Y ? { ...s } : s;
function Pe(s, e) {
  e = e || {};
  const t = {};
  function i(c, h, u, p) {
    return w.isPlainObject(c) && w.isPlainObject(h) ? w.merge.call({ caseless: p }, c, h) : w.isPlainObject(h) ? w.merge({}, h) : w.isArray(h) ? h.slice() : h;
  }
  function n(c, h, u, p) {
    if (w.isUndefined(h)) {
      if (!w.isUndefined(c))
        return i(void 0, c, u, p);
    } else return i(c, h, u, p);
  }
  function r(c, h) {
    if (!w.isUndefined(h))
      return i(void 0, h);
  }
  function o(c, h) {
    if (w.isUndefined(h)) {
      if (!w.isUndefined(c))
        return i(void 0, c);
    } else return i(void 0, h);
  }
  function a(c, h, u) {
    if (u in e)
      return i(c, h);
    if (u in s)
      return i(void 0, c);
  }
  const l = {
    url: r,
    method: r,
    data: r,
    baseURL: o,
    transformRequest: o,
    transformResponse: o,
    paramsSerializer: o,
    timeout: o,
    timeoutMessage: o,
    withCredentials: o,
    withXSRFToken: o,
    adapter: o,
    responseType: o,
    xsrfCookieName: o,
    xsrfHeaderName: o,
    onUploadProgress: o,
    onDownloadProgress: o,
    decompress: o,
    maxContentLength: o,
    maxBodyLength: o,
    beforeRedirect: o,
    transport: o,
    httpAgent: o,
    httpsAgent: o,
    cancelToken: o,
    socketPath: o,
    responseEncoding: o,
    validateStatus: a,
    headers: (c, h, u) => n(Tn(c), Tn(h), u, !0)
  };
  return w.forEach(Object.keys({ ...s, ...e }), function(h) {
    const u = l[h] || n, p = u(s[h], e[h], h);
    w.isUndefined(p) && u !== a || (t[h] = p);
  }), t;
}
const eo = (s) => {
  const e = Pe({}, s);
  let { data: t, withXSRFToken: i, xsrfHeaderName: n, xsrfCookieName: r, headers: o, auth: a } = e;
  e.headers = o = Y.from(o), e.url = Kr(Qr(e.baseURL, e.url, e.allowAbsoluteUrls), s.params, s.paramsSerializer), a && o.set(
    "Authorization",
    "Basic " + btoa((a.username || "") + ":" + (a.password ? unescape(encodeURIComponent(a.password)) : ""))
  );
  let l;
  if (w.isFormData(t)) {
    if ($.hasStandardBrowserEnv || $.hasStandardBrowserWebWorkerEnv)
      o.setContentType(void 0);
    else if ((l = o.getContentType()) !== !1) {
      const [c, ...h] = l ? l.split(";").map((u) => u.trim()).filter(Boolean) : [];
      o.setContentType([c || "multipart/form-data", ...h].join("; "));
    }
  }
  if ($.hasStandardBrowserEnv && (i && w.isFunction(i) && (i = i(e)), i || i !== !1 && mf(e.url))) {
    const c = n && r && gf.read(r);
    c && o.set(n, c);
  }
  return e;
}, yf = typeof XMLHttpRequest < "u", Ef = yf && function(s) {
  return new Promise(function(t, i) {
    const n = eo(s);
    let r = n.data;
    const o = Y.from(n.headers).normalize();
    let { responseType: a, onUploadProgress: l, onDownloadProgress: c } = n, h, u, p, d, m;
    function f() {
      d && d(), m && m(), n.cancelToken && n.cancelToken.unsubscribe(h), n.signal && n.signal.removeEventListener("abort", h);
    }
    let b = new XMLHttpRequest();
    b.open(n.method.toUpperCase(), n.url, !0), b.timeout = n.timeout;
    function y() {
      if (!b)
        return;
      const S = Y.from(
        "getAllResponseHeaders" in b && b.getAllResponseHeaders()
      ), O = {
        data: !a || a === "text" || a === "json" ? b.responseText : b.response,
        status: b.status,
        statusText: b.statusText,
        headers: S,
        config: s,
        request: b
      };
      Yr(function(P) {
        t(P), f();
      }, function(P) {
        i(P), f();
      }, O), b = null;
    }
    "onloadend" in b ? b.onloadend = y : b.onreadystatechange = function() {
      !b || b.readyState !== 4 || b.status === 0 && !(b.responseURL && b.responseURL.indexOf("file:") === 0) || setTimeout(y);
    }, b.onabort = function() {
      b && (i(new A("Request aborted", A.ECONNABORTED, s, b)), b = null);
    }, b.onerror = function() {
      i(new A("Network Error", A.ERR_NETWORK, s, b)), b = null;
    }, b.ontimeout = function() {
      let T = n.timeout ? "timeout of " + n.timeout + "ms exceeded" : "timeout exceeded";
      const O = n.transitional || Vr;
      n.timeoutErrorMessage && (T = n.timeoutErrorMessage), i(new A(
        T,
        O.clarifyTimeoutError ? A.ETIMEDOUT : A.ECONNABORTED,
        s,
        b
      )), b = null;
    }, r === void 0 && o.setContentType(null), "setRequestHeader" in b && w.forEach(o.toJSON(), function(T, O) {
      b.setRequestHeader(O, T);
    }), w.isUndefined(n.withCredentials) || (b.withCredentials = !!n.withCredentials), a && a !== "json" && (b.responseType = n.responseType), c && ([p, m] = Gt(c, !0), b.addEventListener("progress", p)), l && b.upload && ([u, d] = Gt(l), b.upload.addEventListener("progress", u), b.upload.addEventListener("loadend", d)), (n.cancelToken || n.signal) && (h = (S) => {
      b && (i(!S || S.type ? new Je(null, s, b) : S), b.abort(), b = null);
    }, n.cancelToken && n.cancelToken.subscribe(h), n.signal && (n.signal.aborted ? h() : n.signal.addEventListener("abort", h)));
    const E = ff(n.url);
    if (E && $.protocols.indexOf(E) === -1) {
      i(new A("Unsupported protocol " + E + ":", A.ERR_BAD_REQUEST, s));
      return;
    }
    b.send(r || null);
  });
}, Sf = (s, e) => {
  const { length: t } = s = s ? s.filter(Boolean) : [];
  if (e || t) {
    let i = new AbortController(), n;
    const r = function(c) {
      if (!n) {
        n = !0, a();
        const h = c instanceof Error ? c : this.reason;
        i.abort(h instanceof A ? h : new Je(h instanceof Error ? h.message : h));
      }
    };
    let o = e && setTimeout(() => {
      o = null, r(new A(`timeout ${e} of ms exceeded`, A.ETIMEDOUT));
    }, e);
    const a = () => {
      s && (o && clearTimeout(o), o = null, s.forEach((c) => {
        c.unsubscribe ? c.unsubscribe(r) : c.removeEventListener("abort", r);
      }), s = null);
    };
    s.forEach((c) => c.addEventListener("abort", r));
    const { signal: l } = i;
    return l.unsubscribe = () => w.asap(a), l;
  }
}, Rf = function* (s, e) {
  let t = s.byteLength;
  if (t < e) {
    yield s;
    return;
  }
  let i = 0, n;
  for (; i < t; )
    n = i + e, yield s.slice(i, n), i = n;
}, Tf = async function* (s, e) {
  for await (const t of Of(s))
    yield* Rf(t, e);
}, Of = async function* (s) {
  if (s[Symbol.asyncIterator]) {
    yield* s;
    return;
  }
  const e = s.getReader();
  try {
    for (; ; ) {
      const { done: t, value: i } = await e.read();
      if (t)
        break;
      yield i;
    }
  } finally {
    await e.cancel();
  }
}, On = (s, e, t, i) => {
  const n = Tf(s, e);
  let r = 0, o, a = (l) => {
    o || (o = !0, i && i(l));
  };
  return new ReadableStream({
    async pull(l) {
      try {
        const { done: c, value: h } = await n.next();
        if (c) {
          a(), l.close();
          return;
        }
        let u = h.byteLength;
        if (t) {
          let p = r += u;
          t(p);
        }
        l.enqueue(new Uint8Array(h));
      } catch (c) {
        throw a(c), c;
      }
    },
    cancel(l) {
      return a(l), n.return();
    }
  }, {
    highWaterMark: 2
  });
}, us = typeof fetch == "function" && typeof Request == "function" && typeof Response == "function", to = us && typeof ReadableStream == "function", xf = us && (typeof TextEncoder == "function" ? /* @__PURE__ */ ((s) => (e) => s.encode(e))(new TextEncoder()) : async (s) => new Uint8Array(await new Response(s).arrayBuffer())), so = (s, ...e) => {
  try {
    return !!s(...e);
  } catch {
    return !1;
  }
}, Af = to && so(() => {
  let s = !1;
  const e = new Request($.origin, {
    body: new ReadableStream(),
    method: "POST",
    get duplex() {
      return s = !0, "half";
    }
  }).headers.has("Content-Type");
  return s && !e;
}), xn = 64 * 1024, Hs = to && so(() => w.isReadableStream(new Response("").body)), Jt = {
  stream: Hs && ((s) => s.body)
};
us && ((s) => {
  ["text", "arrayBuffer", "blob", "formData", "stream"].forEach((e) => {
    !Jt[e] && (Jt[e] = w.isFunction(s[e]) ? (t) => t[e]() : (t, i) => {
      throw new A(`Response type '${e}' is not supported`, A.ERR_NOT_SUPPORT, i);
    });
  });
})(new Response());
const Cf = async (s) => {
  if (s == null)
    return 0;
  if (w.isBlob(s))
    return s.size;
  if (w.isSpecCompliantForm(s))
    return (await new Request($.origin, {
      method: "POST",
      body: s
    }).arrayBuffer()).byteLength;
  if (w.isArrayBufferView(s) || w.isArrayBuffer(s))
    return s.byteLength;
  if (w.isURLSearchParams(s) && (s = s + ""), w.isString(s))
    return (await xf(s)).byteLength;
}, vf = async (s, e) => {
  const t = w.toFiniteNumber(s.getContentLength());
  return t ?? Cf(e);
}, Nf = us && (async (s) => {
  let {
    url: e,
    method: t,
    data: i,
    signal: n,
    cancelToken: r,
    timeout: o,
    onDownloadProgress: a,
    onUploadProgress: l,
    responseType: c,
    headers: h,
    withCredentials: u = "same-origin",
    fetchOptions: p
  } = eo(s);
  c = c ? (c + "").toLowerCase() : "text";
  let d = Sf([n, r && r.toAbortSignal()], o), m;
  const f = d && d.unsubscribe && (() => {
    d.unsubscribe();
  });
  let b;
  try {
    if (l && Af && t !== "get" && t !== "head" && (b = await vf(h, i)) !== 0) {
      let O = new Request(e, {
        method: "POST",
        body: i,
        duplex: "half"
      }), v;
      if (w.isFormData(i) && (v = O.headers.get("content-type")) && h.setContentType(v), O.body) {
        const [P, B] = Sn(
          b,
          Gt(Rn(l))
        );
        i = On(O.body, xn, P, B);
      }
    }
    w.isString(u) || (u = u ? "include" : "omit");
    const y = "credentials" in Request.prototype;
    m = new Request(e, {
      ...p,
      signal: d,
      method: t.toUpperCase(),
      headers: h.normalize().toJSON(),
      body: i,
      duplex: "half",
      credentials: y ? u : void 0
    });
    let E = await fetch(m, p);
    const S = Hs && (c === "stream" || c === "response");
    if (Hs && (a || S && f)) {
      const O = {};
      ["status", "statusText", "headers"].forEach((z) => {
        O[z] = E[z];
      });
      const v = w.toFiniteNumber(E.headers.get("content-length")), [P, B] = a && Sn(
        v,
        Gt(Rn(a), !0)
      ) || [];
      E = new Response(
        On(E.body, xn, P, () => {
          B && B(), f && f();
        }),
        O
      );
    }
    c = c || "text";
    let T = await Jt[w.findKey(Jt, c) || "text"](E, s);
    return !S && f && f(), await new Promise((O, v) => {
      Yr(O, v, {
        data: T,
        headers: Y.from(E.headers),
        status: E.status,
        statusText: E.statusText,
        config: s,
        request: m
      });
    });
  } catch (y) {
    throw f && f(), y && y.name === "TypeError" && /Load failed|fetch/i.test(y.message) ? Object.assign(
      new A("Network Error", A.ERR_NETWORK, s, m),
      {
        cause: y.cause || y
      }
    ) : A.from(y, y && y.code, s, m);
  }
}), Gs = {
  http: Wu,
  xhr: Ef,
  fetch: Nf
};
w.forEach(Gs, (s, e) => {
  if (s) {
    try {
      Object.defineProperty(s, "name", { value: e });
    } catch {
    }
    Object.defineProperty(s, "adapterName", { value: e });
  }
});
const An = (s) => `- ${s}`, _f = (s) => w.isFunction(s) || s === null || s === !1, io = {
  getAdapter: (s) => {
    s = w.isArray(s) ? s : [s];
    const { length: e } = s;
    let t, i;
    const n = {};
    for (let r = 0; r < e; r++) {
      t = s[r];
      let o;
      if (i = t, !_f(t) && (i = Gs[(o = String(t)).toLowerCase()], i === void 0))
        throw new A(`Unknown adapter '${o}'`);
      if (i)
        break;
      n[o || "#" + r] = i;
    }
    if (!i) {
      const r = Object.entries(n).map(
        ([a, l]) => `adapter ${a} ` + (l === !1 ? "is not supported by the environment" : "is not available in the build")
      );
      let o = e ? r.length > 1 ? `since :
` + r.map(An).join(`
`) : " " + An(r[0]) : "as no adapter specified";
      throw new A(
        "There is no suitable adapter to dispatch the request " + o,
        "ERR_NOT_SUPPORT"
      );
    }
    return i;
  },
  adapters: Gs
};
function Ns(s) {
  if (s.cancelToken && s.cancelToken.throwIfRequested(), s.signal && s.signal.aborted)
    throw new Je(null, s);
}
function Cn(s) {
  return Ns(s), s.headers = Y.from(s.headers), s.data = vs.call(
    s,
    s.transformRequest
  ), ["post", "put", "patch"].indexOf(s.method) !== -1 && s.headers.setContentType("application/x-www-form-urlencoded", !1), io.getAdapter(s.adapter || bt.adapter)(s).then(function(i) {
    return Ns(s), i.data = vs.call(
      s,
      s.transformResponse,
      i
    ), i.headers = Y.from(i.headers), i;
  }, function(i) {
    return Zr(i) || (Ns(s), i && i.response && (i.response.data = vs.call(
      s,
      s.transformResponse,
      i.response
    ), i.response.headers = Y.from(i.response.headers))), Promise.reject(i);
  });
}
const no = "1.11.0", fs = {};
["object", "boolean", "number", "function", "string", "symbol"].forEach((s, e) => {
  fs[s] = function(i) {
    return typeof i === s || "a" + (e < 1 ? "n " : " ") + s;
  };
});
const vn = {};
fs.transitional = function(e, t, i) {
  function n(r, o) {
    return "[Axios v" + no + "] Transitional option '" + r + "'" + o + (i ? ". " + i : "");
  }
  return (r, o, a) => {
    if (e === !1)
      throw new A(
        n(o, " has been removed" + (t ? " in " + t : "")),
        A.ERR_DEPRECATED
      );
    return t && !vn[o] && (vn[o] = !0, console.warn(
      n(
        o,
        " has been deprecated since v" + t + " and will be removed in the near future"
      )
    )), e ? e(r, o, a) : !0;
  };
};
fs.spelling = function(e) {
  return (t, i) => (console.warn(`${i} is likely a misspelling of ${e}`), !0);
};
function Pf(s, e, t) {
  if (typeof s != "object")
    throw new A("options must be an object", A.ERR_BAD_OPTION_VALUE);
  const i = Object.keys(s);
  let n = i.length;
  for (; n-- > 0; ) {
    const r = i[n], o = e[r];
    if (o) {
      const a = s[r], l = a === void 0 || o(a, r, s);
      if (l !== !0)
        throw new A("option " + r + " must be " + l, A.ERR_BAD_OPTION_VALUE);
      continue;
    }
    if (t !== !0)
      throw new A("Unknown option " + r, A.ERR_BAD_OPTION);
  }
}
const Ut = {
  assertOptions: Pf,
  validators: fs
}, ce = Ut.validators;
let Ne = class {
  constructor(e) {
    this.defaults = e || {}, this.interceptors = {
      request: new yn(),
      response: new yn()
    };
  }
  /**
   * Dispatch a request
   *
   * @param {String|Object} configOrUrl The config specific for this request (merged with this.defaults)
   * @param {?Object} config
   *
   * @returns {Promise} The Promise to be fulfilled
   */
  async request(e, t) {
    try {
      return await this._request(e, t);
    } catch (i) {
      if (i instanceof Error) {
        let n = {};
        Error.captureStackTrace ? Error.captureStackTrace(n) : n = new Error();
        const r = n.stack ? n.stack.replace(/^.+\n/, "") : "";
        try {
          i.stack ? r && !String(i.stack).endsWith(r.replace(/^.+\n.+\n/, "")) && (i.stack += `
` + r) : i.stack = r;
        } catch {
        }
      }
      throw i;
    }
  }
  _request(e, t) {
    typeof e == "string" ? (t = t || {}, t.url = e) : t = e || {}, t = Pe(this.defaults, t);
    const { transitional: i, paramsSerializer: n, headers: r } = t;
    i !== void 0 && Ut.assertOptions(i, {
      silentJSONParsing: ce.transitional(ce.boolean),
      forcedJSONParsing: ce.transitional(ce.boolean),
      clarifyTimeoutError: ce.transitional(ce.boolean)
    }, !1), n != null && (w.isFunction(n) ? t.paramsSerializer = {
      serialize: n
    } : Ut.assertOptions(n, {
      encode: ce.function,
      serialize: ce.function
    }, !0)), t.allowAbsoluteUrls !== void 0 || (this.defaults.allowAbsoluteUrls !== void 0 ? t.allowAbsoluteUrls = this.defaults.allowAbsoluteUrls : t.allowAbsoluteUrls = !0), Ut.assertOptions(t, {
      baseUrl: ce.spelling("baseURL"),
      withXsrfToken: ce.spelling("withXSRFToken")
    }, !0), t.method = (t.method || this.defaults.method || "get").toLowerCase();
    let o = r && w.merge(
      r.common,
      r[t.method]
    );
    r && w.forEach(
      ["delete", "get", "head", "post", "put", "patch", "common"],
      (m) => {
        delete r[m];
      }
    ), t.headers = Y.concat(o, r);
    const a = [];
    let l = !0;
    this.interceptors.request.forEach(function(f) {
      typeof f.runWhen == "function" && f.runWhen(t) === !1 || (l = l && f.synchronous, a.unshift(f.fulfilled, f.rejected));
    });
    const c = [];
    this.interceptors.response.forEach(function(f) {
      c.push(f.fulfilled, f.rejected);
    });
    let h, u = 0, p;
    if (!l) {
      const m = [Cn.bind(this), void 0];
      for (m.unshift(...a), m.push(...c), p = m.length, h = Promise.resolve(t); u < p; )
        h = h.then(m[u++], m[u++]);
      return h;
    }
    p = a.length;
    let d = t;
    for (u = 0; u < p; ) {
      const m = a[u++], f = a[u++];
      try {
        d = m(d);
      } catch (b) {
        f.call(this, b);
        break;
      }
    }
    try {
      h = Cn.call(this, d);
    } catch (m) {
      return Promise.reject(m);
    }
    for (u = 0, p = c.length; u < p; )
      h = h.then(c[u++], c[u++]);
    return h;
  }
  getUri(e) {
    e = Pe(this.defaults, e);
    const t = Qr(e.baseURL, e.url, e.allowAbsoluteUrls);
    return Kr(t, e.params, e.paramsSerializer);
  }
};
w.forEach(["delete", "get", "head", "options"], function(e) {
  Ne.prototype[e] = function(t, i) {
    return this.request(Pe(i || {}, {
      method: e,
      url: t,
      data: (i || {}).data
    }));
  };
});
w.forEach(["post", "put", "patch"], function(e) {
  function t(i) {
    return function(r, o, a) {
      return this.request(Pe(a || {}, {
        method: e,
        headers: i ? {
          "Content-Type": "multipart/form-data"
        } : {},
        url: r,
        data: o
      }));
    };
  }
  Ne.prototype[e] = t(), Ne.prototype[e + "Form"] = t(!0);
});
let Ff = class ro {
  constructor(e) {
    if (typeof e != "function")
      throw new TypeError("executor must be a function.");
    let t;
    this.promise = new Promise(function(r) {
      t = r;
    });
    const i = this;
    this.promise.then((n) => {
      if (!i._listeners) return;
      let r = i._listeners.length;
      for (; r-- > 0; )
        i._listeners[r](n);
      i._listeners = null;
    }), this.promise.then = (n) => {
      let r;
      const o = new Promise((a) => {
        i.subscribe(a), r = a;
      }).then(n);
      return o.cancel = function() {
        i.unsubscribe(r);
      }, o;
    }, e(function(r, o, a) {
      i.reason || (i.reason = new Je(r, o, a), t(i.reason));
    });
  }
  /**
   * Throws a `CanceledError` if cancellation has been requested.
   */
  throwIfRequested() {
    if (this.reason)
      throw this.reason;
  }
  /**
   * Subscribe to the cancel signal
   */
  subscribe(e) {
    if (this.reason) {
      e(this.reason);
      return;
    }
    this._listeners ? this._listeners.push(e) : this._listeners = [e];
  }
  /**
   * Unsubscribe from the cancel signal
   */
  unsubscribe(e) {
    if (!this._listeners)
      return;
    const t = this._listeners.indexOf(e);
    t !== -1 && this._listeners.splice(t, 1);
  }
  toAbortSignal() {
    const e = new AbortController(), t = (i) => {
      e.abort(i);
    };
    return this.subscribe(t), e.signal.unsubscribe = () => this.unsubscribe(t), e.signal;
  }
  /**
   * Returns an object that contains a new `CancelToken` and a function that, when called,
   * cancels the `CancelToken`.
   */
  static source() {
    let e;
    return {
      token: new ro(function(n) {
        e = n;
      }),
      cancel: e
    };
  }
};
function Df(s) {
  return function(t) {
    return s.apply(null, t);
  };
}
function kf(s) {
  return w.isObject(s) && s.isAxiosError === !0;
}
const Js = {
  Continue: 100,
  SwitchingProtocols: 101,
  Processing: 102,
  EarlyHints: 103,
  Ok: 200,
  Created: 201,
  Accepted: 202,
  NonAuthoritativeInformation: 203,
  NoContent: 204,
  ResetContent: 205,
  PartialContent: 206,
  MultiStatus: 207,
  AlreadyReported: 208,
  ImUsed: 226,
  MultipleChoices: 300,
  MovedPermanently: 301,
  Found: 302,
  SeeOther: 303,
  NotModified: 304,
  UseProxy: 305,
  Unused: 306,
  TemporaryRedirect: 307,
  PermanentRedirect: 308,
  BadRequest: 400,
  Unauthorized: 401,
  PaymentRequired: 402,
  Forbidden: 403,
  NotFound: 404,
  MethodNotAllowed: 405,
  NotAcceptable: 406,
  ProxyAuthenticationRequired: 407,
  RequestTimeout: 408,
  Conflict: 409,
  Gone: 410,
  LengthRequired: 411,
  PreconditionFailed: 412,
  PayloadTooLarge: 413,
  UriTooLong: 414,
  UnsupportedMediaType: 415,
  RangeNotSatisfiable: 416,
  ExpectationFailed: 417,
  ImATeapot: 418,
  MisdirectedRequest: 421,
  UnprocessableEntity: 422,
  Locked: 423,
  FailedDependency: 424,
  TooEarly: 425,
  UpgradeRequired: 426,
  PreconditionRequired: 428,
  TooManyRequests: 429,
  RequestHeaderFieldsTooLarge: 431,
  UnavailableForLegalReasons: 451,
  InternalServerError: 500,
  NotImplemented: 501,
  BadGateway: 502,
  ServiceUnavailable: 503,
  GatewayTimeout: 504,
  HttpVersionNotSupported: 505,
  VariantAlsoNegotiates: 506,
  InsufficientStorage: 507,
  LoopDetected: 508,
  NotExtended: 510,
  NetworkAuthenticationRequired: 511
};
Object.entries(Js).forEach(([s, e]) => {
  Js[e] = s;
});
function oo(s) {
  const e = new Ne(s), t = jr(Ne.prototype.request, e);
  return w.extend(t, Ne.prototype, e, { allOwnKeys: !0 }), w.extend(t, e, null, { allOwnKeys: !0 }), t.create = function(n) {
    return oo(Pe(s, n));
  }, t;
}
const k = oo(bt);
k.Axios = Ne;
k.CanceledError = Je;
k.CancelToken = Ff;
k.isCancel = Zr;
k.VERSION = no;
k.toFormData = hs;
k.AxiosError = A;
k.Cancel = k.CanceledError;
k.all = function(e) {
  return Promise.all(e);
};
k.spread = Df;
k.isAxiosError = kf;
k.mergeConfig = Pe;
k.AxiosHeaders = Y;
k.formToJSON = (s) => Xr(w.isHTMLForm(s) ? new FormData(s) : s);
k.getAdapter = io.getAdapter;
k.HttpStatusCode = Js;
k.default = k;
const {
  Axios: Cd,
  AxiosError: vd,
  CanceledError: Nd,
  isCancel: _d,
  CancelToken: Pd,
  VERSION: Fd,
  all: Dd,
  Cancel: kd,
  isAxiosError: Ld,
  spread: jd,
  toFormData: Ud,
  AxiosHeaders: Bd,
  HttpStatusCode: Id,
  formToJSON: Md,
  getAdapter: $d,
  mergeConfig: zd
} = k;
class Lf {
  value;
  next;
  constructor(e) {
    this.value = e;
  }
}
class jf {
  #e;
  #t;
  #s;
  constructor() {
    this.clear();
  }
  enqueue(e) {
    const t = new Lf(e);
    this.#e ? (this.#t.next = t, this.#t = t) : (this.#e = t, this.#t = t), this.#s++;
  }
  dequeue() {
    const e = this.#e;
    if (e)
      return this.#e = this.#e.next, this.#s--, e.value;
  }
  peek() {
    if (this.#e)
      return this.#e.value;
  }
  clear() {
    this.#e = void 0, this.#t = void 0, this.#s = 0;
  }
  get size() {
    return this.#s;
  }
  *[Symbol.iterator]() {
    let e = this.#e;
    for (; e; )
      yield e.value, e = e.next;
  }
  *drain() {
    for (; this.#e; )
      yield this.dequeue();
  }
}
function Uf(s) {
  if (!((Number.isInteger(s) || s === Number.POSITIVE_INFINITY) && s > 0))
    throw new TypeError("Expected `concurrency` to be a number from 1 and up");
  const e = new jf();
  let t = 0;
  const i = () => {
    t--, e.size > 0 && e.dequeue()();
  }, n = async (a, l, c) => {
    t++;
    const h = (async () => a(...c))();
    l(h);
    try {
      await h;
    } catch {
    }
    i();
  }, r = (a, l, c) => {
    e.enqueue(n.bind(void 0, a, l, c)), (async () => (await Promise.resolve(), t < s && e.size > 0 && e.dequeue()()))();
  }, o = (a, ...l) => new Promise((c) => {
    r(a, c, l);
  });
  return Object.defineProperties(o, {
    activeCount: {
      get: () => t
    },
    pendingCount: {
      get: () => e.size
    },
    clearQueue: {
      value: () => {
        e.clear();
      }
    }
  }), o;
}
class De extends Error {
  code;
  details;
  data;
  constructor({ code: e, message: t, details: i, data: n }) {
    super(t), this.code = e, this.details = i, this.data = n, this.name = "CliError";
  }
  toString() {
    return `[${this.name}] [${this.code}]: ${this.message}`;
  }
}
class Ks extends De {
  constructor({ file: e, details: t }) {
    super({
      code: "CLI::0002",
      message: `${e ? `${e}` : "File"} not found on current directory. Verify and try again.`,
      details: t
    }), this.name = "FileNotFoundError";
  }
}
class Bf extends De {
  constructor(e) {
    super({
      code: "CLI::0005",
      message: "Unable to load theme asset data. See details for more information.",
      details: e
    }), this.name = "LoadThemeAssetError";
  }
  toString() {
    return `[${this.name}] [${this.code}]: ${this.message}${ge}${this.details}`;
  }
}
class If extends De {
  constructor(e) {
    super({
      code: "CLI::0003",
      message: `${e} not defined. Please verify and try again.`
    }), this.name = "ParameterNotDefinedError";
  }
}
class Mf extends De {
  constructor(e) {
    super({
      code: "CLI::0001",
      message: "Unable to save configuration file. See details for more information.",
      details: e
    }), this.name = "SaveConfigurationFileError";
  }
  toString() {
    return `[${this.name}] [${this.code}]: ${this.message}${ge}${this.details}`;
  }
}
class $f extends De {
  constructor(e) {
    super({
      code: "CLI::0004",
      message: "Unable to save theme asset. See details for more information.",
      details: e
    }), this.name = "SaveThemeAssetError";
  }
  toString() {
    return `[${this.name}] [${this.code}]: ${this.message}${ge}${this.details}`;
  }
}
class st extends De {
  constructor() {
    super({
      code: "CLI::0006",
      message: "Themes files not found on current directory. Verify and try again."
    }), this.name = "ThemeFilesNotFoundError";
  }
}
class zf extends De {
  constructor(e) {
    super({
      code: "CLI::9999",
      message: e ?? "Sorry, a not mapped error happened. Please tray again. If issue persists, open an issue to project."
    }), this.name = "UnknownError";
  }
}
var Nt = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function Wf(s) {
  return s && s.__esModule && Object.prototype.hasOwnProperty.call(s, "default") ? s.default : s;
}
var _s, Nn;
function qf() {
  if (Nn) return _s;
  Nn = 1;
  var s = "[object Symbol]", e = /[^\x00-\x2f\x3a-\x40\x5b-\x60\x7b-\x7f]+/g, t = /[\xc0-\xd6\xd8-\xf6\xf8-\xff\u0100-\u017f]/g, i = "\\ud800-\\udfff", n = "\\u0300-\\u036f\\ufe20-\\ufe23", r = "\\u20d0-\\u20f0", o = "\\u2700-\\u27bf", a = "a-z\\xdf-\\xf6\\xf8-\\xff", l = "\\xac\\xb1\\xd7\\xf7", c = "\\x00-\\x2f\\x3a-\\x40\\x5b-\\x60\\x7b-\\xbf", h = "\\u2000-\\u206f", u = " \\t\\x0b\\f\\xa0\\ufeff\\n\\r\\u2028\\u2029\\u1680\\u180e\\u2000\\u2001\\u2002\\u2003\\u2004\\u2005\\u2006\\u2007\\u2008\\u2009\\u200a\\u202f\\u205f\\u3000", p = "A-Z\\xc0-\\xd6\\xd8-\\xde", d = "\\ufe0e\\ufe0f", m = l + c + h + u, f = "['’]", b = "[" + i + "]", y = "[" + m + "]", E = "[" + n + r + "]", S = "\\d+", T = "[" + o + "]", O = "[" + a + "]", v = "[^" + i + m + S + o + a + p + "]", P = "\\ud83c[\\udffb-\\udfff]", B = "(?:" + E + "|" + P + ")", z = "[^" + i + "]", Te = "(?:\\ud83c[\\udde6-\\uddff]){2}", ds = "[\\ud800-\\udbff][\\udc00-\\udfff]", ke = "[" + p + "]", fi = "\\u200d", di = "(?:" + O + "|" + v + ")", lo = "(?:" + ke + "|" + v + ")", pi = "(?:" + f + "(?:d|ll|m|re|s|t|ve))?", mi = "(?:" + f + "(?:D|LL|M|RE|S|T|VE))?", gi = B + "?", wi = "[" + d + "]?", co = "(?:" + fi + "(?:" + [z, Te, ds].join("|") + ")" + wi + gi + ")*", bi = wi + gi + co, ho = "(?:" + [T, Te, ds].join("|") + ")" + bi, uo = "(?:" + [z + E + "?", E, Te, ds, b].join("|") + ")", fo = RegExp(f, "g"), po = RegExp(E, "g"), mo = RegExp(P + "(?=" + P + ")|" + uo + bi, "g"), go = RegExp([
    ke + "?" + O + "+" + pi + "(?=" + [y, ke, "$"].join("|") + ")",
    lo + "+" + mi + "(?=" + [y, ke + di, "$"].join("|") + ")",
    ke + "?" + di + "+" + pi,
    ke + "+" + mi,
    S,
    ho
  ].join("|"), "g"), wo = RegExp("[" + fi + i + n + r + d + "]"), bo = /[a-z][A-Z]|[A-Z]{2,}[a-z]|[0-9][a-zA-Z]|[a-zA-Z][0-9]|[^a-zA-Z0-9 ]/, yo = {
    // Latin-1 Supplement block.
    À: "A",
    Á: "A",
    Â: "A",
    Ã: "A",
    Ä: "A",
    Å: "A",
    à: "a",
    á: "a",
    â: "a",
    ã: "a",
    ä: "a",
    å: "a",
    Ç: "C",
    ç: "c",
    Ð: "D",
    ð: "d",
    È: "E",
    É: "E",
    Ê: "E",
    Ë: "E",
    è: "e",
    é: "e",
    ê: "e",
    ë: "e",
    Ì: "I",
    Í: "I",
    Î: "I",
    Ï: "I",
    ì: "i",
    í: "i",
    î: "i",
    ï: "i",
    Ñ: "N",
    ñ: "n",
    Ò: "O",
    Ó: "O",
    Ô: "O",
    Õ: "O",
    Ö: "O",
    Ø: "O",
    ò: "o",
    ó: "o",
    ô: "o",
    õ: "o",
    ö: "o",
    ø: "o",
    Ù: "U",
    Ú: "U",
    Û: "U",
    Ü: "U",
    ù: "u",
    ú: "u",
    û: "u",
    ü: "u",
    Ý: "Y",
    ý: "y",
    ÿ: "y",
    Æ: "Ae",
    æ: "ae",
    Þ: "Th",
    þ: "th",
    ß: "ss",
    // Latin Extended-A block.
    Ā: "A",
    Ă: "A",
    Ą: "A",
    ā: "a",
    ă: "a",
    ą: "a",
    Ć: "C",
    Ĉ: "C",
    Ċ: "C",
    Č: "C",
    ć: "c",
    ĉ: "c",
    ċ: "c",
    č: "c",
    Ď: "D",
    Đ: "D",
    ď: "d",
    đ: "d",
    Ē: "E",
    Ĕ: "E",
    Ė: "E",
    Ę: "E",
    Ě: "E",
    ē: "e",
    ĕ: "e",
    ė: "e",
    ę: "e",
    ě: "e",
    Ĝ: "G",
    Ğ: "G",
    Ġ: "G",
    Ģ: "G",
    ĝ: "g",
    ğ: "g",
    ġ: "g",
    ģ: "g",
    Ĥ: "H",
    Ħ: "H",
    ĥ: "h",
    ħ: "h",
    Ĩ: "I",
    Ī: "I",
    Ĭ: "I",
    Į: "I",
    İ: "I",
    ĩ: "i",
    ī: "i",
    ĭ: "i",
    į: "i",
    ı: "i",
    Ĵ: "J",
    ĵ: "j",
    Ķ: "K",
    ķ: "k",
    ĸ: "k",
    Ĺ: "L",
    Ļ: "L",
    Ľ: "L",
    Ŀ: "L",
    Ł: "L",
    ĺ: "l",
    ļ: "l",
    ľ: "l",
    ŀ: "l",
    ł: "l",
    Ń: "N",
    Ņ: "N",
    Ň: "N",
    Ŋ: "N",
    ń: "n",
    ņ: "n",
    ň: "n",
    ŋ: "n",
    Ō: "O",
    Ŏ: "O",
    Ő: "O",
    ō: "o",
    ŏ: "o",
    ő: "o",
    Ŕ: "R",
    Ŗ: "R",
    Ř: "R",
    ŕ: "r",
    ŗ: "r",
    ř: "r",
    Ś: "S",
    Ŝ: "S",
    Ş: "S",
    Š: "S",
    ś: "s",
    ŝ: "s",
    ş: "s",
    š: "s",
    Ţ: "T",
    Ť: "T",
    Ŧ: "T",
    ţ: "t",
    ť: "t",
    ŧ: "t",
    Ũ: "U",
    Ū: "U",
    Ŭ: "U",
    Ů: "U",
    Ű: "U",
    Ų: "U",
    ũ: "u",
    ū: "u",
    ŭ: "u",
    ů: "u",
    ű: "u",
    ų: "u",
    Ŵ: "W",
    ŵ: "w",
    Ŷ: "Y",
    ŷ: "y",
    Ÿ: "Y",
    Ź: "Z",
    Ż: "Z",
    Ž: "Z",
    ź: "z",
    ż: "z",
    ž: "z",
    Ĳ: "IJ",
    ĳ: "ij",
    Œ: "Oe",
    œ: "oe",
    ŉ: "'n",
    ſ: "ss"
  }, Eo = typeof Nt == "object" && Nt && Nt.Object === Object && Nt, So = typeof self == "object" && self && self.Object === Object && self, Ro = Eo || So || Function("return this")();
  function To(R, C, N, fe) {
    for (var J = -1, Et = R ? R.length : 0; ++J < Et; )
      N = C(N, R[J], J, R);
    return N;
  }
  function Oo(R) {
    return R.split("");
  }
  function xo(R) {
    return R.match(e) || [];
  }
  function Ao(R) {
    return function(C) {
      return R?.[C];
    };
  }
  var Co = Ao(yo);
  function yi(R) {
    return wo.test(R);
  }
  function vo(R) {
    return bo.test(R);
  }
  function No(R) {
    return yi(R) ? _o(R) : Oo(R);
  }
  function _o(R) {
    return R.match(mo) || [];
  }
  function Po(R) {
    return R.match(go) || [];
  }
  var Fo = Object.prototype, Do = Fo.toString, Ei = Ro.Symbol, Si = Ei ? Ei.prototype : void 0, Ri = Si ? Si.toString : void 0;
  function ko(R, C, N) {
    var fe = -1, J = R.length;
    C < 0 && (C = -C > J ? 0 : J + C), N = N > J ? J : N, N < 0 && (N += J), J = C > N ? 0 : N - C >>> 0, C >>>= 0;
    for (var Et = Array(J); ++fe < J; )
      Et[fe] = R[fe + C];
    return Et;
  }
  function Lo(R) {
    if (typeof R == "string")
      return R;
    if (Mo(R))
      return Ri ? Ri.call(R) : "";
    var C = R + "";
    return C == "0" && 1 / R == -1 / 0 ? "-0" : C;
  }
  function jo(R, C, N) {
    var fe = R.length;
    return N = N === void 0 ? fe : N, !C && N >= fe ? R : ko(R, C, N);
  }
  function Uo(R) {
    return function(C) {
      C = yt(C);
      var N = yi(C) ? No(C) : void 0, fe = N ? N[0] : C.charAt(0), J = N ? jo(N, 1).join("") : C.slice(1);
      return fe[R]() + J;
    };
  }
  function Bo(R) {
    return function(C) {
      return To(Ho(Wo(C).replace(fo, "")), R, "");
    };
  }
  function Io(R) {
    return !!R && typeof R == "object";
  }
  function Mo(R) {
    return typeof R == "symbol" || Io(R) && Do.call(R) == s;
  }
  function yt(R) {
    return R == null ? "" : Lo(R);
  }
  var $o = Bo(function(R, C, N) {
    return C = C.toLowerCase(), R + (N ? zo(C) : C);
  });
  function zo(R) {
    return qo(yt(R).toLowerCase());
  }
  function Wo(R) {
    return R = yt(R), R && R.replace(t, Co).replace(po, "");
  }
  var qo = Uo("toUpperCase");
  function Ho(R, C, N) {
    return R = yt(R), C = C, C === void 0 ? vo(R) ? Po(R) : xo(R) : R.match(C) || [];
  }
  return _s = $o, _s;
}
var Hf = qf();
const Gf = /* @__PURE__ */ Wf(Hf);
function Vs(s) {
  if (s === Object(s) && !Array.isArray(s) && typeof s != "function") {
    const e = {};
    return Object.keys(s).forEach((t) => {
      const i = Gf(t);
      e[i] = Vs(s[t]);
    }), e;
  }
  return Array.isArray(s) ? s.map((e) => Vs(e)) : s;
}
async function Jf() {
  return Me.readFile("config.json", { encoding: "utf8" }).then((s) => {
    const { token: e, themeId: t, previewUrl: i, debug: n } = Vs(JSON.parse(s)), r = {
      token: e,
      themeId: t,
      previewUrl: i,
      debug: n
    };
    return Promise.resolve(r);
  }).catch((s) => {
    const e = s.code === "ENOENT" ? new Ks({ file: "config.json", details: s.toString() }) : new zf();
    return Promise.reject(e);
  });
}
function ao(s) {
  return s.replace(/\\/g, "/");
}
function Kf(s) {
  const e = ao(s), t = e.startsWith("/") ? e : `/${e}`;
  return Me.readFile(`.${t}`).then((i) => {
    const n = {
      filename: t,
      content: i
    };
    return Promise.resolve(n);
  }).catch((i) => Promise.reject(new Bf(i.message)));
}
function Vf({
  token: s,
  themeId: e,
  previewUrl: t,
  debug: i
}) {
  const r = JSON.stringify({
    token: s,
    theme_id: e,
    preview_url: t,
    debug: i
  }, null, 2);
  return Me.writeFile("config.json", r).then(() => Promise.resolve("Configuration file created")).catch((o) => {
    const a = new Mf(o);
    return Promise.reject(a);
  });
}
function _n(s, e) {
  const t = `.${s}`, i = Ko(t);
  return Me.access(i).catch(() => Me.mkdir(i, { recursive: !0 })).then(() => Me.writeFile(t, e)).then(() => Promise.resolve(!0)).catch((n) => Promise.reject(new $f(n.message)));
}
class Wd {
  token;
  themeId;
  previewUrl;
  debug;
  api;
  limit;
  /**
   * Create new Tray instance
   * @param {ConfigurationFile}
   */
  constructor({ token: e, themeId: t, previewUrl: i, debug: n = !1 }) {
    this.token = e, this.themeId = t, this.previewUrl = i, this.debug = n, this.limit = Uf(100), this.api = new ii({
      token: this.token,
      themeId: this.themeId,
      debug: this.debug
    });
  }
  /**
   * Load configuration settings from config.json and create an instance of class.
   * @return {Promise<Tray>} Returns Tray instance if promises resolves, or CliError otherwise.
   */
  static async initiateFromConfigFile() {
    return Jf().then((e) => new this(e)).catch((e) => Promise.reject(e));
  }
  /**
   * Configure CLI use generating config.json file
   * @return {Promise} Return string if promise resolves, ApiError or CliError otherwise
   */
  async configure() {
    return await this.api.getTheme().then((e) => {
      const t = {
        token: this.token,
        themeId: this.themeId,
        previewUrl: e?.data?.preview ?? "",
        debug: this.debug
      };
      return Vf(t).then((i) => Promise.resolve(i)).catch((i) => Promise.reject(i));
    });
  }
  /**
   * List all available themes.
   * @return {Promise} Return ThemeInstall if promises resolves, BaseError or CliError otherwise.
   */
  async list() {
    return await this.api.getThemes().then((e) => e?.data);
  }
  /**
   * Create a clean theme
   * @returns Promise ThemeInstall.
   */
  async createCleanTheme() {
    return await this.api.createCleanTheme().then((e) => e?.data);
  }
  /**
   * Delete requested theme.
   * @param {number} id Theme id to be deleted
   * @return {Promise} Return true with promise resolves, CliError or BaseError otherwise.
   */
  async delete(e = this.themeId) {
    if (!e) throw new If("ThemeId");
    return await this.api.deleteTheme(e).then((t) => t?.data);
  }
  /**
   * Download configured theme
   * @param {string[]} files Files to be downloaded
   * @return {Promise} Returns DownloadCommandResponse when promises resolves
   */
  async download(e) {
    const t = [], i = await this.api.getThemeAssets().then((c) => c?.data);
    if (!i?.length)
      throw new st();
    let n = [];
    if (e && e.length) {
      const c = e.map((u) => `/${u}`);
      n = i.filter((u) => c.includes(u.path));
    } else
      n = i;
    const r = n.filter((c) => c.dynamic), a = n.filter((c) => c.uri && !c.dynamic).map(async (c) => this.limit(async () => {
      try {
        const h = await k.get(c.uri, {
          responseType: "arraybuffer",
          timeout: 12e4
        }), u = Buffer.from(h.data);
        await _n(c.path, u);
      } catch {
        t.push({ file: c.path, error: new Ks({ file: c.path }) });
      }
    })), l = r.map(async (c) => {
      try {
        const h = await this.api.getThemeAsset(c.id);
        if (!h?.data?.path)
          throw new Ks({ file: c.path });
        const u = Buffer.from(h.data.content ?? "", "utf8");
        await _n(h.data.path, u);
      } catch (h) {
        t.push({ file: c.path, error: h });
      }
    });
    return await Promise.all([...a, ...l]), {
      total: i.length,
      succeed: i.length - t.length,
      fails: t
    };
  }
  /**
   * Upload files to theme
   * @param {string[]} files Files to be uploaded. If not provided all files in current folder and subfolder will be uploaded.
   *                         Config.json and files starting with dot will always be ignored.
   * @return {Promise} Returns UploadCommandResponse object if promises resolves, CliError or ApiError otherwise.
   */
  async upload(e) {
    const t = [], i = await this.api.getThemeAssets();
    if (!i?.data?.length)
      throw new st();
    let n;
    if (e && e.length)
      n = e;
    else {
      let o = Ht("**/*", { nodir: !0 });
      if (o = o.filter((a) => a !== "config.json"), !o.length)
        throw new st();
      n = o;
    }
    const r = n.map(async (o) => {
      try {
        const a = await Kf(o), { filename: l, content: c } = a, h = Buffer.from(c).toString("base64"), u = i.data.find((p) => p.path === l);
        u ? await this.api.updateThemeAsset(l, u.id, h) : await this.api.createThemeAsset(l, h);
      } catch (a) {
        t.push({ file: o, error: a });
      }
    });
    return await Promise.all(r), {
      total: n.length,
      succeed: n.length - t.length,
      fails: t
    };
  }
  /**
   * Upload core files, excluding configs/settings.json and images folder.
   * Config.json and files starting with dot will always be ignored.
   * @return {Promise} Returns UploadCommandResponse object if promises resolves, CliError or ApiError otherwise.
   */
  uploadCore() {
    let e = Ht("**/*", { nodir: !0 });
    return e = e.filter(
      (t) => !t.match(/(img\/(.)*)|(configs\/settings.json)|(config.json)/)
    ), e.length ? this.upload(e) : Promise.reject(new st());
  }
  /**
   * Delete theme files
   * @param {string[]} files Files to be removes.
   * @return {Promise} Returns RemoveCommandResponse object if promises resolves, CliError or ApiError otherwise.
   */
  async remove(e) {
    const t = [], i = await this.api.getThemeAssets();
    if (!i?.data)
      throw new st();
    const n = e.map((a) => ao(a)), o = i.data.filter((a) => {
      const l = a.path.startsWith("/") ? a.path.substring(1) : a.path;
      return n.includes(l);
    }).map(
      (a) => this.api.deleteThemeAsset(a.id).catch((l) => t.push({ file: a, error: l }))
    );
    return Promise.all(o).then(() => {
      const a = e.length - t.length, l = {
        total: e.length,
        succeed: a,
        fails: t
      };
      return Promise.resolve(l);
    });
  }
}
export {
  De as C,
  Ks as F,
  wc as G,
  Bf as L,
  If as P,
  Mf as S,
  Wd as T,
  zf as U,
  $f as a,
  st as b,
  Ht as c,
  Wf as g,
  Yh as h,
  Jf as l,
  ao as n
};
